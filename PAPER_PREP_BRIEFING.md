# بریفینگ آماده‌سازیِ مقاله — پایپلاین تشخیص خودکار آنتی‌بایوگرام (Disk Diffusion)

> **این فایل چیست:** این سند برای دادن به یک نشست *جدید* Claude نوشته شده تا مقاله‌ی کامل (با فرمت/تمپلیتِ وردِ کنفرانسی که شما جداگانه می‌فرستید) را بنویسد. هدف این نیست که خودِ مقاله باشد — هدف این است که **همه‌ی مطالب، شکل‌ها، مقایسه‌ها، منابع و داده‌هایی که در طول این پروژه جمع/تولید شده‌اند** یک‌جا و سازمان‌یافته در دسترسِ آن نشست باشد، به‌همراه یک بخش صریح از چیزهایی که من (Claude) نمی‌دانم و باید شما پر کنید.
>
> **مرجع کامل و دقیق‌تر:** `PROJECT_HISTORY.md` در همین ریپازیتوری، تاریخچه‌ی خط‌به‌خط و کامل هر تصمیم فنی این پروژه است (شامل بسیاری آزمایش‌های رد‌شده که این‌جا هم خلاصه شده‌اند). نشستِ نویسنده‌ی مقاله باید **هر دو فایل** را بخواند: این فایل برای چارچوب/روایت/شکل‌ها، و `PROJECT_HISTORY.md` برای جزئیات فنی دقیق‌تر هر پارامتر در صورت نیاز.

---

## ۱) معرفی/انگیزه‌ی پروژه (Introduction)

**مسئله:** تست آنتی‌بایوگرام دیسک-دیفیوژن (Kirby-Bauer) یک روش استاندارد آزمایشگاهی برای سنجش حساسیت باکتری به آنتی‌بیوتیک‌هاست: دیسک‌های آغشته به آنتی‌بیوتیک روی یک محیط کشت آگار حاوی باکتری قرار می‌گیرند؛ بعد از انکوباسیون، اگر باکتری به آنتی‌بیوتیک حساس باشد، حلقه‌ای بدون رشد (هاله/Zone of Inhibition) دور دیسک شکل می‌گیرد. **قطر این هاله** (معمولاً بر حسب میلی‌متر) طبق جدول‌های استاندارد (مثلاً EUCAST) به دسته‌های حساس/متوسط/مقاوم (S/I/R) تبدیل می‌شود. در عمل، اندازه‌گیری این قطرها معمولاً با کولیس به‌صورت دستی انجام می‌شود — کند، وابسته به مهارت اپراتور، و مستعد خطای انسانی.

**هدف این پروژه:** ساخت یک پایپلاین *کاملاً کلاسیک* بینایی ماشین (بدون یادگیری ماشین/شبکه‌ی عصبی) که از یک عکس ساده‌ی تلفنِ همراه از یک یا چند پتری‌دیش:
1. خودِ ظرف(های) پتری را در عکس پیدا کند (حتی چند ظرف هم‌زمان در یک قاب)،
2. دیسک‌های آنتی‌بیوتیک روی هر ظرف را تشخیص دهد،
3. مرز/قطر هر هاله‌ی بازدارندگی را اندازه‌گیری کند،
4. رخدادهای غیرعادی داخل هاله (حباب/توده‌ی موضعی که ممکن است نشانه‌ی رشد جزئی/کلونی مقاوم یا آلودگی باشد) را علامت‌گذاری کند،
و در نهایت یک گزارش عددی (میلی‌متر) برای هر دیسک/هاله تولید کند.

**چرا بدون یادگیری ماشین؟** (این باید در مقاله به‌عنوان یک تصمیم طراحیِ آگاهانه مطرح شود، نه محدودیت) — یک رویکردِ کاملاً کلاسیک:
- به داده‌ی برچسب‌خورده‌ی حجیم نیاز ندارد (که در این پروژه اساساً وجود ندارد — نبود ground truth، رجوع کنید به بخش ۲ و ۷).
- تفسیرپذیر است: هر عدد نهایی را می‌توان به یک مرحله‌ی هندسی/آماری مشخص ردیابی کرد (مهم برای کاربرد پزشکی/آزمایشگاهی که نیاز به قابلیت ممیزی دارد).
- برای پیاده‌سازی نهایی به ++C قابل ترجمه است (یک الزام صریح پروژه — رجوع به بخش ۶) بدون وابستگی به فریم‌ورک‌های یادگیری عمیق در محیط تولید (احتمالاً یک دستگاه/اپ سبک آزمایشگاهی).
- روی طیف وسیعی از دوربین/نور/مقیاس تصویر باید کار کند بدون آموزش مجدد — بنابراین تمام پارامترهای پایپلاین **نسبی** هستند (نسبت به قطر ظرف/دیسک تعریف می‌شوند، نه پیکسل مطلق) — این یک اصل معماریِ محوری کل پروژه است که در مقاله باید به‌صراحت به‌عنوان یک "design principle" مطرح شود (بخش ۳ و ۴ جزئیات را دارند).

**پیشنهاد برای بخش Introduction مقاله:** با انگیزه‌ی بالینی (چرا اندازه‌گیری دستی مشکل‌ساز است)، سپس معرفی مختصر رویکردهای موجود (نیاز به جستجوی ادبیات توسط شما — بخش ۷)، سپس بیان این‌که این کار یک پایپلاین کلاسیک-CV end-to-end (تشخیص ظرف → دیسک → هاله → رخداد) با تمرکز بر استحکام روی داده‌ی واقعی/متنوع (نه فقط یک نمونه) ارائه می‌دهد.

---

## ۲) داده‌ها (Materials / Dataset)

- **۷۸ عکسِ واقعیِ آنتی‌بایوگرام** در ریشه‌ی ریپازیتوری (`*.jpg`)، گرفته‌شده با دوربین موبایل، هرکدام حاوی یک یا چند ظرف پتری (اکثراً ۱، برخی ۲ تا ۷ ظرف در یک قاب).
- **دیتابیس کش پردازش‌شده:** پوشه‌ی `petri_cache/` — برای هرکدام از ۷۸ عکس، نتیجه‌ی کامل ماژول تشخیص پتری (تا پایان ماژول ۴.۱) به‌صورت `meta.json` + ۳ فایل PNG بدون‌افت (per dish) ذخیره شده — یعنی یک "checkpoint" میانیِ پایپلاین که برای تست سریع مراحل بعدی (بدون اجرای دوباره‌ی کل پایپلاین از اول) استفاده می‌شود. **این یک artifact مهندسی/سرعت است، نه یک دیتاست برچسب‌خورده مجزا.**
- **دو سند مرجع استاندارد EUCAST:**
  - `v_16.0_Breakpoint_Tables.pdf` (جدول‌های نقطه‌قطع/Breakpoint نسخه‌ی ۱۶.۰)
  - `Dosages_v_16.0_Breakpoint_Tables.pdf` (دوزهای دیسک متناظر)
  از این‌ها فقط برای تحقیق درباره‌ی «روش استاندارد خواندن مرز هاله» استفاده شده (بخش ۴.۳)؛ به‌عنوان منبع رسمی برای تفسیر S/I/R در ادامه‌ی کار قابل استفاده‌اند اما در حال حاضر پایپلاین فقط قطر را به میلی‌متر گزارش می‌کند، نه دسته‌بندی حساسیت.

### آنچه در داده‌ها **موجود نیست** (نیاز به ورودی شما دارد — رجوع به بخش ۷):
- **بدون ground truth**: هیچ اندازه‌گیری مرجع (کولیس/دستی) برای هیچ‌کدام از ۷۸ عکس ثبت نشده. یعنی در حال حاضر امکان محاسبه‌ی معیارهای دقتِ کمّی (مثل MAE بین قطر پیش‌بینی‌شده و قطر واقعی) وجود ندارد.
- **بدون برچسب نوع باکتری/آنتی‌بیوتیک/غلظت دیسک** به‌صورت ساختاریافته برای اکثر عکس‌ها (فقط در چند مورد که به‌صورت دستی در طول توسعه بررسی شد، نام آنتی‌بیوتیک از روی متن چاپ‌شده روی خودِ دیسک در تصویر خوانده شد — مثلاً CRO30، CIP، FM300، MEN10 در مثال‌های این سند؛ OCR خودکار این متن‌ها بخشی از پایپلاین *نیست*).
- **بدون متادیتای تصویربرداری** (نوع دوربین، فاصله، نور محیط، تاریخ کشت/انکوباسیون) به‌صورت فایل جداگانه — فقط نامِ فایل‌ها الگوی تاریخ/ساعت دارند (`YYYYMMDD_HHMMSS.jpg`).
- **بدون مجوز/رضایت مشخص برای انتشار تصاویر واقعی در مقاله** (این عکس‌ها احتمالاً از یک محیط آزمایشگاهی واقعی گرفته شده‌اند).

---

## ۳) معماری کلی پایپلاین (Pipeline Overview)

نوت‌بوک اصلی: `disk_detection_pipeline_modular.ipynb` (۵۱ سلول، ماژولار — هر ماژول یک سلول Markdown توضیحی + یک یا چند سلول کد). دو فایل کمکی برای اجرا بدون Jupyter هم موجودند: `disk_detection_pipeline_modular.py` (نسخه‌ی هم‌ارزِ percent-format، تولیدشده خودکار توسط `convert_notebook_to_script.py`) و `batch_process_antibiogram.py` (اجرای دسته‌ای روی یک پوشه از عکس‌ها).

جریان کلی (ترتیب ماژول‌ها):

```
عکس ورودی (ممکن است چند ظرف پتری داشته باشد)
   │
   ▼
[ماژول ۴]  تشخیص همه‌ی ظرف‌های پتری (چندگانه، شکل‌آزاد: دایره/مربع/fallback عمومی)
   │  → dishes: List[Dict]  (هر عضو یک پتری با ماسک/مرکز/شعاع خودش)
   ▼
[برای هر پتری، مستقل:]
   │
[ماژول ۵]      نرمال‌سازی نور (illumination_normalize) + استخراج ویژگی اولیه‌ی دیسک
[ماژول ۶-۹]    شاخه‌ی Blob Analysis (آستانه‌گذاری + Watershed جداسازی دیسک‌های چسبیده)
[ماژول ۱۰]     Hough Circle (شاخه‌ی اصلی/دقیق تشخیص دیسک) + halo_map کمکی
[ماژول ۱۰.۵]   اعتبارسنجی هندسی روی union(blob_mask, disk_feature_mask)
[ماژول ۱۳]     Distance Transform — به‌عنوان ابزار (نه شاخه‌ی رأی‌دهنده): مارکر Watershed + برآورد شعاع
[ماژول ۱۴]     Fusion دو-شاخه‌ای (Hough + Blob) → لیست نهایی دیسک‌ها با امتیاز پیوسته
[ماژول ۱۵]     نمایش نتیجه‌ی نهایی تشخیص دیسک (Fusion Result)
   │
[ماژول ۱۶]     تشخیص شعاع پایه‌ی هاله (پروفایل شعاعیِ omnidirectional + همگرایی به پس‌زمینه)
[ماژول ۱۶.۵]   بسط نامتقارن مرز هاله به‌ازای هر زاویه (گرادیان محلی، مشروط به کنتراست کل)
[ماژول ۱۶.۶]   رفع رخدادهای زاویه‌ای محلی/نویز (میانه‌ی همسایگی، جایگزینیِ outlier)
   │
[ماژول ۱۷]     تشخیص رخداد/حباب داخل هاله (ماسک حلقه‌ای واقعی + Blob Analysis)
   │
[ماژول ۱۸]     گزارش نهایی متنی: قطر دیسک/هاله/رخداد حباب برای هر پتری/دیسک
```

اصول معماریِ ثابت در کل پروژه (باید در بخش Methods مقاله به‌صراحت به‌عنوان اصول طراحی ذکر شوند):
1. **بدون مقدار پیکسلی مطلق** — هر آستانه/کرنل/شعاع به‌صورت نسبت به قطر ظرف پتری یا قطر دیسک تعریف می‌شود، حتی محدودیت‌های شمارشی (تعداد ظرف، تعداد دیسک) تا حد امکان از یک سقف ثابت پرهیز می‌کنند.
2. **بدون overfitting به یک نمونه** — هر تغییر الگوریتمی باید قبل از قطعی‌شدن روی چند عکسِ متنوع (نه فقط عکسی که مشکل در آن دیده شده) اعتبارسنجی شود؛ این پروژه چندین بار طراحی‌هایی را که فقط روی یک عکس خوب کار می‌کردند رد کرده (بخش ۵ این سند مثال‌های مستند دارد).
3. **قطعیت (determinism)** — همه‌ی مرتب‌سازی‌ها/تساوی‌شکنی‌ها صریح‌اند تا نتیجه در هر اجرا یکسان بماند (پیش‌نیاز ترجمه‌ی نهایی به ++C).
4. **ماژولار بودن** — نوت‌بوک هرگز به یک اسکریپت تخت تبدیل نمی‌شود؛ هر مرحله یک سلول/گروه سلول مستقل با ورودی/خروجی مشخص است.

---

## ۴) روش‌شناسی به تفکیک ماژول (Methodology — Detailed)

### ۴.۱) تشخیص ظرف پتری (Petri Dish Detection — Module 4)

**رویکرد اصلی:** سگمنتیشن مبتنی بر ناحیه با **Multi-Otsu thresholding** روی تصویر با نور نرمال‌شده (نه سگمنتیشن مبتنی بر لبه). نرمال‌سازی نور با یک روش **تفریقی** (subtractive flat-fielding: `gray - illum_estimate + mean(illum)`) انجام می‌شود، نه تقسیمی (divisive) — این انتخاب از یک A/B-test مستقیم آمد: نسخه‌ی تقسیمی نویز را در نواحی تاریک تصویر تقویت می‌کرد.

- Hough Circle + Canny به‌عنوان یک مسیر **مکمل/تأییدکننده** نگه داشته شده، نه مسیر اصلی.
- **تشخیص چندگانه:** برخلاف نسخه‌های اولیه (که فقط یک ظرف را نگه می‌داشتند)، اکنون همه‌ی کانتورهای معتبر نگه داشته می‌شوند و با یک فیلتر شمول (containment filter) موارد کاذبِ تودرتو حذف می‌شوند.
- **دو خودآزمایی مستقل (self-check) hypothesize-and-verify:** ماژول ۸.۵ (بررسی ارزان وجود بلاب‌های شبیه دیسک) و ماژول ۱۱.۵ (بررسی قوی‌تر با کاندیدهای Hough-دیسک) — هر دو برای رد کردن کاندیدهای پتریِ کاذب که ظاهر هندسی خوبی دارند ولی محتوای داخلشان با یک پتری واقعی سازگار نیست.

**نتیجه‌ی یک اجرا:** `dishes: List[Dict]` — هر عضو یک پتری مستقل با کلیدهای مربوط به خودش (ماسک، مرکز، شعاع، ROI)؛ تمام ماژول‌های بعدی روی یک حلقه‌ی `for dish in dishes` اجرا می‌شوند (نه یک بار برای کل تصویر).

**شکل پیشنهادی:** `paper_figures/fig_multi_petri_detection_2dish.jpg` — نمونه‌ی موفقِ تشخیص همزمانِ ۲ ظرف پتری واقعی در یک قاب، با کانتور سبز/آبی و برچسب Petri #1 / #2.

### ۴.۲) تشخیص دیسک‌های آنتی‌بیوتیک (Disk Detection — Fusion)

**معماری نهایی (Fusion دو-شاخه‌ای مکمل، نه سه رأی‌دهنده‌ی هم‌وزن):**
- **شاخه‌ی Hough** = شاخه‌ی اصلی/دقیق (تأیید تجربی بهتر روی داده‌ی واقعی).
- **شاخه‌ی Blob Analysis** = شاخه‌ی پوششی/recall، بازطراحی‌شده با:
  - آستانه‌گذاری تطبیقی با کرنل‌های **نسبی به قطر دیسک** (نه پیکسل مطلق).
  - **Watershed** با مارکر از local-maxima تبدیل فاصله، برای جدا کردن دیسک‌های چسبیده/نزدیک به هم.
  - denoising با opening مورفولوژیک نسبی (`disk_feature_denoise_open_frac`).
- **Distance Transform (DT)** دیگر یک شاخه‌ی رأی‌دهنده‌ی مستقل *نیست* — نقشش صرفاً ابزاری است: تولید مارکر Watershed و برآورد شعاع.
- اعتبارسنجی هندسی (`geom_metrics_for_circle`، شامل circularity/solidity) روی **اجتماع (union)** ماسک بلاب و ماسک ویژگیِ دیسک اجرا می‌شود (نه فقط ماسک بلاب)، به‌همراه یک closing نسبی برای پر کردن حفره‌های ناشیِ از متن چاپ‌شده روی خودِ دیسک.
- **رهاسازی از دام outlier شعاعی** (`fusion_radius_outlier_rescue_score`): یک کاندید با شعاعی که در خوشه‌ی رادیوسیِ غالب نمی‌گنجد (تک‌افتاده) فقط در صورتی رد می‌شود که امتیاز Fusion‌اش هم پایین باشد — چون دیسک‌ها می‌توانند به‌طور طبیعی روی محیط کشت پخش/نرم شوند (این یک دانش دامنه‌ای صریح از کاربر بود، نه یک فرض بینایی ماشین عمومی).

**باگ مهم کشف و رفع‌شده در این نسخه — «Fusion cliff-edge»:** یک آستانه‌ی ثابت (`fusion_strong_hough_branch_score=0.50`) قبلاً یک دروازه‌ی دوتایی (binary bypass) برای رد/قبولِ معیارهای هندسی ایجاد می‌کرد — یعنی یک دیسک با امتیاز درست زیر آستانه می‌توانست به‌کل حذف شود درحالی‌که از نظر بصری کاملاً معتبر بود. این فیلد کاملاً حذف شد (نه کامنت‌شده)؛ اکنون هندسه فقط به‌صورت پیوسته وارد `entry["score"]` می‌شود و تنها دروازه‌ی نهایی یک آستانه‌ی نرم (`fusion_min_score`) است. **این تغییر روی ۱۰ عکس مستقل اعتبارسنجی شد** تا مطمئن شویم فقط مورد هدف (دیسک آنتی‌بیوتیک CRO30 که با این باگ حذف می‌شد) را رفع می‌کند و اثر جانبی منفی روی سایر عکس‌ها ایجاد نمی‌کند.

**شکل‌های پیشنهادی:**
- `fig_fusion_cliff_edge_missing_disk_before.jpg` — نمونه‌ی قبل از رفع باگ (دیسک CRO30 گم‌شده).
- `fig_fusion_cliff_edge_disks_numbered.jpg` — همان عکس با شماره‌گذاری دیسک‌ها برای ارجاع در متن.
- `fig_fusion_cliff_edge_CRO30_fixed.jpg` — بعد از رفع باگ (همه‌ی دیسک‌ها ازجمله CRO30 تشخیص داده می‌شوند).

### ۴.۳) تشخیص مرز/قطر هاله (Halo / Zone of Inhibition Boundary Detection)

این بخش پرچالش‌ترین و بیشترین تعداد بازطراحیِ کل پروژه بوده — ارزش دارد در مقاله به‌عنوان یک **case study روش‌شناسیِ تجربی-تکرارشونده (iterative empirical design)** با جزئیات کامل ارائه شود، چون رد شدن‌های مستند و دلیل‌دار خودشان محتوای علمی معتبری‌اند (نه فقط طراحی نهایی).

**تاریخچه‌ی طراحی (مهم‌ترین بخش برای روایت مقاله):**
1. **رویکردهای اولیه‌ی رد‌شده (existence-gate):** سه رویکرد مستقل امتحان شد که هرکدام سعی می‌کردند ابتدا "آیا اصلاً هاله‌ای وجود دارد؟" را با یک آستانه‌ی سخت تصمیم بگیرند: (الف) کنتراست شدت روشنایی، (ب) بافت/واریانس محلی، (پ) پیوستگی کمانی ترکیبی با کالیبراسیون تهی (null-calibration). **هر سه با بازرسی بصری مستقیم کاربر روی نمونه‌های واقعی رد شدند** — همگی یا هاله‌های واقعی را حذف می‌کردند یا نویز را به‌عنوان هاله می‌پذیرفتند.
2. **معماری نهاییِ الزام‌شده توسط کاربر (بدون existence-gate؛ همیشه یک دایره را برازش کن، بعد اصلاحش کن):**
   - **ماژول ۱۶ (پایه):** یک دایره‌ی واحد از طریق پروفایل شعاعیِ تجمیعی (omnidirectional) و **آستانه‌ی «همگرایی به پس‌زمینه» (background-convergence)** برازش می‌شود — نخستین شعاعی که *همه‌ی* حلقه‌های بعدی درون یک باند نویز حول مقدار پس‌زمینه باقی می‌مانند، به‌عنوان مرز نهایی انتخاب می‌شود. این معیار گذارهای بیولوژیک دائمی (شروع رشد باکتری) را از نوسانات گذرای عکاسی (سایه/انعکاس موضعی) تفکیک می‌کند.
   - **ماژول ۱۶.۵ (بسط نامتقارن):** مرز پایه به‌ازای هر زاویه (کمان ۱۸ درجه) با اوج گرادیانِ محلی بسط داده می‌شود — اما فقط وقتی کنتراست کلیِ تجمیعی (`contrast_sigma`) از یک آستانه‌ی حداقلی عبور کند (چون داده‌ی تک-زاویه نویزی‌تر از داده‌ی تجمیعی‌ست و بدون این شرط اعتماد بیش از حد به نویز محلی می‌شد).
   - **ماژول ۱۶.۶ (جدید، این نشست):** رفع رخدادهای زاویه‌ای محلی/ایزوله — وقتی یک رویداد موضعیِ درون‌هاله‌ای (گرادیان/نویز تصویر لوکال، یا آنچه در پروژه «رخداد لنگر-به-لبه‌ی دیسک» / rim-anchoring نامیده شد) باعث می‌شد شعاع یک زاویه‌ی خاص به‌اشتباه به لبه‌ی خودِ دیسک بچسبد/محدود شود. رفع با جایگزینیِ میانه‌ی همسایگیِ زاویه‌ای (وقتی مقدار یک زاویه بیش از یک نسبت آستانه از میانه‌ی همسایگی‌اش منحرف باشد) انجام می‌شود.
   - **سقف‌های هندسیِ خالص (بدون تغییر، بازاستفاده‌شده):** `_neighbor_voronoi_cap` (فرمول بسته‌ی دقیقِ عمودمنصف بین دو دیسک همسایه، نه تقریب Watershed) و `_dish_edge_cap` (پیمایش مستقیم پرتو در ماسک ظرف) — این دو کاملاً هندسی‌اند و به سیگنال تصویر وابسته نیستند؛ مستقل از موارد بالا همیشه فعال می‌مانند تا هاله هرگز از مرز واقعی (لبه‌ی همسایه یا لبه‌ی ظرف) عبور نکند.

**شکل‌های پیشنهادی:**
- `fig_halo_rim_anchoring_bug_CIP.jpg`, `fig_halo_rim_anchoring_bug_FM300.jpg` — نمونه‌ی مشکل قبل از ماژول ۱۶.۶ (اثبات بصری باگ لنگرشدن به لبه‌ی دیسک).
- `fig_module16_6_before_after_overlay.jpg` — مقایسه‌ی قبل/بعدِ همان دیسک بعد از اعمال ماژول ۱۶.۶.
- `fig_module16_6_validation_142413.jpg`, `_143806.jpg`, `_155938.jpg` — سه نمونه از هفت عکسِ اعتبارسنجیِ ماژول ۱۶.۶ (روی مجموع ۶۸ دیسک، هم شواهد سرکوب مثبت‌کاذب و هم عدم فعال‌شدن کاذب روی موارد سالم بررسی شد).
- `fig_bgconvergence_143806.jpg`, `_155938.jpg`, `_143217.jpg` — نمونه‌هایی از رفع مشکل مثبت‌کاذب هاله (قبل از ماژول ۱۶.۶، مربوط به معیار همگرایی به پس‌زمینه‌ی ماژول ۱۶).
- `fig_combined_three_fixes_143806.jpg`, `_155938.jpg` — نمایش ترکیبیِ سه‌گانه‌ی اصلاحات (همگرایی پس‌زمینه + Fusion cliff-edge + رفع رخداد زاویه‌ای) روی همان عکس‌ها.

**یک ایده‌ی جایگزین بزرگ‌مقیاسِ کاربر که آزمایش و رد شد (ارزش مستندسازی در بخش مقایسه‌ی طراحی‌ها را دارد):** کاربر پیشنهاد یک معماری کاملاً متفاوت داد — مرحله‌ی اول: میانه‌ی کمان ۱۰ درجه‌ای؛ مرحله‌ی دوم: دایره‌ی درحال‌رشد با کمینه‌سازیِ انحراف‌معیار گرادیانِ محیطی؛ به‌علاوه یک سقفِ فاصله‌ی مبتنی بر EUCAST. طبق درخواست صریح کاربر («برای اینکه Overfitting رخ نده... راه‌حل‌های کلان تازه رو پیدا کن»)، این ایده **به‌طور تجربی و مستقیم** (نه فقط تئوریک) روی همان ۸ دیسکِ شناخته‌شده با معماری موجود مقایسه شد و به‌وضوح **ضعیف‌تر/خطاپذیرتر** بود؛ بنابراین پیاده‌سازی نشد (فقط در `PROJECT_HISTORY.md` §۱۰.۲۲ مستند شده). همچنین در همین بررسی، سندهای EUCAST موجود در ریپازیتوری به‌دقت جست‌وجو شدند و **هیچ استاندارد عمومیِ فاصله‌ی بین‌دیسکی** پیدا نشد (فقط یک مقدار مخصوصِ D-test برای جفتِ اریترومایسین/کلیندامایسین: ۱۲ تا ۲۰ میلی‌متر لبه‌به‌لبه) — بنابراین سقف‌های هندسیِ Voronoi/لبه‌ی مبتنی بر خودِ تصویر (بند بالا) دقیق‌تر از هر مقدار ثابت فرضی تشخیص داده شدند.

**استاندارد خواندن مرزِ هاله (تحقیق انجام‌شده، خواسته‌ی صریح کاربر):** متن رسمی یافت‌شده در `v_16.0_Breakpoint_Tables.pdf`، صفحه‌ی ۱۴:
> "read zone edges as the point showing no growth viewed from the back of the plate against a dark background illuminated with reflected light."
یعنی استاندارد رسمی، مرز هاله را «نقطه‌ی شروعِ عدم‌رشدِ قابل‌مشاهده» تعریف می‌کند — که دقیقاً همان چیزی است که رویکرد «همگرایی به پس‌زمینه»ی ماژول ۱۶ به‌صورت آماری/عددی مدل‌سازی می‌کند (گذار پایدار به سطح پس‌زمینه = نقطه‌ی شروع عدم‌رشد)؛ این یک نقطه‌ی قوت برای بخش Discussion مقاله است: طراحیِ الگوریتم با تعریفِ استانداردِ رسمی هم‌راستاست، نه فقط یک اکتشافِ تصویری خودسرانه.

### ۴.۴) تشخیص رخداد/حباب داخل هاله (Halo Bubble/Event Detection — Module 17)

هدف: علامت‌گذاریِ رخدادهای موضعیِ غیرعادی داخل ناحیه‌ی هاله (مثلاً کلونیِ مقاومِ نقطه‌ای، حباب هوا، آلودگی) که می‌توانند تفسیر بالینیِ ساده‌ی «قطر هاله» را پیچیده کنند.

**بازطراحیِ این نشست (خواسته‌ی صریح کاربر):** ماسک حلقه‌ای (ring mask) که ناحیه‌ی جست‌وجو را تعریف می‌کند، دیگر از یک تفاضلِ دایره‌ی هندسیِ ساده‌ی مقیاسی (`(rad>=r_disk)&(rad<=r_halo)`) ساخته نمی‌شود؛ به‌جای آن مستقیماً از **شکل واقعیِ نامتقارنِ خروجیِ ماژول‌های ۱۶/۱۶.۵/۱۶.۶** (`halo_result["halo_mask"]`) برش داده می‌شود — یعنی ماسک جست‌وجوی حباب دقیقاً با مرز واقعیِ هاله (که ممکن است در جهات مختلف شعاع متفاوتی داشته باشد) منطبق است، نه یک تقریب دایره‌ای. تشخیص خودِ حباب‌ها با تحلیل بلاب (Blob Analysis) روی همین ماسک انجام می‌شود.

**شکل پیشنهادی:** `paper_figures/full_pipeline_example_155938/03_bubbles_dish1.png` — خروجی این ماژول روی یک نمونه‌ی واقعی.

### ۴.۵) گزارش نهایی (Module 18)

خروجی متنی نهایی برای هر پتری/دیسک: مختصات مرکز، قطر دیسک (mm)، قطر هاله (mm یا «تشکیل نشده»). نمونه‌ی واقعیِ کامل (۹ دیسک روی یک پتری، عکس `20260105_155938.jpg`) در `paper_figures/full_pipeline_example_155938/04_final_report.txt` موجود است — این یک نمونه‌ی خوب برای Table نتایج در مقاله است (قطرهای دیسک از ۴.۴ تا ۶.۸ میلی‌متر و قطرهای هاله از ۵.۰ تا ۱۹.۹ میلی‌متر در این نمونه‌ی خاص، که تنوعِ واقع‌گرایانه‌ای از عدمِ‌رشد را نشان می‌دهد).

**مجموعه‌ی ۴ فایل خروجیِ کامل برای همین نمونه** (پوشه‌ی `paper_figures/full_pipeline_example_155938/`) یک "پایپلاین کامل، سرتاسر" روی یک عکسِ واقعی را نشان می‌دهد — پیشنهاد می‌شود این چهار فایل در مقاله به‌عنوان یک شکلِ چندبخشیِ واحد (Figure با چهار زیرشکل a-d: تشخیص دیسک، هاله، حباب، گزارش) استفاده شود.

---

## ۵) مقایسه‌ی طراحی‌ها — پذیرفته‌شده در برابر ردشده (Design Comparisons — Rejected Alternatives)

این بخش **محتوای علمیِ باارزشی برای بخش Discussion/Ablation مقاله** است — نشان می‌دهد تصمیم‌های نهایی از روی آزمایش تجربیِ مقایسه‌ای آمده‌اند، نه اولین ایده‌ای که به ذهن رسیده. (منبع کامل: `PROJECT_HISTORY.md`، بخش‌های ذکرشده.)

| ایده‌ی ردشده | چرا رد شد | مرجع |
|---|---|---|
| ماژول تأیید ظاهریِ جداگانه (Appearance Verification) | overfit به نمونه‌های خاص، false-reject روی داده‌ی متنوع | §۵.۷–۵.۸ |
| خنثی‌سازیِ رأیِ Blob (Blob-vote neutralization) | اثر جانبی منفی روی موارد سالم | §۵.۷–۵.۸ |
| GrabCut برای مرز هاله | ناپایدار/حساس به مقداردهی اولیه روی نمونه‌های متنوع | §۱۰.۵–۱۰.۶ |
| برازش سیگموئید (sigmoid-fit) برای پروفایل هاله | فرض شکل ثابت که با داده‌ی واقعی (نویزی/نامتقارن) سازگار نبود | §۱۰.۸–۱۰.۱۱ |
| سه رویکرد existence-gate (کنتراست/بافت/پیوستگی کمانی) برای هاله | همگی با بازرسیِ بصریِ مستقیم رد شدند (بخش ۴.۳ بالا) | §۱۰.۸–۱۰.۱۱ |
| معماریِ جایگزینِ کاربر (میانه‌ی کمان ۱۰° + دایره‌ی رشدیابنده + سقفِ EUCAST) | تست تجربیِ مستقیم روی ۸ دیسکِ شناخته‌شده: ضعیف‌تر از معماریِ موجود | §۱۰.۲۲ (این نشست) |
| Fusion سه‌شاخه‌ای هم‌وزن (Hough + Blob + DT مستقل) | DT به‌عنوان رأی‌دهنده‌ی مستقل ناسازگار مقیاس/نویزپذیر بود؛ اکنون فقط ابزار (مارکر Watershed + برآورد شعاع) | بخش ۴.۲ بالا / پلن معماری |
| آستانه‌ی سخت/دوتاییِ Fusion (`fusion_strong_hough_branch_score`) | «پرتگاه» ناگهانی که دیسک‌های معتبرِ نزدیک به آستانه را کاملاً حذف می‌کرد | §۱۰.۲۳ (این نشست) |

اگر خواستید، من می‌توانم برای هرکدام از این موارد جزئیات بیشتر (پارامترهای دقیق، تعداد نمونه‌های تست) را از `PROJECT_HISTORY.md` استخراج و به این جدول اضافه کنم.

---

## ۶) اعتبارسنجی و محدودیت‌های آن (Validation Methodology)

**رویکرد اعتبارسنجی در طول پروژه:** چون ground truth عددی (اندازه‌گیری کولیس) وجود ندارد، اعتبارسنجی عمدتاً **کیفی/بصری** بوده: هر تغییر الگوریتمی قبل از قطعی‌شدن روی چند عکسِ متنوع (نه فقط عکس اصلیِ مشکل‌دار) اجرا و overlay آن با بازرسیِ بصریِ مستقیم کاربر تأیید می‌شود. برای مثال:
- رفع مثبت‌کاذبِ هاله: ۵ عکس.
- رفع باگِ Fusion cliff-edge: ۱۰ عکس.
- رفع رخداد زاویه‌ای ماژول ۱۶.۶: ۷ عکس / ۶۸ دیسک، هم برای سرکوبِ درست و هم برای عدم فعال‌شدنِ کاذب.

**این باید در مقاله به‌صراحت به‌عنوان یک محدودیت مطرح شود** (نه پنهان شود): این یک اعتبارسنجیِ کیفیِ تکرارشونده است، نه یک ارزیابیِ کمّیِ آماری در برابر مرجع مستقل. اگر امکان‌پذیر باشد، پیشنهاد می‌شود پیش از انتشار، حداقل یک زیرمجموعه از عکس‌ها با اندازه‌گیریِ دستیِ کولیس مقایسه شوند تا یک معیارِ خطای کمّی (مثلاً MAE بر حسب میلی‌متر) به مقاله اضافه شود.

**محدودیتِ شناخته‌شده‌ی جدید (کشف حین آماده‌سازیِ این سند، هنوز رفع نشده):** روی عکس `20260105_160124.jpg` (که قاعدتاً باید تنها یک ظرف پتریِ واقعی داشته باشد)، ماژول تشخیص پتری آن را به‌اشتباه به ۷ کاندیدِ هم‌پوشانِ کاذب تقسیم کرد. این خارج از محدوده‌ی درخواست‌های این نشست بود و عمداً رفع نشد، اما باید به‌عنوان یک **محدودیت شناخته‌شده** (Known Limitation) در مقاله ذکر شود — نمونه‌ای صادقانه از این‌که تشخیص چند-پتری هنوز روی همه‌ی ترکیب‌بندی‌های نور/فاصله/همپوشانی کامل نیست.

---

## ۷) آنچه من نمی‌دانم — نیاز به ورودیِ شما (برای نشستِ نویسنده‌ی مقاله)

این بخش را **قبل از فرستادن این فایل** به نشستِ نویسنده، تا حد امکان پر کنید؛ هرچه اینجا نامشخص بماند، نویسنده‌ی مقاله مجبور است حدس بزند یا از شما دوباره بپرسد:

1. **عنوان/نویسندگان/وابستگی سازمانی (affiliation)** — کاملاً نامشخص برای من.
2. **کنفرانس هدف و محدودیت‌های فرمت/طول** — تمپلیتِ وردی که جداگانه می‌فرستید مشخص می‌کند، ولی محدودیتِ تعداد صفحه/کلمه و سبکِ ارجاع‌دهی (IEEE/ACM/...) را باید صریحاً ذکر کنید.
3. **مرور ادبیات/کارهای مرتبط (Related Work):** من نمی‌توانم منابعِ دقیق و به‌روز را با اطمینانِ کافی جست‌وجو/تأیید کنم (خطر ارجاع نادرست/قدیمی). **شما باید جست‌وجوی ادبیاتِ مرتبط را خودتان انجام دهید** (یا از نشستِ نویسنده با ابزار جست‌وجوی وب بخواهید) — به‌خصوص کارهای قبلی درباره‌ی تشخیص خودکار دیسک‌دیفیوژن/آنتی‌بایوگرام (چه مبتنی بر بینایی کلاسیک، چه یادگیری عمیق) تا مقاله بتواند این کار را در ادبیاتِ موجود جای‌گذاری کند.
4. **آماره‌های اعتبارسنجیِ کمّی:** همان‌طور که در بخش ۶ گفته شد، ground truth کولیسی وجود ندارد. اگر بخواهید مقاله ادعای دقتِ عددی داشته باشد، باید حداقل چند اندازه‌گیریِ دستیِ مرجع تهیه کنید.
5. **جزئیات ترکیب نمونه (sample composition):** نوع باکتری/آنتی‌بیوتیک‌های استفاده‌شده در ۷۸ عکس، شرایط انکوباسیون، نوع محیط کشت — این‌ها برای بخش Materials مقاله لازم‌اند و من فقط از روی متنِ چاپ‌شده‌ی روی چند دیسک در طول توسعه (مثل CRO30، CIP، FM300، MEN10) می‌دانم، نه به‌صورت ساختاریافته برای کل دیتاست.
6. **مجوز/اخلاق/رضایت:** اگر عکس‌ها از یک محیط آزمایشگاهیِ واقعی/بیمارستانی گرفته شده‌اند، مقاله معمولاً به یک بیانیه‌ی اخلاق/رضایت یا حداقل تأییدِ نداشتنِ داده‌ی بیمار نیاز دارد — این را فقط شما می‌دانید.
7. **مقایسه با ابزارهای تجاری/دیگرِ موجود** (در صورت وجود، مثل نرم‌افزارهای خوانشگرِ آنتی‌بایوگرامِ تجاری) — اگر بخواهید مقاله ادعای مقایسه‌ای با ابزار خاصی داشته باشد، باید خودتان مشخص کنید کدام ابزار.

---

## ۸) فهرست کامل شکل‌ها (Figure Inventory)

همه در `paper_figures/` (نسبت به ریشه‌ی ریپازیتوری):

| فایل | محتوا |
|---|---|
| `fig_multi_petri_detection_2dish.jpg` | تشخیص همزمان ۲ ظرف پتری واقعی |
| `fig_fusion_cliff_edge_missing_disk_before.jpg` | قبل از رفع باگ Fusion — دیسک CRO30 گم‌شده |
| `fig_fusion_cliff_edge_disks_numbered.jpg` | همان عکس، دیسک‌ها شماره‌گذاری‌شده |
| `fig_fusion_cliff_edge_CRO30_fixed.jpg` | بعد از رفع باگ — همه‌ی دیسک‌ها تشخیص داده شده |
| `fig_halo_rim_anchoring_bug_CIP.jpg` | باگِ لنگرشدنِ هاله به لبه‌ی دیسک — نمونه‌ی CIP |
| `fig_halo_rim_anchoring_bug_FM300.jpg` | همان باگ — نمونه‌ی FM300 |
| `fig_module16_6_before_after_overlay.jpg` | مقایسه‌ی قبل/بعدِ ماژول ۱۶.۶ |
| `fig_module16_6_validation_142413.jpg` / `_143806.jpg` / `_155938.jpg` | نمونه‌های اعتبارسنجیِ ماژول ۱۶.۶ |
| `fig_bgconvergence_143806.jpg` / `_155938.jpg` / `_143217.jpg` | رفع مثبت‌کاذبِ هاله (همگرایی به پس‌زمینه) |
| `fig_combined_three_fixes_143806.jpg` / `_155938.jpg` | نمایشِ ترکیبیِ هر سه اصلاح |
| `full_pipeline_example_155938/01_fusion_disks.png` | خروجیِ کاملِ تشخیص دیسک، یک نمونه‌ی واقعی |
| `full_pipeline_example_155938/02_halo_dish1.png` | خروجیِ نهاییِ هاله، همان نمونه |
| `full_pipeline_example_155938/03_bubbles_dish1.png` | خروجیِ تشخیصِ حباب، همان نمونه |
| `full_pipeline_example_155938/04_final_report.txt` | گزارش نهاییِ متنی، همان نمونه (۹ دیسک) |

---

## ۹) پیوست پارامترها (Parameter Reference)

برای جزئیات کاملِ هر پارامتر (`cfg.xxx`) و توجیه نسبی‌بودنش، به `PROJECT_HISTORY.md` و کلاس `Phase2Config` در `disk_detection_pipeline_modular.ipynb` (سلول Config Extension) مراجعه کنید. مهم‌ترین‌ها که در این سند ذکر شدند:

- `halo_background_convergence_sigma = 2.0`
- `halo_extension_arc_width_frac = 0.05`
- `halo_extension_gradient_min_sigma = 2.0`
- `halo_extension_min_gradient_frac_of_noise = 2.0`
- `halo_extension_require_min_contrast_sigma = 3.0`
- `halo_angular_outlier_window_frac = 0.14`
- `halo_angular_outlier_ratio = 1.4`
- `fusion_radius_outlier_rescue_score = 0.65`
- `fusion_min_score` (تنها دروازه‌ی نهاییِ Fusion، بعد از حذفِ `fusion_strong_hough_branch_score`)
- `disk_feature_denoise_open_frac = 0.30`
- `geom_fill_close_frac = 0.18`

---

## ۱۰) کد و ابزارهای پشتیبان (برای ارجاع در بخش Implementation)

- `disk_detection_pipeline_modular.ipynb` — منبعِ حقیقتِ الگوریتم (۵۱ سلول).
- `disk_detection_pipeline_modular.py` — نسخه‌ی percent-format، تولیدِ خودکار از نوت‌بوک (`convert_notebook_to_script.py`)، برای اجرای بدونِ Jupyter (کلی یا سلول‌به‌سلول).
- `batch_process_antibiogram.py` — اجرای دسته‌ای روی یک پوشه، تولیدِ ۴ خروجی به‌ازای هر عکس.
- `build_petri_cache.py` / `petri_cache_utils.py` — زیرساختِ کشِ تشخیصِ پتری برای تست سریع‌ترِ ماژول‌های بعدی.
- `PROJECT_HISTORY.md` — تاریخچه‌ی کاملِ فنی/تصمیم‌ها (مرجعِ اصلیِ جزئیات برای نویسنده‌ی مقاله).
