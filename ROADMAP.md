# وضعیتِ فعلی، مشکلاتِ باز، و نقشه‌ی راه

> این سند «کجا هستیم و بعد چه می‌کنیم» را نگه می‌دارد. تاریخچه‌ی کاملِ تصمیم‌ها در `PROJECT_HISTORY.md` و اعدادِ ارزیابی در `EVALUATION.md` است.

آخرین به‌روزرسانی: پس از استقرارِ معماریِ چندشاخه‌ایِ هاله (commit `715ad56`).

---

## ۱) وضعیتِ فعلی — اعدادِ سنجیده‌شده

روی ۱۱ عکسِ دارایِ اندازه‌گیریِ کارشناسی، ۹۳ دیسک:

| معیار | مقدار |
|---|---|
| تشخیصِ پتری | ۱۱/۱۱ (۱۰۰٪) |
| تشخیصِ دیسک | P=۰.۹۸۹  R=۰.۹۷۸  F1=۰.۹۸۴ (۱ FP، ۲ FN) |
| حضورِ هاله | TP=۶۰  FN=۳  FP=۱۴  TN=۱۴ — دقت ۰.۸۱۳ |
| قطرِ هاله | MAE=۳.۸۵mm، Bias=۰.۶۱−، BA۹۵٪=[۱۲.۳۳−, ۱۱.۱۱+] |
| قطرِ دیسک | MAE=۰.۱۷mm (بررسیِ خودسازگاریِ کالیبراسیون، نه اعتبارسنجیِ مستقل) |

**معماریِ مستقر:** بومِ آگار (۱۵.۵) → سه شاخه‌ی موازیِ هاله (۱۵.۶ Otsu، ۱۵.۷ Watershed، ۱۶ شعاعی) → ادغام (۱۶.۷). شاخه‌ی آماری (۱۵.۸) ساخته شده ولی عمداً در زنجیره نیست.

---

## ۲) مشکلاتِ بازِ شناخته‌شده

### الف) ۱۴ مثبتِ کاذبِ حضورِ هاله — بدونِ راه‌حلِ اعتبارسنجی‌شده
دیسک‌هایی که کارشناس «بدونِ هاله» ثبت کرده ولی سیستم هاله گزارش می‌دهد. تنها سازوکاری که آزمودیم (گیتِ کنتراست روی تشخیص‌هایِ شعاعیِ ضعیف) در اعتبارسنجیِ leave-one-out **رد شد** — درون‌نمونه‌ای خوب بود ولی زیرِ LOO بدتر از هیچ‌کاری‌نکردن درآمد. جایگزینی پیدا نشده.

### ب) MAE=۳.۸۵mm هنوز بالاست
کاتالوگِ خطاها (`ground_truth/diagnostics/halo_failure_catalogue.csv`) خطایِ باقی‌مانده را عمدتاً به این نسبت می‌دهد که پروفایلِ یک‌بعدیِ شعاعی نمی‌تواند «به لَون رسیدم» را از «هنوز نرسیدم» یا «واردِ قلمروِ همسایه شدم» تفکیک کند. شاخه‌هایِ ناحیه‌ای بخشی از این را حل کردند (MAE واترشد ۱.۰۸ روی مواردی که فعال می‌شود) ولی فقط روی ۱۱ دیسک فعال می‌شوند.

### پ) ۳ دیسکِ خطا در تشخیص (۲ FN، ۱ FP)
`gt_07#6` و `gt_10#8` توسطِ **هر دو** شاخه از دست رفته‌اند (مشکلِ recall، نه اعتبارسنجی). موردِ FP یک تشخیصِ تکراری روی `gt_10` است.

### ت) حجمِ نمونه — محدودیتِ ریشه‌ای
۱۱ عکس برایِ تعدادِ تصمیم‌هایِ طراحیِ سنجیده‌شده کم است. اعتبارسنجیِ متقاطع روی هر تصمیمِ مقدارگزین اعمال شد و پیکربندیِ مستقر هیچ آستانه‌ی برازش‌شده ندارد، ولی **هر عکسِ اندازه‌گیری‌شده‌ی بیشتر از هر پالایشِ روش‌شناختیِ دیگری ارزشمندتر است.** ۷۸ عکسِ خام در ریپازیتوری هست که اندازه‌گیریِ کارشناسی ندارند.

### ث) شکاف‌های عنوانِ پروژه با پیاده‌سازی
- عنوان «یادگیری ماشین» دارد؛ پیاده‌سازی کاملاً کلاسیک و قطعی است (انتخابِ آگاهانه، باید در پایان‌نامه توجیه شود).
- عنوان «مطابق EUCAST» دارد؛ طبقه‌بندیِ S/I/R هنوز پیاده‌سازی نشده.

---

## ۳) نقشه‌ی راه — کارِ برنامه‌ریزی‌شده

### اولویتِ بالا

**۳.۱ — طبقه‌بندیِ S/I/R طبقِ EUCAST.** الزامِ عنوانِ مصوبِ پروژه. علاوه بر آن، تنها راهِ ترجمه‌ی «MAE=۳.۸۵mm» به زبانِ بالینی: «این خطا چند درصد از تصمیم‌هایِ حساس/مقاوم را عوض می‌کند؟» — پرسشی که داورِ پایان‌نامه حتماً می‌پرسد. جدول‌هایِ مرجع (`v_16.0_Breakpoint_Tables.pdf`) در ریپازیتوری موجودند.

**۳.۲ — نرخِ موفقیتِ تشخیصِ پتری روی هر ۷۸ عکس.** کد آماده است؛ دامنه‌ی ادعا را از ۱۱ عکسِ منتخب فراتر می‌برد.

**۳.۳ — تکرارپذیریِ اندازه‌گیریِ دستی.** دورِ دومِ اندازه‌گیریِ کارشناسی نشان می‌دهد خودِ مرجع چقدر قابلِ‌اتکاست — سقفِ بالایِ دقتی که سیستم اصلاً می‌تواند به آن برسد.

### چهار پیشنهادِ فنیِ جدید (تحلیل‌شده، پیاده‌سازی‌نشده)

#### پ۱) تشخیصِ دیسک با سگمنت‌کردنِ کمانِ مقیدبه‌مرکز

هر کاندیدِ Hough با نمونه‌برداریِ کمانی حولِ مرکزِ خودش اعتبارسنجی شود: چه کسری از محیط واقعاً لبه‌ی دایره‌ای نشان می‌دهد.

*ارزیابیِ صادقانه:* با ۳ خطا در ۹۳ دیسک، فضایِ بهبودِ مستقیم کم است. **ارزشِ واقعی‌اش ترکیبی است:** آستانه‌ی Hough را پایین بیاوریم تا دو دیسکِ ازدست‌رفته پیدا شوند، سپس اعتبارسنجیِ کمانی کاندیدهایِ کاذبِ حاصل را سرکوب کند. یعنی recall را بالا ببریم و precision را با این ماژول برگردانیم — نه صرفاً یک فیلترِ اضافی روی وضعِ فعلی.

#### پ۲) تطبیقِ بافتِ خودِ هاله برایِ پالایشِ مرز

از داخلِ هاله‌ای که با بالاترین اطمینان تشخیص داده شده یک مربعِ ۱mm برداشته شود و در نواری حولِ مرز (تا ۴mm بیرون و ۳mm داخل) حرکت کند؛ هرجا تطبیق بالا بود و در کمانی ≥۱۰۰ درجه پیوسته بود، مرز گسترش یابد (یا داخلِ مرز، حذف شود).

*نکته‌ی مهم از داده‌ی خودمان:* کانالِ بافتِ عمومی را قبلاً سنجیدیم و **رد شد** (هم‌پوشانیِ ۹۹–۱۰۰٪ توزیع‌ها روی `gt_11`). ولی این پیشنهاد ساختاراً متفاوت است و ممکن است جواب دهد: پرسشش «آیا این بافت لَون است یا هاله؟» (طبقه‌بندیِ عمومی) نیست، بلکه «آیا این ناحیه شبیهِ *هاله‌ی همین دیسک* است؟» — یک مقایسه‌ی خود-ارجاع و per-disk، که به‌مراتب حساس‌تر است. دقیقاً همان دلیلی که مرجعِ میدانِ دور خوب کار می‌کند.

*پیشنهادِ من دربارهٔ معیارِ تطبیق — و چرا کورلیشن انتخابِ بدی است:* داخلِ هاله تقریباً **همگن** است. همبستگیِ نرمال‌شده (NCC) وقتی هر دو تکه تقریباً یکنواخت‌اند از نظرِ عددی ناپایدار می‌شود، چون مخرجش (انحرافِ‌معیار) به صفر میل می‌کند و نتیجه عملاً نویز می‌شود. الگویِ *مکانی* در ناحیه‌ی همگن اطلاعاتی ندارد.
معیارِ درست، مقایسه‌ی **توزیع** است نه الگو. پیشنهادِ مشخصِ من، یک آزمونِ دو-مرجعی:
- توزیعِ مرجعِ «هاله» = پیکسل‌هایِ داخلِ هاله‌ی همان دیسک
- توزیعِ مرجعِ «لَون» = میدانِ دور (که از قبل داریم)
- برایِ هر پنجره‌ی آزمون: فاصله‌ی استانداردشده تا هر دو مرجع محاسبه و پنجره به نزدیک‌ترین نسبت داده شود.

این یک طبقه‌بندِ نسبت‌درست‌نمایی است، از یک آستانه‌ی مطلقِ شباهت به‌مراتب پایدارتر، و از زیرساختِ موجود (`_effect_size_vs_reference`) استفاده‌ی مجدد می‌کند. شرطِ کمانِ ≥۱۰۰ درجه عالی است و همان اصلِ پیوستگیِ زیستیِ ماژولِ ۱۶.۶ را ادامه می‌دهد.

*ریسک:* این ماژول چند پارامترِ جدید می‌آورد (اندازه‌ی مربع، پهنایِ نوار، آستانه‌ی تطبیق، آستانه‌ی کمان). با توجه به درسِ overfitting، هرکدام یا باید از پیش و بر مبنایِ فیزیک توجیه شوند یا با LOO اعتبارسنجی.

#### پ۳) همان مدلِ تطبیق برایِ کشفِ FP و FN دیسک

بافتِ دیسک‌هایِ پراطمینان به‌عنوانِ الگو، مقایسه با سایرِ دیسک‌ها (کشفِ FP) و با نواحیِ داخلِ پتری منهایِ دیسک‌ها (کشفِ FN).

*ارزیابی:* اینجا برخلافِ پ۲، تطبیقِ الگویی **مناسب است** — دیسک ساختارِ مکانیِ واقعی دارد (دایره‌ی روشن، متنِ تیره، لبه‌ی تیز)، نه یک ناحیه‌ی همگن. ولی یک دامِ مشخص دارد: دیسک‌ها برچسبِ چاپیِ متفاوتی دارند (CRO30، CIP، FM300...)، پس NCC خامِ بینِ دو دیسکِ متفاوت ضعیف خواهد بود.

*پیشنهادِ من:* تطبیق روی ویژگی‌هایِ **مستقل از برچسب و مستقل از چرخش** انجام شود، نه روی خودِ حروف:
- پروفایلِ شعاعیِ خودِ دیسک (میانگینِ شدت بر حسبِ شعاع، نرمال‌شده) — بینِ همه‌ی دیسک‌ها مشترک است
- بزرگیِ گرادیان در لبه‌ی دیسک (تیزیِ مرز)
- تقارنِ دورانی

برایِ کشفِ FN، جاروب‌کردنِ یک فیلترِ تطبیقی روی کلِ پتری در رزولوشنِ کامل گران است. راهِ ارزان‌تر و سازگارتر: آستانه‌ی Hough پایین آورده شود تا کاندیدهایِ بیشتری تولید کند، و این الگو نقشِ اعتبارسنج را بازی کند — که دقیقاً با پ۱ یکی می‌شود. **پ۱ و پ۳ باید یک ماژولِ واحد باشند، نه دو تا.**

#### پ۴) مدل‌سازیِ گذارِ شعاعی با تابعِ نمایی/لگاریتمی

*مبنایِ فیزیکی — و چرا این قوی‌ترینِ چهار پیشنهاد است:* آنتی‌بیوتیک از دیسک طبقِ قانونِ فیک نفوذ می‌کند و رشد جایی متوقف می‌شود که غلظت از MIC بگذرد. رابطه‌ی لگاریتمیِ بینِ قطرِ هاله و غلظت (کوپر–وودمن) پایه‌ی نظریِ کلاسیکِ همین آزمون است. یعنی این مدل‌سازی حدس نیست، از فیزیکِ خودِ پدیده می‌آید.

*پیشنهادِ اصلاحیِ من:* آنچه اندازه می‌گیریم غلظت نیست، **چگالیِ رشد** است. چگالیِ رشد بر حسبِ شعاع باید یک **سیگموئید** باشد (گذار از بدونِ‌رشد به لَونِ کامل)، نه نمایی یا لگاریتمیِ خالص. پس پیشنهاد می‌کنم یک تابعِ لجستیک به پروفایلِ شعاعی برازش شود با چهار پارامتر: دو مجانب (سطحِ هاله و سطحِ لَون)، مرکزِ گذار r₀، و پهنایِ گذار w.

سه چیز از این برازش بیرون می‌آید که هیچ‌کدام را الان نداریم:
1. **مرزِ پیوسته** — به‌جایِ شعاعِ کوانتیزه‌شده به عرضِ حلقه، یک عددِ پیوسته.
2. **معیارِ اطمینانِ درون‌ذاتی** — نسبتِ w/r₀. گذارِ تیز یعنی قابلِ‌اتکا؛ گذارِ بسیار پهن یعنی احتمالاً اصلاً هاله‌ای نیست. **این ممکن است دقیقاً همان چیزی باشد که ۱۴ مثبتِ کاذب را جدا می‌کند** — و چون یک پارامترِ برازش‌شده‌ی فیزیکی است نه یک آستانه‌ی انتخاب‌شده، ریسکِ overfittingش کمتر است.
3. **اعتبارسنجیِ متقاطعِ رایگان** — دو مجانبِ برازش باید با سطحِ لَونِ میدانِ دور بخوانند؛ نخواندنشان یعنی برازش بی‌اعتبار است.

*و یک نکته‌ی ظریف که تعریفِ خودِ شما را دقیق‌تر می‌کند:* «مرز جایی است که رشد کاملاً غایب است» **نقطه‌ی عطفِ سیگموئید نیست** (آن نقطه‌ی میانیِ گذار است — همان چیزی که شما رد کردید). مرزِ منطبق بر تعریفِ شما، شعاعی است که منحنیِ برازش‌شده از مجانبِ هاله *جدا* می‌شود — مثلاً جایی که به ۵٪ راهِ لَون رسیده. برازشِ سیگموئید این را به‌صورتِ یک عددِ دقیق و پیوسته می‌دهد.

### اولویت‌بندیِ پیشنهادیِ من

بر مبنایِ اینکه خطایِ غالب کجاست (اندازه‌گیریِ هاله و ۱۴ FP، نه تشخیصِ دیسک با ۳ خطا):

۱. **پ۴ اول** — ارزان‌ترین آزمون است (تحلیلِ پروفایل‌هایی که از قبل محاسبه می‌شوند، بدونِ ماژولِ جدید)، بیشترین مبنایِ فیزیکی را دارد، و هم‌زمان به هر دو مشکلِ اصلی می‌خورد: دقتِ مرز و تفکیکِ مثبت‌های کاذب.
۲. **پ۲ دوم** — مستقیماً دقتِ مرز را هدف می‌گیرد و از زیرساختِ مرجعِ دو-توزیعی استفاده‌ی مجدد می‌کند.
۳. **پ۱+پ۳ با هم و آخر** — یک ماژولِ واحدِ اعتبارسنجیِ دیسک، با هدفِ «recall را بالا ببر، precision را با اعتبارسنجی برگردان». فضایِ بهبودش کم است ولی ارزان است.

**قاعده‌ی مشترک برایِ هر چهار مورد:** هیچ‌کدام تا سنجشِ کمّی روی هر ۱۱ عکس واردِ مسیرِ اصلی نمی‌شوند، و هر آستانه‌ای که از داده انتخاب شود با leave-one-out اعتبارسنجی می‌گردد.
