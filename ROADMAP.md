# وضعیتِ فعلی، مشکلاتِ باز، و نقشه‌ی راه

> این سند «کجا هستیم و بعد چه می‌کنیم» را نگه می‌دارد. تاریخچه‌ی کاملِ تصمیم‌ها در `PROJECT_HISTORY.md` و اعدادِ ارزیابی در `EVALUATION.md` است.

آخرین به‌روزرسانی: پس از استقرارِ معماریِ چندشاخه‌ایِ هاله (commit `715ad56`).

---

## ۱) وضعیتِ فعلی — اعدادِ سنجیده‌شده

روی ۱۱ عکسِ دارایِ اندازه‌گیریِ کارشناسی، ۹۳ دیسک:

| معیار | مقدار |
|---|---|
| تشخیصِ پتری | ۱۱/۱۱ (۱۰۰٪) |
| تشخیصِ دیسک | P=**۱.۰۰۰**  R=۰.۹۷۸  F1=**۰.۹۸۹** (۰ FP، ۲ FN) — پس از ماژولِ ۱۱.۵ |
| حضورِ هاله | TP=۶۰  FN=۳  FP=**۱۳**  TN=**۱۵** — دقت **۰.۸۲۴** |
| قطرِ هاله | MAE=۳.۸۵mm، میانه=۲.۰۸mm، Bias=۰.۶۱−، BA۹۵٪=[۱۲.۳۳−, ۱۱.۱۱+] |
| دسته‌ی بالینی EUCAST | CA=**۸۳.۲٪**، VME=**۶.۲٪**، ME=۹.۲٪ (سقفِ انسانی: CA≈۹۲–۹۶٪) |
| در باندِ ±۲mm کارشناس | ۴۸٪ اندازه‌گیری‌ها (واترشد ۹۱٪، Otsu ۵۰٪، شعاعی ۳۳٪) |
| قطرِ دیسک | MAE=۰.۱۸mm (بررسیِ خودسازگاریِ کالیبراسیون، نه اعتبارسنجیِ مستقل) |

**معماریِ مستقر:** Hough (۱۱) → اعتبارسنجیِ شکل (۱۱.۵) → Fusion (۱۴) → بومِ آگار (۱۵.۵) → شاخه‌هایِ موازیِ هاله → ادغام (۱۶.۷) → طبقه‌بندیِ EUCAST (۱۸).

| ماژول | شاخه | در زنجیره؟ | MAE مستقل |
|---|---|---|---|
| ۱۶ (+۱۶.۵/۱۶.۶) | شعاعی | ✅ (پیش‌فرضِ پوشش) | ۵.۴۹ mm |
| ۱۵.۶ | Otsu | ✅ (صدکِ ۹۰) | ۳.۶۶ mm |
| ۱۵.۷ | Watershed | ✅ (اولویتِ اول) | ۱.۰۸ mm |
| ۱۵.۸ | آماری | ❌ سنجیده، اثرش از نویز جدا نشد | ۵.۰۲ mm |
| ۱۵.۹ | مدلِ رشد (لجستیک) | ❌ سنجیده، با واترشد هم‌پوشان | ۱.۱۷ mm |
| — | بافت/اسپکل | ❌ حذف شد (هم‌پوشانیِ ۹۹٪ توزیع‌ها) | — |

---

## ۲) مشکلاتِ بازِ شناخته‌شده

### الف) ۱۳ مثبتِ کاذبِ حضورِ هاله — بدونِ راه‌حلِ اعتبارسنجی‌شده
دیسک‌هایی که کارشناس «بدونِ هاله» ثبت کرده ولی سیستم هاله گزارش می‌دهد. تنها سازوکاری که آزمودیم (گیتِ کنتراست روی تشخیص‌هایِ شعاعیِ ضعیف) در اعتبارسنجیِ leave-one-out **رد شد** — درون‌نمونه‌ای خوب بود ولی زیرِ LOO بدتر از هیچ‌کاری‌نکردن درآمد. جایگزینی پیدا نشده.

### ب) MAE=۳.۸۵mm هنوز بالاست — و **سقفش اندازه‌گیری شده**
کاتالوگِ خطاها (`ground_truth/diagnostics/halo_failure_catalogue.csv`) خطایِ باقی‌مانده را عمدتاً به این نسبت می‌دهد که پروفایلِ یک‌بعدیِ شعاعی نمی‌تواند «به لَون رسیدم» را از «هنوز نرسیدم» یا «واردِ قلمروِ همسایه شدم» تفکیک کند. شاخه‌هایِ ناحیه‌ای بخشی از این را حل کردند (MAE واترشد ۱.۰۸ روی مواردی که فعال می‌شود) ولی فقط روی ۱۱ دیسک فعال می‌شوند.

**علتِ ریشه‌ای حالا سنجیده شده و ساختاری است:** روی زیرمجموعه‌ی بحرانی (۳۳ دیسک که کلِ خطا آن‌جاست)، شدتِ متوسطِ داخلِ هاله با لَون فقط **۰.۳σ** فرق دارد — یعنی زیرِ نویزِ فضاییِ خودِ لَون. این عدد با دادنِ مکانِ درستِ نمونه‌گیری از رویِ داده‌ی مرجع هم بهتر نمی‌شود (۰.۲۹σ). مرز در تصویر حاضر نیست، پس **هیچ روشِ مبتنی بر شدت** — از جمله هر شاخه‌ی جدیدی که بسازیم — نمی‌تواند آن را پیدا کند. جزئیات و کنترل‌ها در بندِ «پ۲ — آزمونِ پیش‌فرض» پایین‌تر. راهِ باز، تغییرِ **شرایطِ تصویربرداری** است (بندِ ۵.۳)، نه الگوریتمِ بهتر.

### پ) ۲ دیسکِ خطا در تشخیص (۲ FN، ۰ FP) — بخشی حل شد
موردِ FP با ماژولِ ۱۱.۵ (اعتبارسنجیِ شکل) حذف شد؛ precision حالا ۱.۰۰۰ و F1 برابرِ ۰.۹۸۹ است.

دو FN باقی مانده‌اند و **علتشان یکی نیست**:
- `gt_10#8` — Hough در مکانِ درست دایره می‌دهد ولی با شعاعِ ۹۰ در برابرِ ۳۷–۴۳ دیسک‌هایِ واقعی؛ یعنی رویِ **هاله** قفل شده نه دیسک. پایین آوردنِ آستانه این را حل نمی‌کند (سنجیده شد).
- `gt_07#6` — با آستانه‌ی سهل‌گیرتر پیدا می‌شود، ولی همان آستانه دو دیسکِ دیگر را در عکس‌هایِ دیگر خراب می‌کند، پس خالص منفی است.

راهِ باقی‌مانده برایِ `gt_10#8`: کاندیدسازِ دومی که ذاتاً به شعاعِ لبه‌ی قوی وابسته نباشد — خارج از دامنه‌ی این مرحله.

### ث) نویزِ خودِ مرجع — ±۲ میلی‌متر (**تازه معلوم شد**)
کارشناسِ آزمایشگاه اعلام کرد اندازه‌گیریِ کارشناسانِ آن‌ها «از همه لحاظ ±۲ میلی‌متر تغییر دارد». این خطایِ ما را توجیه نمی‌کند (تصحیحِ واریانس فقط ۳.۸۵ را به ۳.۶۳–۳.۸۰ می‌برد)، ولی **سقفِ قابلِ‌اثبات** را تعیین می‌کند: توافقِ انسان-انسان فقط CA≈۹۲–۹۶٪ است، و هر دقتی زیرِ ~۱.۲mm با این مرجع قابلِ نمایش نیست. یعنی مسیرِ بهبود از جایی به بعد به **مرجعِ بهتر** نیاز دارد، نه الگوریتمِ بهتر — دقیقاً مثلِ بندِ (ب) که به تصویربرداریِ بهتر نیاز دارد.

### ت) حجمِ نمونه — محدودیتِ ریشه‌ای
۱۱ عکس برایِ تعدادِ تصمیم‌هایِ طراحیِ سنجیده‌شده کم است. اعتبارسنجیِ متقاطع روی هر تصمیمِ مقدارگزین اعمال شد و پیکربندیِ مستقر هیچ آستانه‌ی برازش‌شده ندارد، ولی **هر عکسِ اندازه‌گیری‌شده‌ی بیشتر از هر پالایشِ روش‌شناختیِ دیگری ارزشمندتر است.** ۷۸ عکسِ خام در ریپازیتوری هست که اندازه‌گیریِ کارشناسی ندارند.

### ث) شکاف‌های عنوانِ پروژه با پیاده‌سازی
- عنوان «یادگیری ماشین» دارد؛ پیاده‌سازی کاملاً کلاسیک و قطعی است (انتخابِ آگاهانه، باید در پایان‌نامه توجیه شود).
- عنوان «مطابق EUCAST» دارد؛ طبقه‌بندیِ S/I/R هنوز پیاده‌سازی نشده.

---

## ۳) نقشه‌ی راه — کارِ برنامه‌ریزی‌شده

### اولویتِ بالا

**۳.۱ — طبقه‌بندیِ S/I/R طبقِ EUCAST.** ✅ **انجام شد.** پایگاهِ نقاطِ شکست از PDF رسمیِ EUCAST v16.0 استخراج شد (۱۱۱۶ سطر، ۲۷ گروهِ ارگانیسم، ۳۴۹ نقطه‌ی شکستِ عددیِ قابلِ‌استفاده) و ماژولِ ۱۸ به نوت‌بوک اضافه شد. مهم‌تر از خودِ ماژول، **ترجمه‌ی خطا به زبانِ بالینی** انجام شد و نتیجه‌اش تعیین‌کننده است:

| معیار (ISO 20776-2) | مقدار | سقفِ پذیرش | وضعیت |
|---|---:|---:|---|
| توافقِ دسته‌ای (CA) | ۸۲.۵٪ | — | — |
| خطایِ بسیار عمده (VME) | ۶.۹٪ | ۱.۵٪ | ❌ رد |
| خطایِ عمده (ME) | ۹.۲٪ | ۳.۰٪ | ❌ رد |

**سیستم در وضعِ فعلی به آستانه‌ی پذیرشِ بالینی نمی‌رسد** و برایِ رسیدن، MAE باید از ۳.۸۵ به حدودِ **۱.۰mm** برسد. این عدد از این پس معیارِ پذیرشِ هر ایده‌ی بعدی است، به‌جایِ «هرچه کمتر بهتر». جزئیات و اسکریپت‌هایِ بازتولید: `ground_truth/eucast/`.

**۳.۲ — نرخِ موفقیتِ تشخیصِ پتری روی هر ۷۸ عکس.** کد آماده است؛ دامنه‌ی ادعا را از ۱۱ عکسِ منتخب فراتر می‌برد.

**۳.۳ — تکرارپذیریِ اندازه‌گیریِ دستی.** ✅ **از خودِ کارشناس گرفته شد: ±۲ میلی‌متر.** کارشناسِ آزمایشگاه اعلام کرد اندازه‌گیریِ کارشناسانِ آن‌ها «از همه لحاظ ±۲ میلی‌متر تغییر دارد». این عدد سه چیز را عوض کرد (`ground_truth/eucast/expert_repeatability.py`):

۱. **خطایِ ما را توجیه نمی‌کند.** با کسرِ واریانسِ مرجع، MAE از ۳.۸۵ فقط به ۳.۶۳–۳.۸۰ می‌رسد. بیشترِ خطا مالِ خودِ سیستم است.

۲. **ولی سقفِ منصفانه ۱۰۰٪ نیست.** شبیه‌سازیِ کارشناسِ دوم (همان خوانش + نویزِ ±۲mm) نشان می‌دهد توافقِ **انسان با انسان** فقط CA=۹۲–۹۶٪ و VME=۱.۷–۳.۳٪ است. یعنی با ±۲mm، حتی دو کارشناسِ انسانی هم سقفِ ISO (VME ۱.۵٪) را برآورده نمی‌کنند. آن سقف‌ها برایِ مقایسه با MICِ مرجع طراحی شده‌اند، نه با خوانشِ کولیسیِ کارشناس.

۳. **هدفِ ۱.۰mm اثبات‌ناپذیر است.** آن هدف **زیرِ** تکرارپذیریِ انسانی (۱.۱۶–۲.۲۶mm) قرار می‌گیرد؛ دست‌یافتنی هست (دستگاه می‌تواند از انسان باثبات‌تر باشد) ولی با این مرجع قابلِ نشان دادن نیست. برایِ اثباتش مرجعِ بهتری لازم است: میانگینِ چند خوانشِ مستقل رویِ همان پلیت، یا مرجعِ MIC از رقتِ براث.

**هدفِ عملیِ جایگزین:** رساندنِ MAE به باندِ ±۲mm انسانی. الان **۴۸٪** اندازه‌گیری‌ها آن‌جا هستند و میانه‌ی خطا ۲.۰۸mm است — یعنی عملاً رویِ لبه‌ی باند.

### چهار پیشنهادِ فنیِ جدید (تحلیل‌شده، پیاده‌سازی‌نشده)

#### پ۱) تشخیصِ دیسک با سگمنت‌کردنِ کمانِ مقیدبه‌مرکز

هر کاندیدِ Hough با نمونه‌برداریِ کمانی حولِ مرکزِ خودش اعتبارسنجی شود: چه کسری از محیط واقعاً لبه‌ی دایره‌ای نشان می‌دهد.

*ارزیابیِ صادقانه:* با ۳ خطا در ۹۳ دیسک، فضایِ بهبودِ مستقیم کم است. **ارزشِ واقعی‌اش ترکیبی است:** آستانه‌ی Hough را پایین بیاوریم تا دو دیسکِ ازدست‌رفته پیدا شوند، سپس اعتبارسنجیِ کمانی کاندیدهایِ کاذبِ حاصل را سرکوب کند. یعنی recall را بالا ببریم و precision را با این ماژول برگردانیم — نه صرفاً یک فیلترِ اضافی روی وضعِ فعلی.

#### پ۲) تطبیقِ بافتِ خودِ هاله برایِ پالایشِ مرز

از داخلِ هاله‌ای که با بالاترین اطمینان تشخیص داده شده یک مربعِ ۱mm برداشته شود و در نواری حولِ مرز (تا ۴mm بیرون و ۳mm داخل) حرکت کند؛ هرجا تطبیق بالا بود و در کمانی ≥۱۰۰ درجه پیوسته بود، مرز گسترش یابد (یا داخلِ مرز، حذف شود).

*نکته‌ی مهم از داده‌ی خودمان:* کانالِ بافتِ عمومی را قبلاً سنجیدیم و **رد شد** (هم‌پوشانیِ ۹۹–۱۰۰٪ توزیع‌ها روی `gt_11`). ولی این پیشنهاد ساختاراً متفاوت است و ممکن است جواب دهد: پرسشش «آیا این بافت لَون است یا هاله؟» (طبقه‌بندیِ عمومی) نیست، بلکه «آیا این ناحیه شبیهِ *هاله‌ی همین دیسک* است؟» — یک مقایسه‌ی خود-ارجاع و per-disk، که به‌مراتب حساس‌تر است. دقیقاً همان دلیلی که مرجعِ میدانِ دور خوب کار می‌کند.

*پیشنهادِ من دربارهٔ معیارِ تطبیق — و چرا کورلیشن انتخابِ بدی است:* داخلِ هاله تقریباً **همگن** است. همبستگیِ نرمال‌شده (NCC) وقتی هر دو تکه تقریباً یکنواخت‌اند از نظرِ عددی ناپایدار می‌شود، چون مخرجش (انحرافِ‌معیار) به صفر میل می‌کند و نتیجه عملاً نویز می‌شود. الگویِ *مکانی* در ناحیه‌ی همگن اطلاعاتی ندارد.
معیارِ درست، مقایسه‌ی **توزیع** است نه الگو. پیشنهادِ مشخصِ من، یک آزمونِ دو-مرجعی:
- توزیعِ مرجعِ «هاله» = پیکسل‌هایِ داخلِ هاله‌ی همان دیسک
- توزیعِ مرجعِ «لَون» = میدانِ دور (که از قبل داریم)
- برایِ هر پنجره‌ی آزمون: فاصله‌ی استانداردشده تا هر دو مرجع محاسبه و پنجره به نزدیک‌ترین نسبت داده شود.

این یک طبقه‌بندِ نسبت‌درست‌نمایی است، از یک آستانه‌ی مطلقِ شباهت به‌مراتب پایدارتر، و از زیرساختِ موجود (`_effect_size_vs_reference`) استفاده‌ی مجدد می‌کند. شرطِ کمانِ ≥۱۰۰ درجه عالی است و همان اصلِ پیوستگیِ زیستیِ ماژولِ ۱۶.۶ را ادامه می‌دهد.

*ریسک:* این ماژول چند پارامترِ جدید می‌آورد (اندازه‌ی مربع، پهنایِ نوار، آستانه‌ی تطبیق، آستانه‌ی کمان). با توجه به درسِ overfitting، هرکدام یا باید از پیش و بر مبنایِ فیزیک توجیه شوند یا با LOO اعتبارسنجی.

> ⛔ **وضعیت: آزموده و متوقف‌شده.** قاعده‌ی دو-مرجعی (بدونِ هیچ پارامترِ آزاد) پیاده و روی هر ۹۱ دیسک سنجیده شد. روی ۳۱ از ۳۳ دیسکِ بحرانی اصلاً شلیک نمی‌کند، چون **جدایشِ داخلِ هاله از لَون آن‌جا ۰.۳σ است** — یعنی ورودیِ لازمِ این روش در تصویر وجود ندارد. جزئیات و کنترل‌ها در بندِ «پ۲ — آزمونِ پیش‌فرض پیش از ساختِ ماژول» در بخشِ ۴. ایده بد نیست؛ داده‌اش موجود نیست.

#### پ۳) همان مدلِ تطبیق برایِ کشفِ FP و FN دیسک

بافتِ دیسک‌هایِ پراطمینان به‌عنوانِ الگو، مقایسه با سایرِ دیسک‌ها (کشفِ FP) و با نواحیِ داخلِ پتری منهایِ دیسک‌ها (کشفِ FN).

*ارزیابی:* اینجا برخلافِ پ۲، تطبیقِ الگویی **مناسب است** — دیسک ساختارِ مکانیِ واقعی دارد (دایره‌ی روشن، متنِ تیره، لبه‌ی تیز)، نه یک ناحیه‌ی همگن. ولی یک دامِ مشخص دارد: دیسک‌ها برچسبِ چاپیِ متفاوتی دارند (CRO30، CIP، FM300...)، پس NCC خامِ بینِ دو دیسکِ متفاوت ضعیف خواهد بود.

*پیشنهادِ من:* تطبیق روی ویژگی‌هایِ **مستقل از برچسب و مستقل از چرخش** انجام شود، نه روی خودِ حروف:
- پروفایلِ شعاعیِ خودِ دیسک (میانگینِ شدت بر حسبِ شعاع، نرمال‌شده) — بینِ همه‌ی دیسک‌ها مشترک است
- بزرگیِ گرادیان در لبه‌ی دیسک (تیزیِ مرز)
- تقارنِ دورانی

برایِ کشفِ FN، جاروب‌کردنِ یک فیلترِ تطبیقی روی کلِ پتری در رزولوشنِ کامل گران است. راهِ ارزان‌تر و سازگارتر: آستانه‌ی Hough پایین آورده شود تا کاندیدهایِ بیشتری تولید کند، و این الگو نقشِ اعتبارسنج را بازی کند — که دقیقاً با پ۱ یکی می‌شود. **پ۱ و پ۳ باید یک ماژولِ واحد باشند، نه دو تا.**

#### پ۴) مدل‌سازیِ گذارِ شعاعی با تابعِ نمایی/لگاریتمی

*مبنایِ فیزیکی — و چرا این قوی‌ترینِ چهار پیشنهاد است:* آنتی‌بیوتیک از دیسک طبقِ قانونِ فیک نفوذ می‌کند و رشد جایی متوقف می‌شود که غلظت از MIC بگذرد. رابطه‌ی لگاریتمیِ بینِ قطرِ هاله و غلظت (کوپر–وودمن) پایه‌ی نظریِ کلاسیکِ همین آزمون است. یعنی این مدل‌سازی حدس نیست، از فیزیکِ خودِ پدیده می‌آید.

*پیشنهادِ اصلاحیِ من:* آنچه اندازه می‌گیریم غلظت نیست، **چگالیِ رشد** است. چگالیِ رشد بر حسبِ شعاع باید یک **سیگموئید** باشد (گذار از بدونِ‌رشد به لَونِ کامل)، نه نمایی یا لگاریتمیِ خالص. پس پیشنهاد می‌کنم یک تابعِ لجستیک به پروفایلِ شعاعی برازش شود با چهار پارامتر: دو مجانب (سطحِ هاله و سطحِ لَون)، مرکزِ گذار r₀، و پهنایِ گذار w.

سه چیز از این برازش بیرون می‌آید که هیچ‌کدام را الان نداریم:
1. **مرزِ پیوسته** — به‌جایِ شعاعِ کوانتیزه‌شده به عرضِ حلقه، یک عددِ پیوسته.
2. **معیارِ اطمینانِ درون‌ذاتی** — نسبتِ w/r₀. گذارِ تیز یعنی قابلِ‌اتکا؛ گذارِ بسیار پهن یعنی احتمالاً اصلاً هاله‌ای نیست. **این ممکن است دقیقاً همان چیزی باشد که ۱۴ مثبتِ کاذب را جدا می‌کند** — و چون یک پارامترِ برازش‌شده‌ی فیزیکی است نه یک آستانه‌ی انتخاب‌شده، ریسکِ overfittingش کمتر است.
3. **اعتبارسنجیِ متقاطعِ رایگان** — دو مجانبِ برازش باید با سطحِ لَونِ میدانِ دور بخوانند؛ نخواندنشان یعنی برازش بی‌اعتبار است.

*و یک نکته‌ی ظریف که تعریفِ خودِ شما را دقیق‌تر می‌کند:* «مرز جایی است که رشد کاملاً غایب است» **نقطه‌ی عطفِ سیگموئید نیست** (آن نقطه‌ی میانیِ گذار است — همان چیزی که شما رد کردید). مرزِ منطبق بر تعریفِ شما، شعاعی است که منحنیِ برازش‌شده از مجانبِ هاله *جدا* می‌شود — مثلاً جایی که به ۵٪ راهِ لَون رسیده. برازشِ سیگموئید این را به‌صورتِ یک عددِ دقیق و پیوسته می‌دهد.

### اولویت‌بندیِ پیشنهادیِ من

بر مبنایِ اینکه خطایِ غالب کجاست (اندازه‌گیریِ هاله و ۱۴ FP، نه تشخیصِ دیسک با ۳ خطا):

۱. **پ۴ اول** — ارزان‌ترین آزمون است (تحلیلِ پروفایل‌هایی که از قبل محاسبه می‌شوند، بدونِ ماژولِ جدید)، بیشترین مبنایِ فیزیکی را دارد، و هم‌زمان به هر دو مشکلِ اصلی می‌خورد: دقتِ مرز و تفکیکِ مثبت‌های کاذب.
۲. **پ۲ دوم** — مستقیماً دقتِ مرز را هدف می‌گیرد و از زیرساختِ مرجعِ دو-توزیعی استفاده‌ی مجدد می‌کند.
۳. **پ۱+پ۳ با هم و آخر** — یک ماژولِ واحدِ اعتبارسنجیِ دیسک، با هدفِ «recall را بالا ببر، precision را با اعتبارسنجی برگردان». فضایِ بهبودش کم است ولی ارزان است.

**قاعده‌ی مشترک برایِ هر چهار مورد:** هیچ‌کدام تا سنجشِ کمّی روی هر ۱۱ عکس واردِ مسیرِ اصلی نمی‌شوند، و هر آستانه‌ای که از داده انتخاب شود با leave-one-out اعتبارسنجی می‌گردد.

### وضعیتِ نهاییِ چهار پیشنهاد (پس از اجرا)

| پیشنهاد | وضعیت | نتیجه |
|---|---|---|
| پ۴ — مدلِ سیگموئیدِ گذار | ✅ ساخته شد (ماژولِ ۱۵.۹) | دقیق ولی افزونه؛ واردِ زنجیره‌ی تصمیم نشد |
| پ۲ — تطبیقِ بافتِ هاله | ⛔ آزموده و متوقف | ورودیِ لازم در تصویر نیست (جدایش ۰.۳σ) |
| پ۱+پ۳ — اعتبارسنجیِ دیسک | ✅ نیمی پذیرفته | ماژولِ ۱۱.۵ ساخته شد و F1 را از ۰.۹۸۴ به ۰.۹۸۹ برد؛ ولی گامِ «پایین آوردنِ آستانه» رد شد |

### پ۱+پ۳ — ماژولِ ۱۱.۵ ساخته شد، ولی نیمِ دومِ نقشه رد شد

نقشه‌ی اصلی دو گام داشت: (۱) آستانه‌ی Hough را پایین بیاور تا دو دیسکِ ازدست‌رفته پیدا شوند، (۲) با اعتبارسنجیِ شکل، کاندیدهایِ کاذبِ حاصل را سرکوب کن. هر دو گام سنجیده شد و نتیجه **نامتقارن** بود.

#### تشخیصی که فرضِ گامِ ۱ را عوض کرد

اول فرض کردم هر دو FN «مشکلِ آستانه» دارند. اندازه‌گیری نشان داد این‌طور نیست: رویِ `gt_10` دیسکِ ۸، Hough در همان آستانه‌ی فعلی دایره تولید *می‌کند*، ولی با **شعاعِ ۹۰** در حالی که دیسک‌هایِ واقعیِ همان عکس شعاعِ ۳۷–۴۳ دارند — یعنی رویِ خودِ **هاله** قفل شده، نه رویِ دیسک. قاعده‌ی سازگاریِ شعاعِ ماژولِ ۱۴ به‌درستی ردش می‌کند.

> ⚠️ نسخه‌ی اولِ آزمونِ من فقط **مکان** را تطبیق می‌داد و به همین دلیل این مورد را «قابلِ بازیافت در همه‌ی آستانه‌ها» گزارش کرد — که گمراه‌کننده بود. با افزودنِ شرطِ شعاع، معلوم شد پایین آوردنِ آستانه این مورد را اصلاً حل نمی‌کند.

#### گامِ ۲ — ماژولِ ۱۱.۵، پذیرفته شد

پ۱ (کمانِ پیوسته) و پ۳ (الگویِ دیسک) در یک ماژول ادغام شدند، چون هر دو یک پرسش دارند: «آیا این دایره واقعاً یک دیسکِ کاغذی است؟» الگو رویِ حروف نیست (برچسب‌ها متفاوت و جهتشان تصادفی است) بلکه رویِ سه ویژگیِ **مستقل از برچسب و چرخش**: پیوستگیِ کمانیِ لبه، پله‌ی شعاعیِ لبه، و تقارنِ دورانی. مرجعِ تصمیم **اجماعِ کاندیدهایِ همان تصویر** است، نه عددِ سراسری.

| پیکربندی | TP | FP | FN | Precision | Recall | F1 |
|---|---:|---:|---:|---:|---:|---:|
| خطِ پایه (بدونِ ۱۱.۵) | ۹۰ | ۱ | ۲ | ۰.۹۸۹ | ۰.۹۷۸ | ۰.۹۸۴ |
| **+ ماژولِ ۱۱.۵** | ۹۱ | **۰** | ۲ | **۱.۰۰۰** | ۰.۹۷۸ | **۰.۹۸۹** |
| + ۱۱.۵ + آستانه‌ی ۱۱ | ۹۰ | ۰ | ۳ | ۱.۰۰۰ | ۰.۹۶۸ | ۰.۹۸۴ |

ماژولِ ۱۱.۵ تنها مثبتِ کاذبِ باقی‌مانده را حذف می‌کند و **هیچ دیسکِ واقعی‌ای را از دست نمی‌دهد**. هیچ پارامترِ انتخاب‌شده از داده هم ندارد: کفِ پوششِ کمانیِ ۰.۷۵ از هندسه می‌آید (یک دیسکِ واقعی ممکن است تا ~یک‌چهارمِ محیطش با همسایه یا لبه‌ی ظرف هم‌پوشانی داشته باشد) و ۳σ همان سطحِ استانداردِ ماژولِ ۱۶.۵ است.

#### گامِ ۱ — رد شد

جاروبِ آستانه رویِ هر ۱۱ عکس:

| param2 | پوششِ دیسکِ واقعی | کاندیدِ کاذبِ خام | FN بازیافته |
|---:|---:|---:|---:|
| ۲۰ (فعلی) | ۹۱/۹۳ | ۵ | ۰/۲ |
| ۱۶ | ۹۳/۹۳ | ۱۶ | ۱/۲ |
| ۱۳ | **۹۰**/۹۳ | ۲۴ | ۱/۲ |
| ۱۱ | ۹۳/۹۳ | ۳۷ | ۲/۲ |

**رفتار یکنوا نیست** — پوشش از ۲۰ به ۱۶ بالا می‌رود، در ۱۳ *پایین* می‌آید، و در ۱۱ دوباره بالا. یعنی آستانه یک دستگیره‌ی تمیزِ recall نیست.

و آزمونِ end-to-end با `param2=11` این را تایید کرد: `gt_07` درست شد ولی `gt_08` و `gt_11` **خراب شدند** و F1 به ۰.۹۸۴ برگشت. علتش روشن است: با شلوغ شدنِ انباشتگرِ Hough، سرکوبِ `minDist` خودش شروع به حذفِ دیسک‌هایِ *واقعی* می‌کند و مرحله‌ی اعتبارسنجی هم به‌جایِ ۱، هفت دیسکِ واقعی را رد می‌کند.

**پس فرضِ نقشه‌ی راه غلط بود:** پایین آوردنِ آستانه، precision را با recall معاوضه نمی‌کند؛ recall را با recall معاوضه می‌کند. آستانه در ۲۰ دست‌نخورده ماند.

**درسِ مشترکِ این چهار آزمون:** سه بار شاخه‌ی جدید ساختیم و بعد فهمیدیم افزونه است؛ بارِ چهارم اول پیش‌فرض را سنجیدیم و در چند دقیقه فهمیدیم نباید ساخته شود. **سنجشِ پیش‌فرض پیش از پیاده‌سازی، الگویِ درست است** و برای هر پیشنهادِ بعدی هم باید همین باشد.

---

## ۴) برنامه‌ی اجراییِ پ۴ (در حالِ انجام)

تاییدشده با کاربر، به‌همراهِ اصلاحِ سیگموئید. مسیرِ اجرا عمداً از «مطالعه‌ی امکان‌سنجیِ آفلاین» شروع می‌شود، نه از ساختنِ ماژول — چون اگر سیگموئید اصلاً به این پروفایل‌ها برازش نشود، ساختنِ ماژول اتلافِ وقت است.

### گامِ ۱ — استخراجِ پروفایل‌ها (یک اجرا)
برایِ هر دیسک ذخیره می‌شود: پروفایلِ شعاعیِ کاملِ رویِ **بومِ آگار** (نه تصویرِ خام)، با حذفِ جهت‌آگاهِ قلمروِ همسایه، و مهم‌تر — **تا سقفِ فیزیکیِ لبه‌ی ظرف، نه تا پنجره‌ی تطبیقیِ ماژولِ ۱۶**. دلیل: برایِ برازشِ سیگموئید باید *هر دو مجانب* دیده شوند؛ پنجره‌ی فعلی گاهی حتی به مجانبِ بیرونی نمی‌رسد (همان سازوکارِ کم‌برآوردها). کنارِ آن: میانگین/انحرافِ‌معیارِ لَونِ میدانِ دور، `px_per_mm`، شعاعِ دیسک، و مقدارِ کارشناس.

### گامِ ۲ — برازشِ آفلاین و آزمونِ سه فرضیه
مدل: `y(r) = A + (B − A) / (1 + exp(−(r − r₀)/w))`

**ساده‌سازیِ مهم:** `B` (مجانبِ لَون) لازم نیست پارامترِ آزاد باشد — از قبل و **مستقل** اندازه‌گیری شده: میانگینِ میدانِ دور. `A` هم از میانه‌ی چند حلقه‌ی داخلی می‌آید. پس فقط دو پارامترِ آزاد می‌ماند (`r₀`, `w`). کمترین پارامترِ آزاد یعنی پایدارترین برازش و کمترین ریسکِ overfitting — و یک اعتبارسنجیِ رایگان: اگر `A` برازش‌شده با سطحِ لَون یکی درآید، یعنی اصلاً هاله‌ای وجود ندارد.

سه پرسشی که این مطالعه باید جواب دهد:
1. **آیا اصلاً برازش می‌شود؟** — R² رویِ دیسک‌هایِ دارایِ هاله‌ی تاییدشده.
2. **آیا مرزِ حاصل دقیق‌تر است؟** — مرز طبقِ تعریفِ کاربر (نقطه‌ی *جدا شدن* از مجانبِ هاله، نه نقطه‌ی عطف) در برابرِ مقدارِ کارشناس، مقایسه با MAE=۳.۸۵ فعلی.
3. **آیا `w/r₀` مثبت‌هایِ کاذب را جدا می‌کند؟** — توزیعِ این نسبت رویِ ۶۰ موردِ واقعی در برابرِ ۱۴ موردِ کاذب. اگر جدا کند، اولین سازوکارِ اعتبارسنجی‌شده برایِ مشکلِ باز (الف) خواهد بود.

### گامِ ۳ — تصمیم
ماژول فقط در صورتی ساخته می‌شود که گامِ ۲ جوابِ مثبت بدهد؛ و مثلِ بقیه، تا سنجشِ کامل رویِ ۱۱ عکس واردِ مسیرِ اصلی نمی‌شود. هر آستانه‌ای که از داده انتخاب شود با LOO اعتبارسنجی می‌گردد.

### قیدِ مهندسی: بدونِ scipy
`scipy` در محیط نصب نیست و **عمداً هم نصب نمی‌شود**: پایپلاین تا امروز فقط به `cv2`/`numpy` وابسته است و یکی از الزاماتِ مستندِ پروژه، قابلیتِ انتقال به C++ است. افزودنِ یک وابستگیِ سنگین برایِ یک برازشِ دوپارامتری هزینه‌ی بی‌موردی است. برازش با جست‌وجویِ شبکه‌ای رویِ `(r₀, w)` و پالایشِ محلی، فقط با numpy، پیاده می‌شود — که هم قطعی است (بدونِ وابستگی به نقطه‌ی شروع یا الگوریتمِ بهینه‌سازی) و هم مستقیماً به C++ ترجمه می‌شود.

### نتیجه‌ی مطالعه‌ی امکان‌سنجیِ پ۴ (گامِ ۲ انجام شد)

**پرسشِ ۱ — آیا سیگموئید برازش می‌شود؟** تا حدی. R² میانه ۰.۵۱ رویِ دیسک‌هایِ دارایِ هاله — نه عالی، ولی کافی.

**پرسشِ ۲ — آیا مرز دقیق‌تر است؟ بله، و به‌طورِ معنادار — ولی نه در نقطه‌ای که انتظار داشتیم.**

مقایسه‌ی *جفتی* رویِ ۵۷ دیسکی که هر دو روش عدد داده‌اند (آزمونِ درست، چون همان دیسک‌ها با هم مقایسه می‌شوند):

| | ادغامِ فعلی | سیگموئید (نقطه‌ی عطف) |
|---|---:|---:|
| میانه‌ی قدرِمطلقِ خطا | ۲.۱۰ mm | **۱.۰۳ mm** |
| MAE | ۳.۷۸ mm | ۲.۷۹ mm |
| برنده در چند مورد | ۱۹ | **۳۸** |

آزمونِ علامت: z=+۲.۵۲ (معنادار). یعنی سیگموئید **خطایِ معمول را نصف می‌کند**. اختلافِ *میانگین* (۰.۹۹mm) از نظرِ آماری معنادار نیست (t=۰.۹۱) — ولی دلیلش دقیقاً دو موردِ فاجعه‌بار است که هردو قابلِ‌تشخیص‌اند: یکی قطرِ **منفی** داد (−۵.۶mm، آشکارا نامعتبر) و دیگری ۸۰.۵mm با R²≈۰ (برازشِ تباه). یک نگهبانِ اعتبارسنجیِ ساده هردو را حذف می‌کند.

**یافته‌ای که با تعریفِ اولیه در تضاد است — و باید صادقانه گفته شود:** «نقطه‌ی جدا شدن از مجانبِ هاله» (تفسیرِ ما از تعریفِ کاربر) MAE=۲۰.۴۴mm با بایاسِ ۱۸.۲۶− داد — بسیار بدتر. آن‌چه بهتر جواب می‌دهد دقیقاً **نقطه‌ی عطف** است، یعنی همان نقطه‌ی میانیِ گذار که کاربر صراحتاً رد کرده بود.

توضیحِ محتملِ این تضاد: مرجعِ ما «حقیقتِ زیستی» نیست، **خوانشِ کارشناس با کولیس رویِ عکس** است. ادراکِ انسانی از یک لبه معمولاً نزدیکِ تیزترین نقطه‌ی گذار می‌نشیند، نه در آغازِ جدا شدن. پس این نتیجه می‌گوید ما داریم *اندازه‌گیریِ کارشناس* را مدل می‌کنیم، نه نقطه‌ی زیستیِ توقفِ کاملِ رشد. این دو لزوماً یکی نیستند — و تفاوتشان (~۹mm در شعاع) قابلِ‌توجه است.

**پرسشِ ۳ — آیا w/r₀ مثبت‌هایِ کاذب را جدا می‌کند؟ نه.** Cohen d = ۰.۲۸−؛ دامنه/انحرافِ‌لَون ۰.۳۸−؛ R² برازش ۰.۰۶−. هیچ‌کدام تفکیک نمی‌کنند. **این فرضیه رد شد** و مشکلِ باز (الف) همچنان بدونِ راه‌حل است.

**کرانِ بالایِ اوراکل:** اگر برایِ هر دیسک بهترینِ دو روش انتخاب می‌شد، MAE=۱.۳۷mm. فاصله‌ی زیادِ این عدد با هر دو روش یعنی این دو رویِ دیسک‌هایِ *متفاوتی* شکست می‌خورند — همان الگویی که ادغامِ شاخه‌ها را ارزشمند کرد.

**تصمیم:** پ۴ ارزشِ ساختن دارد، به‌عنوانِ شاخه‌ی پنجم با نگهبانِ اعتبارسنجی (ردِ قطرِ نامعتبر و برازشِ تباه) — نه به‌عنوانِ جایگزینِ کلِ زنجیره.

### صورت‌بندیِ LCCDE — پیشنهادِ کاربر، بررسی‌شده

**پرسش:** آیا می‌شود مسئله را مثلِ یک معادله‌ی دیفرانسیلِ خطیِ ضریب‌ثابت دید، که فرمولِ فعلی حکمِ *پاسخِ گذرا* را داشته باشد و عبارتی جدا *جوابِ حالتِ دائمی* را بسازد — و فاصله‌ی خوانشِ کارشناس تا حقیقتِ زیستی در آن عبارت جا بگیرد؟

#### نگاشتِ ریاضی — تا کجا درست است

**۱) مدلِ فعلی از قبل ساختارِ گذرا+دائمی دارد.** لجستیک را می‌توان بازنویسی کرد:

```
y(r) = B − (B−A)·σ(−(r−r₀)/w)
       ↑ حالتِ دائمی   ↑ جزءِ گذرا (با دور شدن از دیسک میرا می‌شود)
```

`B` (سطحِ لَون) همان جوابِ حالتِ دائمی است — و در پیاده‌سازیِ ما *مستقلاً* از میدانِ دور اندازه‌گیری می‌شود، نه برازش. پس شهودِ کاربر درست است و از قبل در مدل حضور دارد.

**۲) LCCDE فقط در مختصاتِ لگاریتمی-شعاعی برقرار است.** معادله‌ی نفوذ در حالتِ دائمی:

```
(1/r)·d/dr(r·dC/dr) = 0   →   r·C″ + C′ = 0
```

این ضریب‌ثابت **نیست** (آن `1/r` از هندسه‌ی شعاعی می‌آید؛ معادله از نوعِ اویلر–کوشی است). ولی با `u = ln(r)`:

```
d²C/du² = 0
```

که **دقیقاً ضریب‌ثابت است**. یعنی صورت‌بندیِ LCCDE از نظرِ ریاضی معتبر است — و همین‌جاست که رابطه‌ی لگاریتمیِ کلاسیکِ کوپر–وودمن ریشه دارد.

#### آزمونِ تجربی — و نتیجه‌ی منفیِ فرضیه‌ی (الف)

اگر این ساختار بر مشاهدات حاکم باشد، برازش در فضایِ `ln(r)` باید بهتر باشد. سنجیده شد (`ground_truth/diagnostics/p4_lccde_test.py`):

| | فضایِ `r` | فضایِ `ln(r)` |
|---|---:|---:|
| R² میانه | ۰.۵۵۲ | ۰.۵۵۱ |
| برازشِ بهتر در | ۶۲ از ۹۱ | ۲۹ از ۹۱ (z=۳.۴۶−) |
| MAE مرز | **۳.۴۰ mm** | ۴.۷۱ mm |

**فرضیه رد شد.** فضایِ لگاریتمی نه‌تنها کمک نمی‌کند، بلکه از نظرِ کیفیتِ برازش به‌طورِ معنادار بدتر است.

**چرا — و این نکته‌ی مفهومیِ مهمی است:** ساختارِ LCCDE بر *میدانِ غلظت* حاکم است، ولی آن‌چه ما مشاهده می‌کنیم غلظت نیست؛ **رشدِ آستانه‌خورده** است (`growth = H(MIC − C)` به‌علاوه‌ی تاریِ نوری). آستانه‌گذاری دقیقاً همان عملیاتِ *غیرخطی* است که چارچوبِ معادله‌ی خطی را می‌شکند. شکلِ سیگموئیدِ مشاهده‌شده را تیزیِ آستانه‌ی زیستی و تاریِ تصویربرداری تعیین می‌کند، نه هندسه‌ی نفوذ. پس ظرافتِ ریاضی به سودِ تجربی ترجمه نمی‌شود.

#### فرضیه‌ی (ب) — تایید شد، ولی نه به‌عنوانِ جمله‌ی تصحیح

اگر کارشناس روی نقطه‌ی عطف ببُرد و حقیقتِ زیستی نقطه‌ی جدا شدن باشد، خودِ لجستیک ایجاب می‌کند فاصله‌شان **ثابت نباشد بلکه متناسب با پهنایِ گذار** باشد:

```
δ = 2·k·w / ppm        با  k = ln(0.95/0.05) = ۲.۹۴۴
```

اندازه‌گیری‌شده: میانه‌ی δ = **۰.۵۹ mm**، و در **۸۹٪ موارد زیرِ ۲ میلی‌متر**؛ فقط در ۶٪ موارد بالایِ ۱۰ میلی‌متر (دنباله‌ی بسیار سنگین).

یعنی نگرانیِ «فاصله‌ی خوانشِ انسانی با زیست‌شناسی» برایِ اکثریتِ قاطعِ دیسک‌ها عملاً ناچیز است و فقط در موردهایِ لبه‌پهن معنا پیدا می‌کند — و همان‌ها مواردی‌اند که اندازه‌گیری‌شان به‌هرحال بی‌اعتماد است:

| | MAE مرز |
|---|---:|
| لبه‌هایِ تیز (δ زیرِ میانه) | **۲.۳۸ mm** |
| لبه‌هایِ پهن (δ بالایِ میانه) | ۴.۳۸ mm |

**نتیجه‌ی عملی:** `w` نباید به‌عنوانِ یک جمله‌ی تصحیحِ افزوده استفاده شود (چون δ برایِ اکثریت ناچیز است و در دنباله بی‌ثبات)، بلکه به‌عنوانِ **معیارِ اعتمادِ per-disk** — تقریباً دو برابر شدنِ خطا بینِ لبه‌ی تیز و پهن، یک سیگنالِ قابلِ‌اتکا برایِ وزن‌دهی در ادغام است.

سیستم می‌تواند **هر دو عدد** را گزارش کند: مقدارِ قابلِ‌مقایسه با کارشناس (نقطه‌ی عطف) و مقدارِ زیستی (`r₀ − k·w`)، به‌همراهِ فاصله‌شان به‌عنوانِ عدمِ‌قطعیت — که برایِ پایان‌نامه صورت‌بندیِ صادقانه‌تری است از انتخابِ یکی و نادیده‌گرفتنِ دیگری.

### نتیجه‌ی سنجشِ ماژولِ ۱۵.۹ روی هر ۱۱ عکس — ساخته شد، ولی واردِ زنجیره نشد

| شاخه | TP | FN | FP | TN | دقت | MAE | Bias |
|---|---:|---:|---:|---:|---:|---:|---:|
| **مدلِ رشد (۱۵.۹)** | ۱۴ | ۴۹ | ۳ | ۲۵ | ۰.۴۲۹ | **۱.۱۷ mm** | **۰.۰۱−** |
| مقدارِ زیستی (r₀−k·w) | ۱۴ | ۴۹ | ۳ | ۲۵ | ۰.۴۲۹ | ۱.۷۷ mm | ۱.۶۱− |
| Watershed | ۱۱ | ۵۲ | ۲ | ۲۶ | ۰.۴۰۷ | ۱.۰۸ mm | ۰.۰۸− |

دقتِ مستقلِ این شاخه عالی است — MAE=۱.۱۷ با بایاسِ عملاً صفر. **ولی واردِ زنجیره نشد**، و دلیلش را باید صریح گفت:

**دلیلِ ۱ — همان دیسک‌ها را پوشش می‌دهد.** از ۱۷ دیسکی که فعال می‌شود، ۱۲ تا را واترشد هم پوشش می‌دهد. در مقایسه‌ی *جفتی* رویِ همان ۱۰ دیسکِ مشترک: واترشد MAE=۱.۱۲، مدلِ رشد ۱.۰۷ — اختلافِ ۰.۰۵mm با t=۰.۲۳، یعنی **از هم قابلِ‌تفکیک نیستند**.

**دلیلِ ۲ — سهمِ منحصربه‌فرد فقط ۲ دیسک است.** رویِ همان دو (`gt_01#4`, `gt_09#1`) واقعاً بهتر از شاخه‌ی شعاعی است (۱۴.۹ در برابرِ ۱۷.۴ برایِ مرجعِ ۱۴؛ ۱۴.۴ در برابرِ ۱۸.۱ برایِ مرجعِ ۱۳)، ولی ۲ دیسک از ۶۰ یعنی اثرِ کلیِ ۰.۱۷mm رویِ MAE — که از خطایِ استانداردِ خودِ MAE کوچک‌تر است.

شبیه‌سازیِ چهار ترتیبِ مختلفِ ادغام: هر چهار تا **دقیقاً همان** TP/FN/FP/TN=۶۰/۳/۱۴/۱۴ می‌دهند و MAE بینِ ۳.۶۸ تا ۳.۸۵ نوسان می‌کند. هیچ‌کدام تفاوتِ معناداری نیست.

### یافته‌ی راهبردی — گلوگاه دیگر دقت نیست، **پوشش** است

این سومین باری است که همین الگو تکرار می‌شود (شاخه‌ی آماری، کانالِ بافت، حالا مدلِ رشد): یک شاخه‌ی جدیدِ *دقیق* ساخته می‌شود، ولی رویِ همان زیرمجموعه‌ی «آسان»ی فعال می‌شود که شاخه‌هایِ موجود از قبل خوب حل کرده‌اند.

اعداد این را صریح می‌گویند: از ۶۳ هاله‌ی واقعی، شاخه‌هایِ ناحیه‌ای رویِ حدودِ ۲۷ تا فعال می‌شوند و آن‌جا MAE ~۱–۳.۷mm دارند. باقیِ ~۳۳ تا به شاخه‌ی شعاعی می‌افتند که MAE~۵.۵ دارد. **کلِ خطایِ باقی‌مانده آن‌جاست.**

پس ساختنِ شاخه‌ی دقیق‌ترِ دیگر رویِ همان زیرمجموعه بی‌فایده است. آن‌چه لازم است شاخه‌ای است که **جایی فعال شود که فعلی‌ها نمی‌شوند** — یعنی رویِ هاله‌هایِ کم‌کنتراست، به‌هم‌چسبیده، یا با مرزِ نامنظم.

این پ۲ (تطبیقِ بافتِ خودِ هاله) را از آن‌چه فکر می‌کردیم ارزشمندتر می‌کند: آن ماژول رویِ مرزی که شاخه‌ی *شعاعی* تولید کرده کار می‌کند — یعنی دقیقاً همان ۳۳ دیسکی که الان بدترین خطا را دارند.

> این پیش‌بینی **آزموده و رد شد**. نتیجه‌اش در بندِ بعد — و آن نتیجه، خودِ همین «یافته‌ی راهبردی» را هم به یک لایه عمیق‌تر می‌بَرد.

### پ۲ — آزمونِ پیش‌فرض پیش از ساختِ ماژول، و علتِ ریشه‌ایِ مشترک

بعد از سه بارِ ساختنِ شاخه‌ای که درست کار می‌کرد ولی افزونه بود، این‌بار **اول پیش‌فرض سنجیده شد** و بعد سراغِ پیاده‌سازی رفتیم (`ground_truth/diagnostics/p2_texture_probe.py`).

قاعده‌ی پ۲ به‌صورتِ یک آزمونِ دو-مرجعی پیاده شد: مرجعِ اول بافتِ داخلِ هاله‌ی *خودِ همان دیسک*، مرجعِ دوم لَونِ میدانِ دور؛ هر حلقه به نزدیک‌ترینِ آن دو تخصیص می‌یابد و مرز جایی است که تخصیص از «هاله» به «لَون» می‌رود و برنمی‌گردد. مزیتِ طراحی این است که **هیچ پارامترِ آزادی ندارد** — نقطه‌ی برش خودش از دو مرجعِ همان تصویر بیرون می‌آید. بعد از شکستِ گیتِ ۴σ در LOO، این مزیتِ واقعی است.

| زیرمجموعه | تعداد | پ۲ شلیک کرد | MAE فعلی | MAE پ۲ |
|---|---:|---:|---:|---:|
| بحرانی (به شاخه‌ی شعاعی سپرده) | ۳۳ | **۲** | ۴.۲۳ | ۱.۱۳ |
| آسان (شاخه‌ی ناحیه‌ای حل کرده) | ۲۷ | ۱۱ | ۱.۳۰ | ۵.۰۳ |

باز هم دقیقاً همان الگو، این‌بار در حادترین شکلش: از ۱۳ شلیک، ۱۱ تا روی زیرمجموعه‌ی آسان است و آن‌جا نتیجه را **بدتر** می‌کند؛ روی زیرمجموعه‌ی بحرانی فقط ۲ بار شلیک می‌کند.

**ولی این‌بار علتِ نشلیک‌کردن را اندازه گرفتیم، و همان‌جا سازوکارِ مشترکِ هر چهار شکست پیدا شد.** از ۳۳ دیسکِ بحرانی، ۳۱ تا را نگهبانِ «تفکیک‌پذیریِ دو مرجع» رد می‌کند. علتش این نیست که نگهبان سخت‌گیر است:

| جدایشِ «داخلِ هاله» از «لَون» بر حسبِ انحرافِ معیارِ خودِ لَون | زیرمجموعه‌ی بحرانی | زیرمجموعه‌ی آسان |
|---|---:|---:|
| مرجع از حلقه‌های نزدیکِ دیسک | **۰.۳۷ σ** | ۰.۸۵ σ |
| مرجع از ۷۰٪ داخلیِ هاله‌ی **مرجعِ کارشناسی** (کنترل) | **۰.۲۹ σ** | ۰.۶۷ σ |
| نسبتی که به ۱σ می‌رسند | ۶٪ | ۲۷٪ |

دو کنترل نشان می‌دهد این عدد مصنوعِ روشِ نمونه‌گیری نیست:
- **مرجع بیرونِ هاله نیفتاده:** میانه‌ی `r_مرجع / r_هاله` برابرِ ۰.۳۹ است و در **۰٪** موارد بیرونِ هاله می‌افتد. ضمناً هاله‌های زیرمجموعه‌ی بحرانی اتفاقاً *بزرگ*‌اند (میانه ۲۳mm در برابرِ دیسکِ ۶mm)، پس جا کم نبوده.
- **حتی با دادنِ جوابِ مرجع هم بهتر نمی‌شود:** وقتی مرجع را از داخلِ هاله‌ی *کارشناسی* می‌گیریم — یعنی مکانِ درست را مجانی به الگوریتم می‌دهیم — جدایش به ۰.۲۹σ می‌رسد، حتی **کمتر**.

**یعنی: روی نیمی از دیسک‌ها، داخلِ یک هاله‌ی واقعیِ ۲۳ میلی‌متری با لَونِ باکتریایی، در شدتِ متوسط، حدودِ یک‌سومِ انحرافِ معیارِ فضاییِ خودِ لَون فرق دارد.** مرز، در سطحِ کنتراستی که مراجعِ ما می‌توانند تفکیک کنند، در تصویر **حاضر نیست**.

**و این یک علت است با چهار نشانه، نه چهار شکستِ مستقل:**

| نشانه‌ی مشاهده‌شده | همان علت |
|---|---|
| Otsu/واترشد روی این دیسک‌ها شلیک نمی‌کنند | نه دوقله‌گی هست نه لبه‌ای برای سیلاب |
| کانالِ بافت ۹۹–۱۰۰٪ هم‌پوشانیِ توزیع داشت | دو توزیع تقریباً یکی‌اند |
| مدلِ رشد (۱۵.۹) دقیق ولی افزونه بود | پله‌ای برای برازش وجود ندارد |
| پ۲ روی ۳۱ از ۳۳ شلیک نمی‌کند | جدایش ۰.۳σ |

**و چرایِ شکستِ تبدیل‌های نوری (بندِ ۶) هم همین است:** هر تبدیلِ نقطه‌ایِ یکنوا فقط محور را بازنگاشت می‌کند؛ نمی‌تواند بینِ دو توزیعِ هم‌پوشان جدایی **بسازد**. اطلاعاتی که در تصویر نیست با هیچ تابعی از همان تصویر بازیافتنی نیست.

#### پیامد برای نقشه‌ی راه

۱. **کارِ بیشتر رویِ شدتِ پیکسل سقفِ اندازه‌گیری‌شده دارد.** ساختنِ شاخه‌ی پنجم بی‌فایده است. این پ۲ را به‌عنوانِ ماژولِ جدید **متوقف** می‌کند (نه به‌عنوانِ ایده‌ی بد، بلکه به‌عنوانِ ایده‌ای که ورودیِ لازمش موجود نیست).

۲. **بندِ ۵.۳ (نورپردازیِ پس‌زمینه‌ای) حالا هدفِ کمّی دارد:** باید جدایش را از ~۰.۳σ به بالای ~۲σ ببرد تا روش‌های موجود روی زیرمجموعه‌ی بحرانی هم فعال شوند. این دیگر یک پیشنهادِ کیفی نیست، یک مشخصه‌ی قابلِ‌سنجش است.

۳. **اولویت به طبقه‌بندیِ EUCAST منتقل می‌شود.** MAE=۳.۸۵mm به‌تنهایی بد به‌نظر می‌رسد، ولی معیارِ بالینی «قطرِ دقیق» نیست، **دسته‌ی S/I/R** است و باندهای EUCAST پهن‌اند. ممکن است همین دقتِ فعلی توافقِ دسته‌ایِ قابلِ‌قبولی بدهد — و تا وقتی اندازه نگیریم نمی‌دانیم. این هم‌راستا با عنوانِ مصوبِ پروژه است و برخلافِ شاخه‌ی پنجم، سقفِ اندازه‌گیری‌شده‌ای ندارد.

---

## ۵) شرایطِ تصویربرداری — چه قیدی معادله را حل‌پذیر می‌کند؟

معادله‌ی کوپر–وودمن سه مجهول دارد که **از یک قابِ تکی قابلِ‌تفکیک نیستند**: `D` (ضریبِ نفوذ)، `t` (زمانِ بحرانی)، و `C_crit` (همان MIC). پرسش این است که کدام شرطِ تصویربرداری کدام مجهول را آزاد می‌کند.

### ۵.۱) عکس‌برداریِ زمان‌گذر (time-lapse) — قوی‌ترین قید

اگر از **یک پلیت در چند زمان** عکس گرفته شود (مثلاً ساعت‌هایِ ۶/۱۰/۱۶/۲۰):

```
r²(t) = 4·D·t·ln(...)     →     d(r²)/dt  مستقیماً D را می‌دهد
```

`D` دیگر مجهول نیست، **اندازه‌گیری** می‌شود. مهم‌تر از آن: رشدِ لبه در زمان یک قیدِ مستقل رویِ مکانِ مرز می‌گذارد که **کاملاً مستقل از شدتِ نور** است — یعنی همان چیزی که هیچ‌کدام از پنج شاخه‌ی فعلی ندارند، چون همه در نهایت به شدت نگاه می‌کنند. این تنها شرطی است که مسئله را از «حدس زدن از یک قاب» به «حل کردن با داده» تبدیل می‌کند.

*هزینه:* نیازمندِ عکس‌برداریِ مکرر از همان پلیت با موقعیتِ ثابت — یک تغییرِ واقعی در پروتکلِ آزمایشگاه، ولی بدونِ تجهیزاتِ گران.

### ۵.۲) کالیبراسیونِ نوری با کارتِ خاکستریِ مرجع در قاب

یک نوارِ خاکستریِ استاندارد کنارِ پلیت، `A` و `B` را از مقادیرِ *نسبی* به **مطلق** تبدیل می‌کند. آن‌وقت «چگالیِ رشد» واقعاً کمّی می‌شود و مقایسه‌ی بینِ عکس‌هایِ مختلف (و بینِ آزمایشگاه‌ها) معنا پیدا می‌کند. ارزان‌ترین قید از نظرِ اجرایی.

### ۵.۳) نورپردازیِ پس‌زمینه‌ای (transillumination) — بیشترین اثرِ عملیِ محتمل

نور از **زیرِ** پلیت به‌جایِ بالا. آن‌وقت شدتِ تصویر مستقیماً تابعِ کدورتِ (turbidity) لَون است — دقیقاً همان کمیتی که می‌خواهیم بسنجیم — به‌جایِ بازتابِ سطحی که به زاویه‌ی نور، بافتِ سطحِ آگار، و انعکاسِ درِ ظرف وابسته است. این همان کاری است که کدورت‌سنج‌هایِ آزمایشگاهی می‌کنند.

**چرا احتمالاً بیشترین اثر را دارد:** یافته‌ی راهبردیِ بخشِ قبل نشان داد گلوگاه *پوشش* است — ~۳۳ هاله‌ای که هیچ شاخه‌ی ناحیه‌ای رویشان فعال نمی‌شود چون کنتراستشان کم است. نورِ پس‌زمینه‌ای دقیقاً همان کنتراست را می‌سازد.

### ۵.۴) قیدهایی که ارزشِ کمی دارند

عمقِ آگار، غلظتِ تلقیح، و زمانِ انکوباسیون همگی در EUCAST استاندارد شده‌اند (۴mm، ۰.۵ مک‌فارلند، ۱۶–۲۰ ساعت). اگر آزمایشگاه استاندارد را رعایت کند این‌ها ثابت‌اند و ثبتشان ارزشِ حاشیه‌ایِ کمی دارد — مگر برایِ توضیحِ پلیت‌هایِ پرت.

---

## ۶) تغییرِ فضایِ معادله — پیشنهادِ کمیتِ نوری

تا این‌جا مدل را رویِ **شدتِ خامِ پیکسل** برازش می‌دهیم. ولی شدت با چگالیِ سلولی رابطه‌ی خطی ندارد؛ رابطه‌ی نوریِ اشباع‌شونده دارد. اگر به فضایی برویم که در آن رابطه خطی شود، گذارِ سیگموئیدی باید شکلِ ساده‌تر و پایدارتری پیدا کند و دو مجانب معنایِ فیزیکی بگیرند (چگالیِ صفر و چگالیِ اشباع).

دو تبدیلِ نامزد:

**الف) بیر–لامبرت (مناسبِ عبورِ نور):**  `OD(r) = −ln( I(r) / I_lawn )`

**ب) کوبلکا–مونک (مناسبِ بازتاب — یعنی حالتِ فعلیِ ما):**  `K/S = (1−R)² / (2R)` با `R = I/I_lawn`

نکته‌ی مهم: تصاویرِ فعلیِ ما **بازتابی**‌اند نه عبوری — لَون به‌خاطرِ پراکندگیِ نور *روشن‌تر* است، برخلافِ حالتِ عبوری که پرسلول‌تر یعنی تاریک‌تر. پس از نظرِ فیزیکی، کوبلکا–مونک تبدیلِ درست‌تری است و بیر–لامبرت فقط اگر روزی به نورِ پس‌زمینه‌ای (بندِ ۵.۳) برویم دقیق می‌شود.

**قابلِ‌آزمون همین حالا و بدونِ داده‌ی جدید** — دقیقاً مثلِ آزمونِ فضایِ لگاریتمی-شعاعی که انجام شد و رد شد. نتیجه‌اش در ادامه اضافه می‌شود.

### نتیجه‌ی آزمونِ فضایِ نوری — فیزیک تایید شد، ولی مرز بدتر شد

| فضا | R² میانه | MAE | میانه‌ی خطا |
|---|---:|---:|---:|
| raw (شدتِ خام، خطِ پایه) | ۰.۵۵۲ | **۳.۴۰ mm** | ۱.۱۷ |
| بیر–لامبرت | ۰.۵۷۷ | ۳.۸۶ mm | ۱.۲۲ |
| کوبلکا–مونک | **۰.۶۵۲** | ۷.۰۰ mm | ۱.۱۵ |

**پیش‌بینیِ فیزیکی درست از آب درآمد:** کوبلکا–مونک — که تبدیلِ *مناسبِ بازتاب* است — بهترین کیفیتِ برازش را می‌دهد (R² از ۰.۵۵ به ۰.۶۵). یعنی واقعاً شکلِ منحنی را بهتر توصیف می‌کند.

**ولی مرز بدتر می‌شود:** MAE از ۳.۴۰ به ۷.۰۰ (t=۲.۷۶−، یعنی به‌طورِ معنادار بدتر). آزمونِ علامت هیچ برتری نشان نمی‌دهد (۲۸ از ۶۳).

**چرا — و این نکته‌ی فنیِ مهمی است:** میانه‌ی خطا در KM حتی کمی *بهتر* است (۱.۱۵ در برابرِ ۱.۱۷)، ولی میانگین منفجر می‌شود. یعنی باز هم چند موردِ فاجعه‌بار.

> ⚠️ **تصحیح.** تشخیصِ اولیه‌ی من برایِ این انفجار غلط بود. نوشته بودم «`(1−R)²/(2R)` در `R→0` واگرا می‌شود و `R` داخلِ هاله کوچک است». اندازه‌گیریِ مستقیمِ دامنه‌ی `R` رویِ هر ۹۱ پروفایل این را رد می‌کند — `R` اصلاً به صفر نزدیک نمی‌شود:
>
> | آماره‌ی `R = I/I_lawn` | مقدار |
> |---|---:|
> | میانه‌ی **کمینه**ی هر پروفایل | ۰٫۹۰۷ |
> | صدکِ ۱۰ از کمینه‌ها | ۰٫۸۳۸ |
> | کمینه‌ی مطلق رویِ کلِ داده | ۰٫۳۲ |
> | میانه‌ی **بیشینه**ی هر پروفایل | ۱٫۰۵۷ |
>
> سازوکارِ واقعی در بندِ ۶.۱ توضیح داده شده.

### ۶.۱) پیش‌پردازشِ تصویر برایِ پایدارسازیِ تبدیل — آزمون و نتیجه

اگر شکستِ بالا فقط ناشی از شرایطِ تصویربرداری باشد، باید بشود با آماده‌سازیِ ورودی رفعش کرد. این فرض آزموده شد (`ground_truth/diagnostics/p4_optical_precond.py`).

**سازوکارِ واقعیِ شکست — دو چیز، هیچ‌کدام واگرایی:**

۱. **بریدنِ سقف.** به‌طور میانه **۳۵٪** از هر پروفایل `R ≥ 1` دارد (لَون نویز دارد و حولِ میانگینش نوسان می‌کند)، و `clip(R, ε, 1−ε)` همه‌ی آن را دقیقاً به صفر می‌بَرد. یعنی فلاتِ لَون — همان مجانبِ بالایی که پارامترِ `B` سیگموئید رویش تکیه دارد — صاف می‌شود. در ۷۴ پروفایل از ۹۱، بیش از ۲۰٪ داده بریده می‌شد.

۲. **فشرده‌سازی، نه خطی‌سازی.** با `R ≈ 0.9` مقدارِ `(1−R)² = 0.01` است، پس دامنه‌ی دینامیکیِ کلِ سیگنالِ KM به میانه‌ی ۰٫۰۰۵ می‌رسد. تبدیل کنتراستِ ۱۰٪ ما را *فشرده* می‌کند.

ریشه‌ی هر دو یکی است: KM به بازتابِ **مطلق** نیاز دارد (نسبت به سفیدِ مرجع)، ولی ما فقط بازتابِ **نسبی به لَون** را داریم. ضریبِ گم‌شده `R_lawn` (بازتابِ مطلقِ خودِ لَون) دقیقاً همان چیزی است که یک وصله‌ی سفیدِ مرجع در قاب می‌داد (بندِ ۵.۲). پس به‌جای حدس زدنِ یک پیش‌پردازش، `R_lawn` **جاروب** شد: اگر برای هیچ مقدارِ معقولی تبدیل بهتر نشود، ایده ذاتاً کمکی نمی‌کند.

| پیش‌پردازش | R² میانه | MAE | میانه‌ی خطا | آزمونِ علامت |
|---|---:|---:|---:|---:|
| raw (خطِ پایه) | ۰.۵۵۲ | **۳.۴۰ mm** | ۱.۱۷ | — |
| raw + میانه‌ی متحرکِ ۳ حلقه‌ای | ۰.۵۱۹ | ۳.۴۰ | ۱.۱۷ | ۲۰/۶۳ (z=−۲.۹۰) |
| وارونه‌سازیِ قطبیت + بیر–لامبرت | ۰.۳۴۹ | ۴.۹۳ | ۱.۳۸ | ۲۱/۶۳ (z=−۲.۶۵) |
| KM با `R_lawn`=۰.۲۵ (بهترینِ جاروب) | ۰.۵۸۸ | ۴.۵۷ | ۱.۲۱ | ۳۰/۶۳ (z=−۰.۳۸) |
| KM با `R_lawn`=۰.۹۵ | **۰.۷۲۶** | ۶.۵۶ | ۱.۱۴ | ۲۷/۶۳ (z=−۱.۱۳) |
| BL با `R_lawn`=۰.۹۰ (بهترینِ جاروب) | ۰.۵۵۹ | ۳.۷۹ | ۱.۲۲ | ۳۱/۶۳ (z=−۰.۱۳) |

**پیش‌پردازش دقیقاً همان چیزی را که هدف گرفته بود درست کرد:** با آزادکردنِ `R_lawn`، بریدنِ سقف از بین می‌رود و MAEِ کوبلکا–مونک از **۷.۰۰ به ۴.۵۷** می‌آید و R² از ۰.۶۵۲ به ۰.۷۲۶. یعنی تشخیصِ اصلاح‌شده درست بود.

**ولی نتیجه‌ی نهایی عوض نمی‌شود:** حتی این برآوردِ *خوش‌بینانه* — که در آن `R_lawn` رویِ خودِ داده‌ی ارزیابی انتخاب شده و هیچ اعتبارسنجیِ جدایی ندارد — از خطِ پایه بدتر است (۴.۵۷ و ۳.۷۹ در برابرِ ۳.۴۰). هیچ آزمونِ علامتی به معناداری نزدیک نمی‌شود. وقتی سقفِ خوش‌بینانه هم زیرِ خطِ پایه بماند، دیگر نیازی به LOO نیست: ایده رد است.

**مهم‌ترین یافته‌ی جانبی — R² و دقتِ مرز با هم ناسازگارند.** در جاروبِ KM، با بالا رفتنِ `R_lawn` از ۰.۲۰ به ۰.۹۵، R² یکنواخت **بهتر** می‌شود (۰.۵۸۶ → ۰.۷۲۶) و MAE یکنواخت **بدتر** (۴.۵۷ → ۶.۵۶). این یک نتیجه‌ی روش‌شناختیِ تعیین‌کننده است: **کیفیتِ برازش معیارِ معتبری برایِ انتخاب در این مسئله نیست.** یک تبدیل می‌تواند منحنی را سیگموئیدی‌تر (و برازش‌پذیرتر) کند دقیقاً از راهِ فشردنِ یکی از دو مجانب — و همان فشردگی نقطه‌ی عطف را از مرزِ زیستی دور می‌کند. این همان چیزی است که در پ۴ هم دیده شد و حالا سازوکارش روشن است.

**دو نتیجه‌ی فرعی:**

- **نویز عاملِ محدودکننده نیست.** میانه‌ی متحرک MAE را دقیقاً بدونِ تغییر می‌گذارد (۳.۴۰ → ۳.۴۰). پس محدودیت از نویزِ اندازه‌گیری نمی‌آید، از این می‌آید که *نقطه‌ی عطفِ یک سیگموئید همان چیزی نیست که کارشناس با کولیس می‌خواند*.
- **وارونه‌سازیِ حسابی، نورِ پس‌زمینه‌ای را شبیه‌سازی نمی‌کند.** با وارونه کردنِ تصویرِ بازتابی نمی‌شود فیزیکِ عبور را بازسازی کرد؛ محتوایِ اطلاعاتی عوض نمی‌شود، فقط محور برمی‌گردد و لگاریتم سرِ پرنویز را پرنویزتر می‌کند (MAE ۴.۹۳).

**پس ادعایِ قبلیِ من باید محدودتر شود.** قبلاً نوشته بودم «این شکستِ ذاتیِ ایده نیست، شکستِ آن در شرایطِ تصویربرداریِ فعلی است». چیزی که حالا سنجیده شده این است: شکست *در بخشِ عددی* واقعاً از شرایطِ تصویربرداری می‌آمد و با آزاد کردنِ `R_lawn` تا حدِ زیادی جبران شد — ولی حتی پس از جبران هم تبدیل کمکی نمی‌کند. استدلالِ بندِ ۵.۳ (نورپردازیِ پس‌زمینه‌ای) هنوز سرِ جایش است، ولی **فقط با تصاویرِ واقعیِ پس‌زمینه‌ای قابلِ آزمون است، نه با شبیه‌سازیِ نرم‌افزاری** — و آن آزمون اینجا انجام نشده چون چنین تصاویری نداریم.

**نتیجه:** هیچ‌کدام از تبدیل‌های نوری واردِ پیاده‌سازی نمی‌شوند — همان دسته‌ی آزمونِ فضایِ لگاریتمی-شعاعی. اسکریپت و خروجیِ کاملِ جاروب در `ground_truth/diagnostics/p4_optical_precond.py` و `p4_optical_precond_sweep.txt` بایگانی شده است.

---

## ۸) پیشنهادِ کاربر: تطبیقِ بافتِ فوریه برایِ تصحیحِ دایره‌هایِ بیش‌برآوردشده

### شهود — و کجایش درست بود

پیشنهاد از این مشاهده شروع شد: «هر کسی مرزِ هاله را راحت می‌بیند، پس سیستم هم باید
بتواند». این شهود **درست** است و دقیقاً با آزمونِ اوراکلِ بندِ ۶ هم‌خوان: بهترین
آستانه‌ی ممکن روی پروفایلِ شعاعی مرز را با خطایِ میانه‌یِ ۰.۱۵mm بازمی‌یابد.

نکته‌ای که تناقضِ ظاهری با «۰.۳σ» را حل می‌کند: آن اوراکل و ادراکِ انسان هر دو رویِ
**میانگینِ ناحیه‌ای** کار می‌کنند (پروفایلِ شعاعی خودش میانگینِ ۷۲ زاویه است؛ چشم هم
حلقه را یک‌جا می‌بیند)، نه رویِ یک پیکسلِ تنها. پیشنهادِ کاربر هم درست همین اصل را
به‌کار می‌برد: میانگین‌گیری رویِ کرنل‌هایِ ۱mm×۱mm به‌جایِ یک پیکسل.

### پیشنهاد، به‌طورِ دقیق

ساختِ «کرنلِ معیارِ رشد» از طیفِ فوریه‌یِ ناحیه‌یِ قطعاً-رشدکرده، و کوچک‌کردنِ دایره‌یِ
هر هاله تا جایی که داخلش دیگر با این کرنل تطبیق نداشته باشد.

### دو اصلاحِ روشی، پیش از آزمون

۱. **مرجعِ «رشدِ قطعی» را نمی‌توان از «پتری منهایِ دایره‌هایِ هاله» ساخت** — به خروجیِ
   خودِ سیستم وابسته می‌شود و یک حلقه‌یِ خودتاییدکننده می‌سازد. مرجعِ امن همان چیزی
   است که پایپ‌لاین از قبل دارد: **میدانِ دور** (`far_field_ref`)، که به‌لحاظِ هندسی
   مستقل از هر دایره‌ای است.
۲. **کرنلِ ۱mm (~۱۳×۱۳ پیکسل) برایِ FFT بسیار کوچک است** و به لبه‌هایِ پنجره و
   بلوک‌بندیِ JPEG حساس می‌شود. رفعش: میانگین‌گیری رویِ ده‌ها پنجره (نه یکی)، با
   پنجره‌یِ Hann برایِ کاهشِ نشتِ لبه.

### آزمون (پیش از ساخت، طبقِ همان الگویِ پ۱ تا پ۴)

سرِ خودِ مرزِ مرجع (همان ۶۱ دیسکِ آزمونِ بندِ ۶)، امضایِ فرکانسیِ رشد از میدانِ دور
ساخته و با نواحیِ درون/بیرونِ مرز مقایسه شد (`texture_fft_probe.py`):

| توصیفگر | AUC (میانه) |
|---|---:|
| روشناییِ خام (بندِ ۶) | **۰.۷۱۱** |
| بافتِ فوریه (این آزمون) | ۰.۶۳۲ |

بافت **بدتر** از روشناییِ خام است، نه بهتر — فقط رویِ ۷ از ۶۱ دیسک از روشنایی
پیشی می‌گیرد (`z=-6.02`). این **سومین تاییدِ مستقل** همین یافته است: کانالِ بافتِ
واریانس‌محور (تسکِ #۴۴) هم‌پوشانیِ ۹۹–۱۰۰٪ توزیع‌ها را نشان داده بود؛ آزمونِ
دو-مرجعیِ پ۲ رویِ زیرمجموعه‌یِ بحرانی فقط ۲ بار از ۳۳ شلیک کرد؛ و حالا بافتِ فوریه
هم به همان نتیجه رسید. **در این رزولوشن، بافت اطلاعاتی فراتر از میانگینِ روشنایی
ندارد** — نتیجه‌ای که با سه روشِ متفاوت اندازه‌گیری شده.

### یک واقعیتِ جداگانه که مسیرِ پیشنهاد را هم محدود می‌کرد

حتی اگر بافت جواب می‌داد، پیشنهاد فقط **بیش‌برآورد** را هدف می‌گرفت. رویِ ۶۰ دیسکِ
دارایِ هاله در هر دو طرف:

| جهتِ خطا | تعداد | مجموعِ خطایِ مطلق | سهم از کل |
|---|---:|---:|---:|
| بیش‌برآورد | ۳۳ | ۹۶.۷mm | ۴۲٪ |
| کم‌برآورد | ۲۰ | ۱۳۳.۱mm | **۵۸٪** |

**کم‌برآورد سهمِ بزرگ‌تری دارد**، و بدترین موارد منفرد همه کم‌برآوردند (تا
۲۱.۱mm رویِ `gt_08`#۵). پس مکانیسمی که فقط دایره‌ها را کوچک می‌کند، حتی در بهترین
حالت، کمتر از نیمِ خطایِ باقی‌مانده را می‌توانست هدف بگیرد.

### نتیجه

پیشنهاد **ساخته نشد** — نه به‌خاطرِ ایده‌ی بد، بلکه چون هر دو پیش‌فرضش (بافت
اطلاعاتِ تازه دارد؛ مشکل عمدتاً بیش‌برآورد است) با اندازه‌گیری رد شد. مسیرِ
بهتر برایِ کاهشِ خطایِ باقی‌مانده، ریشه‌یابیِ مستقیمِ بدترین خطاهایِ منفرد (به‌ویژه
دسته‌یِ کم‌برآوردهایِ بزرگِ ۱۳ تا ۲۱ میلی‌متری) است، نه افزودنِ منبعِ اطلاعاتیِ تازه —
چون منبعِ اطلاعاتیِ تازه (بافت) دیگر آزموده و رد شده است.

**شواهد:** `ground_truth/diagnostics/texture_fft_probe.py`، `.csv`، `_run.txt`.

---

## ۷) دو پرسشِ صریح: فیلترِ تصویر، و OCRِ کدِ دیسک

هر دو با اندازه‌گیری بررسی شدند، نه استدلالِ نظری.

### ۷.۱) آیا با فیلتر رویِ تصویرِ ورودی می‌توان σ را بهتر کرد؟

**تفکیکی که مسیرِ آزمون را تعیین می‌کند.** دو خانواده‌ی کاملاً متفاوت وجود دارد:

- **تبدیلِ نقطه‌ای** (گاما، لگاریتم، CLAHEی سراسری، کوبلکا–مونک، هر تابعِ پیکسل‌به‌پیکسل): اگر یکنوا و وارون‌پذیر باشد، ترتیبِ پیکسل‌ها را حفظ می‌کند، پس **AUC را دقیقاً تغییر نمی‌دهد**. می‌تواند عددِ اندازه‌اثر را باد کند بدونِ آن‌که یک پیکسل واقعاً تفکیک‌پذیرتر شود. این یک قضیه است، نه حدس — و آزمونِ کوبلکا–مونک در بندِ ۶.۱ همین را تجربی نشان داد (R² بهتر، MAE بدتر).
- **فیلترِ فضایی** (میانگین رویِ همسایگی): نقطه‌ای نیست، پس *می‌تواند* کمک کند. اگر نویز مستقل باشد، میانگین رویِ N پیکسل اندازه‌اثر را √N برابر می‌کند — برایِ رفتن از ۰.۳σ به ۲σ فقط N≈۴۴ لازم است.

**پس پرسشِ واقعی: نویزِ لَون مستقل است یا ساختاریافته؟** رویِ هر ۵۹ دیسکِ دارایِ هاله سنجیده شد (`sigma_filter_study.py`):

| کرنل (×r_disk) | N پیکسل | اندازه‌اثر | AUC | اگر نویز مستقل بود | تاریِ مرز |
|---:|---:|---:|---:|---:|---:|
| ۰ (بدونِ فیلتر) | ۱ | ۰.۳۶ | ۰.۷۵۲ | ۰.۳۶ | — |
| ۰.۰۵ | ۹ | ۰.۴۲ | ۰.۷۵۶ | ۱.۰۸ | ۰.۲۳ mm |
| ۰.۱۰ | ۲۵ | ۰.۴۷ | ۰.۷۸۳ | ۱.۸۰ | ۰.۳۸ mm |
| ۰.۲۰ | ۸۱ | ۰.۵۴ | ۰.۸۰۷ | ۳.۲۴ | ۰.۶۸ mm |
| ۰.۴۰ | ۳۶۱ | ۰.۵۹ | ۰.۸۵۲ | ۶.۸۴ | ۱.۴۳ mm |
| ۰.۸۰ | ۱۲۲۵ | ۰.۷۱ | ۰.۸۵۰ | ۱۲.۶۰ | ۲.۶۳ mm |

**جوابِ اصلی: بله کمی، ولی نه به اندازه‌ای که مسئله را حل کند — و علتش قابلِ اندازه‌گیری است.**

اندازه‌اثر با `N^0.095` رشد می‌کند، نه `N^0.5`. میانگین‌گیری رویِ ۱۲۲۵ پیکسل فقط **۱.۹۶ برابر** بهبود داد، در حالی که نویزِ مستقل ۳۵ برابر می‌داد. یعنی **۹۴٪ از پراکندگیِ لَون نویزِ ساختاریافته است** (بافتِ خودِ لَون، ناهمواریِ آگار، شیبِ باقی‌مانده‌ی نور) و با هیچ میانگین‌گیری‌ای از بین نمی‌رود.

**و یک بده‌بستانِ تعیین‌کننده که ستونِ آخر نشان می‌دهد:** فیلتر مرز را هم به همان اندازه تار می‌کند. بهترین AUC (۰.۸۵) در کرنلِ ۰.۸ به‌دست می‌آید که **۲.۶۳ میلی‌متر عدمِ‌قطعیتِ قطر** وارد می‌کند — بدتر از باندِ ±۲mm انسانی و هم‌مرتبه با کلِ MAEی فعلی. یعنی همان فیلتری که کنتراست می‌خرد، دقتِ مکانی‌ای را که برایش می‌خواستیم از بین می‌برد.

**تنها نقطه‌ی احتمالاً مفید:** کرنلِ ۰.۲۰ — AUC از ۰.۷۵۲ به ۰.۸۰۷ با تنها ۰.۶۸mm هزینه. بهبودِ خالصِ کوچکی هست، ولی AUC=۰.۸۱ یعنی هر پنجمین پیکسل هنوز اشتباه طبقه‌بندی می‌شود؛ برایِ مکان‌یابیِ یک مرز کافی نیست.

**نتیجه:** فیلتر جایگزینِ نورپردازیِ پس‌زمینه‌ای نمی‌شود. نورِ پس‌زمینه‌ای **سیگنال** را عوض می‌کند (هاله نور را عبور می‌دهد، لَون نمی‌دهد — یک تفاوتِ فیزیکیِ جدید)، در حالی که فیلتر فقط همان سیگنالِ موجود را بازآرایی می‌کند. اطلاعاتی که در تصویر نیست با هیچ فیلتری ساخته نمی‌شود.

### ۷.۲) آیا OCRِ کدِ رویِ دیسک به مرجعِ بهتر می‌رساند؟

**جوابِ مستقیم: نه.** نویزِ ±۲mm مالِ **خوانشِ کولیسیِ کارشناس از لبه‌ی هاله** است. برچسبِ رویِ دیسک آنتی‌بیوتیک را می‌گوید، نه این‌که لبه‌ی هاله کجاست. این دو کمیتِ کاملاً بی‌ربط‌اند.

**ولی سه چیزِ ارزشمندِ دیگر می‌دهد:**

۱. **ماژولِ ۱۸ خودکار می‌شود** — الان آنتی‌بیوتیکِ هر دیسک باید دستی در `cfg.eucast_disk_agents` وارد شود.
۲. **ارزیابیِ دسته‌ای دقیق می‌شود** به‌جایِ میانگین‌گیری رویِ ۳۴۹ نقطه‌ی شکست. این بهبودِ واقعی در کیفیتِ **ارزیابی** است، نه کیفیتِ مرجع — و کم‌ارزش نیست.
۳. **تنها مسیرِ غیرمستقیم به مرجعِ بهتر:** با دانستنِ آنتی‌بیوتیک + گونه، بازه‌هایِ کنترلِ کیفیِ EUCAST و سازگاریِ بینِ پلیت‌ها اجازه می‌دهند خوانش‌هایِ پرتِ *خودِ کارشناس* علامت بخورند. مسیرِ واقعی ولی ضعیفی است.

**امکان‌سنجی — سنجیده شد، نه حدس زده (`ocr_feasibility.py`، تصویر: `disk_labels_gt01.png`):**

کدها در همین رزولوشن (~۱۳.۳ پیکسل بر میلی‌متر) **برایِ چشمِ انسان خوانا هستند** — از ۹ دیسکِ `gt_01` هشت‌تا دستی خوانده شد: `RA 5`، `TE 30`، `FOX 30`، `GM 10`، `SXT`، `CP 5`، `E 15`، `CC 2`.

ولی OCRِ آماده **۰ از ۸** می‌گیرد، در دو تلاشِ جدی: ماسکِ دایره‌ایِ دیسک، آستانه‌ی Otsu معکوس، بزرگ‌نماییِ ۶ برابر، CLAHE، و راست‌سازی با `minAreaRect` در چهار جهت با psm ۷ و ۸. بدتر آن‌که Tesseract **با اطمینانِ بالا اشتباه** می‌کند (اطمینانِ ۸۵ برایِ «G» رویِ دیسکی که «TE 30» است) — یعنی حتی نمی‌شود با آستانه‌ی اطمینان فیلترش کرد.

علت ساختاری است نه تنظیمات: OCRِ عمومی برایِ متنِ چاپیِ افقیِ سند آموزش دیده، در حالی که این‌ها مهرِ دستیِ منحنی، با چرخشِ تصادفی، ارتفاعِ نویسه ~۲۰ پیکسل، و گاهی بسیار کم‌رنگ‌اند.

**ابزارِ درست OCR نیست، طبقه‌بندیِ واژگانِ بسته است.** مجموعه‌ی کدها بسته و کوچک است (~۳۰–۴۰ کد)، پس مسئله «تشخیصِ متنِ آزاد» نیست بلکه «انتخاب از فهرستی معلوم» است — به‌مراتب ساده‌تر و کاملاً شدنی.

**ولی مرغ‌و‌تخم‌مرغ دارد:** آموزش یا اعتبارسنجیِ چنین طبقه‌بندی به برچسبِ مرجعِ کدها نیاز دارد که در دیتاستِ ما نیست. کسی باید اول آن‌ها را برچسب بزند — و اگر بزند، برایِ همین ۱۱ پلیت دیگر به OCR نیازی نیست. ارزشش در **مقیاس** است (۷۸ عکسِ خام، و پلیت‌هایِ آینده)، نه در این دیتاست.

### ۷.۳) پس چه چیزی واقعاً مرجع را بهتر می‌کند؟

| راه | اثر | هزینه |
|---|---|---|
| **میانگینِ n خوانشِ مستقل از همان پلیت** | نویز با ۱/√n کم می‌شود: ۴ خوانش → ±۱mm، ۸ خوانش → ±۰.۷mm | فقط وقتِ کارشناس؛ **ارزان‌ترین و مستقیم‌ترین راه** |
| مرجعِ MIC از رقتِ براث | یک روشِ مرجعِ کاملاً مستقل | گران، آزمایشگاهِ مجهز |
| OCRِ کدِ دیسک | مرجعِ قطر را عوض نمی‌کند | متوسط |

**توصیه‌ی من:** اگر قرار است یک کارِ دیگر رویِ داده انجام شود، **دورِ دومِ اندازه‌گیریِ کارشناسی رویِ همان ۱۱ پلیت** بیشترین ارزش را دارد. هم مرجع را ±۲ → ±۱.۴ میلی‌متر بهتر می‌کند، و هم — مهم‌تر — عددِ ±۲ را که الان **اظهارِ شفاهی** است به یک اندازه‌گیریِ خودمان تبدیل می‌کند.

