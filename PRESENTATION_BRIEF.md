# بسته‌ی آماده‌سازیِ ارائه برایِ استادِ راهنما

این فایل **خودبسنده** است: چت مقصد به مخزن دسترسی ندارد، پس هر عددی که لازم باشد
همین‌جا آمده. دو بخش دارد:

- **بخشِ ۱** — پرامپتی که در چتِ جدید کپی می‌کنید.
- **بخشِ ۲** — محتوا و اعداد، که همراهِ پرامپت می‌فرستید.

> ⚠️ **قاعده‌ی همه‌ی این سند:** هیچ عددی حدس زده نشده. اگر چیزی سنجیده نشده، صریح
> نوشته شده «سنجیده نشده». در ارائه هم همین باید رعایت شود — استادِ راهنما دقیقاً
> همان جایی سؤال می‌کند که ادعا از شواهد جلو بزند.

---

---

# بخشِ ۱ — پرامپت (این را در چتِ جدید کپی کنید)

```
سلام. می‌خواهم یک ارائه‌ی پاورپوینت برای استاد راهنمای پایان‌نامه‌ام آماده کنم.
موضوع: یک سیستم بینایی ماشین کلاسیک برای اندازه‌گیری خودکار هاله‌ی عدم رشد در
تست آنتی‌بیوگرام (دیسک دیفیوژن).

در پیام بعدی، یک سند کامل با همه‌ی اعداد و جزئیات فنی می‌فرستم. لطفاً فقط از همان
سند استفاده کن و هیچ عددی از خودت اضافه یا گرد نکن.

خروجی مورد نظر من:
۱) یک اسکلت اسلاید به اسلاید (حدود ۲۰ تا ۲۵ اسلاید) شامل عنوان هر اسلاید،
   متن روی اسلاید (بولت، کوتاه)، و یادداشت گوینده (Speaker Notes) که خودم موقع
   ارائه بخوانم.
۲) برای هر اسلایدی که نمودار یا شکل لازم دارد، بگو دقیقاً چه نموداری و با چه
   داده‌ای — لازم نیست خودت رسم کنی.
۳) در پایان، فهرست سوالاتی که احتمالاً استاد می‌پرسد به همراه پاسخ کوتاه.

لحن و سبک:
- علمی ولی با زبان ساده و قابل فهم. جایی که مفهوم فنی است، اول با یک تشبیه
  روزمره توضیح بده بعد اصطلاح دقیق را بگو.
- بدون اغراق. این پروژه نتایج منفی مهمی دارد و باید صادقانه گفته شوند —
  نقطه‌ی قوت این ارائه دقیقاً همین صداقت است.
- بدیهیات را نگو. مثلاً توضیح نده «آنتی‌بیوگرام چیست» بیش از یک اسلاید، یا
  «چرا اتوماسیون خوب است». استاد راهنما اینها را می‌داند.
- هر ادعا باید به یک عدد یا یک آزمون وصل باشد.

ساختار ارائه که می‌خواهم:

بخش الف) نقطه‌ی شروع و مشکل
   - از کجا شروع کردیم: خطای اندازه‌گیری MAE = ۵.۵ میلی‌متر
   - چرا این بد بود و ریشه‌ی فنی‌اش چه بود

بخش ب) چه چیزهایی به پایپ‌لاین اضافه کردیم (مفهومی، نه کد)
   - هر ایده: مشکلی که حل می‌کرد، ایده به زبان ساده، نتیجه‌ی عددی

بخش پ) نتایج فعلی و دفاعیات
   - اعداد نهایی
   - دفاعیاتی که واقعاً قابل دفاع‌اند (در سند مشخص شده کدام‌ها)
   - و صریحاً: چه ادعاهایی را نباید بکنیم

بخش ت) مسیر مدل‌سازی رشد — یک روایت کامل از یک نتیجه‌ی منفی
   این بخش باید مفصل باشد، چون ارزش علمی‌اش زیاد است:
   - چرا سراغ مدل‌سازی رفتیم (مبنای فیزیکی)
   - چه تصحیحی روی ایده‌ی اولیه زدیم و چرا
   - چه چالش‌هایی خوردیم و هرکدام را چطور حل کردیم
   - چه چیزی در نهایت حل نشد، و چرا (با عدد)
   - و مهم‌ترین بخش: تحت چه شرایطی این راه‌حل کار می‌کند — این را به عنوان
     پیشنهاد آینده در مقاله و پایان‌نامه ذکر می‌کنیم

بخش ث) جمع‌بندی و کار آینده

لطفاً اول یک طرح کلی (outline) بده تا تایید کنم، بعد اسلایدها را کامل بنویس.
```

---

---

# بخشِ ۲ — محتوا و اعداد (این را بعد از پرامپت بفرستید)

## ۰) یک‌خطی درباره‌ی پروژه

سیستمی که از عکسِ معمولیِ یک پلیتِ آنتی‌بیوگرام، ظرفِ پتری را پیدا می‌کند، دیسک‌هایِ
آنتی‌بیوتیک را تشخیص می‌دهد، قطرِ هاله‌ی عدمِ رشد را اندازه می‌گیرد، و طبقِ
استانداردِ EUCAST دسته‌ی حساس/میانی/مقاوم را گزارش می‌کند. کاملاً **کلاسیک و قطعی**
است (بدونِ یادگیریِ ماشین) — یعنی یک ورودیِ یکسان همیشه خروجیِ یکسان می‌دهد.

---

## بخشِ الف) نقطه‌ی شروع و مشکل

### وضعیتِ اولیه

روشِ اولیه یک ایده‌ی ساده بود: از مرکزِ هر دیسک به بیرون حرکت کن، روشناییِ پیکسل‌ها
را در حلقه‌های متحدالمرکز میانگین بگیر، و هرجا منحنی «صاف» شد بگو به لَونِ باکتری
رسیدیم. اسمش را می‌گذاریم **شاخه‌ی شعاعی**.

| سنجه | مقدار |
|---|---|
| خطایِ متوسطِ قطرِ هاله (MAE) | **۵.۵۰ میلی‌متر** |
| دقتِ تشخیصِ «هاله دارد یا ندارد» | ۰.۷۸۰ |
| حدودِ توافقِ ۹۵٪ (Bland–Altman) | [−۱۶.۱۲ , +۱۵.۰۵] میلی‌متر |

### چرا ۵.۵ میلی‌متر خیلی بد است

نه با مقایسه‌ی سلیقه‌ای، بلکه با دو معیارِ بیرونی:

۱. **کلِ بازه‌ی تصمیم‌گیریِ بالینی چند میلی‌متر است.** مثلاً برایِ سیپروفلوکساسین
   رویِ Enterobacterales: قطرِ ≥۲۵ یعنی حساس، <۲۲ یعنی مقاوم. یعنی کلِ فاصله‌ی
   بینِ «این دارو جواب می‌دهد» و «نمی‌دهد» **۳ میلی‌متر** است. خطایِ ۵.۵ میلی‌متری
   از کلِ آن بازه بزرگ‌تر است.
۲. **خودِ کارشناسِ انسانی ±۲ میلی‌متر پراکندگی دارد** (اظهارِ آزمایشگاهِ مرجع).
   یعنی خطایِ ما تقریباً سه برابرِ پراکندگیِ خودِ انسان بود.

### 🔎 نکته‌ای که دیدنِ آن مهم بود

میانگینِ خطایِ **علامت‌دار** (bias) فقط −۰.۵۳ میلی‌متر بود — عددی که به‌نظر عالی
می‌آید. ولی این فریبنده بود: خطاها در **هر دو جهت** بودند و در میانگین همدیگر را
خنثی می‌کردند. یعنی سیستم گاهی خیلی بیش‌برآورد و گاهی خیلی کم‌برآورد می‌کرد، و
میانگینِ نزدیک به صفر این را پنهان می‌کرد.

> **درسِ قابلِ ارائه:** برایِ یک ابزارِ اندازه‌گیری، bias معیارِ کافی نیست. باید
> MAE و حدودِ توافق را هم دید. این یک نکته‌ی روش‌شناختیِ واقعی است.

### ریشه‌ی فنیِ مشکل (به زبانِ ساده)

پروفایلِ شعاعی یک منحنیِ **تک‌بعدی** است: در هر شعاع، میانگینِ همه‌ی جهت‌ها. این
منحنی نمی‌تواند سه حالتِ زیر را از هم تشخیص دهد:

- «به لَون رسیدم» ✅ (درست)
- «هنوز نرسیدم، ولی این‌جا کمی روشن‌تر شده» ❌ (کم‌برآورد)
- «از هاله‌ی خودم رد شدم و واردِ هاله‌ی دیسکِ بغلی شدم» ❌ (بیش‌برآورد)

چون هر سه در یک منحنیِ میانگین‌گرفته‌شده شبیهِ هم به‌نظر می‌رسند.

---

## بخشِ ب) چه چیزهایی اضافه کردیم

هر مورد را با این قالب ارائه کنید: **مشکل → ایده به زبانِ ساده → چرا کار می‌کند**.

### ب-۱) بومِ آگار — تصحیحِ نورِ ناهمگن

**مشکل:** عکس‌ها نورِ یکنواخت ندارند؛ یک طرفِ پلیت روشن‌تر است. این شیب با تفاوتِ
واقعیِ هاله و لَون قاطی می‌شود.

**اشتباهی که اول کردیم و بعد ریشه‌یابی شد:** تصحیحِ نور را رویِ **کلِ تصویر** انجام
دادیم. ولی دیسک‌هایِ کاغذی خیلی روشن‌اند و وقتی واردِ محاسبه‌ی «نورِ زمینه» می‌شوند،
تخمین را بالا می‌برند — و تفریقِ آن، **حلقه‌ی تاریکِ مصنوعی** دورِ هر دیسک می‌سازد.
یعنی تصحیح، دقیقاً همان چیزی را می‌ساخت که قرار بود حذف کند.

**ایده:** تخمینِ نور فقط از **پیکسل‌هایِ آگار** گرفته شود — دیسک‌ها و لبه‌ی پلاستیکیِ
ظرف کنار گذاشته شوند.

> **تشبیه:** برایِ اندازه‌گیریِ روشناییِ یک دیوار، چراغِ رویِ دیوار را جزوِ دیوار
> حساب نکنید.

### ب-۲) مرجعِ میدانِ دور — تعریفِ عملیاتیِ «لَون»

**مشکل:** «لَونِ باکتری چقدر روشن است؟» جوابش در هر عکس فرق می‌کند. هر عددِ ثابتی
که بگذاریم، در عکسِ بعدی غلط است.

**ایده:** به‌جایِ عددِ ثابت، **از خودِ همان عکس** بپرس. پیکسل‌هایی از آگار که از
*همه‌ی* دیسک‌ها دورترند، از نظرِ فیزیکی نمی‌توانند داخلِ هیچ هاله‌ای باشند. پس
آن‌ها به‌طورِ قطع لَون‌اند.

> **چرا این دفاعِ خوبی است:** این یک آستانه‌ی تنظیم‌شده نیست، یک **استدلالِ هندسی**
> است. هر عکس مرجعِ خودش را می‌سازد.

### ب-۳) دو شاخه‌ی «ناحیه‌ای» به‌جایِ فقط منحنیِ شعاعی

**مشکل:** منحنیِ تک‌بعدی سه حالتِ بالا را قاطی می‌کند.

**ایده:** به‌جایِ دنبال کردنِ یک منحنی، **کلِ ناحیه را دو‌بعدی** نگاه کن:

- **شاخه‌ی Otsu:** آستانه‌ی روشنایی که خودِ تصویر پیشنهاد می‌دهد (نقطه‌ای که
  هیستوگرام را به بهترین شکل به دو دسته می‌شکند).
- **شاخه‌ی واترشد:** مثلِ ریختنِ آب از مرکزِ هاله — آب پخش می‌شود تا به یک «تپه»
  (لبه‌ی واقعی) برسد و متوقف شود. هیچ آستانه‌ی سراسری ندارد.

### ب-۴) ادغام — مهم‌ترین تصمیمِ طراحی

**مشاهده‌ی کلیدی (اندازه‌گیری‌شده، نه فرض):**

| شاخه | چند هاله را پیدا می‌کند | چقدر دقیق اندازه می‌گیرد | چند بارِ کاذب |
|---|---:|---:|---:|
| شعاعی | ۵۷ از ۶۳ (بهترین پوشش) | MAE ۵.۴۹ (بدترین) | ۱۴ |
| Otsu | ۲۶ از ۶۳ | MAE ۳.۶۶ | **۱** |
| واترشد | ۱۱ از ۶۳ (کمترین) | **MAE ۱.۰۸** (بهترین) | ۲ |

**الگو:** شاخه‌ی شعاعی *پیدا کردن* را خوب بلد است ولی *اندازه گرفتن* را بد. شاخه‌های
ناحیه‌ای برعکس — کم پیدا می‌کنند ولی وقتی می‌کنند، تقریباً هرگز اشتباه نمی‌کنند و
سه تا پنج برابر دقیق‌ترند.

**پس قاعده‌ی ادغام «میانگینِ سه شاخه» نیست، بلکه تقسیمِ کار است:**

```
اگر واترشد نظر داد     →  حرفِ واترشد
وگرنه اگر Otsu نظر داد →  حرفِ Otsu
وگرنه                  →  حرفِ شاخه‌ی شعاعی
```

> **این ترتیب از رویِ داده استخراج شد، نه از پیش‌فرض.** و همین نکته دفاعِ خوبی است:
> قاعده از اندازه‌گیری آمده.

### ب-۵) اعتبارسنجیِ شکلِ دیسک

**ایده:** برایِ هر دایره‌ای که الگوریتم پیدا کرده بپرس «آیا این واقعاً یک دیسکِ
کاغذی است؟» — ولی نه از رویِ حروفِ چاپیِ رویش (که برایِ هر آنتی‌بیوتیک فرق دارد و
جهتش هم تصادفی است)، بلکه از رویِ ویژگی‌هایی که **همه‌ی دیسک‌ها مشترک** دارند:
لبه‌ی پیوسته در همه‌ی جهت‌ها، پله‌ی روشناییِ لبه، و تقارنِ دورانی.

**نتیجه:** تنها مثبتِ کاذبِ باقی‌مانده حذف شد، بدونِ از دست دادنِ هیچ دیسکِ واقعی.

---

## بخشِ پ) نتایجِ فعلی و دفاعیات

### اعدادِ نهایی (رویِ ۱۱ عکس، ۹۳ دیسک، با اندازه‌گیریِ کارشناس)

| مرحله | نتیجه |
|---|---|
| تشخیصِ ظرفِ پتری | ۱۱ از ۱۱ (۱۰۰٪) |
| تشخیصِ دیسک | Precision = ۱.۰۰۰، Recall = ۰.۹۷۸، **F1 = ۰.۹۸۹** |
| تشخیصِ حضورِ هاله | TP=۶۰، FN=۳، FP=۱۳، TN=۱۵ — دقت **۰.۸۲۴** |
| قطرِ هاله | **MAE = ۳.۸۵ mm**، میانه = ۲.۰۸ mm، Bland–Altman [−۱۲.۳۳, +۱۱.۱۱] |
| دسته‌ی بالینی EUCAST | CA = ۸۳.۲٪، VME = ۶.۱۹٪، ME = ۹.۲۴٪ |

### مسیرِ بهبود (نمودارِ پیشنهادی برایِ یک اسلاید)

| پیکربندی | دقتِ حضور | MAE |
|---|---:|---:|
| فقط شاخه‌ی شعاعی (شروع) | ۰.۷۸۰ | ۵.۵۰ |
| + ادغامِ سه‌شاخه‌ای | ۰.۸۱۳ | **۳.۸۵** |
| + اعتبارسنجیِ شکل (فعلی) | **۰.۸۲۴** | ۳.۸۵ |

**۳۰٪ کاهشِ خطا**، و سه هاله‌ی واقعی که قبلاً از دست می‌رفت بازیابی شد (FN از ۶ به ۳).

---

### ✅ دفاعیاتِ واقعی (اینها را بگویید)

**۱. ما اورفیتِ خودمان را گرفتیم و حذفش کردیم.**
یک «گیت» طراحی کردیم که رویِ همان ۱۱ عکس دقت را به ۰.۸۳۵ می‌برد — عالی به‌نظر
می‌رسید. با اعتبارسنجیِ leave-one-out (آستانه رویِ ۱۰ عکس انتخاب، رویِ عکسِ یازدهم
سنجیده) دقت به **۰.۷۵۸** رسید، یعنی **بدتر از هیچ‌کاری‌نکردن (۰.۷۸۰)**. حذفش کردیم.

> این قوی‌ترین دفاعِ روش‌شناختیِ پروژه است: نشان می‌دهد اعدادِ گزارش‌شده از فیلترِ
> اعتبارسنجی رد شده‌اند.

**۲. پیکربندیِ مستقر هیچ آستانه‌ی برازش‌شده‌ای ندارد.**
هر عددی یا از هندسه می‌آید یا از خودِ همان تصویر. مثلاً کفِ پوششِ کمانیِ ۰.۷۵ از
این استدلال می‌آید که یک دیسکِ واقعی ممکن است تا یک‌چهارمِ محیطش با همسایه یا لبه‌ی
ظرف هم‌پوشانی داشته باشد — نه از جست‌وجو رویِ داده.

**۳. قطعیت و بازتولیدپذیریِ سه‌گانه.**
ورودیِ یکسان همیشه خروجیِ یکسان می‌دهد (هیچ گامِ تصادفی وجود ندارد). نتایج رویِ
**سه ماشینِ کاملاً بی‌ربط** عیناً تکرار شد.

**۴. داده‌ی مرجع مستقل از سیستمِ تحتِ ارزیابی ساخته شد.**
شماره‌گذاریِ دیسک‌ها برایِ داده‌ی مرجع با یک اسکریپتِ ساده و جدا انجام شد، نه با
خودِ پایپ‌لاینی که قرار است سنجیده شود. اگر پایپ‌لاین دیسکی را جا بیندازد، این خطا
دیده می‌شود.

**۵. خطا را به زبانِ بالینی ترجمه کردیم، نه فقط میلی‌متر.**
جدولِ نقاطِ شکستِ EUCAST v16.0 از PDF رسمی استخراج شد (۱۱۱۶ سطر، ۲۷ گروهِ ارگانیسم)
و خطا رویِ **۳۴۹ نقطه‌ی شکستِ واقعی** به توافقِ دسته‌ای ترجمه شد.

**۶. سقفِ منصفانه را هم اندازه گرفتیم.**
با ±۲ میلی‌متر پراکندگیِ کارشناس، توافقِ **انسان با انسان** فقط CA ≈ ۹۲–۹۶٪ است.
پس فاصله‌ی ما تا سقف حدودِ ۱۰ واحد است، نه ۱۸. و نکته‌ی مهم‌تر: با ±۲ میلی‌متر،
**حتی دو کارشناسِ انسانی هم** سقفِ استانداردِ VME = ۱.۵٪ را برآورده نمی‌کنند.

**۷. نیمی از اندازه‌گیری‌ها در باندِ خطایِ خودِ انسان هستند.**
۴۸٪ اندازه‌گیری‌ها خطایِ ≤۲ میلی‌متر دارند و **میانه‌ی خطا ۲.۰۸ میلی‌متر** است —
یعنی عملاً رویِ لبه‌ی باندِ انسانی. میانگین را اقلیتی از خطاهایِ بزرگ بالا می‌کشد.

**۸. نتایجِ منفی اندازه‌گیری شدند، نه رها.**
شش ایده آزموده و با عدد رد شدند. این هم بخشی از دستاورد است.

---

### ❌ ادعاهایی که **نباید** بکنید

| نگویید | چون |
|---|---|
| «سیستم آماده‌ی استفاده‌ی بالینی است» | VME = ۶.۱۹٪ در برابرِ سقفِ ۱.۵٪. **بگویید: ابزارِ غربالگری، نه ابزارِ تصمیم.** |
| «دقتِ قطرِ دیسک ۰.۱۸ میلی‌متر است پس سیستم دقیق است» | این یک **بررسیِ خودسازگاری** است نه اعتبارسنجیِ مستقل — چون مقیاسِ میلی‌متر از خودِ همان دیسک‌ها کالیبره می‌شود. |
| «۱۱ عکس کافی است» | نیست. صریح بگویید که مجموعه کوچک است. |
| «MAE ۳.۸۵ خوب است» | نسبت به شروع بهتر شده، ولی هنوز بالاست. **بگویید: میانه ۲.۰۸ و ۴۸٪ در باندِ انسانی** — این دقیق‌تر و صادقانه‌تر است. |

---

### مشکلاتِ بازِ باقی‌مانده (صریح بگویید)

۱. **۱۳ مثبتِ کاذبِ حضورِ هاله** — تنها راه‌حلی که آزمودیم در اعتبارسنجی رد شد.
۲. **۲ دیسکِ ازدست‌رفته** از ۹۳، با دو علتِ متفاوت.
۳. **۱۱ عکس** برایِ تعدادِ تصمیم‌هایِ طراحی کم است.
۴. **عنوانِ پروژه «یادگیری ماشین» دارد ولی پیاده‌سازی کلاسیک است** — این یک
   انتخابِ آگاهانه بود (قطعیت، قابلیتِ انتقال به ++C، و نبودِ داده‌ی کافی برایِ
   آموزش) و باید در پایان‌نامه توجیه شود، نه پنهان.

---

## بخشِ ت) مسیرِ مدل‌سازیِ رشد — روایتِ کاملِ یک نتیجه‌ی منفی

> این بخش قلبِ علمیِ ارائه است. یک ایده‌ی درست، که تا انتها دنبال شد، و دقیقاً
> معلوم شد کجا و **چرا** شکست می‌خورد — و تحتِ چه شرطی کار می‌کند.

### ت-۱) چرا اصلاً سراغِ مدل‌سازی رفتیم

تا این‌جا مرزِ هاله را با «قواعدِ تصویری» پیدا می‌کردیم. سؤال این بود: چرا به‌جایِ
قاعده، از **فیزیکِ خودِ پدیده** استفاده نکنیم؟

فیزیک روشن است: آنتی‌بیوتیک از دیسک طبقِ قانونِ فیک **نفوذ** می‌کند، غلظتش با فاصله
کم می‌شود، و رشد جایی متوقف می‌شود که غلظت از حدِ کشندگی (MIC) پایین‌تر برود.
رابطه‌ی لگاریتمیِ بینِ قطرِ هاله و غلظت (معادله‌ی **کوپر–وودمن**) پایه‌ی نظریِ
کلاسیکِ خودِ همین آزمونِ دیسک‌دیفیوژن است:

```
r² = 4·D·t · ln( M₀ / (π·h·D·t·C_crit) )
```

**پس این مدل‌سازی حدس نبود، از فیزیکِ شناخته‌شده‌ی خودِ آزمایش می‌آمد.**

### ت-۲) تصحیحی که رویِ ایده‌ی اولیه زدیم

ایده‌ی اولیه «برازشِ یک تابعِ نمایی/لگاریتمی» بود. ولی یک نکته‌ی مهم وجود داشت:

> **آن‌چه در عکس می‌بینیم غلظتِ آنتی‌بیوتیک نیست، چگالیِ رشدِ باکتری است.**

و چگالیِ رشد بر حسبِ شعاع نمی‌تواند نمایی باشد — باید یک **سیگموئید** (منحنیِ S
شکل) باشد، چون از «صفر رشد» شروع می‌شود و به «لَونِ کامل» می‌رسد و هر دو سر
**اشباع** می‌شوند.

پس تابعِ برازش این شد:

```
y(r) = A + (B − A) / (1 + exp( −(r − r₀)/w ))
```

با چهار پارامتر: `A` سطحِ داخلِ هاله، `B` سطحِ لَون، `r₀` مرکزِ گذار، `w` پهنایِ گذار.

**سه چیز از این برازش به‌دست می‌آید که قبلاً نداشتیم:**

۱. **مرزِ پیوسته** — به‌جایِ شعاعی که به عرضِ حلقه‌ها گِرد شده، یک عددِ پیوسته.
۲. **معیارِ اطمینانِ درون‌ذاتی** — نسبتِ `w/r₀`. گذارِ تیز یعنی قابلِ‌اتکا؛ گذارِ
   خیلی پهن یعنی احتمالاً اصلاً هاله‌ای وجود ندارد.
۳. **اعتبارسنجیِ رایگان** — دو مجانبِ برازش باید با سطحِ لَونِ مستقلاً اندازه‌گرفته‌شده
   بخوانند؛ نخواندنشان یعنی برازش بی‌اعتبار است.

### ت-۳) یک ظرافتِ تعریفی که مهم است

تعریفِ استاندارد این است که «مرز جایی است که رشد **کاملاً** غایب است». این نقطه
**نقطه‌ی عطفِ سیگموئید نیست** — نقطه‌ی عطف وسطِ گذار است. مرزِ منطبق بر تعریف، جایی
است که منحنی از مجانبِ هاله **جدا می‌شود** (مثلاً ۵٪ راه به سمتِ لَون).

> این یک نکته‌ی دقیقِ ارائه است: نشان می‌دهد تعریف را جدی گرفته‌ایم.

### ت-۴) چالش‌ها و راه‌حل‌هایشان

| چالش | چه کردیم |
|---|---|
| **بدونِ کتابخانه‌ی بهینه‌سازی.** پایپ‌لاین فقط به OpenCV و NumPy وابسته است (شرطِ انتقال به ++C). | برازش با **جست‌وجویِ شبکه‌ای + پالایشِ محلی**، فقط با NumPy. مزیتِ جانبی: قطعی است و به نقطه‌ی شروعِ بهینه‌ساز وابسته نیست. |
| **برازش‌ها همه در `r₀ ≈ ۱.۳۳ × شعاعِ دیسک` گیر می‌کردند.** | ریشه‌یابی: ماسکِ آگار دیسک‌ها را تا ۱.۳۵ برابر کنار می‌گذارد، پس حلقه‌هایِ اولِ پروفایل **هیچ پیکسلی** نداشتند و مقدارِ صفر ثبت می‌شد — یک لبه‌ی مصنوعی. با فیلترِ پوششِ حلقه حل شد. |
| **مقیاسِ میلی‌متر در نقطه‌ی استخراج هنوز محاسبه نشده بود.** | همان قاعده‌ی محاسبه‌ی مقیاس آفلاین بازسازی شد. |

### ت-۵) نتیجه‌ی خودِ مدل: دقیق، ولی **افزونه**

مدل ساخته و رویِ هر ۱۱ عکس سنجیده شد:

- جایی که برازش موفق است، **MAE = ۱.۱۷ میلی‌متر** — یعنی خیلی دقیق.
- ولی رویِ همان زیرمجموعه‌ی «آسان»ی فعال می‌شود که شاخه‌هایِ ناحیه‌ای از قبل حل
  کرده‌اند. یعنی چیزِ جدیدی اضافه نمی‌کند.

**تصمیم:** واردِ زنجیره‌ی تصمیم نشد؛ به‌عنوانِ ماژولِ تشخیصیِ مستند نگه داشته شد.

### ت-۶) دو تلاشِ دیگر برایِ نجاتِ مدل (هر دو سنجیده و رد شدند)

**تلاشِ اول — تغییرِ فضایِ معادله به لگاریتمِ شعاع.**
معادله‌ی نفوذِ حالتِ دائمی (`r·C″ + C′ = 0`) یک معادله‌ی اویلر–کوشی است و با تغییرِ
متغیرِ `u = ln(r)` به `d²C/du² = 0` تبدیل می‌شود که **ضریب‌ثابت** است. پس اگر فیزیکِ
نفوذ حاکم باشد، برازش در فضایِ `ln(r)` باید بهتر شود.
→ **رد شد:** به‌طورِ معنادار **بدتر** شد.

**تلاشِ دوم — تغییرِ فضایِ نوری.**
شدتِ پیکسل با چگالیِ سلولی رابطه‌ی خطی ندارد. دو تبدیلِ فیزیکیِ استاندارد آزموده شد:
بیر–لامبرت (مناسبِ عبورِ نور) و کوبلکا–مونک (مناسبِ بازتاب — یعنی حالتِ ما).

| فضا | کیفیتِ برازش (R²) | خطایِ مرز (MAE) |
|---|---:|---:|
| شدتِ خام | ۰.۵۵۲ | **۳.۴۰ mm** |
| بیر–لامبرت | ۰.۵۷۷ | ۳.۸۶ mm |
| کوبلکا–مونک | **۰.۶۵۲** | ۷.۰۰ mm |

**پیش‌بینیِ فیزیکی درست درآمد** — کوبلکا–مونک بهترین کیفیتِ برازش را داد. **ولی
مرز بدتر شد.**

### 🔬 ت-۷) مهم‌ترین یافته‌ی روش‌شناختیِ کلِ پروژه

برایِ فهمیدنِ علت، ضریبِ گم‌شده‌ی کوبلکا–مونک (بازتابِ مطلقِ لَون) را **جاروب** کردیم.
نتیجه یک الگویِ کاملاً تمیز بود:

| ضریب | کیفیتِ برازش (R²) | خطایِ مرز (MAE) |
|---|---:|---:|
| کم | ۰.۵۸۶ | ۴.۵۷ |
| متوسط | ۰.۶۳۸ | ۴.۹۱ |
| زیاد | **۰.۷۲۶** | **۶.۵۶** |

**هرچه برازش بهتر، مرز بدتر — به‌طورِ یکنواخت.**

**چرا:** یک تبدیل می‌تواند منحنی را «سیگموئیدی‌تر» و برازش‌پذیرتر کند، دقیقاً از راهِ
**فشردنِ یکی از دو سرِ منحنی**. و همان فشردگی، نقطه‌ی عطف را از مرزِ زیستیِ واقعی
دور می‌کند.

> **نتیجه‌ی قابلِ ارائه:** «کیفیتِ برازش» معیارِ معتبری برایِ انتخابِ مدل در این
> مسئله **نیست**. این یک هشدارِ روش‌شناختیِ عمومی است و ارزشِ مستقل دارد.

### 🎯 ت-۸) علتِ ریشه‌ایِ نهایی — که همه‌چیز را توضیح می‌دهد

سؤالِ نهایی این بود: چرا **هیچ‌کدام** از این روش‌ها رویِ نیمی از دیسک‌ها کار نمی‌کند؟

اندازه‌گیریِ مستقیم جواب داد. رویِ **زیرمجموعه‌ی بحرانی** (۳۳ دیسکی که تقریباً کلِ
خطا آن‌جاست):

| جدایشِ «داخلِ هاله» از «لَون» (بر حسبِ انحرافِ معیارِ خودِ لَون) | مقدار |
|---|---:|
| نمونه‌گیریِ عادی | **۰.۳۷ σ** |
| نمونه‌گیری از داخلِ هاله‌ی **مرجعِ کارشناسی** (یعنی با دادنِ جوابِ درست به الگوریتم) | **۰.۲۹ σ** |
| نسبتی که به ۱σ می‌رسند | ۶٪ |

**یعنی: داخلِ یک هاله‌ی واقعیِ ۲۳ میلی‌متری، با لَونِ باکتری، در روشناییِ متوسط
حدودِ یک‌سومِ نوسانِ طبیعیِ خودِ لَون فرق دارد.**

**به زبانِ ساده: مرز در تصویر حاضر نیست.** نه این‌که پیدایش نمی‌کنیم — اصلاً آن‌جا
نیست، در سطحِ کنتراستی که قابلِ تفکیک باشد.

**و این یک علت است با پنج نشانه:**

| نشانه‌ای که دیده بودیم | توضیحش با همین علت |
|---|---|
| Otsu و واترشد آن‌جا شلیک نمی‌کنند | نه دوقله‌گی هست، نه لبه‌ای برایِ سیلاب |
| کانالِ بافت ۹۹–۱۰۰٪ هم‌پوشانی داشت | دو توزیع تقریباً یکی‌اند |
| مدلِ رشد افزونه بود | پله‌ای برایِ برازش وجود ندارد |
| تبدیل‌هایِ نوری شکست خوردند | تبدیلِ نقطه‌ای نمی‌تواند بینِ دو توزیعِ هم‌پوشان جدایی **بسازد** |
| تطبیقِ بافتِ هاله شلیک نکرد | جدایش ۰.۳σ |

### ت-۹) آیا با فیلترِ تصویر قابلِ حل است؟ سنجیده شد — نه

یک تفکیکِ نظری: **تبدیلِ نقطه‌ای** (هر پیکسل مستقل) اگر یکنوا باشد ترتیبِ پیکسل‌ها
را حفظ می‌کند و **تفکیک‌پذیریِ واقعی را اصلاً تغییر نمی‌دهد**. ولی **فیلترِ فضایی**
(میانگین رویِ همسایگی) *می‌تواند* کمک کند — به شرطی که نویز **مستقل** باشد،
چون میانگینِ N پیکسل نویز را √N برابر کم می‌کند.

**پس سؤالِ واقعی: نویزِ لَون مستقل است یا ساختاریافته؟**

| کرنل | تعدادِ پیکسل | جدایشِ واقعی | اگر نویز مستقل بود |
|---|---:|---:|---:|
| بدونِ فیلتر | ۱ | ۰.۳۶ | ۰.۳۶ |
| متوسط | ۸۱ | ۰.۵۴ | ۳.۲۴ |
| بزرگ | ۱۲۲۵ | ۰.۷۱ | **۱۲.۶۰** |

میانگین‌گیری رویِ ۱۲۲۵ پیکسل فقط **۱.۹۶ برابر** بهبود داد، نه ۳۵ برابر.
یعنی **۹۴٪ از پراکندگیِ لَون نویزِ ساختاریافته است** (بافتِ خودِ لَون، ناهمواریِ
آگار) و با هیچ میانگین‌گیری‌ای پاک نمی‌شود.

**و یک بده‌بستانِ تعیین‌کننده:** فیلتر **مرز را هم به همان اندازه تار می‌کند**.
بهترین حالت، ۲.۶۳ میلی‌متر عدمِ‌قطعیتِ قطر وارد می‌کند — بدتر از باندِ ±۲ میلی‌متریِ
انسانی.

> **جمله‌ی کلیدیِ ارائه:** فیلتر فقط سیگنالِ موجود را **بازآرایی** می‌کند؛ نورِ
> پس‌زمینه‌ای خودِ **سیگنال** را عوض می‌کند. اطلاعاتی که در تصویر نیست، با هیچ
> فیلتری ساخته نمی‌شود.

---

### 🔑 ت-۱۰) شروطی که این راه‌حل را کار می‌اندازد

**این بخش را به‌عنوانِ «پیشنهادِ کارِ آینده» در مقاله و پایان‌نامه بیاورید.** نکته‌ی
قوتش این است که هر شرط با یک **عددِ هدفِ مشخص** همراه است، نه یک توصیه‌ی کلی.

#### شرطِ ۱ — نورپردازیِ پس‌زمینه‌ای (transillumination)

نور از **زیرِ** ظرف بتابد، نه از بالا.

**چرا کار می‌کند (نه صرفاً «بهتر است»):** در حالتِ فعلی (بازتابی)، هاله و لَون هر
دو نور را بازمی‌تابانند و فقط ۱۰٪ فرق دارند. در حالتِ عبوری، **مکانیسمِ فیزیکی
عوض می‌شود**: هاله آگارِ شفاف است و نور را عبور می‌دهد، لَون کدر است و جلویش را
می‌گیرد. این یک تفاوتِ **جنسی** است نه درجه‌ای.

سه فایده‌ی هم‌زمان:
- قانونِ بیر–لامبرت **مستقیماً** برقرار می‌شود (مبنایِ فیزیکیِ محکم).
- هاله **روشن** می‌شود نه تاریک، پس تبدیل در ناحیه‌ی پرنویز واگرا نمی‌شود.
- جدایش باید بالا برود.

> **هدفِ کمّیِ قابلِ سنجش:** جدایش باید از ~۰.۳σ به **بالایِ ۲σ** برسد.
> **صادقانه بگویید:** این با تصاویرِ واقعیِ پس‌زمینه‌ای باید آزموده شود؛ ما آن
> تصاویر را نداشتیم. شبیه‌سازیِ نرم‌افزاریِ آن را آزمودیم و **کار نکرد** (که
> انتظار هم می‌رفت — وارونه کردنِ یک عکس، فیزیکِ عبور را نمی‌سازد).

#### شرطِ ۲ — کارتِ مرجعِ سفید/خاکستری در کادرِ عکس

**چرا:** کوبلکا–مونک به بازتابِ **مطلق** نیاز دارد، ولی ما فقط بازتابِ **نسبی به
لَون** را داریم. ضریبِ گم‌شده دقیقاً همان چیزی است که یک وصله‌ی سفیدِ مرجع می‌دهد —
و ما نشان دادیم این همان درجه‌ی آزادیِ گم‌شده است.

**هزینه: تقریباً صفر.** فقط یک کارتِ خاکستریِ استاندارد کنارِ پلیت.

#### شرطِ ۳ — عکس‌برداریِ زمان‌گذر (time-lapse)

**چرا:** معادله‌ی کوپر–وودمن سه مجهول دارد — `D` (ضریبِ نفوذ)، `t` (زمانِ بحرانی)،
`C_crit` (همان MIC) — که **از یک قابِ تکی قابلِ تفکیک نیستند**. دو یا چند نقطه‌ی
زمانی، `D` را از `t` جدا می‌کند.

#### شرطِ ۴ — یک پارامتر از آزمایشگاه: مقدارِ MIC

اگر آزمایشگاه برایِ همان جفتِ باکتری–آنتی‌بیوتیک مقدارِ **MIC** را (که با روشِ رقتِ
براث به‌طورِ روتین اندازه می‌گیرند) در اختیار بگذارد، `C_crit` معلوم می‌شود و
معادله یک مجهولِ کمتر پیدا می‌کند.

#### شرطِ ۵ — مرجعِ بهتر برایِ اعتبارسنجی

**این شرط از جنسِ دیگری است: به بهترشدنِ سیستم کمک نمی‌کند، به قابلِ‌اثبات شدنش
کمک می‌کند.**

با ±۲ میلی‌متر پراکندگیِ کارشناس، هیچ دقتی زیرِ حدودِ ۱.۲ میلی‌متر **قابلِ نمایش
نیست** — نمی‌شود دقتی را نشان داد که از نویزِ خودِ خط‌کشِ سنجش کوچک‌تر است.

| راه | اثر | هزینه |
|---|---|---|
| **میانگینِ n خوانشِ مستقل از همان پلیت** | نویز با ۱/√n کم می‌شود: ۴ خوانش → ±۱ میلی‌متر | فقط وقتِ کارشناس |
| مرجعِ MIC از رقتِ براث | روشِ مرجعِ کاملاً مستقل | گران‌تر |

> **توصیه‌ی عملیِ فوری:** یک **دورِ دومِ اندازه‌گیریِ کارشناسی** رویِ همان ۱۱ پلیت.
> هم مرجع را بهتر می‌کند، و هم عددِ ±۲ را که الان یک **اظهارِ شفاهی** است به یک
> اندازه‌گیریِ خودمان تبدیل می‌کند — که برایِ پایان‌نامه قابلِ استنادتر است.

---

## بخشِ ث) جمع‌بندی

### سه محدودیتِ اصلیِ پروژه — و نکته‌ی مشترکشان

| محدودیت | چه چیزی لازم است |
|---|---|
| جدایشِ ۰.۳σ رویِ نیمی از دیسک‌ها | **تصویربرداریِ بهتر** (نورِ پس‌زمینه‌ای) |
| نویزِ ±۲ میلی‌متریِ مرجع | **مرجعِ بهتر** (چند خوانش، یا MIC) |
| ۱۱ عکس برایِ این تعداد تصمیمِ طراحی | **داده‌ی بیشتر** |

> **هیچ‌کدام با الگوریتمِ بهتر حل نمی‌شوند.** و این خودش یک یافته است: نشان می‌دهد
> فضایِ راه‌حلِ نرم‌افزاری تا کجا کاوش شده و مرزِ واقعی کجاست — و هر سه مرز با
> **اندازه‌گیری** مشخص شده‌اند، نه با حدس.

### جمله‌ی نهاییِ پیشنهادی

> سیستم در وضعِ فعلی یک **ابزارِ غربالگری** است، نه ابزارِ تصمیمِ بالینی. مسیرِ
> رسیدن به سطحِ بالینی مشخص و کمّی شده است، و اولین قدمش الگوریتم نیست —
> **شرایطِ تصویربرداری** است.

---

## پیوست — نمودارهایِ پیشنهادی برایِ اسلایدها

۱. **نمودارِ میله‌ای مسیرِ بهبود:** MAE در سه مرحله (۵.۵۰ → ۳.۸۵ → ۳.۸۵) کنارِ
   دقتِ حضور (۰.۷۸۰ → ۰.۸۱۳ → ۰.۸۲۴).
۲. **نمودارِ Bland–Altman:** اختلافِ سیستم و کارشناس در برابرِ میانگینشان، با
   حدودِ ۹۵٪ — استانداردِ مقایسه‌ی دو روشِ اندازه‌گیری.
۳. **جدولِ مقایسه‌ی شاخه‌ها** (پوشش در برابرِ دقت) — مبنایِ منطقِ ادغام.
۴. **نمودارِ تضادِ R² و MAE** در جاروبِ کوبلکا–مونک — یافته‌ی روش‌شناختی.
۵. **نمودارِ رشدِ جدایش با اندازه‌ی کرنل**، در کنارِ منحنیِ √N — نشان می‌دهد
   فاصله چقدر زیاد است.
۶. **تصویرِ خروجیِ پایپ‌لاین** رویِ یک پلیتِ واقعی، با دایره‌ها و اعداد.
۷. **دو هیستوگرامِ هم‌پوشان** (داخلِ هاله و لَون) برایِ زیرمجموعه‌ی بحرانی —
   تصویری‌ترین راهِ نشان دادنِ «مرز در تصویر نیست».
