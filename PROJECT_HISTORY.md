# تاریخچه و مستندات فنی پروژه آنتی‌بایوگرام (Antibiogram Detection Pipeline)

این سند برای حفظ تاریخچه‌ی پروژه، مفروضات، تصمیم‌های طراحی، باگ‌های پیدا‌شده و کارهای باقی‌مانده نوشته شده تا در جلسات بعدی (چه با کلود، چه با هر توسعه‌دهنده‌ی دیگر) زمینه‌ی کامل کار از دست نرود.

آخرین به‌روزرسانی: بر اساس شاخه‌ی `claude/antibiogram-detection-completion-kcq6si`.

---

## ۱) هدف پروژه

سیستمی بینایی ماشین برای **تشخیص خودکار نتایج آزمایش آنتی‌بایوگرام** از روی عکس، طبق استاندارد **EUCAST** (اتحادیه‌ی اروپا برای تست حساسیت میکروبی به آنتی‌بیوتیک). پایپلاین باید به‌ترتیب:

1. **ظرف(های) پتری** را در عکس پیدا کند (ممکن است بیش از یک پتری در قاب باشد).
2. **دیسک‌های آنتی‌بیوتیک** روی هر پتری را پیدا کند (سفید با نوشته‌ی سیاه، در دو قطر ۶ و ۸ میلی‌متر، تعداد رندوم).
3. **هاله‌ی مهار (Inhibition Halo)** دور هر دیسک را (در صورت تشکیل) تشخیص دهد و قطرش را تخمین بزند.
4. **رخدادهای روی هاله** (مثل حباب‌های ریز) را شناسایی و گزارش کند (تعداد + اندازه/حجم تقریبی).
5. در نهایت گزارشی بدهد: قطر دیسک → قطر هاله → رخدادهای حباب، برای هر دیسک در هر پتری.

هدف بلندمدت‌تر (خارج از محدوده‌ی کار فعلی): استفاده از جداول Breakpoint استاندارد EUCAST (فایل‌های PDF موجود در ریپازیتوری: `v_16.0_Breakpoint_Tables.pdf` و `Dosages_v_16.0_Breakpoint_Tables.pdf`) برای طبقه‌بندی نتیجه‌ی هر دیسک به حساس/متوسط/مقاوم (S/I/R) بر اساس قطر هاله. **این بخش هنوز پیاده‌سازی نشده است.**

---

## ۲) مفروضات و الزامات کلیدی پروژه

این‌ها الزاماتی هستند که از ابتدای پروژه توسط کاربر تعیین شده‌اند و باید در هر توسعه‌ی بعدی رعایت شوند:

### ۲.۱) پتری
- ممکن است **دایره‌ای یا مربعی/مستطیلی** باشد.
- تعداد پتری در تصویر **محدودیت ندارد** (می‌تواند صفر، یک یا چند تا باشد).
- حتی اگر تشخیص دقیق شکل ممکن نباشد، حداقل باید بک‌گراندی که جزو پتری نیست حذف شود (fallback عمومی).

### ۲.۲) دیسک
- سفید رنگ با نوشته‌ی سیاه روی خودش.
- تعداد دیسک در هر پتری **رندوم** است.
- دو سایز استاندارد: **۶ میلی‌متر** و **۸ میلی‌متر** قطر — ممکن است هر دو سایز هم‌زمان در یک پتری باشند.

### ۲.۳) هاله
- ممکن است تشکیل شود یا نشود.
- نتیجه‌ی آزمایش هر دیسک به **قطر هاله** بستگی دارد.
- روی هاله ممکن است **حباب‌های ریز** شکل بگیرد؛ شناسایی آن‌ها (تعداد + حجم تقریبی توده) هم مهم است.

### ۲.۴) الزام robust بودن (بسیار مهم — نقض‌نشدنی)
> **هیچ مقداردهی مستقیم فیزیکی (طول به پیکسل، تعداد ثابت، آستانه‌ی ثابت شدت و مانند آن) نباید برای هیچ پارامتری — به‌خصوص پارامترهای فیزیکی — انجام شود.**
> همه‌ی اندازه‌ها و بازه‌ها باید **نسبت به اندازه‌ی خودِ تصویر، پتری یا دیسک** تعریف شوند (مثلاً کرنل مورفولوژی = فلان‌درصد قطر دیسک، نه فلان پیکسل ثابت).

این الزام در کل کد رعایت شده: تمام پارامترهای جدیدی که در این نشست اضافه شدند (کانفیگ‌های `cfg.dish_detect_*`, `cfg.bubble_*`, `cfg.morph_*_frac` و غیره) نسبت‌محورند. برخی فیلدهای **قدیمی و مطلق** در `Phase2Config` (سلول ۳) که در طراحی اولیه‌ی نوت‌بوک (پیش از این نشست) وجود داشتند و در کد فعلی استفاده نمی‌شوند (مثلاً `HoughSweepConfig`، `dish_roi_*`، `score_*_weight`) عمداً دست‌نخورده باقی مانده‌اند چون مرده/بلااستفاده‌اند؛ پاک‌سازی کامل آن‌ها خارج از محدوده‌ی این نشست بوده تا ریسک اضافه به پروژه تحمیل نشود.

### ۲.۴.۱) دو قانون تکمیلیِ نقض‌نشدنی (تأکید صریح کاربر — همیشه باید رعایت شوند)

**(الف) هیچ سقف/محدودیت ثابتی روی پارامترهای فیزیکی — نه فقط اندازه، بلکه تعداد هم — گذاشته نشود.**
یعنی نه‌فقط طول/قطر/شعاع نباید پیکسل مطلق باشند (بند ۲.۴ بالا)، بلکه **تعداد** (مثلاً حداکثر تعداد پتری در یک عکس، حداکثر تعداد دیسک در یک پتری، حداکثر تعداد کاندید Hough که پردازش می‌شود) هم نباید با یک عدد ثابت و دلخواه سقف زده شود — مگر این‌که واقعاً به این نتیجه برسیم که **هیچ راه‌حل دیگری وجود ندارد** (یعنی آخرین راه‌حل، نه اولین). اگر برای کارایی/سرعت نیاز به محدود کردن یک حلقه یا جست‌وجو بود، باید راه‌حل الگوریتمی (مثل early-stopping بر اساس همگرایی امتیاز، نه یک عدد ثابت دلخواه مثل «حداکثر ۱۰ پتری») در اولویت باشد.

**(ب) Overfitting روی عکس‌های موجود در ریپازیتوری قطعاً ممنوع است — تکرار: به هیچ عنوان.**
هیچ پارامتر، آستانه، یا مسیر منطقی نباید طوری تنظیم/نوشته شود که مخصوص یکی از عکس‌های موجود در ریپازیتوری کار کند (حتی اگر نتیجه‌ی آن عکس خاص را بهتر کند). هر اصلاح باید بر اساس **استدلال عمومی و مستقل از عکس خاص** باشد (تئوری CV، فیزیک مسئله مثل دو سایز استاندارد دیسک، یا رفتار آماری robust) — نه reverse-engineering یک آستانه از روی اندازه‌گیری دستی یک عکس. عکس‌های موجود فقط برای **تست/اعتبارسنجی** استفاده می‌شوند، نه برای طراحی. قبل از هر کامیت، باید حداقل روی چند عکس **متنوع و قبلاً بررسی‌نشده** (نه فقط عکس‌هایی که مشکل ازشون کشف شده) هم تست شود تا اطمینان حاصل شود رفتار عمومی است.

### ۲.۵) ساختار کد
- نوت‌بوک Jupyter، **ماژولار و سلول‌به‌سلول** — هر سلول یک بلوک/فیلتر مستقل با یک خروجی تصویری.
- سلول‌ها پشت‌سرهم متصل‌اند (خروجی سلول قبلی = ورودی سلول بعدی).
- این ساختار باید **حفظ شود** — رویه‌ی توسعه نباید آن را به یک اسکریپت تک‌تکه تبدیل کند.

### ۲.۶) هدف انتقال به ++C
- پروژه قرار است در آینده به C++ ترجمه/کامپایل شود.
- به همین دلیل، الگوریتم‌ها باید **deterministic** باشند (مرتب‌سازی صریح، tie-break مشخص، بدون وابستگی به رفتار تصادفی یا ترتیب هش).
- ساختارهای داده باید ساده و قابل‌نگاشت به `struct` باشند (در کد فعلی: دیکشنری‌های ساده با کلیدهای ثابت).

---

## ۳) معماری فعلی پایپلاین (نسخه‌ی بعد از این نشست)

فایل اصلی: **`disk_detection_pipeline_modular.ipynb`** (۴۳ سلول).

| ماژول | نام | نقش |
|---|---|---|
| ۱ | Config + Config Extension | تعریف تمام پارامترهای نسبی (`cfg`) |
| ۲ | Helper Functions | توابع مشترک: کرنل بیضوی، نمایش تصویر، NMS، متریک‌های شکل، Watershed، multi-Otsu، پروفایل شعاعی |
| ۳ | بارگذاری تصویر | خواندن عکس ورودی |
| **۴** | **تشخیص همه‌ی پتری‌ها** | **بازطراحی کامل در این نشست — بخش ۴ همین سند را ببینید** |
| ۴.۱ | ماسک دقیق پتری | فید مرز + ماسک دقیق‌تر، به‌صورت per-dish |
| ۵ | Disk Feature Image | جایگزین White Top-Hat ساده؛ ترکیب Top-Hat + کنتراست محلی با نرمال‌سازی robust |
| ۶ | Adaptive Threshold | **بازطراحی‌شده در این نشست** (رجوع به بخش ۵.۱) |
| ۷/۸ | Morphological Closing/Opening | کرنل‌ها نسبت به قطر کمینه‌ی دیسک (نه پیکسل ثابت) |
| ۹ | Distance Transform | ورودی مشترک برای Watershed (ماژول ۱۲) و نمایش تشخیصی (ماژول ۱۳) |
| ۱۰ | Halo Gradient Map | سیگنال گرادیان که اکنون در ماژول ۱۷ (حباب) بازیافت می‌شود |
| ۱۰.۵ | Disk-Specific Edge Image | ورودی edge-support برای شاخه‌ی Hough |
| ۱۱ | Hough (شاخه‌ی اصلی/دقیق) | شاخه‌ی قابل‌اعتماد تشخیص دیسک |
| ۱۲ | Blob Analysis | **بازطراحی‌شده در این نشست** (رجوع به بخش ۵.۲) |
| ۱۳ | DT Peaks / Watershed Markers | دیگر شاخه‌ی مستقل نیست؛ فقط نمایش تشخیصی نشانگرهایی که ماژول ۱۲ استفاده می‌کند |
| ۱۴ | Fusion (ادغام دو‌شاخه‌ای) | **بازطراحی‌شده** — Hough + Blob، بدون DT به‌عنوان رأی‌دهنده مستقل |
| ۱۵ | نمایش نتایج نهایی | رسم دیسک‌های نهایی + پرچم پتری‌های مشکوک به FP |
| ۱۶ | تشخیص هاله‌ی مهار | پروفایل شعاعی نسبی — بدون تغییر منطقی، فقط per-dish شد |
| **۱۷** | **تشخیص حباب روی هاله** | **ماژول کاملاً جدید** |
| ۱۸ | گزارش نهایی یکپارچه | **سلول جدید** — خروجی نهایی: قطر دیسک → قطر هاله → رخداد حباب |

**معماری per-dish:** به‌جای متغیرهای سراسری تکی (`roi_gray`, `processing_mask_roi`, ...)، یک لیست `dishes: List[Dict]` ساخته می‌شود؛ هر عضو یک پتری با تمام فیلدهای مربوط به خودش (ماسک، ROI، شعاع، کاندیدهای هر شاخه، نتایج هاله/حباب و ...) است. هر ماژول بعدی با `for dish in dishes:` روی همه‌ی پتری‌ها حلقه می‌زند و خروجی هرکدام را جداگانه نمایش/چاپ می‌کند.

---

## ۴) بازطراحی ماژول ۴ (تشخیص پتری) — مهم‌ترین تغییر این نشست

### مشکل اولیه (گزارش‌شده توسط کاربر)
کد قبلی فقط **بهترین کاندید تک** را نگه می‌داشت؛ حتی با وجود چند پتری در تصویر، فقط یکی تشخیص داده می‌شد.

### نسخه‌ی اول اصلاح (Canny + Contour + Hough چندگانه)
ابتدا `detect_petri_dish` به `detect_all_petri_dishes` تبدیل شد تا همه‌ی کاندیدهای معتبر (نه فقط بهترین) را نگه دارد، با پشتیبانی از دایره/مربع/fallback عمومی (بر اساس circularity، rectangularity، solidity کانتور). ماسک هر پتری از کانتور واقعی ساخته می‌شود (`cv2.drawContours`)، نه یک دایره‌ی اجباری.

### مشکل بزرگ‌تری که در تست روی عکس واقعی پیدا شد
با آپلود ۴۱ عکس واقعی توسط کاربر و تست سیستماتیک، مشخص شد:
- **`HoughCircles` به‌تنهایی روی یک عکس ۴۰۰۰×۳۰۰۰ حدود ۶۰ ثانیه طول می‌کشد.**
- **مسیر Canny+Contour تقریباً هیچ‌وقت روی عکس واقعی به یک مرز بسته نمی‌رسد** (نور ناهموار، بازتاب لبه‌ی شیشه‌ای پتری، پس‌زمینه‌ی شلوغ) — حتی با closing خیلی بزرگ.
- در نتیجه تقریباً هر عکس واقعی مجبور بود به fallback کند (Hough) سقوط کند، و آن Hough هم گاهی چند دایره‌ی کاذب برمی‌گرداند (چون هیچ اعتبارسنجی مرزی نداشت) یا هیچ‌چیز پیدا نمی‌کرد.

### راه‌حل نهایی: Segmentation ناحیه‌ای (Region-based) به‌جای لبه‌محور
جایگزین شد با:
1. **آستانه‌گذاری Multi-Otsu دوگانه** (بسط کلاسیک Otsu به ۳ کلاس: پس‌زمینه / آگار / برچسب روشن دیسک) — تابع `_multi_otsu_threshold` در سلول Helper Functions. روی نسخه‌ی کوچک‌شده‌ی تصویر (400×400) محاسبه می‌شود، پس فقط چند صدم ثانیه طول می‌کشد.
2. ماسک foreground = پیکسل‌های بالاتر از آستانه‌ی اول (`t1`) → یعنی «آگار + برچسب»، جدا از پس‌زمینه‌ی تیره.
3. **closing سبک** برای پر کردن حفره‌های ریز بافت آگار.
4. هر مولفه‌ی متصل هم‌اندازه‌ی یک پتری = یک کاندید.
5. اگر segmentation ناحیه‌ای هیچ کاندیدی پیدا نکرد، **یا** همه‌ی کاندیدها فقط با مسیر عمومی/کم‌اعتماد (`region_generic` — یعنی نه دایره‌ای نه مربعی قابل‌تشخیص، مثلاً دو پتری که در یک ناحیه‌ی متصل ادغام شده‌اند) طبقه‌بندی شدند، مسیر قدیمی Canny+Contour+Hough به‌عنوان **تاییدکننده/تکمیل‌کننده** (نه پیش‌فرض) اجرا و کاندیدهایش به استخر کاندیدها اضافه می‌شود؛ امتیازدهی + NMS موجود تصمیم می‌گیرد کدام مجموعه بهتر است.

### یک مسیر رفته و برگشته: Watershed-splitting در سطح پتری
در حین توسعه، ابتدا برای مولفه‌های بزرگ (احتمال ادغام دو پتری چسبیده) یک شکستن با Watershed (نشانگر از قله‌های Distance Transform، در مقیاس پتری) اضافه شد. اما تست روی عکس واقعی نشان داد **این کار روی پتری‌های منفرد معتبر (به‌خصوص مربعی، که Distance Transform آن‌ها یک «تیغه» دارد نه یک قله‌ی تیز) هم قله‌های کاذب پیدا می‌کند** و به‌جای پتری کامل، یک تکه‌ی کوچک‌تر و ظاهراً «تمیزتر» را برنده‌ی امتیازدهی می‌کند — یعنی خودِ مکانیزم شکستن باعث false positive جدید می‌شد. **این مکانیزم حذف شد.** راه‌حل واقعی برای پتری‌های چسبیده همان مکانیزم «تاییدکننده‌ی Hough برای کاندیدهای `region_generic`» در بالاست که به‌شکل ایمن‌تری همین مشکل را حل می‌کند.

### نتایج تایید‌شده (روی عکس‌های واقعی)
- دو پتری چسبیده/نزدیک‌به‌هم در یک قاب: قبلاً یا ۷ پتری کاذب یا فقط ۱ پتری تشخیص داده می‌شد؛ الان هر دو با شعاع دقیق پیدا می‌شوند.
- هر دو پتری مربعی نمونه (`1.1.1. original.jpg`, `2.2.1. original.jpg`): قبلاً بدترین حالت پایپلاین بودند (شعاع خیلی کوچک‌تر از واقعی چون فقط دایره فیت می‌شد)؛ الان با روش `region_rect` و شعاع تقریباً دقیق (~۵۵۶px، در عرض ۰.۰۵ ثانیه) تشخیص داده می‌شوند.
- پتری‌های ساده‌ی تک‌دایره: نتیجه درست است (از طریق fallback ایمن)، ولی هنوز همیشه از مسیر سریع استفاده نمی‌کند — **این یک هدف بهینه‌سازی باز است، نه یک باگ** (بخش ۷ را ببینید).

### ۴.۱) باگ جدید (کشف‌شده بعد از تست گسترده‌ی ۶ عکس واقعی): پتری‌های کاذب اضافی

بعد از بازطراحی بالا، بازرسی چشمی thumbnail چند عکس واقعی (که از قبل مشخص بود دقیقاً ۱ پتری واقعی دارند) نشان داد پایپلاین همچنان **پتری‌های کاذب اضافی** گزارش می‌کند — همیشه از طریق مسیر `hough_fallback`، نه segmentation ناحیه‌ای. نتیجه‌ی اسکن کامل (با فیلتر containment، بخش بعد را ببینید):

| عکس | تعداد پتری واقعی | تعداد گزارش‌شده | یادداشت |
|---|---|---|---|
| `20260101_142413.jpg` | ۱ | ۱ | ✅ رفع شد |
| `20260101_142534.jpg` | ۱ | ۲ | ۱ کاذب باقی‌مانده |
| `20260101_142659.jpg` | ۱ | ۳ | ۲ کاذب باقی‌مانده |
| `20260101_142548.jpg` | ۱ | ۵ | ۴ کاذب باقی‌مانده (قبل از فیلتر containment: ۶) |
| `20260101_142502.jpg` | ۲ (واقعی) | ۳ | ۱ کاذب باقی‌مانده |
| `20260101_142518.jpg` | ۲ (واقعی) | ۲ | ✅ درست (دو پتری چسبیده) |

**اولین تلاش برای رفع — فیلتر containment (کامیت `3668eab`):** فرض اولیه این بود که پتری‌های کاذب داخل/هم‌پوشان با پتری واقعی هستند (هاله مرز کاذب نزدیک به مرز پتری می‌سازد)، پس یک فیلتر اضافه شد: هر کاندید کم‌امتیازتر که ≥۸۰٪ مساحتش داخل یک کاندید پرامتیازتر دیگر باشد، حذف می‌شود (`cfg.dish_containment_reject_frac`, تابع `_circle_overlap_ratio` که به سلول Helper Functions منتقل شد تا در ماژول ۴ و ۱۴ به اشتراک برود).

**نتیجه‌ی عیب‌یابی دقیق (محاسبه‌ی واقعی `_circle_overlap_ratio` روی کاندیدهای `20260101_142534.jpg`):**
```
region candidates: [('region2_generic', 0.699, r=1741)]  → این تقریباً خودِ پتری واقعی است
needs_edge_check: True   (چون کاندید فقط generic است)
edge candidates: [('hough_fallback', 0.848, r=1347, x=1331, y=2113),   ← پتری واقعی (Hough آن را هم پیدا کرد)
                   ('hough_fallback', 0.587, r=699,  x=2170, y=115)]    ← کاذب، در گوشه‌ی مقابل تصویر
overlap(کاذب, واقعی) = 0.0000   ← کاملاً منفصل، نه تودرتو!
```
یعنی فرض اولیه غلط بود: کاندید کاذب **هیچ هم‌پوشانی‌ای با پتری واقعی ندارد** — یک تشخیص کاملاً مستقل و بی‌ربط در گوشه‌ی دیگر عکس است (احتمالاً یک لبه‌ی ضعیف نامرتبط با پتری: انعکاس، برچسب دوربین، یا بافت پس‌زمینه که به‌طور تصادفی معیارهای شکل/edge_support را رد کرده). **فیلتر containment از نظر طراحی هرگز نمی‌توانست این حالت را بگیرد** چون مبتنی بر هم‌پوشانی مکانی است.

**تحلیل ریشه‌ای اولیه (ایده‌ی «Anchoring» — بررسی بیشتر آن را رد کرد، نگه داشته شده به‌عنوان یک مسیر رفته‌وبرگشته‌ی مستند):** طبق مستندسازی خودِ کد (بخش ۴)، مسیر Hough قرار است فقط به‌عنوان «تاییدکننده/تکمیل‌کننده»ی کاندیدهای segmentation ناحیه‌ای اجرا شود، نه یک جست‌وجوی کاملاً مستقل. اما در پیاده‌سازی فعلی، وقتی اجرا می‌شود همه‌ی کاندیدهایش (هرجای تصویر) بدون قید به استخر مشترک اضافه می‌شوند. ایده‌ی اول: «فقط کاندید Hough را نگه دار اگر با حداقل یک کاندید ناحیه‌ای هم‌پوشانی معنادار داشته باشد؛ اگر segmentation ناحیه‌ای اصلاً چیزی پیدا نکرد، همه را نگه دار.» روی `20260101_142518.jpg` (دو پتری واقعی و چسبیده) این ایده امیدوارکننده به نظر رسید: segmentation ناحیه‌ای یک بلاب ادغام‌شده (`region_generic`, یک ناحیه‌ی بزرگ شامل هر دو پتری) پیدا کرد، و **هر دو** کاندید واقعی Hough با این بلاب هم‌پوشانی داشتند (۱.۰۰۰ و ۰.۶۳۷) — برخلاف کاندید کاذب در `142534.jpg` (هم‌پوشانی = ۰.۰۰۰).

**اما بررسی دو عکس دیگر این ایده را رد کرد:**
- `20260101_142548.jpg`: segmentation ناحیه‌ای **هیچ کاندیدی اصلاً پیدا نکرد** (`region_c` کاملاً خالی) — یعنی طبق قاعده‌ی «اگر ناحیه‌ای خالی بود، همه‌ی Hough نگه داشته شوند»، هر ۶ کاندید (که ۵ تای آن‌ها کاذب‌اند) بدون فیلتر باقی می‌مانند. این ایده برای این عکس هیچ کمکی نمی‌کند.
- `20260101_142659.jpg`: segmentation ناحیه‌ای کاندیدهایی با شعاع خیلی بزرگ پیدا کرد (`r=1779`, `r=1673` — به‌احتمال زیاد oversized، بلعیدن بخشی از پس‌زمینه، همان مشکل شناخته‌شده‌ی بخش ۷). چون این بلاب‌ها آن‌قدر بزرگ‌اند، **هر سه** کاندید Hough (هم ۲ تای واقعی و هم ۱ تای کاذب) با آن‌ها هم‌پوشانی بالا دارند (۱.۰۰۰, ۱.۰۰۰, ۰.۹۶۰) — معیار هم‌پوشانی دیگر هیچ تفکیکی نمی‌دهد.
- حتی با IoU (اشتراک/اجتماع) به‌جای «هم‌پوشانی نسبت به کوچک‌تر»، مشکل حل نشد: IoU پتری دوم واقعی در `142518.jpg` (~۰.۱۵) عملاً هم‌مرتبه‌ی IoU کاندیدهای کاذب در `142659.jpg` (~۰.۱۴–۰.۲۳) است. یعنی از نظر آماری، «پتری دوم واقعی داخل یک بلاب ادغام‌شده» و «کاندید کاذب داخل یک بلاب oversized» با این سیگنال قابل تفکیک نیستند.

**نتیجه‌گیری صادقانه:** ایده‌ی anchoring به‌تنهایی **کافی نیست** و کنار گذاشته شد. ریشه‌ی مشکل عمیق‌تر است: وقتی segmentation ناحیه‌ای یا (الف) هیچ‌چیز پیدا نمی‌کند یا (ب) یک بلاب oversized/نادقیق پیدا می‌کند، هیچ سیگنال هندسی ساده‌ای (هم‌پوشانی، IoU، فاصله) نمی‌تواند «پتری واقعی دوم» را از «تشخیص کاذب Hough» تفکیک کند — چون از نظر مکانی هر دو مشابه به نظر می‌رسند.

### ۴.۲) راه‌حل نهایی (نشست بعدی) — اصلاح ریشه‌ای/کلان به‌جای patch لایه‌ی Hough

طبق تحلیل بند قبل، پیشنهاد شماره‌ی ۲ («رفع مستقیم مشکل شناخته‌شده‌ی segmentation ناحیه‌ای به‌جای patch کردن پیامدش») دنبال شد. **ریشه‌ی مشترک** هم شکست/oversized-شدن segmentation ناحیه‌ای و هم ناتمام ماندن کانتور Canny در fallback: **نور ناهموار روی کل قاب عکس** — یک محدودیت کلاسیک و شناخته‌شده‌ی thresholding سراسری (Gonzalez & Woods). راه‌حل استاندارد: **shading correction** (تصحیح میدان روشنایی) قبل از هر آستانه‌گذاری/تشخیص لبه.

**پیاده‌سازی (`illumination_normalize`, سلول توابع کمکی):** تصویر منهای تخمین صاف/بزرگ‌مقیاس روشنایی خودش (Gaussian با کرنل بزرگ‌تر از اندازه‌ی هر جسم واقعی در قاب، `cfg.dish_detect_illum_kernel_frac = 0.5 × min(h,w)`) به‌علاوه‌ی میانگین کلی، سپس نرمال‌سازی به ۰-۲۵۵. یک بار در ابتدای `detect_all_petri_dishes` محاسبه و به **هر دو** مسیر (segmentation ناحیه‌ای و fallback لبه‌محور) داده می‌شود — نه فقط یکی.

**یافته‌ی جانبی مهم (کنترل کیفیت خودِ راه‌حل):** نسخه‌ی اول به‌روش **تقسیمی** (`gray / illum`) بود — روش کلاسیک‌تر flat-fielding. تست روی `20260101_142548.jpg` نشان داد در نواحی خیلی تاریک این عکس، تقسیم بر مقدار نزدیک صفر **نویز را تقویت می‌کند** و پتری‌های کاذب را از ۳ به ۶ افزایش داد (regression مشخص و قابل تکرار). با داده (نه حدس) به نسخه‌ی **تفریقی** (`gray − illum + mean(illum)`) تغییر یافت که این ناپایداری عددی را ندارد؛ نتیجه روی همان عکس: ۶ → ۲ (بهتر از حتی نسخه‌ی قبل از این اصلاح).

**نتیجه‌ی نهایی (۶ عکس مشکل‌دار قبلی + ۳ عکس کاملاً جدید/بررسی‌نشده):**

| عکس | قبل (baseline) | بعد | زمان قبل | زمان بعد |
|---|---|---|---|---|
| `20260101_142534.jpg` | ۲ پتری (۱ کاذب) | **۱ ✅** | ۲۴۴ث | **۶۳ث (۳.۹×)** |
| `20260101_142659.jpg` | ۳ پتری (۲ کاذب) | **۱ ✅** | ۲۴۵ث | **۶۸ث (۳.۶×)** |
| `20260101_142502.jpg` | ۳ پتری (۱ کاذب) | **۲ ✅ (مطابق واقعیت)** | ۲۶۴ث | **۷۲ث (۳.۷×)** |
| `20260101_142751.jpg` | ۳-۴ پتری کاذب | **۱ ✅** | (کند) | **۱۲۲ث** |
| `20260101_142518.jpg` (۲ واقعی) | ۲ ✅ | ۲ ✅ (بدون رگرسیون) | ۸۲ث | ۴۶ث |
| `20260101_142548.jpg` | ۳ پتری کاذب | ۲ (بهتر، هنوز ۱ کاذب) | ۳۷۲ث | ۲۵۸ث |

۴ از ۶ مورد **کاملاً حل شدند**؛ همه با کاهش زمان اجرای ۲ تا ۴ برابر (چون دیگر لازم نیست به fallback کند Hough سقوط کند). `20260101_142548.jpg` بهتر شد ولی هنوز ۱ پتری کاذب دارد — **باقی‌مانده، کار باز** (بخش ۷).

### ۴.۳) دو لایه‌ی self-check تکمیلی (hypothesize-and-verify)

مستقل از تصحیح نور، دو ماژول جدید اضافه شدند که پتری‌های تشخیص‌داده‌شده را با شواهد **مستقل از مسیر تشخیص خودشان** تایید/رد می‌کنند (چون تاییدِ `edge_support` در fallback خودارجاع بود — از همان نقشه‌ی Canny که کاندید را تولید کرده):
- **ماژول ۸.۵ (تیر اول، ارزان):** بعد از Opening (ماژول ۸)، اگر هیچ بلاب هم‌اندازه‌ی دیسک در `thr_open_small` نباشد، پتری رد می‌شود (`cfg.petri_min_plausible_disk_blobs`). سیگنال نسبتاً سست‌گیرانه (گاهی بلاب کاذب هم‌اندازه به تصادف وجود دارد) ولی ارزان و زودهنگام.
- **ماژول ۱۱.۵ (تیر دوم، قوی):** بعد از Hough دیسک (ماژول ۱۱)، اگر هیچ کاندید کاملاً اعتبارسنجی‌شده (لبه+کنتراست+شعاع) نباشد، پتری رد می‌شود (`cfg.petri_min_hough_disk_candidates`).

این دو لایه مکمل تصحیح نورند، نه جایگزین آن — روی موارد باقی‌مانده (مثل `142548.jpg`) به‌تنهایی کافی نبودند (چون کاندید کاذب باقی‌مانده شواهد Hough واقعی هم دارد)، اما به‌عنوان لایه‌ی دفاعی دوم مفیدند و رگرسیونی روی هیچ عکس تست‌شده (شامل ۳ عکس کاملاً جدید) ایجاد نکردند.

### ۴.۴) فیلتر سازگاری شعاع دیسک (Fusion، ماژول ۱۴)

مستقل از موارد بالا، یک فیلتر مکمل به Fusion اضافه شد: طبق فیزیک مسئله (بخش ۲.۲)، دیسک‌ها فقط ۲ اندازه دارند (۶mm/۸mm)؛ شعاع دیسک‌های واقعی یک پتری باید حداکثر در ۲ خوشه قرار گیرد. کاندیدی که تنها عضو خوشه‌ی خودش باشد (singleton) — وقتی حداقل یک خوشه‌ی دیگر با پشتیبانی واقعی هست — رد می‌شود (`cfg.disk_radius_cluster_gap_frac = 0.20`، مبتنی بر نسبت فیزیکی ۸/۶≈۱.۳۳، نه تنظیم‌شده روی یک عکس). هدف: کاهش false positive سطح دیسک (متن روی دیسک، لبه‌ی هاله) — هنوز به‌صورت مجزا روی نمونه‌ی واقعیِ دارای دیسک‌های اندازه‌ی مختلط اعتبارسنجی نشده.

---

## ۵) سایر باگ‌های واقعی پیدا و رفع‌شده در این نشست

### ۵.۱) ماژول ۶ (Adaptive Threshold) — یافته‌ی ساختاری مهم
آستانه‌گذاری **محلی/windowed** (`cv2.adaptiveThreshold`) روی تصویر feature (که خودش از Top-Hat/کنتراست محلی ساخته شده) در حضور یک هاله‌ی نسبتاً پهن، **حدود ۶۵ تا ۷۰٪ از کل تصویر را به‌اشتباه «فورگراند» تشخیص می‌دهد**. دلیل: هاله یک گرادیان نرم و پیوسته است، نه نویز موضعی؛ آستانه‌گذاری محلی روی چنین سیگنالی، اصولاً نیمی از هر پنجره را «بالاتر از میانگین محلی» علامت می‌زند (یک خاصیت ریاضی خودِ adaptive threshold، نه ایراد یک عکس خاص). این ریشه‌ی اصلی ضعف شاخه‌ی Blob Analysis بود (که قبل از این اصلاح همیشه ۰ کاندید برمی‌گرداند).

**راه‌حل:** جایگزینی با آستانه‌ی سراسری robust (percentile نسبت به آمار خودِ پتری) — هم‌راستا با منطقی که ماژول ۵ از قبل داشت.

### ۵.۲) ماژول ۱۲ (Blob Analysis) — بازطراحی کامل
مشکلات ریشه‌ای که پیدا شد (علاوه بر ۵.۱):
- `blob_min_area`/`blob_max_area` مقدار **پیکسل مطلق** بودند (نقض مستقیم الزام ۲.۴) — در رزولوشن‌های دیگر، بازه‌ی واقعی دیسک از این بازه‌ی ثابت خارج می‌شد.
- فیلتر اعتبارسنجی شعاع از `hough_min_r`/`hough_max_r` (فیلدهای مطلق یک نسخه‌ی خیلی قدیمی‌تر) استفاده می‌کرد، بدون ربط به بازه‌ی نسبی واقعی دیسک.
- کرنل‌های مورفولوژی (`adaptive_block_size`, `morph_closing_ksize_small`, `morph_opening_ksize_small`) پیکسل مطلق بودند.
- دیسک‌های چسبیده/نزدیک به هم هیچ مکانیزم جداسازی نداشتند.

**راه‌حل:**
1. بازه‌ی مساحت/شعاع از همان سیستم نسبی مشترک (`disk_radius_bounds_px`, تابع کمکی جدید) مشتق می‌شود.
2. کرنل‌های closing/opening نسبت به قطر کمینه‌ی دیسک.
3. منبع باینری از **اجتماع** دو ماسک (`thr_open_small` و `disk_feature_mask` ماژول ۵) برای پوشش بهتر.
4. **Watershed** (نشانگر از قله‌های Distance Transform ماژول ۹) برای جدا کردن دیسک‌های چسبیده — این‌جا دقیقاً همان جایی است که DT به‌عنوان **ابزار کمکی** (نه شاخه‌ی مستقل) به کار می‌آید.

### ۵.۳) نقش ماژول DT (۱۳) — بازتعریف فلسفی
پارامترهای قدیمی DT (`dt_peak_min_value=8.0`, `dt_peak_min_distance=35`, `candidate_r_min/max=8/60`) همگی **پیکسل مطلق** بودند و DT را به‌عنوان شاخه‌ی سوم مستقل (رأی‌دهنده در Fusion) ناسازگار با مقیاس و نویزپذیر می‌کردند.

**تصمیم طراحی:** DT دیگر کاندید مستقل تولید نمی‌کند. نقش آن به ابزار زیرساختی نسبی تغییر کرد:
- **ابزار ۱:** نشانگر Watershed برای جداسازی دیسک‌های چسبیده در ماژول ۱۲.
- **ابزار ۲:** تخمین شعاع بلاب‌ها (`dt_patch` در ماژول ۱۲).
- **ابزار ۳ (ماژول ۱۷ جدید):** تخمین شعاع هر حباب روی هاله.

سلول ماژول ۱۳ اکنون فقط یک **نمایش تشخیصی** است (چه نشانگرهایی برای Watershed استفاده شدند)، نه تولیدکننده‌ی کاندید.

### ۵.۴) ماژول ۱۴ (Fusion) — از سه‌شاخه به دو‌شاخه
فلسفه‌ی جدید: **Hough** (شاخه‌ی اصلی/دقیق، طبق تایید کاربر روی داده‌ی واقعی خوب کار می‌کند) + **Blob اصلاح‌شده** (شاخه‌ی پوششی/recall). منطق clustering/NMS موجود (`_circle_overlap_ratio`) بدون تغییر بازاستفاده شد، فقط ورودی از ۳ به ۲ شاخه کاهش یافت.

### ۵.۵) باگ Quoting در شِل (خطای مهندسی، نه الگوریتمی)
هنگام اصلاح دستی یک شرط از طریق `python3 -c "..."` در Bash، quoting دوتایی باعث حذف کوتیشن‌های داخلی و ایجاد `KeyError` شد. رفع شد با نوشتن پچ‌ها همیشه به‌صورت فایل اسکریپت مستقل (نه inline `-c`).

### ۵.۶) باگ امتیازدهی دوگانه در Hough Fallback
کاندیدهای بازیابی‌شده از Hough (در نقش تاییدکننده) دو بار جریمه می‌شدند: یک‌بار با gate حداقلی `edge_support`، و یک‌بار دیگر با ضرب شدن دوباره‌ی `edge_support` در امتیاز نهایی. این باعث می‌شد کاندیدهای درستِ Hough در NMS از یک بلاب `region_generic` نادرست (که تخفیف مشابهی نداشت) امتیاز کمتری بگیرند و ببازند. رفع شد: `edge_support` فقط به‌عنوان gate استفاده می‌شود، نه ضریب امتیاز.

### ۵.۷) بررسی و رفع مشکل‌های ۲ و ۳ (شاخه‌ی Hough سطح دیسک) — **حل شد (برای اکثریت موارد)**

بعد از رفع مشکل ۱ (پتری کاذب)، طبق درخواست کاربر به سراغ مشکل ۲ (FP در Hough دیسک) و ۳ (recall پایین) رفتیم. کاربر صریحاً خواست قبل از هر راه‌حل، تصویرها و منابع مرجع بررسی شوند (نه فقط patch سریع).

**روش:** یک هارنس تشخیصی جدید (`hough_diag2.py` در `/tmp/.../scratchpad/`) ساخته شد که فقط ماژول‌های ۱ تا ۱۱.۵ (تا Hough دیسک) را روی یک عکس اجرا می‌کند و هم تصویر خام (`roi_gray` + دایره‌های Hough) هم تصویر ویژگی (`disk_feature_image` بعد از White Top-Hat، ماژول ۵) را ذخیره می‌کند — برای بررسی چشمی مستقیم (نه فقط شمارش عدد).

**یافته‌ی ۱ (مثبت، غیرمنتظره):** روی ۴ عکس گرد معمولی (۶ پتری، ~۵۰+ دیسک، شامل هاله‌های به‌هم‌چسبیده) شاخه‌ی Hough دیسک **۱۰۰٪ recall و ۱۰۰٪ precision** داشت — هیچ FP و هیچ miss‌ای، حتی روی عکس‌های کاملاً جدید/بررسی‌نشده. یعنی فرض اولیه (Hough به‌طور کلی مشکل دارد) برای این دسته از عکس‌ها تایید نشد.

**یافته‌ی ۲ (مشکل واقعی، محلی‌سازی‌شده):** روی پتری‌های مربعی متراکم (`1.1.1. original.jpg`, `2.2.1. original.jpg`) شاخه‌ی Hough **کاملاً شکست می‌خورد** — همه‌ی ۱۴+ دیسک واقعی (با هاله‌ی کاملاً واضح) هیچ‌کدام تشخیص داده نشدند، و ۴۰+ دایره‌ی کاذب در وسط دیش، روی یک الگوی نویزی/خوشه‌ای، تولید شد.

**علت ریشه‌ای (تایید‌شده با بررسی تصویر ویژگی):** در وسط این دیش‌ها یک الگوی **درخشش/بازتاب نور** (specular glare — احتمالاً از سطح خیس آگار یا درپوش پلاستیکی) وجود دارد که در تصویر White Top-Hat (ماژول ۵) حتی **روشن‌تر از خودِ دیسک‌های واقعی** ظاهر می‌شود. چون White Top-Hat هر بلاب روشنِ کوچک‌تر از کرنلش را بدون در نظر گرفتن مقیاس واقعی برجسته می‌کند (نه فقط بلاب‌های هم‌اندازه‌ی دیسک)، این نویز ریزدانه بر سیگنال دیسک‌های واقعی (که در این عکس‌ها کم‌کنتراست‌تر/خاکستری‌ترند) غالب می‌شود.

**تلاش اول برای رفع (رد شد با داده):** طبق تئوری کلاسیک scale-space در blob detection (Lindeberg؛ برای تشخیص جسمی در مقیاس σ باید قبل از عملیات با یک Gaussian هم‌مقیاس صاف کرد تا نویز کوچک‌مقیاس‌تر حذف شود)، فرضیه این بود که بلر اولیه‌ی ماژول ۵ (`cfg.disk_feature_blur_frac = 0.10 × قطر کمینه‌ی دیسک`) خیلی کوچک است. **آزمایش کنترل‌شده** (۳ مقدار: ۰.۱۰/۰.۲۰/۰.۳۵) نشان داد:
- روی `1.1.1.jpg`: ۴۶ کاندید کاذب → ۲۳ → ۱۰ (همه روی موقعیت واقعی دیسک، صفر کاذب) — عالی!
- روی `2.2.1.jpg`: مشابه، ۱۲ کاندید همه درست در ۰.۳۵.
- **اما تست رگرسیون روی عکس‌های عادی (که قبلاً ۱۰۰٪ درست بودند) شکست خورد:** `142502.jpg` از ۹ کاندید درست به ۶ افت کرد؛ `171003.jpg` از ۹ به فقط **۲** افت کرد. یعنی بلر بیشتر هم نویز را حذف می‌کند هم لبه‌ی واقعی دیسک را (که خودِ شاخه‌ی Hough به آن وابسته است) — یک مقدار ثابت برای همه‌ی عکس‌ها **کار نمی‌کند**.

**نتیجه‌گیری صادقانه:** این رگرسیون دقیقاً همان تله‌ای است که قانون ۲.۴.۱(ب) (ممنوعیت overfitting) هشدار می‌دهد — یک تنظیم که روی نمونه‌ی مشکل‌دار خوب جواب می‌دهد ولی روی نمونه‌ی عادی بد می‌شکند، قابل‌قبول نیست. **مقدار `disk_feature_blur_frac` در کد اصلی دست‌نخورده باقی ماند (هنوز ۰.۱۰)** — هیچ تغییری کامیت نشد.

**مسیر پیشنهادی بعدی (طراحی، هنوز پیاده‌سازی نشده):** به‌جای «بلر بیشتر» (که غیرانتخابی است و هم نویز هم سیگنال را می‌زند)، یک فیلتر **انتخابیِ مقیاس** (scale-selective) مثل LoG/DoG matched به شعاع دقیق دیسک — که هم ساختارهای کوچک‌تر و هم بزرگ‌تر از مقیاس هدف را سرکوب می‌کند ولی خودِ مقیاس دیسک را دست‌نخورده نگه می‌دارد — طراحی استاندارد و شناخته‌شده‌ی blob detection است. جایگزین/مکمل دیگر: تشخیص **تطبیقی** الگوی نویز/درخشش (مثلاً چگالی غیرعادی بلاب‌های ریز در تصویر top-hat خام) و اعمال تصحیح قوی‌تر **فقط** روی دیش‌های دارای این الگو، نه همه‌ی عکس‌ها.

**یافته‌ی جانبی (ثانویه، هنوز بررسی نشده):** حتی در بهترین حالت (`blur_frac=0.35` روی عکس‌های مربعی)، دیسک‌های نزدیک به گوشه/لبه‌ی عکس هنوز کم‌رنگ‌تر (پاسخ ضعیف‌تر در top-hat) بودند و تشخیص داده نشدند — احتمالاً vignetting یا افت نور به سمت گوشه‌های عکس؛ ممکن است ربطی به مشکل ۳ (recall در نواحی تاریک) که کاربر گزارش داده بود داشته باشد، ولی هنوز مستقل بررسی نشده.

**دسترسی به گوگل‌درایو:** در طول این نشست، اتصال به Google Drive مکرراً قطع/وصل شد (خارج از کنترل ما، مشکل سرویس نه دسترسی) — فقط فایل «دستور پخت پروژه‌ها.docx» یک‌بار با موفقیت خوانده شد (راهنمای عمومی مهندسی پروژه که در سبک «آزمایش کنترل‌شده + رگرسیون» این بخش به کار گرفته شد). کتاب‌های تخصصی CV با وجود تلاش مکرر **در طول باقی این بخش قابل‌دسترس نبودند**؛ رفع مشکلات زیر صرفاً بر پایه‌ی تئوری کلاسیک CV (که از قبل بخشی از دانش موجود بود) و آزمایش/شواهد مستقیم روی تصاویر واقعی انجام شد، نه مطالعه‌ی مستقیم منابع در طول این بخش.

### ۵.۸) رفع دو مشکل ریشه‌ای مکمل (بعد از یافته‌های ۵.۷) — جایگزین راه‌حل LoG که کنار گذاشته شد

بعد از رد شدن ایده‌ی «بلر بیشتر»، بررسی عمیق‌تر تصویر ویژگی نشان داد راه‌حل درست‌تر و **کم‌خطرتر از بازطراحی کامل به LoG/DoG**، دو اصلاح مجزا و مکمل بود:

**الف) فیلتر مقیاس با Morphological Opening (نه Gaussian Blur) در ماژول ۵:**
به‌جای بلر یکنواخت (که هم نویز هم لبه‌ی دیسک را نرم می‌کند)، یک **opening خاکستری** با کرنل کوچک‌تر از کوچک‌ترین دیسک مجاز (`cfg.disk_feature_denoise_open_frac = 0.30 × r_min`) روی سیگنال ترکیبی (Top-Hat + کنتراست محلی) اعمال شد. Opening اصولاً متفاوت از بلر عمل می‌کند: بلاب‌های کوچک‌تر از کرنل به‌طور کامل حذف می‌شوند، ولی بلاب‌های بزرگ‌تر (دیسک واقعی) تقریباً دست‌نخورده می‌مانند (فقط کمی فرسایش و بازسازی) — پس لبه‌ی دیسک واقعی برخلاف بلر تار نمی‌شود.

نتیجه روی ۱۰ عکس (۲ عکس مشکل‌دار + ۸ عکس سالم/جدید): **صفر رگرسیون** روی همه‌ی عکس‌های سالم (`142502`, `171003`, `142518`, `143518`, `143357`, `155938`, `142633`, `130617` همه بدون تغییر)؛ روی `1.1.1.jpg`: ۴۶ کاذب → ۱۹ کاندید (اکثراً روی موقعیت دیسک واقعی)؛ روی `2.2.1.jpg`: ۱۳→۲۷ (افزایش عددی ولی همه‌ی ۱۶ دیسک واقعی هم پیدا شدند، فقط یک خوشه‌ی کاذب متمرکز باقی ماند).

**فیلتر رد چگالی پرت (تکمیلی، برای خوشه‌ی کاذب باقی‌مانده):** یک فیلتر آماری (بدون آستانه‌ی ثابت — با MAD خودِ همان پتری) به `run_disk_hough` اضافه شد که کاندیدهایی با چگالی همسایه‌ی محلی بسیار بالاتر از میانه‌ی همان پتری را رد می‌کند (چون دیسک‌های واقعی طبق پروتکل هم‌پوشان/چسبیده نیستند). آزمایش نشان داد پنجره باید بر مبنای `d_min` (نه `d_max`، که خیلی بزرگ می‌شود) با ضریب ~۳.۰ باشد — کالیبراسیون اولیه (۱.۲×d_min) خیلی کوچک بود و هیچ اثری نداشت.

**ب) رفع گلوگاه واقعی: گیت هندسی Fusion (ماژول ۱۴) به‌طور سیستماتیک کاندیدهای درست Hough را رد می‌کرد:**
بررسی عمیق‌تر `2.2.1.jpg` نشان داد مشکل اصلی باقی‌مانده اصلاً در Hough نبود — در **Fusion** بود: `geom_metrics_for_circle` هر کاندید را (حتی کاندیدهای خالص Hough با شواهد داخلی قوی) روی `dish["blob_mask"]` اعتبارسنجی می‌کرد. وقتی شاخه‌ی Blob روی یک عکس شکست می‌خورد (این‌جا `blob_candidates=1` از ۲۴ کاندید Hough!)، این گیت هندسی سخت‌گیرانه (`min_circularity=0.75`, `min_solidity=0.9`) حتی دیسک‌های کاملاً واقعی را هم رد می‌کرد — نتیجه: از ۱۶ دیسک واقعی فقط ۵ باقی ماندند. این دقیقاً همان نگرانی اولیه‌ی کاربر درباره‌ی Blob/DT بود که با داده تایید شد.

سه اصلاح مکمل روی Fusion:
1. **اعتبارسنجی روی اجتماع ماسک‌ها:** `blob_mask | disk_feature_mask` (نه فقط blob_mask) — تا شکست یک شاخه به‌تنهایی کل اعتبارسنجی را نکشد.
2. **Closing برای پر کردن حفره‌ی متن دیسک:** یک closing کوچک (`cfg.geom_fill_close_frac=0.18 × r`) قبل از محاسبه‌ی معیارهای هندسی — چون نوشته‌ی سیاه روی دیسک سفید حفره‌های کوچک در ماسک باینری ایجاد می‌کند. (اثر این یکی به‌تنهایی کم بود، ولی هزینه‌ای هم نداشت.)
3. **گیت سخت‌گیرانه مشروط به branch_score (اصلاح اصلی):** اگر یک کاندید از قبل شواهد مستقل قوی از شاخه‌ی خودش دارد (`branch_score >= cfg.fusion_strong_hough_branch_score`)، رد سخت‌گیرانه‌ی circularity/solidity/no_geometry اعمال نمی‌شود — هندسه فقط برای کاندیدهای ضعیف/مبهم به‌عنوان شبکه‌ی ایمنی می‌ماند، نه یک وتوی مطلق روی هر کاندید.

**کالیبراسیون آستانه (داده‌محور، نه حدس):** تلاش اول با آستانه‌ی ۰.۶ روی `2.2.1.jpg` عالی بود (۵→۱۳) ولی روی دو عکس سالم دیگر رگرسیون داد (`142502`: ۹→۷؛ `171003`: ۹→۶) چون ۲ دیسک واقعی در آن‌ها branch_score=۰.۵۴/۰.۵۸ داشتند — کمی زیر ۰.۶. با مقایسه‌ی مستقیم بین چند عکس مختلف (نه فقط عکس مشکل‌دار)، مشخص شد نویز همیشه branch_score ≤۰.۴۷ دارد و دیسک واقعی همیشه ≥۰.۵۴ — یک شکاف طبیعی حدود ۰.۵۰. آستانه به **`cfg.fusion_strong_hough_branch_score = 0.50`** تغییر یافت (مبتنی بر شکاف مشاهده‌شده در داده‌ی چند-عکسی، نه عدد دلخواه یا برازش‌شده روی یک عکس).

**نتیجه‌ی نهایی (تست کامل end-to-end تا Fusion، ۷ عکس):**

| عکس | Hough خام | نهایی (قبل) | نهایی (بعد) |
|---|---|---|---|
| `1.1.1.jpg` | ۱۳ | ۰-۵ (تخمینی) | **۱۳/۱۳** ✅ |
| `2.2.1.jpg` | ۲۴ | ۵ | **۱۳/۲۴** ✅ (همه‌ی واقعی‌ها) |
| `142502.jpg` | ۹+۲ | — | **۹/۹ + ۲/۲** ✅ |
| `171003.jpg` | ۹ | — | **۹/۹** ✅ |
| `142518.jpg` | ۹+۲ | — | **۹/۹ + ۲/۲** ✅ |
| `143357.jpg` | ۱۰+۵+۶ | — | دیش۱: **۱۰/۱۰** ✅؛ دیش۲/۳: پایین را ببینید |
| `130617.jpg` | ۹ | — | **۹/۹** ✅ |

**یافته‌ی جانبی مهم (نه یک رگرسیون):** روی `143357.jpg`، دیش۲ و دیش۳ (که تصور می‌شد نتیجه‌شان افت کرده) بررسی و **تبرئه شدند** — دیش۳ اصلاً یک پتری کاذب است (فقط بافت پس‌زمینه، بدون هیچ دیسکی؛ همان دسته‌باگ ۴.۲ که هنوز کامل حل نشده) و ۲ کاندید «پذیرفته‌شده»‌اش حتی با گیت سخت‌گیرانه‌ی قدیمی هم قبول می‌شدند (circularity=۰.۷۶/۰.۸۳، هر دو بالای آستانه‌ی قدیمی ۰.۷۵) — یعنی ربطی به تغییرات امروز نداشت. دیش۲ هم فقط ۱ دیسک واقعی در محدوده‌اش دارد؛ ۴ کاندید رد‌شده همه branch_score زیر ۰.۴ داشتند (خیلی پایین‌تر از آستانه‌ی جدید ۰.۵۰) پس اصلاً تحت‌تاثیر تغییرات جدید قرار نگرفتند.

**وضعیت کامیت:** تمام این تغییرات (denoise-opening، فیلتر چگالی، اجتماع ماسک، closing، گیت مشروط، آستانه‌ی ۰.۵۰) روی ۷ عکس (۲ مشکل‌دار + ۵ سالم/جدید، مطابق قانون ۲.۴.۱ب) تست و تایید شدند — آماده‌ی کامیت.

---

## ۶) ماژول جدید ۱۷ — تشخیص حباب روی هاله

برای هر دیسکِ دارای هاله‌ی تاییدشده:
1. از پروفایل شعاعی هاله (که ماژول ۱۶ از قبل محاسبه می‌کند) به‌عنوان **پس‌زمینه‌ی نرم محلی** استفاده می‌شود؛ `residual = شدت واقعی − مقدار درون‌یابی‌شده‌ی پروفایل در همان شعاع`.
2. سیگنال `halo_map` (ماژول ۱۰ — که تا این نشست خروجی‌اش هیچ‌جا استفاده نمی‌شد، یک dead output) به‌عنوان سیگنال گرادیان کمکی به residual اضافه می‌شود — بازیافت یک خروجی موجود، هم‌راستا با فلسفه‌ی «خروجی هر سلول = ورودی سلول بعد».
3. آستانه‌گذاری با ضریب سیگمای robust (MAD) — بدون هیچ مقدار مطلق.
4. Opening نسبی + `connectedComponents` + **Distance Transform** برای شعاع هر حباب (سومین کاربرد DT-as-tool).
5. خروجی: تعداد حباب، مجموع مساحت (mm²)، و برآورد حجم توده (Σ (4/3)πr³ بر مبنای شعاع هر حباب) — **با ذکر صریح که این یک تقریب کروی از تصویر دوبعدی است، نه اندازه‌گیری مستقیم حجم.**

⚠️ این ماژول هنوز روی داده‌ی واقعی (حباب‌های واقعی روی هاله‌ی واقعی) عمیقاً اعتبارسنجی نشده — چون در عکس‌های تست‌شده تا الان، اکثر دیسک‌ها یا هاله نداشتند یا هاله‌شان حباب نداشت. **کالیبراسیون این بخش با نمونه‌ی واقعی حباب‌دار، کار باقی‌مانده است.**

---

## ۷) وضعیت تست و محدودیت‌های شناخته‌شده

### تست‌شده و تایید‌شده (به‌صورت کامل، همه‌ی ماژول‌ها)
- تصویر مصنوعی (چند پتری دایره/مربع، دیسک‌های دوسایز، هاله، حباب، دیسک‌های چسبیده) — end-to-end بدون خطا.
- چند عکس واقعی متنوع: دو پتری دایره‌ای چسبیده، پتری ساده‌ی تک‌دایره، هر دو پتری مربعی نمونه.

### در حال انجام / قدم بعدی فوری (اولویت ۱) — جلسه‌ی بعد از اینجا شروع شود

**وضعیت خلاصه:** مشکل ۱ (پتری کاذب) عمدتاً حل شد (۴ از ۶ مورد کامل، ۱ مورد باقی‌مانده جزئی — `142548.jpg`). مشکل ۲/۳ (شاخه‌ی Hough سطح دیسک + گیت هندسی Fusion) هم **حل شد** — بخش ۵.۷ و ۵.۸ را ببینید. خلاصه: علت اصلی، هم نویز درخشش/بازتاب در تصویر ویژگی (رفع با opening خاکستری + فیلتر چگالی) و هم گیت هندسی بیش‌ازحد سخت‌گیر Fusion (رفع با اجتماع ماسک + گیت مشروط به branch_score) بود. روی ۷ عکس تست کامل شد؛ نتیجه: recall نزدیک به کامل (۹۰-۱۰۰٪ روی اکثر عکس‌ها)، بدون false positive جدید.

قدم‌های بعدی، به ترتیب اولویت:
1. عیب‌یابی `142548.jpg` (پتری کاذب باقی‌مانده، بخش ۴.۲) — همچنان باز.
2. اسکن کامل ماژول ۴ روی هر ۷۸ عکس واقعی ریپازیتوری (۴۱ اولیه + ۳۷ جدید) برای اطمینان کلی از تعمیم‌پذیری همه‌ی اصلاحات این نشست — هنوز انجام نشده، فقط روی نمونه‌ی هدفمند (~۱۵-۲۰ عکس) تست شده.
3. یافته‌ی جانبی هنوز باز: دیسک‌های نزدیک به گوشه/لبه‌ی عکس در برخی تصاویر همچنان کم‌رنگ‌تر/رد می‌شوند (احتمالاً vignetting) — ممکن است همان مشکل ۳ (recall نواحی تاریک) کاربر باشد؛ هنوز مستقل بررسی نشده.
4. اگر گوگل‌درایو در دسترس بود، فصل‌های مرتبط با Hough transform limitations و blob detection را برای اعتبارسنجی/تعمیق تصمیم‌های این نشست بخوان (در طول این بخش هرگز واقعاً در دسترس نبود).
5. ماژول ۱۷ (حباب) با نمونه‌ی واقعی حباب‌دار هنوز اعتبارسنجی نشده.

### محدودیت‌های شناخته‌شده (کارهای باز)
0. **۱ پتری کاذب باقی‌مانده در `142548.jpg`** — بخش ۴.۲.
0.۱. **اسکن کامل ماژول ۴ روی هر ۷۸ عکس** هنوز انجام نشده (فقط روی نمونه‌ی هدفمند ~۱۵-۲۰ عکس تست شده).
1. **کندی باقی‌مانده در برخی عکس‌ها:** بعد از تصحیح نور به‌طور چشمگیری کاهش یافت (بخش ۴.۲) ولی صفر نشد — بعضی عکس‌ها (مثل `142548.jpg`) هنوز به fallback کند نیاز دارند، چون segmentation ناحیه‌ای هنوز گاهی هیچ‌چیز پیدا نمی‌کند یا oversized می‌شود. علت دقیق (بعد از تصحیح نور) هنوز عمیقاً بررسی نشده.
2. **دیسک‌های نزدیک لبه/گوشه‌ی عکس گاهی کم‌رنگ‌تر و رد می‌شوند** (بخش ۵.۷/۵.۸، یافته‌ی جانبی) — بهبود چشمگیر یافت (مثلاً `2.2.1.jpg` از ۵ به ۱۳ از ۱۶) ولی کامل نشد؛ علت دقیق (vignetting یا محدودیت باقی‌مانده‌ی Fusion) هنوز مستقل بررسی نشده.
3. **ماژول ۱۷ (حباب)** هنوز با نمونه‌ی واقعی حباب‌دار اعتبارسنجی نشده.
4. **طبقه‌بندی S/I/R بر اساس EUCAST breakpoint tables** اصلاً پیاده‌سازی نشده — فایل‌های PDF مرجع در ریپازیتوری هست ولی هنوز به کد وصل نشده.
5. **پاک‌سازی فیلدهای کانفیگ مرده/قدیمی** (`HoughSweepConfig`, `dish_roi_*`, `score_*_weight`, و غیره در سلول ۳) عمداً انجام نشده — نه یک باگ، بلکه یک تصمیم آگاهانه برای کاهش ریسک در این نشست.
6. **ترجمه به C++** هنوز شروع نشده — کد فعلی طوری نوشته شده که این کار را ساده‌تر کند (الگوریتم‌های deterministic، دیکشنری‌های ساده)، ولی هیچ کد C++ای هنوز تولید نشده.

---

## ۸) منابع مرجع موجود در پروژه

- `v_16.0_Breakpoint_Tables.pdf`, `Dosages_v_16.0_Breakpoint_Tables.pdf` — جداول استاندارد EUCAST (برای مرحله‌ی طبقه‌بندی S/I/R، هنوز استفاده نشده).
- پوشه‌ی گوگل‌درایو «Computer Vision Textbooks» (به اشتراک‌گذاشته‌شده توسط کاربر) شامل: *Digital Image Processing* (Gonzalez & Woods، ویرایش ۴)، *Digital Image Processing: PIKS Inside* (Pratt)، *Feature Extraction and Image Processing* (Nixon & Aguado)، *Computer Vision: Algorithms and Applications* (Szeliski)، *Multiple View Geometry in Computer Vision* (Hartley & Zisserman)، *Computer Vision: Foundations and Applications* (Stanford/Krishna)، *Fundamentals of Digital Image Processing* (Easton)، و دو کتاب DSP/Kalman Filtering. این‌ها برای طراحی الگوریتم‌های بعدی (به‌خصوص بخش segmentation و رفع محدودیت‌های بخش ۷) قرار است استفاده شوند.
- `disk_detection_pipeline_modular - Claude Eddition.ipynb` — نسخه‌ای که کاربر اجرا کرده بود؛ در زمان بررسی، فقط سلول‌های ۱، ۳ و ۸ خروجی ذخیره‌شده داشتند (بقیه اجرا/ذخیره نشده بودند)، پس برای عیب‌یابی مستقیم قابل استفاده نبود.

---

## ۹) خلاصه‌ی درخواست‌های اصلی کاربر (برای مرجع سریع)

1. رفع تشخیص چند‌پتری + هر ماژول بعدی روی همه‌ی پتری‌ها اجرا و خروجی هرکدام جدا نمایش داده شود. ✅ انجام شد.
2. رفع/بهینه‌سازی شاخه‌ی Blob Analysis (کل زنجیره‌ی منتهی به آن، نه فقط فیلتر نهایی) + تصمیم‌گیری در مورد نقش DT. ✅ انجام شد.
3. بازطراحی کامل جمع‌بندی سه‌شاخه بر اساس فلسفه‌ی هرکدام + استفاده از شاخه‌های نگه‌داشته‌شده برای baseline segmentation هاله و رخدادهای درون آزمایش. ✅ انجام شد.
4. تست روی همه‌ی عکس‌های واقعی در ریپازیتوری + گزارش مشکلات هر ماژول + پیشنهاد اصلاحات کلیدی بعدی. 🔄 در حال انجام (این سند بخشی از آن است).
5. عدم overfitting به نمونه‌های آپلودشده — برنامه باید جامع/عمومی بماند. ✅ در تمام تصمیم‌های طراحی رعایت شده (رجوع به بخش ۷، بند ۲ برای نمونه‌ی صریح خودداری از tune کردن).

---

## ۱۰) چهار مشکل نهایی قبل از اعلام تکمیل پروژه (تعیین‌شده توسط کاربر)

طبق نظر کاربر، این چهار مورد آخرین موانع قبل از اعلام تکمیل کامل و باکیفیت پروژه هستند. ترتیب زیر همان ترتیب اولویت پیشنهادی کاربر است (مشکل ۴ عمداً بعد از حل ۱ تا ۳ بررسی می‌شود، چون به خروجی نهایی آن‌ها وابسته است).

### ۱۰.۱) مشکل ۱ — شاخه‌ی Hough Circles همچنان False Positive و False Negative دارد
**سوال کاربر:** آیا می‌توان با یک ماژول در ادامه‌ی همین شاخه (بعد از ماژول Hough)، از روی خروجی خودِ Hough، FPها و/یا FNها را تشخیص و اصلاح کرد؟

### ۱۰.۲) مشکل ۲ — شاخه‌های Blob و DT نمی‌توانند دیسک‌های گم‌شده‌ی Hough را قابل‌اطمینان جبران کنند
دو مسیر تصمیم پیشنهادی کاربر:
- **(الف)** بازطراحی کامل این دو شاخه (هم خودِ بلوک، هم ورودی، هم خروجی) تا واقعاً بتوانند دیسک‌های گم‌شده یا اشتباه‌انتخاب‌شده‌ی Hough را اصلاح کنند؛ یا
- **(ب)** بازطراحی کامل همین دو شاخه (هم بلوک، هم ورودی، هم خروجی) برای این‌که به‌جای تشخیص دیسک، بعد از تشخیص هاله، وظیفه‌ی پیدا کردن **رخدادهای غیرمعمول** (طبق فایل‌های استاندارد EUCAST موجود در ریپازیتوری) را برعهده بگیرند.

### ۱۰.۳) مشکل ۳ — Segmentation هاله‌ی دور دیسک‌ها ضعیف است
**دلایل مطرح‌شده توسط کاربر:**
- تشکیل هاله یک واکنش بیولوژیکی نسبت به سطح کشت‌شده است، پس شکل، گرادیان لبه، تاری و ضخامت مرزش الزاماً یکسان/دایره‌ای نیست (برخلاف دیسک که یک جسم فیزیکی با شکل ثابت است).
- محدودیت‌های آرایش دیسک‌ها (نزدیک بودن دو دیسک باعث هم‌پوشانی هاله‌هایشان می‌شود؛ برخورد مرز هاله به لبه‌ی پتری باعث ناقص/غیردایره‌ای شدن مرز می‌شود) شکل واقعی هاله را از یک دایره‌ی ساده دور می‌کند.

**پیشنهاد کاربر:** استفاده از الگوریتمی شبیه به تشخیص پتری (ماژول ۴) برای مشخص کردن هاله‌ی هر دیسک — چون شکل/گرادیان هاله هم مثل پتری از پیش قابل‌پیش‌بینی نیست، با این تفاوت که می‌دانیم هاله حول یک دیسک مشخص (با مرکز و شعاع معلوم) شکل می‌گیرد.

### ۱۰.۴) مشکل ۴ — اندازه‌گیری دقیق قطر دیسک‌ها و هاله‌ها (بعد از حل ۱ تا ۳)
طبق نظر کاربر، این مورد **عمداً به تعویق افتاده** تا بعد از حل مشکلات ۱ تا ۳ بررسی شود — چون دقت اندازه‌گیری قطر مستقیماً به کیفیت تشخیص دیسک (مشکل ۱/۲) و کیفیت segmentation هاله (مشکل ۳) وابسته است؛ اندازه‌گیری روی یک تشخیص/segmentation ناقص بی‌معنی است. جزئیات این مشکل هنوز به‌طور کامل از زبان کاربر مستند نشده و در جلسه‌ی مربوطه با او دقیق‌تر تعریف خواهد شد.

### وضعیت این بخش
تحلیل ریشه‌ای + پیشنهادهای راه‌حل برای مشکلات ۱ تا ۳ آماده و به کاربر گزارش شد؛ کاربر ترتیب ۱→۲→۳ را تایید و دستور شروع به ۱ و ۲ را داد (۳ هنوز شروع نشده).

### ۱۰.۵) پیاده‌سازی مشکل ۱ — تایید ظاهری مستقل کاندیدهای Hough — **نتیجه‌ی منفی، برگردانده شد (کد فعلی = بدون این ماژول)**

**طراحی:** ماژول جدید ۱۱.۴ («Appearance Verification») بلافاصله بعد از ماژول ۱۱ (Hough خام): برای هر کاندید، سهم پیکسل‌های «روشن» داخل دایره را روی `roi_gray` (تصویر خام، نه `disk_feature_image`) می‌سنجد — یک سیگنال کاملاً مستقل از فیلترهای فعلی Hough (که همه از همان feature image ساخته شده‌اند).

**تلاش ۱ (رد شد با داده) — آستانه‌ی سراسری:** صدک ۸۵ کل پیکسل‌های پتری. روی `20260102_130617.jpg` یک دیسک واقعی (`FM 300`) رد شد چون در ناحیه‌ای کمی تاریک‌تر از عکس قرار داشت — دقیقاً همان مشکل نور ناهموار سراسری که در بخش ۴.۲ حل شده بود، این‌بار در لایه‌ی جدید دوباره ظاهر شد.

**تلاش ۲ (رد شد با داده) — آستانه‌ی محلی:** صدک ۸۵ فقط در پنجره‌ی ~۳.۵× شعاع کاندید. همان دیسک `FM 300` باز هم رد شد (`bright_frac=0.288`, آستانه‌ی محلی=۱۸۱). بررسی دقیق‌تر نشان داد این دیسک ذاتاً روی این عکس یک سایه‌ی نسبتاً موضعی و شدید دارد (نه یک گرادیان نرم سراسری) که با یک پنجره‌ی هرچند کوچک هم به‌طور کامل قابل تصحیح نیست.

**تلاش ۳ (رد شد با داده) — تصحیح نور تفریقی سراسری (illumination_normalize) روی کل ROI پتری، سپس آستانه‌ی سراسری روی تصویر تصحیح‌شده:** همان تابع اعتبارسنجی‌شده‌ی بخش ۴.۲ روی ROI هر پتری اجرا شد. باز هم `FM 300` رد شد (`bright_frac=0.012`!) — بررسی pixel-level نشان داد میانگین داخل این دیسک حتی بعد از تصحیح (۱۴۲.۷) به‌طور محسوسی پایین‌تر از میانگین ۸ دیسک واقعی دیگر همان عکس (۱۸۴–۲۱۱) است؛ یک تست contrast مبتنی بر حلقه‌ی محلی (`interior_mean - ring_mean`) هم امتحان شد و همین‌طور شکست خورد (این دیسک: contrast=۱۴.۲ در برابر ۵۸–۱۰۸ برای دیسک‌های واقعی دیگر). یعنی این دیسک خاص، در این عکس خاص، ذاتاً روشنایی/کنتراست پایین‌تری نسبت به بقیه دارد (به احتمال زیاد سایه‌ی موضعی یا vignetting نزدیک لبه‌ی قاب) و **هیچ آستانه‌ی مبتنی‌بر روشنایی خام (سراسری، محلی، یا تصحیح‌شده) نمی‌تواند آن را از یک FP احتمالی به‌طور قابل‌اتکا تفکیک کند** — چون این پارامتر (روشنایی خام) اصلاً سیگنال تفکیک‌کننده‌ی خوبی برای این مورد نیست.

**یافته‌ی حیاتی که کل مسیر را باطل کرد:** یک تست کنترل‌شده (حذف کامل سلول ماژول ۱۱.۴ از پایپلاین، بدون هیچ فیلتر ظاهری) نشان داد **Fusion موجود (گیت هندسی + `branch_score`، که خودش از قبل در بخش ۵.۸ همین نشست کالیبره و روی همین ۷ عکس تایید شده بود) به‌تنهایی هر ۹ دیسک `130617.jpg` از جمله `FM 300` را درست قبول می‌کند** (`circularity=0.64`, `solidity=0.88`, `branch_score=0.83` — همه بالای آستانه). جدول پایه‌ی مستندشده در بخش ۵.۸ هم از قبل `130617.jpg → 9/9 ✅` را ثبت کرده بود. یعنی ماژول ۱۱.۴ اصلاً یک FP واقعی و موجود را رفع نمی‌کرد — چیزی که از قبل (توسط Fusion) درست حل شده بود را دوباره خراب می‌کرد. رگرسیون تست کامل روی هر ۷ عکس (با نسخه‌ی تصحیح‌نور-سراسری) این را تایید کرد: ۴ از ۷ عکس دقیقاً مطابق baseline ماندند، ولی ۳ عکس رگرسیون داشتند (`1.1.1.jpg`: ۱۳→۱۲، `2.2.1.jpg`: ۱۳→۱۴ [تغییر با علت نامعلوم]، `130617.jpg`: ۹→۸) — و صفر بهبود قابل‌اندازه‌گیری در FP روی هیچ‌کدام از ۷ عکس (چون baseline از قبل ۰ FP شناخته‌شده داشت).

**نتیجه‌گیری و اقدام:** ماژول ۱۱.۴ **به‌طور کامل برگردانده شد** (کد فعلی هیچ اثری از آن ندارد؛ فایل به‌طور دقیق با commit `f260133` یکسان است). پاسخ به سؤال اصلی مشکل ۱ («آیا یک ماژول بعد از Hough می‌تواند FP/FN خودش را تصحیح کند؟»): روی مجموعه‌تست فعلی (۷ عکس)، این کار از قبل توسط ترکیب Fusion (گیت هندسی + `branch_score`) به‌درستی انجام می‌شود و هیچ FP زنده‌ای برای رفع باقی نمانده بود — اضافه‌کردن یک لایه‌ی جدید بر مبنای روشنایی خام صرفاً ریسک رگرسیون داشت بدون فایده‌ی قابل‌اندازه‌گیری. **این یک نتیجه‌ی منفی مستند و ارزشمند است، نه شکست:** اگر در آینده کاربر یک عکس واقعی مشخص با یک FP زنده‌ی پس از Fusion پیدا کند، طراحی هر فیلتر جدید باید از روی آن نمونه‌ی مشخص هدایت شود (نه یک فرضیه‌ی نظری درباره‌ی «روشنایی دیسک سفید»)، تا از افتادن در همین چرخه‌ی سه‌باره‌ی شکست‌خورده پرهیز شود.

### ۱۰.۶) پیاده‌سازی مشکل ۲ — خنثی‌سازی رأی شاخه‌ی Blob در Fusion — **نتیجه‌ی منفی، برگردانده شد**

**فرضیه:** چون شاخه‌ی Blob از همان سیگنال بالادستی Hough تغذیه می‌شود (بخش ۵.۸)، رأی آن مستقل نیست؛ پس `cfg.fusion_w_blob` را به ۰.۰ تغییر دادیم (`cfg.fusion_w_hough=1.0`) تا فقط Hough تصمیم بگیرد.

**تست و رگرسیون واقعی پیدا‌شده:** رگرسیون کامل روی ۷ عکس نشان داد ۶ عکس بدون تغییر ماندند، اما `2.2.1.jpg` از ۱۳ به ۱۹ کاندید پذیرفته‌شده افزایش یافت. بررسی بصری تصویر annotate‌شده و لیست دقیق کاندیدها نشان داد **۵ تای این کاندیدهای جدید، false positive واقعی روی بافت خط‌خطی وسط دیش هستند** (`circularity` تا پایین ۰.۲۵، `fill_ratio` تا ۰.۰۱، در مختصاتی که هیچ دیسکی وجود ندارد) — و در همان حال، ۳ دیسک واقعی نزدیک گوشه/لبه‌ی عکس (`AM 10`, `MEM 10`, `PEF 5`) هنوز هم اصلاً تشخیص داده نشدند. یعنی این تغییر recall واقعی را بهبود نداد ولی precision را خراب کرد.

**علت ریشه‌ای (پیدا‌شده با بررسی دقیق):** آستانه‌ی `cfg.fusion_strong_hough_branch_score = 0.50` (که کاندیدهای بالای آن از گیت `circularity/solidity` معاف می‌شوند) در بخش ۵.۸ **زیر وزن‌های پیش‌فرض** (`fusion_w_hough=0.45`, `fusion_w_blob=0.30`) کالیبره شده بود. با صفر کردن وزن Blob، توزیع `branch_score` کاملاً عوض می‌شود (اکنون ۱۰۰٪ از اطمینان خودِ Hough می‌آید، بدون تخفیفی که «نبودِ تایید Blob» قبلاً اعمال می‌کرد) — در نتیجه چند کاندید کاذبِ ضعیف (روی بافت خط‌خطی) که قبلاً `branch_score` را زیر ۰.۵۰ نگه می‌داشتند (و بنابراین وارد گیت هندسی سخت‌گیرتر می‌شدند و رد می‌شدند)، اکنون از آستانه‌ی معافیت عبور می‌کنند.

**نتیجه‌گیری و اقدام:** این تغییر هم **کامل برگردانده شد**. درس مهم: `fusion_w_blob` و `fusion_strong_hough_branch_score` دو پارامتر جفت‌شده‌اند — تغییر یکی بدون بازکالیبراسیون هم‌زمان دیگری، پایپلاین را از حالت validated خارج می‌کند. یک راه‌حل درست برای مشکل ۲ (اگر در آینده دنبال شود) باید این دو را با هم، روی مجموعه‌ای بزرگ‌تر از عکس‌ها (نه فقط ۷ عکس فعلی)، به‌طور مشترک کالیبره کند — کاری که در این نشست به‌خاطر محدودیت زمان و برای رعایت انضباط «هر تغییر باید قبل از commit با تست کامل تایید شود» به آینده موکول شد. مشکل شناخته‌شده‌ی «دیسک‌های نزدیک گوشه/لبه‌ی عکس گاهی رد می‌شوند» (بخش ۷) **مستقل از این تغییر است و هنوز حل‌نشده باقی می‌ماند**.

### وضعیت فعلی کد (بعد از این بررسی)
فایل نوت‌بوک در مخزن **دقیقاً برابر با commit `f260133`** بود (قبل از شروع بخش ۱۰.۷) — هیچ‌کدام از دو ایده‌ی بالا (تایید ظاهری / خنثی‌سازی رأی Blob) در کد باقی نماند، چون هر دو با تست واقعی رد شدند. مشکل ۱ و ۲ (طبق تعریف بخش ۱۰.۱/۱۰.۲ کاربر) عملاً از قبل توسط معماری موجود (بخش ۵.۷/۵.۸) به‌اندازه‌ی قابل‌قبولی روی مجموعه‌تست فعلی حل شده‌اند؛ برای پیشرفت بیشتر روی این دو مورد، به شواهد جدید (عکس‌های بیشتر با FP/FN زنده و مشخص) نیاز است، نه حدس نظری بیشتر — دقیقاً همان اصل «صفر Overfitting، اثبات با داده» که در بخش ۲.۴.۱ تعیین شده.

### ۱۰.۷) پیاده‌سازی مشکل ۳ — Segmentation دوبعدی/شعاعی هاله (ماژول ۱۶ بازطراحی) — **پیاده‌سازی شد، با محدودیت‌های شناخته‌شده**

**طراحی اول (رد شد، حتی قبل از commit) — Segmentation دوبعدی آزاد با Watershed:** طبق پیشنهاد اولیه‌ی کاربر (بخش ۱۰.۳)، مشابه ماژول ۴: آستانه‌ی Otsu محلی روی حلقه‌ی هر دیسک (`[r_disk, r_out]`) یک ماسک باینری «ناحیه‌ی شفاف» می‌سازد، سپس همه‌ی دیسک‌های یک پتری با هم در یک Watershed نشانگر-محور (نشانگرها = مرکز خودِ دیسک‌ها، نه قله‌های DT) ادغام و جدا می‌شوند.

- **باگ ۱ (off-by-one در برچسب‌گذاری Watershed):** `_watershed_from_marker_image` الگوی `markers = marker_seeds + 1` را از `build_watershed_instances` (ماژول ۱۲) کپی کرد، اما آنجا این شیفت هرگز برگردانده نمی‌شود چون Module 12 فقط برچسب‌ها را بدون توجه به عدد دقیق‌شان می‌شمارد؛ اینجا برعکس، هر دیسک باید دقیقاً برچسب خودش را پس بگیرد. نتیجه: `labels==marker_id` گاهی ناحیه‌ی یک دیسک **دیگر** را برمی‌گرداند — دقیقاً همان رفتار «تصادفی/نویزی» که کاربر گزارش کرد. رفع شد با تفریق ۱ از برچسب‌های خروجی.
- **باگ ۲ (قطع اتصال نشانگر-annulus):** بعد از رفع باگ ۱، یک دیسک (`CZ 30`) هنوز فقط دایره‌ی خودش را پس می‌گرفت (هیچ رشدی) با این‌که ماسک محلی‌اش بزرگ و متصل به نظر می‌رسید. علت: `ann` دقیقاً از `r_disk` شروع می‌شد و rasterization دایره‌ی cv2.circle (نشانگر) با مقایسه‌ی اعشاری `rad>=r_disk` همیشه دقیقاً روی هم نمی‌افتند؛ چند پیکسل فاصله کافی بود تا Watershed نتواند از نشانگر به ماسک محلی برسد. رفع موقت: شروع annulus از `0.9×r_disk`.
- **رد نهایی طراحی Watershed (به دستور صریح کاربر، با شواهد بصری):** حتی بعد از رفع هر دو باگ، خروجی هنوز **بلاب‌های کاملاً آزاد و بی‌قاعده** بود (دندانه‌دار، گاهی اصلاً حول مرکز دیسک متقارن نبود). کاربر تاکید کرد: «هاله معمولاً از همه‌ی اطراف دیسک رشد می‌کند و با اینکه ممکن است دایره‌ی کامل نباشد، باز هم به‌صورت شعاعی رشد می‌کند و حداقلی از دایره‌ای‌بودن دارد» — یعنی segmentation دوبعدی آزاد (بدون هیچ قید ساختاری نسبت به مرکز دیسک) اصولاً طراحی نادرستی برای این پدیده‌ی فیزیکی است، حتی وقتی باگ‌هایش رفع شوند.

**طراحی نهایی (پیاده‌سازی‌شده) — مرز قطبی شعاعی-زاویه‌ای `r(θ)`:** به‌جای یک ماسک دوبعدی آزاد، مرز هر دیسک یک تابع قطبی است — برای هر یک از N جهت (`cfg.halo_num_angles`)، یک شعاع مستقل محاسبه می‌شود؛ این ساختار ذاتاً تضمین می‌کند نتیجه حول مرکز دیسک «رشدیافته» است (نمی‌تواند بلاب آزاد/نامتصل تولید کند):
1. هیستوگرام دوبعدی (حلقه × زاویه) از شدت پیکسل‌ها ساخته می‌شود.
2. **صافی دایره‌ای (wrap-around) روی خودِ پروفایل** (نه فقط خروجی نهایی) اعمال می‌شود — چون یک پرتوی تکی نمونه‌ی پیکسلی بسیار کمتر از میانگین‌گیری روی کل حلقه دارد و نویزی‌تر است.
3. مرز هر جهت با **عبور از یک آستانه‌ی کالیبره‌شده** (نه gradient-extremum) پیدا می‌شود — تلاش اول با gradient-extremum (تیزترین لبه‌ی هر پرتو) رد شد چون یک لبه‌ی قوی و نامرتبط (مثلاً خط خط‌وخش روی آگار) می‌توانست به‌اشتباه انتخاب شود؛ آستانه (میانگین وزنی سطح داخلی/پس‌زمینه، با `cfg.halo_threshold_inner_weight` قابل‌سخت‌گیرتر کردن) در برابر چنین لبه‌ی تک‌افتاده‌ای مقاوم‌تر است.
4. **باگ ۳ (جهت پیش‌فرض غلط):** اگر هیچ عبوری از آستانه در یک جهت پیدا نشود، پیش‌فرض اولیه `r_out` (بیشترین شعاع جست‌وجو) بود — یعنی «نبود شاهد» به‌اشتباه به‌عنوان «قوی‌ترین شاهد ممکن» تفسیر می‌شد. این دقیقاً علت یک تیغه‌ی کاذب بزرگ (`WEM 10`، تا مرز جست‌وجو) بود که نه صافی زاویه‌ای و نه سخت‌گیرتر کردن آستانه به‌تنهایی حلش نکردند، چون خودِ منطق سطح‌پایین معیوب بود. رفع شد: پیش‌فرض به `r_in` (کمترین شعاع، یعنی عملاً بدون رشد) تغییر یافت.
5. هم‌پوشانی با دیسک همسایه با **فرمول بسته‌ی هندسیِ دقیق نیمساز عمود** (نه تقریب Watershed) قطع می‌شود — برای هر جهت، سقف شعاع = فاصله تا خط نیمساز فاصله با نزدیک‌ترین دیسک دیگر؛ ساده‌تر، دقیق‌تر و بدون باگ‌های برچسب‌گذاری Watershed.
6. برخورد به لبه‌ی پتری با پیمایش مستقیم پرتو در `dish_mask` برای هر جهت جداگانه محاسبه می‌شود (دقیق‌تر از یک مقدار محافظه‌کارانه‌ی مشترک بین همه‌ی جهت‌ها).
7. نتیجه یک چندضلعی ستاره‌ای حول مرکز دیسک است (`cv2.fillPoly`) — قابل‌انتقال مستقیم به C++ (فقط حلقه و فرمول‌های بسته).

**اعتبارسنجی:** تست کامل روی هر ۷ عکس مجموعه‌ی رگرسیون — بدون کرش، روی پتری‌های دایره‌ای و مربعی، تک‌دیش و چنددیش. کیفیت بصری روی عکس‌های با دیسک‌های جدا از هم (مثل `142502.jpg`) بسیار خوب است (مرزها صاف، تقریباً حول دیسک متقارن، و در محل هم‌پوشانی دو دیسک نزدیک به‌درستی مرز مشترک تشکیل می‌دهند). روی عکس‌های با رشد بسیار متراکم/به‌هم‌پیوسته (`1.1.1.jpg`) نتیجه هنوز کمی دندانه‌دار است — قابل انتظار روی چنین عکسی چون حتی مرز واقعی هاله‌ها هم دیداری به‌سختی قابل‌تفکیک است.

**محدودیت شناخته‌شده‌ی باقی‌مانده (مستقل از این بخش):** گیت «آیا اصلاً هاله‌ای هست؟» (`contrast_sigma`/`confidence` در `_halo_radial_profile`، از نسخه‌ی قبلی بدون تغییر مانده) گاهی یک دیسک بدون هیچ منطقه‌ی مهار واقعی (مثلاً `FM 300` روی `130617.jpg`، یک سویه‌ی کاملاً مقاوم) را `status=ok` تشخیص می‌دهد، چون یک روند نور کلی/ملایم (نه یک هاله‌ی واقعی) از آستانه‌ی سیگمای کنتراست عبور می‌کند. این مشکل به segmentation شکل (موضوع همین بخش) مربوط نیست؛ برای رفع آن باید معیار «آیا هاله‌ای هست» بازبینی شود — پیشنهاد برای جلسه‌ی بعد (یا بخش مشکل ۴، چون مستقیماً روی اندازه‌گیری اثر می‌گذارد).

### ۱۰.۸) مقایسه‌ی ۵ طراحی کلان برای مرزیابی هاله (به درخواست صریح کاربر: «طراحی‌های کلان مختلف پیشنهاد بده، تست کن، گزارش بده»)

کاربر بعد از دیدن طراحی نهایی بخش ۱۰.۷ («A»، مبتنی بر آستانه‌ی مستقل هر پرتو) دو نقد مطرح کرد: (۱) هنوز گاهی بر پایه‌ی نویز تصمیم می‌گیرد نه سیگنال؛ (۲) خواست چند طراحی کلان متفاوت (نه فقط تنظیم پارامتر) پیشنهاد و تک‌تک تست و گزارش شوند. همه‌ی طراحی‌ها **دقیقاً همان لوله و قرارداد خروجی A** را دارند (لنگر از A، سقف Voronoi همسایه، سقف پرتوزنی به لبه‌ی پتری) — فقط مرحله‌ی هسته‌ای «تعیین شعاع هر جهت» فرق می‌کند، تا مقایسه کاملاً کنترل‌شده باشد. همه به‌صورت تابع مستقل در همان سلول ماژول ۱۶ اضافه شدند، **بدون جایگزینی A در حلقه‌ی تولیدی** (تصمیم نهایی هنوز باز است).

- **B — بهینه‌سازی سراسری با Dynamic Programming (Livewire/Intelligent Scissors):** هزینه‌ی هر سلول (حلقه×زاویه) از قدرت گرادیان در جهت مورد انتظار می‌آید؛ یک جریمه‌ی کشسانی (فاصله‌ی شعاعی بین زوایای مجاور به‌توان ۲) **داخل خودِ بهینه‌سازی** صافی مسیر را تضمین می‌کند، نه به‌عنوان یک پاس صافی جداگانه‌ی بعدی (که ضعف A بود). لنگر از شعاع A در زاویه‌ی صفر گرفته می‌شود (به‌جای جست‌وجوی گران روی همه‌ی نقاط شروع ممکن برای بستن دقیق دایره). **نتیجه: روی هر دو عکس تست‌شده بهتر از A** (صاف‌تر، دایره‌ای‌تر، به‌خصوص در گوشه‌های بریده‌ی A مثل `AMS20`/`PTZ`)؛ بدون کرش و بدون رگرسیون در تعداد دیسک روی کل ۷ عکس رگرسیون.
- **C — GrabCut (segmentation دوبعدی آزاد، Rother و همکاران ۲۰۰۴):** لنگرهای سطح روشنایی (نزدیک دیسک=FGD مطمئن، دنباله‌ی دور=BGD مطمئن) به GrabCut داده می‌شود؛ خروجی با همان سقف Voronoi/لبه‌ی پتری A محدود می‌شود. **رد شد:** یک نشتی جدی و قابل‌بازتولید پیدا شد (دیسک `WEM 10` روی `130617.jpg`، شعاع معادل ۴۰۱px در برابر ۱۰۲-۱۶۰px در بقیه‌ی طراحی‌ها) — بدون لبه‌ی قوی مخالف، انرژی‌ی سراسری graph-cut به‌راحتی ناحیه‌ی احتمالی را بیش‌ازحد بزرگ پر می‌کند. به‌خاطر این ضعف بنیادی (نه یک باگ در نحوه‌ی اعمال سقف)، تست کامل ۷‌عکسی برای C انجام نشد.
- **D — تطبیق سیگموئید (matched filter) با رگرسیون خطی بسته:** با تبدیل لوجیت (`y = ln((1-frac)/frac)`, `frac=(profile-bg)/(inner-bg)`) یک مدل لجستیک با دو سطح مرجع robust (همان `inner_val`/`bg_val` طراحی A) به یک رگرسیون خطی بسته تبدیل می‌شود — شعاع گذار از تمام حلقه‌های ناحیه‌ی گذار می‌آید، نه یک نقطه‌ی تکی. **نتیجه‌ی غیرمنتظره: بدتر از B، حتی گاهی بدتر از A** — چند تیغه‌ی جدید تولید کرد (بین `CZ30`/`NA30`، دور `GXT`/`CRO`/`WEM10`). **یافته‌ی مهم روش‌شناختی:** استفاده از داده‌ی بیشتر در هر پرتو (D) به‌تنهایی کافی نیست؛ چون هر پرتو هنوز **مستقل** تصمیم می‌گیرد (بدون قید مشترک با پرتوهای مجاور)، هنوز می‌تواند به‌شدت از پرتوهای همسایه فاصله بگیرد. نقطه‌ضعف واقعی A/D یکی بود: نبودِ قید سراسری، نه کمبود داده‌ی محلی.
- **E — هیبرید B+D (بهترین نتیجه تا این لحظه):** هزینه‌ی سلولی از فاصله تا سطح گذار کالیبره‌شده (`|frac-0.5|`، مرجع robust طراحی D) می‌آید — نه گرادیان خام محلی B — ولی بهینه‌سازی مشترک/سراسری B (جریمه‌ی کشسانی داخل خودِ بهینه‌سازی) حفظ می‌شود. **نتیجه: بدون تیغه‌های D، هم‌تراز یا کمی بهتر از B** (مثلاً `WEM 10` کاملاً صاف)؛ بدون کرش و بدون رگرسیون در تعداد دیسک روی کل ۷ عکس رگرسیون.

**نتیجه‌گیری فعلی:** B و E هر دو به‌وضوح از A بهترند؛ E احتمالاً برنده‌ی نهایی است اما هنوز به‌طور قطعی/کمّی روی همه‌ی ۷ عکس در برابر B مقایسه نشده (فقط شمارش n_ok مقایسه شد، نه کیفیت بصری مرزها). C به‌طور کامل کنار گذاشته شد. **تصمیم نهایی بین B/E و جایگزینی A در حلقه‌ی تولیدی هنوز باز است** — طبق دستور کاربر برای جلسه‌ی بعد گذاشته شد.

### ۱۰.۹) یافته‌ی جدید — نقطه‌کور معیار «آیا اصلاً هاله‌ای هست» با میانگین‌گیری همه‌جهته (کشف‌شده حین بررسی، حل‌نشده — قدم بعدی صریح کاربر)

کاربر روی عکس `130617.jpg` پرسید چرا دیسک `CFM 5` (که در کراپ تصویر خام یک ناحیه‌ی روشن‌تر و نسبتاً بزرگ و دایره‌ای‌شکل دور خودش دارد — با چشم کاملاً واضح) هیچ هاله‌ای علامت نخورده. بررسی مستقیم:

- `_halo_radial_profile` (گیت «آیا هاله‌ای هست»، از قبل از بخش ۱۰.۷ بدون تغییر): `contrast_sigma=1.52` (آستانه: `cfg.halo_min_contrast_sigma=2.0`)، `confidence=0.342` (آستانه: `cfg.halo_min_confidence=0.35`) — **هر دو خیلی نزدیک اما زیر آستانه** → `status=no_halo`.
- دیباگ مستقیم پروفایل دوبعدی (حلقه×زاویه، همان هیستوگرامی که برای شکل مرز در بخش ۱۰.۷/۱۰.۸ ساخته می‌شود) نشان داد: **دامنه‌ی واقعی نوسان روشنایی به‌تفکیک هر زاویه ۴۲.۸ واحد است** (سیگنال قوی، هم‌تراز با هاله‌های تاییدشده‌ی دیگر همین عکس) — ولی وقتی همه‌ی ۷۲ جهت با هم میانگین‌گیری می‌شوند (همان چیزی که `_halo_radial_profile` انجام می‌دهد)، این سیگنال به فقط **۶.۱ واحد** افت می‌کند و از آستانه رد می‌شود.
- **علت ریشه‌ای:** گیت «آیا هاله‌ای هست» هنوز دقیقاً همان معماری قدیمی (میانگین‌گیری همه‌جهته در یک پروفایل ۱بعدی) را دارد که کل بازطراحی بخش ۱۰.۷/۱۰.۸ برای مرحله‌ی *شکل مرز* کنار گذاشت — فقط هنوز به این مرحله‌ی زودتر (تصمیم وجود/عدم‌وجود) اعمال نشده. خط پایه‌ی روشنایی در جهت‌های مختلف این دیسک متفاوت است (مثلاً زاویه‌ی ۰°: inner≈۱۶۱/bg≈۱۶۸؛ زاویه‌ی ۱۸۰°: inner≈۱۷۶/bg≈۱۷۲) — میانگین‌گیری ساده این تفاوت‌های نامرتبط بین‌جهتی را با سیگنال واقعی هاله قاطی و خنثی می‌کند.
- **نکته‌ی جانبی مهم، هنوز بررسی‌نشده:** علامت تفاوت (inner-bg) هم بین جهت‌های مختلف عوض می‌شود (بعضی +، بعضی −) — ممکن است این هاله‌ی خاص بیشتر از طریق **بافت** (ناحیه‌ی شفاف و صاف در برابر لَون خط‌خطی/دانه‌دار) قابل‌تشخیص باشد تا میانگین روشنایی؛ نیاز به بررسی جداگانه دارد.

**وضعیت:** حل‌نشده، عمداً. کاربر صریحاً درخواست کرد این مشکل **در جلسه‌ی بعد** (نه همین حالا) دنبال شود. **قدم بعدی مشخص‌شده برای شروع جلسه‌ی بعد:** بازطراحی گیت «آیا هاله‌ای هست» با همان اصل per-angle (نه میانگین همه‌جهته) — مثلاً با یک آماره‌ی robust روی کنتراست‌های per-angle (نه فقط میانگین ساده، چون علامت‌ها مخلوط‌اند)، به‌علاوه‌ی بررسی اینکه آیا سیگنال بافت/واریانس (نه فقط میانگین شدت) باید به‌عنوان یک سیگنال کمکی اضافه شود.

### ۱۰.۱۰) چرا کل طراحی گیت «آیا هاله‌ای هست» از ابتدا بازطراحی می‌شود (تصمیم جلسه‌ی جاری)

**قدم اول (تلاش ترمیمی، نه بازطراحی):** طبق نقشه‌ی راه بخش ۱۰.۹، اولین راه‌حل پیشنهادی (per-angle local contrast) به‌صورت تابع تشخیصی مستقل `_per_angle_contrast_sigma` پیاده و **بدون جایگزینی گیت تولیدی** کنار `_halo_radial_profile` اضافه شد (کامیت `2687717`) — دقیقاً همان کنتراست inner/bg هر گیت قدیمی، ولی به‌جای یک مقدار میانگین‌گیری‌شده روی کل ۷۲ جهت، یک آرایه‌ی طول ۷۲ (یک سیگما به‌ازای هر جهت)، با نویز robust (MAD) برآوردشده از باقیمانده‌ی کل هیستوگرام دوبعدی (نه یک پرتوی تکی، تا برآورد پایدار بماند).

**تست در مقیاس کامل:** اسکریپت `test_halo_gate_diagnostic.py` این تابع را روی هر ۷ عکس رگرسیون، برای هر دیسک، اجرا و overlay رنگی (سبز=قبلاً هم تایید بود؛ نارنجی=قبلاً رد شده ولی معیار جدید `frac_strong≥۰.۱۵` سیگنال قوی می‌بیند؛ قرمز=هر دو رد) تولید کرد. کاربر خودش overlayهای `2.2.1.jpg` و `1.1.1.jpg` را دیداً بررسی کرد.

**نتیجه‌ی کاربر (رد صریح این راه‌حل ترمیمی):** «بعضی از نارنجی‌ها هاله دارند و بعضی هم ندارند» — یعنی معیار `frac_strong` (سیگنال کنتراست محلیِ قوی در حداقل یک جهت) هیچ جداکنندگی قابل‌اعتمادی بین دیسک‌های واقعاً دارای هاله و دیسک‌های بدون هاله ندارد. این با تحلیل کمّیِ خودم هم‌خوان بود: مقادیر بالای `frac_strong` روی چند دیسک **تایید‌شده بدون هاله** هم دیده شد (مثلاً در `1.1.1.jpg` دیسک ۱۱: `۰.۷۴`؛ دیسک ۱۳: `۰.۸۹`؛ در `2.2.1.jpg` دیسک ۱: `۰.۷۴`) — یعنی «حداقل یک جهت با کنتراست قوی» هم روی نویز/بافت لَون هم روی هاله‌ی واقعی رخ می‌دهد و به‌تنهایی سیگنال کافی نیست.

**نتیجه‌گیری کاربر (نقل مستقیم):** «این نشون میده که کاملا اصول و طراحی ما برای سگمنت کردن هاله کاملا تحت‌الشعاع قرار گرفته و باید از اول طراحی بشه» — یعنی مشکل صرفاً یک آستانه‌ی نادرست یا یک متریک ناقص نیست؛ **کل فرض بنیادی** («هاله فقط از طریق تفاوت میانگین روشنایی، بین یک حلقه‌ی نزدیک و یک حلقه‌ی دور، در یک یا چند جهت، قابل‌تشخیص است») ناکافی است، چون:
1. میانگین روشنایی به تنهایی نمی‌تواند نویز خط‌خطی/لکه‌ای تصادفی agar را از افت/رشد واقعی و پیوسته‌ی هاله تشخیص دهد (هر دو می‌توانند در یک یا چند جهت مجزا کنتراست بالا تولید کنند).
2. هیچ قید ساختاری‌ای روی **الگوی مکانی** جهت‌های دارای سیگنال وجود ندارد — یک خط خط‌خوردگی (scratch) که از وسط دیسک عبور می‌کند می‌تواند دقیقاً مثل یک هاله‌ی واقعی در ۲-۳ جهت مجزا کنتراست قوی ایجاد کند، چون معیار فقط شدت را می‌بیند نه شکل توزیع آن.
3. فقط یک کانال فیزیکی (شدت/روشنایی pixel) بررسی می‌شود؛ در حالی که تفاوت واقعی زیست‌شناختی هاله در برابر لَون باکتریایی، **بافت** (agar شفاف و صاف در برابر لَون دانه‌دار/خط‌خطی با واریانس محلی بالا) است، نه فقط سطح روشنایی میانگین — نکته‌ای که در بخش ۱۰.۹ («نکته‌ی جانبی مهم، هنوز بررسی‌نشده») هم دیده شده بود ولی در راه‌حل ترمیمی اول اعمال نشده بود.

**تصمیم:** به‌جای تنظیم مجدد آستانه یا افزودن یک متریک دیگر روی همان پایه (که طبق یافته‌ی بالا کافی نیست)، **کل گیت «آیا هاله‌ای هست» از صفر با سه اصل مستقل بازطراحی می‌شود** (هر سه در برابر داده‌ی واقعی/متنوع تک‌تک تست و به کاربر گزارش می‌شوند، نه یک‌جا و نه بر اساس یک عکس):

1. **کانال دوم مستقل: واریانس/بافت محلی** (نه فقط شدت خام) — روی همان معماری هیستوگرام حلقه×زاویه (`_build_smoothed_profile_2d`) اما به‌جای میانگین شدت، واریانس محلی (یا دامنه‌ی بین‌کوانتایلی) پیکسل‌های هر سلول محاسبه می‌شود؛ فرض: هاله (agar شفاف) واریانس پایین دارد، لَون باکتریایی و خط‌خوردگی‌های تصادفی واریانس بالاتر/نامنظم‌تری دارند.
2. **مقایسه‌ی آماری جمعیت پیکسل‌ها (نه میانگین حلقه‌ها):** به‌جای مقایسه‌ی دو عدد میانگین (`inner_val` در برابر `background`)، توزیع کامل پیکسل‌های ناحیه‌ی نزدیک دیسک با توزیع کامل پیکسل‌های دنباله‌ی دور با یک اندازه‌ی اثر رتبه‌محور/robust (مثلاً بر پایه‌ی رتبه‌بندی، نه فرض توزیع نرمال) مقایسه می‌شود — کمتر حساس به دم‌های پرت و نویز پراکنده.
3. **فیلتر شکل شواهد (رد کردن الگوی خطی):** جهت‌های دارای سیگنال قوی بررسی می‌شوند که آیا یک **کمان پیوسته** (سازگار با نفوذ شعاعی زیستی هاله، طبق تاکید صریح کاربر در بخش قبل) تشکیل می‌دهند یا **پراکنده/مجزا** هستند (سازگار با یک خط خط‌خوردگی که فقط از ۱-۲ جهت عبور می‌کند) — معیاری که در راه‌حل ترمیمی اول اصلاً وجود نداشت.

هر سه بخش ابتدا به‌صورت تابع مستقل/تشخیصی (نه جایگزین بی‌واسطه‌ی `_halo_radial_profile`) پیاده و روی همان مجموعه‌ی ۷ عکس رگرسیون تست می‌شوند، دقیقاً با همان روش گزارش‌دهی incremental این نشست (overlay + چاپ آماره‌ها + بازخورد کاربر قبل از رفتن به بخش بعدی). **وضعیت در لحظه‌ی نوشتن این بخش:** کاربر پیشنهاد را تایید کرد («بله شروع کن»)؛ پیاده‌سازی بخش ۱ (کانال واریانس/بافت) در حال شروع است، هنوز کدی برای هیچ‌کدام از سه بخش commit نشده.

### ۱۰.۱۱) سه تلاش ترمیمی پشت‌سرهم روی همان معماری per-angle — همه رد شدند؛ بازطراحی نهایی و کامل ماژول ۱۶ (تصمیم قطعی این بخش)

بعد از بخش ۱۰.۱۰، **بخش ۱ (کانال بافت/واریانس)** پیاده و روی هر ۷ عکس رگرسیون تست شد:
- آماره‌ی جمعیتی درست بود (میانگین `bg_texture−inner_texture` در گروه `ok`=+۰.۷۵ در برابر گروه `no_halo`=−۰.۶۸ — جهت نظری درست، برخلاف کانال شدت که چنین جدایی نداشت)، اما هم‌پوشانی تک‌دیسکی زیاد بود (۳۲٪ از `ok`ها علامت اشتباه، ۲۹٪ از `no_halo`ها علامت کاذب مثبت) — علتش یک آرتیفکت لبه‌ی دیسک بود (حلقه‌ی نزدیک `r_in=1.10×r_disk` به لبه‌ی فیزیکی دیسک خیلی نزدیک است و واریانس محلی مصنوعی می‌گیرد).
- کاربر با بازبینی مستقیم عکس‌ها رد کرد: «همچنان در تمامی تصاویر... هم دیسک‌های قرمز و هم دیسک‌های نارنجی بعضیاشون دارای هاله هستند».

سپس **گیت ترکیبی کمان پیوسته** (بخش ۳ طرح، شدت+بافت با رأی OR در هر جهت + طولانی‌ترین کمان دایره‌ای پیوسته‌ی بالای یک آستانه‌ی سیگمای ثابت، به‌جای شمارش ساده‌ی جهات) پیاده شد:
- نسخه‌ی اول (آستانه‌ی مطلق سیگما=۱.۰ + حداقل کمان=۲۰٪ محیط): روی ۷ عکس، **۸۲٪ از دیسک‌های قبلاً‌ردشده قبول شدند بدون هیچ رگرسیونی** — جهش به‌قدری بزرگ بود که مشکوک بود. کاربر با چشم تایید کرد: «فقط یدونه خاکستری‌ای دیدم که واقعاً هاله نداشت؛ فیروزه‌ای‌ها هم بعضیاشون هاله‌ی خیلی خوبی دارند و بعضی اصن ندارند». علت ریشه‌ای که پیدا شد: `_build_smoothed_profile_2d` از قبل یک صافی میانگین‌گیر دایره‌ای روی محور زاویه اعمال می‌کند؛ یعنی جهت‌های مجاور مستقل نیستند، از قبل خودهمبسته‌اند — یک آستانه‌ی مطلق ثابت روی «طول کمان» این خودهمبستگی را نادیده می‌گرفت و مقدار واقعی false-positive را دست‌کم می‌گرفت.
- نسخه‌ی دوم (کالیبراسیون خودکار per-image: طول کمان واقعی در برابر طول کمانِ null برآوردشده از دو نیمه‌ی متوالی ناحیه‌ی پس‌زمینه‌ی همان دیسک): رگرسیون‌های جدید و جدی پدید آورد (۱۰ مورد در ۷ عکس) — چون تقسیم به نیمه‌ی اول/دوم یک روند شعاعی ملایم و *واقعی* (هاله‌ای به‌قدری بزرگ/قوی که فراتر از بازه‌ی جست‌وجو ادامه دارد) را هم به null منتقل می‌کرد؛ درست قوی‌ترین/واضح‌ترین هاله‌ها را رد می‌کرد (بدترین‌حالت: `run_len` و `null_run_len` هر دو تقریباً کل محیط).
- نسخه‌ی سوم (تقسیم شانه‌ای زوج/فرد حلقه‌ها برای null، به‌جای نیمه‌ی اول/دوم — خنثی‌کردن روند شعاعیِ ملایم): رگرسیون به ۲ مورد در ۷ عکس رسید (۶۰ توافق، ۱۳ نجات‌یافته، ۴ توافق روی بی‌هاله، ۲ رگرسیون). کاربر این‌بار حتی این ۲ رگرسیون را هم رد کرد: **«حتی قرمزها نیز در واقعیت دارای زون هستند و نباید حذف بشوند»**.

**نتیجه‌گیری نهایی (تصمیم کاربر، رد کامل کل مسیر per-angle-gate):** سه رویکرد کاملاً مستقل (کنتراست شدت per-angle، بافت per-angle، گیت ترکیبی کمان پیوسته با کالیبراسیون null) همگی — با وجود سه دور اصلاح ریشه‌ای — نهایتاً به همان دیوار برخوردند: هر کدام هنوز دیسک‌هایی را که واقعاً هاله دارند رد می‌کردند. کاربر نتیجه گرفت که خودِ سوال «آیا هاله‌ای هست؟» اشتباه است: طبق فیزیک نفوذ آنتی‌بیوتیک، هر دیسک یک ناحیه‌ی مهار (هرچند خیلی کوچک) تشکیل می‌دهد — پس نباید گیت وجود/عدم‌وجود داشته باشیم، باید همیشه یک دایره برازش کنیم.

**دستور صریح کاربر برای بازطراحی کامل:** «ازت میخوام کلا این ماژول سگمنت کردن هاله رو حذف کنیم و از ابتدا بر حسب اینکه یک دایره قراره شکل بگیره درست کن و بعد در ماژول بعدی اون دایره رو بر اساس اینکه همپوشانی شده و یا به پتری برخورد کرده بود را دقیق‌تر پیدا کند مثلا با تشکیل ماسک در ماژول قبلی».

**معماری نهایی پیاده‌سازی‌شده (بازطراحی سوم، جایگزین کامل هر پنج طراحی A-E و هر سه رویکرد گیت وجود):**
- کل کد Design B/C/D/E و کل ماشین‌آلات گیت وجود (`_build_smoothed_profile_2d`, `_per_angle_transition_radii`, `_dp_radial_boundary[_hybrid]`, `_sigmoid_fit_radii`, `_per_angle_contrast_sigma`, `_local_texture_map`, `_per_angle_texture_contrast`, `_circular_longest_run`, `_per_angle_band_contrast`, `_halo_arc_evidence`, `_grabcut_halo_mask`, `_radial_cap_mask`, `_circular_median_smooth`, و توابع `segment_dish_halos_design_{b,c,d,e}`) **کامل حذف شدند**.
- `_halo_radial_profile` بازنویسی شد: پروفایل شعاعی همه‌جهته (میانگین/میانه‌ی تجمیعی کل پیکسل‌های هر حلقه، همان معماری قدیمی قبل از هر پنج طراحی A-E) محاسبه می‌شود، سپس شعاع دایره از اولین عبور آستانه (وزن‌دار بین سطح داخلی/پس‌زمینه، پیش‌فرض محافظه‌کارانه `r_in` اگر گذاری پیدا نشود) برازش می‌شود — **بدون هیچ گیت وجود/عدم‌وجود**؛ `status` فقط برای شکست‌های واقعی داده (دیسک بیرون از ماسک، خیلی نزدیک لبه، پوشش حلقه‌ای ناکافی) غیر از `"ok"` می‌شود، هرگز به‌خاطر سیگنال ضعیف.
- چرا این پروفایل تجمیعی نسبت به هر سه رویکرد قبلی مقاوم‌تر است: هر حلقه از صدها/هزاران پیکسل ساخته می‌شود (نه چند ده پیکسل یک پرتوی تکی)، پس واریانس نمونه‌گیری‌اش به‌طور بنیادی پایین‌تر است — نه به این دلیل که الگوریتم هوشمندتر است.
- `segment_dish_halos` (تولیدی) بازنویسی شد: شعاع دایره‌ی ثابت با دو سقف کاملاً هندسی/قطعی (نه آماری) محدود می‌شود — هم‌پوشانی همسایه (`_neighbor_voronoi_cap`، فرمول بسته‌ی نیمساز عمود، بدون تغییر) و برخورد به لبه‌ی پتری (`_dish_edge_cap`، پیمایش مستقیم در ماسک، بدون تغییر). این دقیقاً دستور کاربر است: تنها منبع تغییر شعاع بر حسب جهت، هندسه‌ی واقعی صحنه است، نه یک تصمیم مستقل و نویزی per-angle — که علت مشترک شکست هر سه رویکرد قبلی بود.
- پارامترهای cfg مرده (`halo_min_contrast_sigma`, `halo_min_confidence`, `halo_angular_smooth_frac`, `halo_dp_smoothness_weight[_hybrid]`, `halo_grabcut_iterations`, `halo_texture_win_frac_of_ring`, `halo_arc_evidence_sigma`, `halo_min_arc_frac`, `halo_arc_null_safety_margin`) حذف شدند.

**تست:** رگرسیون کامل روی هر ۷ عکس — **۷۹ از ۷۹ دیسک `status=ok`** (همان‌طور که کاربر پیش‌بینی کرد، همه دایره‌ای برازش می‌کنند)، بدون هیچ استثنا/کرش. شعاع‌های برازش‌شده در بازه‌ی فیزیکی معقول (≈۱.۱× تا ۳.۲× شعاع دیسک) قرار دارند.

**بازخورد کاربر (تایید کامل):** «مرز سبز رنگ بسیار عالی بوده و یک مرز هاله‌ی حداقلی را برای دیسک‌ها تشکیل داده که کافی هست» — دایره‌ی حداقلی این بازطراحی به‌عنوان baseline نهایی و درست تایید شد.

### ۱۰.۱۲) ماژول جدید ۱۶.۵ — رشد نامتقارن هاله بیرون از دایره‌ی حداقلی

درخواست کاربر (بعد از تایید بالا): «در یک ماژول بعد از آن، برای تشخیص دقیق‌تر هاله... رشدهای هاله در بیرون دایره‌ی سبز را نیز سگمنت کند». نکته‌ی مهم که این ماژول را از هر سه رویکرد گیت وجود/عدم‌وجود (بخش ۱۰.۱۱) بنیادی متفاوت می‌کند: اینجا سوال «آیا هاله‌ای هست» اصلاً پرسیده نمی‌شود — از قبل مطمئنیم هست (دایره‌ی ماژول ۱۶). فقط پرسیده می‌شود «آیا در این جهت خاص سیگنال از لبه‌ی دایره به بیرون ادامه دارد؟»، با پیش‌فرض ایمن دقیقاً همان دایره‌ی قبلاً تاییدشده (نه صفر، نه یک بیشینه‌ی دلخواه) — یعنی در بدترین حالت نتیجه هرگز از baseline تاییدشده بدتر نمی‌شود.

**پیاده‌سازی (`_per_angle_halo_extension` + `extend_dish_halos`، سلول جدید بین ماژول ۱۶ و ۱۷):**
- برای هر جهت، از `start_radii[j]` (شعاع همان جهت که ماژول ۱۶ قبلاً با سقف‌های هندسی محدود کرده بود) تا `cap_radii[j]` (همان سقف هندسی مطلق: هم‌پوشانی همسایه + لبه‌ی پتری، دوباره محاسبه‌شده با همان توابع بدون‌تغییر ماژول ۱۶) پیمایش می‌شود.
- سیگنال با یک نسخه‌ی محلی صاف‌شده از تصویر (میانگین جعبه‌ای با پنجره‌ای هم‌مقیاس با عرض یک حلقه‌ی ماژول ۱۶، نه یک مقدار مطلق) سنجیده می‌شود — نه پیکسل خام — تا نویز تک‌پیکسلی مسیر پیمایش را گمراه نکند.
- همان آستانه‌ی robust سراسری‌ای که ماژول ۱۶ برای برازش دایره کالیبره کرده (`threshold`، اکنون در خروجی ماژول ۱۶ منتشر می‌شود) دوباره استفاده می‌شود — نه یک آستانه‌ی تازه‌ی per-angle که در سه رویکرد قبلی منبع اصلی شکست بود.
- گام پیمایش نسبت به شعاع دیسک است (`cfg.halo_extension_step_frac`، نه پیکسل مطلق).
- بعد از پیمایش، صافی میانه‌ی دایره‌ای (برای پیوستگی، `cfg.halo_extension_smooth_frac`) و کلمپ اجباری به بازه‌ی `[start_radii, cap_radii]` (صافی هرگز نباید از سقف مطلق رد شود یا زیر baseline بیفتد).
- فیلدهای اضافه‌شده به خروجی ماژول ۱۶ برای پشتیبانی از این ماژول (افزایشی، بدون تغییر رفتار ماژول ۱۶): `r_out`, `polarity_sign`, `threshold`, `r_circle` (شعاع خام پیش از سقف هندسی)، `final_radii` (آرایه‌ی per-angle پس از سقف هندسی ماژول ۱۶).

**تست:** رگرسیون کامل روی هر ۷ عکس — ۷۹ از ۷۹ دیسک بدون استثنا اجرا شد؛ هیچ‌کدام از نتایج از سقف‌های هندسی یا از baseline قبلی کوچک‌تر نشدند (تضمین ساختاری کد، نه فقط مشاهده‌ی تجربی). منتظر بازبینی چشمی کاربر روی overlayهای این ماژول (مرز نارنجی = رشد شناسایی‌شده بیرون از دایره‌ی حداقلی).

**وضعیت:** پیاده‌سازی شد و رگرسیون ساختاری گذراند؛ در انتظار تایید بصری کاربر (آیا شکل نهایی دایره‌ها/برش‌های هندسی درست است) پیش از قطعی‌شدن.

### ۱۰.۱۳) باگ کشف‌شده در ماژول ۱۶.۵ — شرط «هنوز شبیه هاله است» برعکس نوشته شده بود

حین ساخت یک تشخیصیِ جدید برای مقایسه‌ی معیار فعلی پایپ‌لاین با پیشنهاد کاربر (بخش بعد)، با بازآفرینی همان منطق `_per_angle_halo_extension` در یک اسکریپت جدا، کشف شد که شرط `still_halo` در ماژول ۱۶.۵ **برعکسِ** قرارداد خودِ ماژول ۱۶ نوشته شده بود:

- ماژول ۱۶ (درست): در حلقه‌ی خودش، تا وقتی `val` هنوز روشن‌تر از آستانه است (یعنی هنوز داخل هاله)، پیمایش **بدون ثبت** ادامه پیدا می‌کند؛ اولین حلقه‌ای که `val<=threshold` شد (گذار به لَون)، همان‌جا ثبت و متوقف می‌شود.
- ماژول ۱۶.۵ (باگ‌دار، نسخه‌ی قبلی): `still_halo = (val <= threshold)` بود -- یعنی دقیقاً برعکس: پیمایش تا وقتی `val<=threshold` (شبیه لَون/پس‌زمینه) ادامه پیدا می‌کرد و دقیقاً وقتی به یک پیکسل روشنِ واقعی هاله می‌رسید متوقف می‌شد.

**اصلاح:** `still_halo = (val > threshold) if polarity_sign >= 0 else (val < threshold)` -- هم‌راستا با قرارداد ماژول ۱۶.

**پیامد:** overlayهای «رشد نامتقارن» که قبلاً برای بازبینی فرستاده شده بودند بر پایه‌ی منطق برعکس ساخته شده‌اند و باید نامعتبر تلقی شوند؛ رگرسیون ۷عکسی بعد از این اصلاح دوباره اجرا و overlayهای اصلاح‌شده دوباره برای بازبینی کاربر فرستاده می‌شود.

### ۱۰.۱۴) بررسی موازی کاربر: مقایسه‌ی معیار فعلی با یک پیشنهاد جایگزین (کمان‌های ۱۸ درجه + رأی اکثریت)

کاربر یک طراحی جایگزین برای خودِ ماژول ۱۶ پیشنهاد داد: مرحله‌ی اول یک دایره که به محض عبور **هر تک پیکسلی** از یک آستانه‌ی گرادیان روی کل محیط متوقف شود؛ مرحله‌ی دوم رشد مستقل ۲۰ کمان ۱۸-درجه‌ای تا آستانه‌ی دوم (سهل‌گیرانه‌تر)؛ در نهایت انتخاب شعاع بر اساس **بیشترین فراوانی + کمترین انحراف‌معیار** بین کمان‌ها.

**تحلیل نظری:** مرحله‌ی اول (توقف با کمینه‌ی مطلق روی کل محیط) از نظر آماری از هر رویکردی که این نشست امتحان و رد کرد شکننده‌تر است — کمینه‌ی یک مجموعه‌ی نویزی کاملاً تحت‌سلطه‌ی پرت‌ترین پیکسل تک است. مرحله‌ی دوم تقریباً همان «طراحی A» (رشد per-ray مستقل) است که قبلاً به‌خاطر نبود قید سراسری کنار گذاشته شد؛ کمان‌های ۱۸ درجه فقط به‌خاطر تجمیع بیشتر پیکسل، کمی مقاوم‌ترند.

**تست تجربی (نه فقط نظری):** یک تابع تشخیصی (`_per_arc_candidate_radii` + `_mode_consensus_radius`) ساخته و روی ۲ عکس (`130617.jpg`, `1.1.1.jpg`) اجرا شد که برای هر دیسک، شعاع کاندید هر یک از ۲۰ کمان (با همان آستانه‌ی سراسری calibrated ماژول ۱۶) و معیار اکثریت را در برابر معیار فعلی پایپ‌لاین مقایسه می‌کرد.

- **نتیجه‌ی اول (قبل از رفع باگ بخش ۱۰.۱۳):** چون تابع تشخیصی از همان الگوی برعکس بخش ۱۰.۱۳ کپی شده بود، تقریباً همه‌ی کمان‌ها بلافاصله در همان حلقه‌ی اول متوقف می‌شدند (چون شرط برعکس بود) — این خودِ کشفِ باگ ۱۰.۱۳ بود.
- **نتیجه‌ی دوم (بعد از رفع باگ):** وقتی سیگنال هاله قوی/یکنواخت است، دو معیار تقریباً یکی می‌شوند (مثلاً `130617` دیسک ۱: ۲۸۶ در برابر ۲۷۷؛ `1.1.1` دیسک ۲: ۳۱.۱ در برابر ۳۱.۰). اما در دیسک‌های چگال/نزدیک‌به‌هم، توزیع کاندیدهای کمان‌ها **دوقطبی** می‌شود (نیمی نزدیک لبه‌ی دیسک، نیمی نزدیک انتهای بازه‌ی جست‌وجو — اثر هم‌پوشانی با همسایه)، و معیار «بیشترین فراوانی» عملاً یک انتخاب شانسی بین دو خوشه‌ی تقریباً مساوی می‌شود (مثلاً `mode_count`=۱۰ از ۲۰). در چند دیسک با هاله‌ی ضعیف‌تر هم اجماع سست است (`mode_count` فقط ۸ از ۲۰).

**نتیجه‌گیری:** معیار پیشنهادی وقتی سیگنال قوی است چیزی بهتر از معیار فعلی نمی‌دهد (هر دو یکی می‌شوند)، و وقتی سیگنال ضعیف/هم‌پوشان است به یک انتخاب ناپایدار تبدیل می‌شود — دقیقاً همان کلاس مشکلی که معماری فعلی (تجمیع کامل روی کل محیط برای شعاع پایه + محدودسازی هندسیِ قطعیِ Voronoi برای هم‌پوشانی، نه رأی‌گیری آماری) با آن مقابله می‌کند. **تصمیم: معیار فعلی نگه داشته می‌شود، چیزی جایگزین نشد.**

### ۱۰.۱۵) بررسی موازی دوم: آیا بازه‌ی جست‌وجوی شعاعی برای هاله‌های بزرگ کافی است؟

کاربر همزمان درخواست کرد بررسی شود چرا برای دیسک‌های با هاله‌ی خیلی بزرگ، به‌نظر می‌رسد حتی نصف شعاع واقعی هم بررسی نمی‌شود.

**یافته:** `cfg.halo_r_max_scale=4.0` یک ضریب ثابت است که بازه‌ی جست‌وجوی `_halo_radial_profile` را تعریف می‌کند (`r_out = min(4.0×r_disk, max_allowed)`). این یک سقف دلخواه است که می‌تواند هاله‌های واقعاً بزرگ‌تر از ۴ برابر شعاع دیسک را قطع کند -- محدودیت جست‌وجو با محدودیت واقعیِ زیستی هاله قاطی می‌شود، دقیقاً همان الگوی «سقف دلخواه» که اصول پروژه آن را منع می‌کند مگر جایگزین الگوریتمی نباشد.

**راه‌حل:** `_halo_radial_profile` به‌صورت تطبیقی بازنویسی شد: با همان ضریب ۴.۰ شروع می‌کند، ولی اگر پروفایل تجمیعی تا انتهای پنجره‌ی فعلی هنوز «داخل هاله» بود (گذار واقعی پیدا نشد)، پنجره با ضریب `cfg.halo_r_max_scale_growth=1.6` بزرگ‌تر و از نو محاسبه می‌شود -- تا حداکثر `cfg.halo_r_search_max_widenings=3` بار، یا تا رسیدن به سقف فیزیکی واقعی صحنه (`max_allowed`، فاصله تا لبه‌ی پتری). یعنی محدودیت جست‌وجو دیگر یک عدد ثابت نیست؛ فقط توسط هندسه‌ی واقعی صحنه محدود می‌شود.

**نتیجه‌ی تست روی هر ۷ عکس:** در این مجموعه‌ی خاص، گسترش تطبیقی **هیچ‌گاه لازم نشد** (`n_widened_search=0` در همه‌ی ۷۹ دیسک) -- یعنی معیار تجمیعیِ کل‌محیط ماژول ۱۶ در این عکس‌ها همیشه در همان پنجره‌ی اولیه‌ی ۴ برابری به گذار می‌رسید. این نشان می‌دهد علت اصلیِ چیزی که کاربر مشاهده کرده بود، به‌احتمال زیاد **همان باگ بخش ۱۰.۱۳** بوده (منطق برعکسِ ماژول ۱۶.۵ که overlayهای قبلی را ساخته بود)، نه محدودیت بازه‌ی جست‌وجوی ماژول ۱۶. با این‌حال اصلاح بازه‌ی تطبیقی به‌عنوان یک محافظ ساختاری درست (رفع یک سقف دلخواه واقعی در کد) نگه داشته می‌شود -- برای عکس‌های دیگر با هاله‌های واقعاً بزرگ‌تر از ۴ برابر شعاع دیسک، این می‌تواند لازم شود.

**وضعیت:** هر دو اصلاح (۱۰.۱۳ و بازه‌ی تطبیقی) در رگرسیون کامل ۷عکسی/۷۹دیسک بدون استثنا تست شدند؛ در انتظار بازبینی بصری کاربر روی overlayهای اصلاح‌شده.

### ۱۰.۱۶) اصلاح/پس‌گیری صریح نتیجه‌گیری بخش ۱۰.۱۵ — علت واقعی کم‌برآوردِ هاله چیز دیگری بود

بعد از overlayهای اصلاح‌شده‌ی بخش ۱۰.۱۳، کاربر گزارش داد: «نارنجی‌ها بهتر شدند، اما همچنان برخی هاله‌ها بزرگ‌تر [از] دایره‌ی کشیده‌شده هستند؛ تقریباً توی ۷۰٪ دیسک‌ها اشتباه زیاد و غیرقابل‌چشم‌پوشی هست» و صریحاً خواست جمله‌ی «به‌احتمال زیاد همون باگ شرط برعکس بوده» **با جزئیات کامل رفع/رد شود**، نه فقط حدس زده شود.

**اصلاح صریح:** آن جمله (بخش ۱۰.۱۵) **ناقص و نادرست** بود. باگ شرط برعکس (۱۰.۱۳) واقعی و رفع‌شده بود، اما تنها علت کم‌برآورد نبود — یک باگ/ضعف طراحیِ **دومِ کاملاً مستقل** هم در همان تابع (`_per_angle_halo_extension`) وجود داشت که حتی بعد از رفع علامت هم باقی می‌ماند:

- پیمایش رو به بیرون **پیکسل‌به‌پیکسل روی یک پرتوی نازک** بود (فقط با یک صافی جعبه‌ای محلی کوچک، نه تجمیع روی یک کمان کامل). قانون توقف این بود: **اولین گامی که مقدار به زیر آستانه افتاد، پیمایش برای همیشه متوقف می‌شود.**
- این یعنی هر افت گذرا و غیرواقعی در طول مسیر شعاعی (نویز طبیعی بافت agar، یک بازتاب نور، حتی یک پیکسل تیره‌ی تکی) — حتی اگر سیگنال بلافاصله بعدش دوباره به سطح هاله برمی‌گشت — همان‌جا برای همیشه پیمایش را می‌بست، در حالی که گذار واقعی می‌توانست خیلی دورتر باشد.
- این دقیقاً همان کلاس شکنندگیِ «تصمیم بر پایه‌ی یک نمونه‌ی تکی» است که کل این نشست با تجمیع کامل حلقه (نه یک پرتوی تکی) در ماژول ۱۶ حل شده بود — اما در پیمایش شعاعیِ ماژول ۱۶.۵ (که برخلاف ماژول ۱۶ روی مسیر شعاعی، نه محیط زاویه‌ای، کار می‌کند) این ضعف دوباره و به‌طور مستقل از باگ علامت وجود داشت. رفع باگ علامت (۱۰.۱۳) این ضعف دوم را برطرف نمی‌کرد — فقط جهت اشتباه را درست می‌کرد، نه ماهیتِ «توقف با یک نمونه‌ی تکی» را.

**راه‌حل نهایی (طبق طراحی دقیق پیشنهادی خودِ کاربر):** `_per_angle_halo_extension` به‌طور کامل با `_per_angle_gradient_extension` جایگزین شد:
1. هر جهت با یک **کمان** به پهنای `cfg.halo_extension_arc_width_frac×۳۶۰` درجه (پیش‌فرض ۱۸ درجه) سنجیده می‌شود، نه یک پرتوی تک‌پیکسلی — پروفایل حلقه‌ای محلی از تجمیع پیکسل‌های همان کمان ساخته می‌شود.
2. به‌جای «اولین عبور از یک سطح»، **مکان قله‌ی گرادیان** (بیشترین تغییر شدت بین حلقه‌های مجاور) پیدا می‌شود — این خودِ تعریف یک لبه‌ی گذار واقعی است.
3. قله فقط وقتی به‌عنوان لبه‌ی معتبر پذیرفته می‌شود که z-score آن (نسبت به نویز robust/MAD گرادیان‌های همان کمان) از `cfg.halo_extension_gradient_min_sigma=2.0` بیشتر باشد — یعنی هم بزرگیِ تغییر گرادیان و هم یکنواختی/عدم‌نویزی‌بودنش لازم است، دقیقاً طبق درخواست کاربر («بیشترین تغییر گرادیان کمان با کمترین انحراف‌معیار در اندازه‌ی تغییر گرادیان»).
4. جست‌وجو هرگز از `cfg.halo_extension_max_frac_of_petri_radius=0.5` (نصف شعاع پتری) فراتر نمی‌رود — طبق درخواست صریح کاربر، هر رشدی که برای رسیدن به لبه نیاز به این‌قدر دور رفتن داشته باشد، به‌جای پذیرفته‌شدن، نامعتبر اعلام و پیش‌فرض ایمن (همان baseline قبلی) حفظ می‌شود.
5. اگر هیچ قله‌ی معتبری در آن کمان پیدا نشد، شعاع همان baseline (بدون تغییر) می‌ماند — دقیقاً همان fallback ایمن قبلی، حفظ‌شده.

**تست روی هر ۷ عکس بعد از این بازطراحی:** روی دیسک‌های چگال `130617.jpg` که قبلاً کم‌برآورد داشتند، رشد به‌وضوح بزرگ‌تر و منطقی‌تر شد (مثلاً دیسک ۲: `r_circle=160.4` → `r_final=409.0`؛ دیسک ۴: `108.7`→`307.3`؛ دیسک ۶: `274.4`→`356.1`؛ دیسک ۸: `182.8`→`361.4`) — بدون هیچ استثنا در کل ۷۹ دیسک.

**وضعیت:** پیاده‌سازی و رگرسیون ساختاری کامل شد؛ در انتظار بازبینی بصری نهایی کاربر روی overlayهای این نسخه.

### ۱۰.۱۷) اعتبارسنجی روی ۵ عکس کاملاً تصادفی/ندیده — یافته‌ی جدید: دایره‌ی کاذب روی دیسک‌های واقعاً مقاوم

بعد از تایید کاربر روی ماژول ۱۶.۵، درخواست بعدی شروع کار روی مشکل ۴ (اندازه‌گیری دقیق قطر) بود، با این پیش‌قدم: برای هر دیسک یک **دایره‌ی نماینده** (به مرکزیت مرکز دیسک، شعاع = `halo_radius_px` یعنی همان عددی که در گزارش نهایی به میلی‌متر تبدیل می‌شود) رسم و روی ۵ عکس تصادفیِ **کاملاً خارج از ۷ عکس همیشگی** (انتخاب‌شده با `random.sample` از کل ۷۸ عکس موجود در ریپازیتوری) نمایش داده شود -- هم برای دیدن دایره‌ی نماینده، هم به‌عنوان یک اعتبارسنجی اضافه روی داده‌ی کاملاً ندیده.

کاربر عکس‌ها را با چشم خودش بررسی کرد و گزارش داد: «دایره‌ها منطقی و خوب به‌نظر نمی‌رسند... اختلاف بعضی دیسک‌ها که زیاده، و نیز برخی دیسک‌هایی که اصلاً زونی ندارند [ولی دایره‌ی بزرگ کشیده شده]».

بررسی مستقیم عکس‌های ارسالی توسط خودم (نه فقط اعداد) این را تایید کرد: در چند دیسک (مثلاً `PTZ`, `CAZ30`, `AMS20` در یکی از عکس‌ها)، آگار دور دیسک از نظر بصری **کاملاً یکنواخت و پرکلونی است** (هیچ ناحیه‌ی تیره/شفاف قابل‌تشخیصی وجود ندارد -- یعنی این آنتی‌بیوتیک احتمالاً روی این باکتری مقاوم/بی‌اثر بوده)، اما پایپ‌لاین همچنان یک دایره‌ی نسبتاً بزرگ (قابل‌مقایسه با دیسک‌های دیگر همان عکس که واقعاً هاله‌ی تیره و قابل‌مشاهده دارند مثل `CL10`) رسم کرده است. این الگو مخصوصاً در عکس‌هایی دیده شد که اکثر دیسک‌هایشان مقاوم بودند (فقط ۱-۲ دیسک واقعاً هاله داشتند) -- که به این فرضیه دامن می‌زند که سیگنال کاذب یا از یک روند نوری/عکاسیِ کلی (vignetting) می‌آید، یا از نشتِ هاله‌ی واقعیِ یک همسایه‌ی قوی به دیسک‌های مجاورِ بدون‌هاله.

**فرضیه‌ی فنی محتمل:** معیار پذیرش قله‌ی گرادیان در ماژول ۱۶.۵ (بخش ۱۰.۱۶) صرفاً یک **z-score نسبیِ** محلی است (نسبت به نویز MAD همان کمان) -- نه یک الزام مطلق روی بزرگیِ خودِ گرادیان. یک روند نوری بسیار **صاف** (نویز محلی خیلی کم) می‌تواند z-score بالایی امتیاز بگیرد حتی اگر بزرگیِ مطلق تغییرش ناچیز و کاملاً غیرزیستی باشد -- یعنی معیار «هم بزرگ هم کم‌نویز» طبق درخواست کاربر (بخش ۱۰.۱۶) واقعاً هر دو شرط را همزمان و درست اعمال نمی‌کند؛ به‌طور ضمنی فقط شرط دوم (کم‌نویزی نسبی) به‌درستی کار می‌کند، شرط اول (بزرگی مطلق) عملاً غایب است.

**وضعیت:** یک تشخیصیِ کمّی (`test_falsezone_diag.py`) روی همین ۵ عکس اجرا شد که برای هر دیسک `contrast_sigma`، `r_circle`/`r_final`، و تعداد جهت‌های پذیرفته‌شده (`gradient_z>0`) را چاپ می‌کند.

**تایید کمّیِ فرضیه (و کشف یک باگ عددیِ اضافه):** روی هر ۵ عکس، **تقریباً همیشه ۶۴ تا ۷۲ از ۷۲ جهت پذیرفته می‌شدند** -- حتی روی دیسک‌هایی که به چشم هیچ زونی ندارند. علاوه بر این، یک باگ عددیِ جدی هم پیدا شد: در یک مورد `max_z=28,690,094` بود -- یعنی مخرج z-score (نویز محلیِ MAD همان کمان) گاهی تقریباً صفر می‌شد و z را منفجر می‌کرد.

**اصلاح اول (پیاده‌سازی شد، رگرسیون‌شده، ولی ناقص):**
- نویز robust سراسریِ ماژول ۱۶ (`profile_noise`، از قبل محاسبه می‌شد ولی منتشر نمی‌شد) اکنون در خروجی ماژول ۱۶ قرار می‌گیرد.
- `_per_angle_gradient_extension` اکنون **دو شرط مستقل** برای پذیرش قله دارد: (۱) بزرگیِ مطلق قله باید حداقل `cfg.halo_extension_min_gradient_frac_of_noise=2.0` برابر همین نویز سراسری باشد (نه فقط z-score نسبی محلی)، (۲) نویز محلیِ هر کمان با یک کف حداقلی (`cfg.halo_extension_local_noise_floor_frac=0.15`×نویز سراسری) محدود می‌شود تا z دیگر منفجر نشود.
- **نتیجه‌ی رگرسیون مجدد روی همان ۵ عکس:** باگ انفجار عددی رفع شد (`max_z` دیگر میلیونی نیست)؛ چند دیسک درست به صفر رشد رسیدند (مثلاً `143424` دیسک ۴: `72/72`→`0/72`؛ `155938` دیسک ۲: →`0/72`). اما **روی اکثر دیسک‌ها هنوز ۴۰ تا ۷۰ از ۷۲ جهت پذیرفته می‌شوند** -- یعنی مشکل فقط جزئی حل شد، نه کامل. این نتیجه صادقانه به کاربر گزارش شد (بدون اعلام «حل‌شد» زودهنگام).

**علت ریشه‌ایِ عمیق‌تر (کشف‌شده حین تحلیل):** خودِ `contrast_sigma` ماژول ۱۶ (که الان هم به‌عنوان مرجع سقف مطلق استفاده می‌شود) روی این دیسک‌ها مقادیر پرت و بزرگ دارد (۲۵- تا ۴۰-) با اینکه به چشم هیچ هاله‌ای نیست. علت: نویز MAD حلقه‌به‌حلقه‌ی ماژول ۱۶ یک **روند نرم سیستماتیک** (vignetting نور دوربین از مرکز به لبه‌ی پتری، یا شیب تدریجی چگالی کلونی) را از نویز واقعی پیکسل‌به‌پیکسل تشخیص نمی‌دهد -- یک روند نرم اما ناچیز و کاملاً غیرزیستی، چون به‌طور محلی «یکنواخت» است، نویز محاسبه‌شده را مصنوعاً کوچک نشان می‌دهد و contrast_sigma را منفجر می‌کند. چون سقف مطلق جدید هم به همین نویز (که خودش آلوده است) وصل است، به‌اندازه‌ی کافی سخت‌گیر نیست.

**قدم بعدی پیشنهادی (هنوز پیاده نشده، منتظر تصمیم کاربر):** جایگزینیِ منبع «نویز سراسری» از MAD حلقه‌به‌حلقه (که روند سیستماتیک را با نویز واقعی قاطی می‌کند) به انحراف‌معیارِ **پیکسل‌های داخل یک حلقه‌ی واحد** (نه بین حلقه‌ها) -- که فقط نویز واقعی سنسور/بافت ریز را می‌سنجد و از هر روند سیستماتیک بزرگ‌مقیاس (vignetting) مستقل است.

**تصمیم کاربر:** «اولی رو انجام بده، رفع ریشه‌ای». پیاده‌سازی شد: در `_halo_radial_profile`، به‌جای MAD حلقه‌به‌حلقه (`median(|profile-median(profile)|)`)، اکنون از انحراف‌معیارِ *درون* هر حلقه محاسبه می‌شود -- با `sumsq = bincount(weights=patch**2)` علاوه بر `sums`، سپس `ring_var = sumsq/cnts - mean**2` و `ring_std = sqrt(max(ring_var,0))` برای هر حلقه، و در نهایت میانه‌ی `ring_std` روی حلقه‌های با تعداد پیکسل کافی (`cnts>=5`) به‌عنوان `pixel_noise` گرفته می‌شود. این معیار فقط شکل نویز/گزارش (`contrast_sigma`, `profile_noise`, سقف مطلق ماژول ۱۶.۵) را عوض می‌کند -- منطق تعیین‌کننده‌ی خودِ شعاع دایره (threshold-crossing روی `profile`) دست‌نخورده باقی ماند، پس ریسک بی‌ثبات‌کردن چیزی که کاربر قبلاً تایید کرده بود وجود ندارد.

**نتیجه‌ی رگرسیون مجدد روی همان ۵ عکس (بهبود بزرگ):**
- `contrast_sigma` از بازه‌ی غیرمنطقی (۲۵- تا ۴۰-) به بازه‌ی معقول (اکثراً ۰.۰۴- تا ۸-، چند مورد حتی مثبت نزدیک صفر) رسید.
- چند دیسک اکنون درست `contrast_sigma≈0` **و** `0/72` جهت پذیرفته‌شده دارند (مثلاً `170913` دیسک‌های ۲،۳،۷،۸،۹؛ `155938` دیسک ۵؛ `143424` دیسک ۴) -- دقیقاً منطبق با دیسک‌هایی که به چشم هیچ زونی ندارند.
- بقیه‌ی دیسک‌ها به بازه‌ی بسیار محدودتر و منطقی‌تری از تعداد جهت پذیرفته‌شده رسیدند (اکثراً ۰ تا ۲۰ از ۷۲، به‌جای ۴۰ تا ۷۲ قبلی) -- تعداد کم جهت‌های پذیرفته‌شده با یک لبه‌ی واقعیِ محدود/جزئی سازگارتر است تا یک روند کاذبِ همه‌جاحاضر.
- چند مورد باقی‌مانده با پذیرش بالا (مثلاً `143217` دیسک ۲: `72/72`؛ `155938` دیسک ۸: `53/72`) هنوز نیاز به بررسی بصری مستقیم کاربر دارند تا مشخص شود این‌ها هاله‌ی واقعیِ بزرگ هستند یا هنوز باقی‌مانده‌ی همان مشکل.

**وضعیت:** رفع ریشه‌ای پیاده و رگرسیون کمّی نشان‌دهنده‌ی بهبود بزرگ است؛ overlayهای تصویری روی همان ۵ عکس دوباره تولید و برای بازبینی نهایی کاربر ارسال می‌شود.

### ۱۰.۱۸) بازبینی چشمی کاربر روی overlayهای اصلاح‌شده — سه مشکل مستقل دیگر پیدا و رفع شد

کاربر بعد از دیدن overlayهای اصلاح‌شده، سه مشکل گزارش کرد: (۱) دیسک وسط هر پتری اصلاً تشخیص داده نمی‌شود، (۲) هنوز چند دیسک بدون‌زون، زون کاذب می‌گیرند، (۳) چند زون واقعاً بزرگ دایره‌ی کوچک‌تر از واقعیت می‌گیرند.

**مشکل ۱ — ریشه‌یابی با شواهد مستقیم:** یک اسکریپت تشخیصی جدید (`debug_center_disk.py`) خروجی خام Hough/Blob/Fusion را قبل و بعد از فیلتر سازگاری شعاع چاپ کرد. روی هر ۳ عکسی که بررسی شد، Hough دیسک مرکزی را با اطمینان بالا پیدا کرده بود (`confidence` بین ۰.۸۴ تا ۰.۹۶، در یک مورد بالاترین اطمینان کل عکس!) اما فیلتر «سازگاری شعاع» ماژول ۱۴ (که فرض می‌کند دیسک‌های واقعی فقط در ۲ خوشه‌ی شعاعی قرار می‌گیرند) آن را به‌عنوان singleton رد کرده بود، چون شعاعش (۱۲۲-۱۳۹px) با خوشه‌ی بقیه‌ی دیسک‌ها (۷۱-۹۷px) جور درنمی‌آمد.

کاربر توضیح داد این لزوماً خطای اندازه‌گیری نیست: «دیسک‌ها بعضی اوقات در محل کشت ممکن است خود را ول کنند و کمی پهن شوند» — یعنی فرض «فقط ۲ اندازه‌ی فیزیکی» مطلق نیست. **راه‌حل:** به‌جای شل‌کردن فیلتر برای همه، یک نجاتِ مبتنی‌بر-شواهد اضافه شد: singleton فقط وقتی رد می‌شود که امتیاز فیوژنش هم پایین باشد (`cfg.fusion_radius_outlier_rescue_score=0.65`) — یعنی شواهد مستقلِ قوی (همان امتیازی که `fusion_min_score` هم از آن استفاده می‌کند) می‌تواند فرض «۲ اندازه» را نقض کند، ولی singletonهای واقعاً ضعیف (احتمالاً متن روی دیسک یا آرتیفکت) همچنان رد می‌شوند.

**مشکل ۲ و ۳ — ریشه‌ی مشترک:** نویز `pixel_noise` بخش ۱۰.۱۷ هنوز *محلی به هر دیسک* بود (از حلقه‌های همان دیسک). این دو مشکل مستقل ایجاد می‌کرد: نشتِ هاله‌ی واقعیِ دیسک مرکزی (که اغلب هاله‌ی بزرگی دارد) به تخمین نویز دیسک‌های همسایه‌ی بدون‌هاله (کمک به مشکل ۲)، و بزرگ‌نماییِ نویز برای دیسکی که خودش لبه‌ی هاله‌ی ناهموار/جاگ‌دار دارد -- آن ناهمواری سیگنال واقعی است نه نویز (علت مشکل ۳).

**راه‌حل:** تابع جدید `_compute_dish_background_noise` یک نویز **مشترک برای کل پتری** می‌سازد: ناحیه‌ای که مطمئناً بیرون از قلمرو هاله‌ی *هر* دیسکی است (فراتر از سقف `cfg.halo_extension_max_frac_of_petri_radius` که هر دیسک مجاز است رشد کند) پیدا می‌شود، سپس انحراف‌معیار محلی (`cv2.blur` روی تصویر و تصویر به‌توان ۲، با پنجره‌ای نسبت به کوچک‌ترین شعاع دیسک) روی همین ناحیه محاسبه و میانه‌اش گرفته می‌شود. `_halo_radial_profile` حالا یک پارامتر اختیاری `bg_noise` می‌گیرد که وقتی داده شود جایگزین نویز محلیِ قبلی می‌شود؛ `segment_dish_halos` این نویز مشترک را یک‌بار در ابتدای هر پتری محاسبه و به همه‌ی دیسک‌های همان پتری می‌دهد (`profile_noise` در خروجی، که ماژول ۱۶.۵ هم از همان استفاده می‌کند -- بدون نیاز به تغییر در ۱۶.۵).

**تست رگرسیون ۷عکسی (بدون تغییر منطق تعیین شعاع، فقط نویز):** بدون استثنا؛ تعداد جهت‌های پذیرفته‌شده در ماژول ۱۶.۵ به‌طور کلی محافظه‌کارانه‌تر شد (مثلاً `142502` از تقریباً همه‌جا رشد به فقط ۴/۹ دیسک) بدون آسیب به دو عکسی که کاربر قبلاً تایید کرده بود (`1.1.1`/`2.2.1` هر دو همچنان ۱۳/۱۳).

**وضعیت:** هر سه اصلاح پیاده و رگرسیون ساختاری (۷ عکس قدیم + ۵ عکس مشکل‌دار) در حال اجراست؛ نتیجه‌ی کمّی/تصویری per-image برای بازبینی نهایی کاربر ارسال می‌شود.

**نتیجه:** تعداد دیسک تشخیص‌داده‌شده دقیقاً در همان ۳ عکسی که مشکل داشتند ۱ عدد افزایش یافت (`143217`: ۷→۸، `143424`: ۷→۸، `143806`: ۸→۹)؛ ۲ عکس دیگر از اول این مشکل را نداشتند (مطابق با یافته‌ی ریشه‌یابی). رگرسیون ۷عکسی قدیم هم بدون رگرسیون در نتیجه‌ی تاییدشده باقی ماند.

### ۱۰.۱۹) بازبینی چشمی روی عکس sample جدید — سه مشکل مستقل دیگر، شامل یک باگ واقعی در فرمول شعاع گزارشی

کاربر یک عکس واقعی (بعد از اصلاح‌های ۱۰.۱۸) با چشم بررسی کرد و ۴ مشکل گزارش داد:

**مشکل الف (باگ واقعی، تایید و رفع شد):** «شعاع دایره‌ی بنفش از خودِ شعاع دیسک کمتر است و درون دیسک کشیده می‌شود» و «دایره‌های بنفش به‌خاطر برخی رخدادها که درون هاله هستند، درون خودِ هاله کشیده می‌شوند در حالی که مرز هاله بزرگ‌تر است».

*ریشه‌یابی:* `halo_radius_px` (شعاع گزارشی نهایی، همان که به میلی‌متر تبدیل می‌شود) از `sqrt(area_final/π)` محاسبه می‌شد، جایی که `area_final` مساحت **حلقه‌ای/annulus** بود (مساحت کل چندضلعی منهای دایره‌ی خودِ دیسک که صرفاً برای نمایش کانتور کم شده بود). فرمول `sqrt(area/π)` فرض می‌کند مساحت متعلق به یک **دایره‌ی کامل** است -- برای مساحت یک *حلقه‌ی نازک* این فرمول عدد بسیار کوچک‌تری نسبت به شعاع بیرونی واقعی می‌دهد (می‌تواند حتی از `r_disk` هم کمتر شود)، و هر حفره/بی‌نظمی داخل حلقه (مثلاً یک رخداد/حباب که سیگنال محلی متفاوتی دارد و ممکن است در نسخه‌های بعدی از مساحت حذف شود) شعاع گزارشی را باز هم کوچک‌تر از مرز واقعی نشان می‌دهد.

*راه‌حل:* شعاع گزارشی حالا مستقیماً میانگین آرایه‌ی `final_radii` (شعاع per-angle که همیشه `>= r_disk` است، طبق تضمین ساختاری موجود) است -- نه یک برآورد غیرمستقیم مبتنی بر مساحت. این هم در ماژول ۱۶ (`segment_dish_halos`) و هم ۱۶.۵ (`extend_dish_halos`) اصلاح شد. مساحت ماسک (`halo_area_px`) به‌عنوان یک فیلد جدا و صحیح (برای گزارش مساحت mm² در آینده) دست‌نخورده می‌ماند -- فقط دیگر برای برآورد *شعاع* استفاده نمی‌شود. این اصلاح یک اثر جانبی مثبت هم دارد: ماژول ۱۷ (تشخیص حباب) هم از همین `halo_radius_px` برای مرز بیرونیِ ناحیه‌ی جست‌وجوی حباب استفاده می‌کند؛ فرمول قبلی (کوچک‌تر از واقعیت) باعث از دست رفتن حباب‌های واقعی در قسمت بیرونی هاله می‌شد.

**مشکل ب (هنوز باز):** «هنوز دیسک‌های بدون‌زون، دایره‌ی کاذب می‌گیرند» -- علیرغم اصلاح نویز مشترک پتری (بخش ۱۰.۱۸)، چند مورد باقی مانده است. نیاز به بررسی کمّی تازه بعد از اعمال اصلاح مشکل الف (چون تغییر فرمول شعاع می‌تواند تصویر را عوض کند).

**مشکل ج (در حال بررسی):** در یکی از عکس‌های ارسالی، یک دیسک (به نظر می‌رسد `CRO30`) اصلاً هیچ‌دایره‌ای (نه حتی مرز قرمز دیسک) ندارد -- یعنی این یک شکست در خودِ *تشخیص دیسک* (ماژول ۱۴) است، نه ماژول هاله. یک تشخیصی جدید (`debug_missing_disk_143806.py`) که کل `fusion_rejected` + `final_candidates` را با موقعیت/امتیاز/دلیل رد چاپ می‌کند، برای یافتن ریشه‌ی این مورد در حال اجراست.

**وضعیت:** مشکل الف رفع شد. مشکل ب و ج در حال بررسی؛ نتیجه در بخش بعدی مستند می‌شود.

### ۱۰.۲۰) مشکل ج بسته شد (کاذب بود) — و ریشه‌یابی نهاییِ مشکل ب: پروفایل «برجسته» (hump) به‌جای گذار یک‌طرفه

**مشکل ج (دیسک بدون‌دایره در `20260108_143806.jpg`):** اسکریپت `debug_missing_disk_143806.py` نشان داد هر ۹ عضوِ `fusion_rejected+final_candidates` این پتری `status=accepted` هستند و هیچ‌کدام رد نشده‌اند؛ بازبینی مستقیمِ overlay تولیدشده (`missing_disk_143806_fusionreport.jpg`) هم تایید کرد هر ۹ دیسک (شامل `CRO30`) در مرحله‌ی Fusion دایره‌ی سبز (پذیرفته‌شده) دارند. سپس `debug_halo_status_143806.py` وضعیت هر ۹ دیسک را در خروجی ماژول ۱۶/۱۶.۵ چاپ کرد: همه `status="ok"` با `halo_radius_px` معقول. یک overlay شماره‌گذاری‌شده مستقیماً روی عکس اصلی (بدون فشرده‌سازی/تغییر رزولوشن غیرضروری) رسم و با عکس خام مقایسه شد: هر ۹ دیسک واقعی (`NA30`,`SXT`,`FOS200`,`CZ30`, دیسک مرکزی,`FM300`,`CFM5`,`CRO30`, و یک دیسک بدون‌برچسب) دقیقاً یک‌به‌یک با ۹ دایره‌ی رسم‌شده منطبق بودند. **نتیجه: مشکل ج در این عکس دیگر بازتولید نمی‌شود** — به‌احتمال زیاد همان نجاتِ radius-outlier (بخش ۱۰.۱۸) که قبلاً برای دیسک مرکزی پیاده شد، این مورد را هم (که دیسک مرکزی همین عکس بود، `r=122` در برابر خوشه‌ی ۷۵-۸۶) از قبل حل کرده بود؛ گزارش کاربر مربوط به overlayِ قبل از آن اصلاح بوده است.

**مشکل ب — ریشه‌یابی کامل (با بررسی مستقیم پروفایل‌های خام):** یک عکسِ کاملاً جدید و ندیده (`20260110_143424.jpg`) با یک اسکریپت تشخیصیِ جدید (`diag_143424_falsezone.py`) بررسی شد که برای هر دیسک، علاوه بر `contrast_sigma`/`r_circle`/`n_accepted`، **خودِ آرایه‌ی پروفایل شعاعی خام** را هم چاپ می‌کند. از ۸ دیسک این پتری، ۴ تای آن‌ها (`contrast_sigma` بین ۰.۴۴- تا ۲.۲۱-، یعنی عملاً در سطح نویز) با بررسی چشمیِ مستقیمِ crop شده از عکسِ اصلی (نه overlay) تایید شدند که **هیچ ناحیه‌ی مهار قابل‌مشاهده‌ای ندارند** (لَون باکتری دقیقاً تا لبه‌ی دیسک می‌رسد) — با این‌حال `r_circle` ماژول ۱۶ برایشان ۱.۵ تا ۱.۹ برابر `r_disk` بود و بعد از ماژول ۱۶.۵ به ۲ تا ۲.۴ برابر می‌رسید.

بررسی خودِ آرایه‌ی پروفایل (نه فقط `contrast_sigma`) علت دقیق را روشن کرد: پروفایل این دیسک‌ها **یک‌طرفه/یکنواخت نیست** — از نزدیک دیسک شروع می‌شود، در میانه‌ی بازه‌ی جست‌وجو به یک «برجستگی» (hump) کوچک اما غیرصفر می‌رسد، سپس **دوباره به سمت مقدار اولیه برمی‌گردد** (مثلاً پروفایل یکی از این دیسک‌ها: `160→…→174.4 (اوج در میانه)→…→169.7`). منطق threshold-crossing قبلیِ ماژول ۱۶ (`اولین حلقه‌ای که از یک آستانه‌ی میان‌یابی‌شده‌ی inner/background عبور کند`) این برجستگیِ موقت را به‌اشتباه به‌عنوان گذارِ واقعی و دائمی می‌پذیرفت، چون فقط یک لحظه (یک حلقه) را می‌سنجید، نه اینکه آیا این گذار **برای همیشه** باقی می‌ماند یا خیر. از نظر زیستی، گذارِ واقعیِ هاله-به-لَون یک‌طرفه و دائمی است (وقتی چگالیِ لَونِ همگن شروع شد، دیگر برای همیشه همان‌جا می‌ماند) — پس یک برجستگیِ موقت که دوباره برمی‌گردد، به‌طور قطع سیگنال زیستی نیست (به‌احتمال زیاد بازتاب نور نزدیک دیسکِ سفید، یا شیب جزئی نورِ صحنه).

**اصلاح ریشه‌ای (ماژول ۱۶، `_halo_radial_profile`):** threshold-crossing لحظه‌ای با یک معیارِ **«همگرایی دائمی به پس‌زمینه»** جایگزین شد: به‌جای اولین حلقه‌ای که از آستانه عبور کند، اولین حلقه‌ی `k` پیدا می‌شود که **از آن‌جا تا انتهای پروفایل، تمام مقادیر** در یک باند نویز-محور (`cfg.halo_background_convergence_sigma=2.0`× نویز) حول مقدار پس‌زمینه بمانند. اگر چنین همگرایی‌ای هرگز رخ ندهد، شعاع در `r_in` (کمینه‌ی مطلق) می‌ماند — دقیقاً رفتار مطلوب برای دیسک بدون سیگنال قابل‌اتکا، بدون نیاز به هیچ گیت وجود/عدم‌وجودِ صریح (طبق همان معماریِ تاییدشده‌ی «همیشه یک دایره»).

**اصلاح مکمل (ماژول ۱۶.۵):** حتی بعد از اصلاح بالا، رشدِ کمانیِ ماژول ۱۶.۵ هنوز روی این ۴ دیسک ۲۵ تا ۳۱ از ۷۲ جهت را می‌پذیرفت (با `max_z` تا ۳۲!) — چون هر کمانِ ۱۸-درجه به‌تنهایی بسیار پرنویزتر از پروفایلِ همه‌جهته است (ده‌ها پیکسل به‌جای صدها/هزاران در هر حلقه)، و آزمودنِ ۷۲ کمانِ نسبتاً مستقل در سطح استاندارد z=2 روی نویزِ واقعیِ عکس (که همبسته/ساختاریافته است — بافت agar، فشردگیِ JPEG، بازتاب نور — نه i.i.d. گاوسیِ خالص) به‌طور سیستماتیک بیش از حدِ انتظار آماری قله می‌پذیرد (یک مسئله‌ی کلاسیکِ «آزمون‌های چندگانه»). راه‌حل: رشدِ کمانی اکنون فقط وقتی امتحان می‌شود که `|contrast_sigma|` **تجمیعیِ** ماژول ۱۶ (که از صدها/هزاران پیکسل محاسبه شده و به‌مراتب قابل‌اتکاتر است) از یک سطحِ استانداردِ ۳σ عبور کند (`cfg.halo_extension_require_min_contrast_sigma=3.0`) — وقتی آماره‌ی مطمئن‌تر خودش می‌گوید سیگنالی نیست، به یک آماره‌ی پرنویزتر برای همان تصمیم اعتماد نمی‌شود؛ در غیر این صورت شعاع همان `r_circle` ماژول ۱۶ (که با اصلاح بالا خودش دیگر تورم کاذب ندارد) باقی می‌ماند.

**نتیجه‌ی رگرسیون بصری/کمّی (روی ۵ عکس، شامل تصویر جدید و ندیده‌ی `20260110_143424.jpg`):**
- هر ۴ دیسکِ کاملاً بدون‌زون در `20260110_143424.jpg` اکنون دایره‌ای بسیار نزدیک به کمینه دارند (`r_circle` از ۱۳۶-۱۷۳ به ۱۰۶-۱۴۹، و رشد کمانی از ۲۵-۳۱/۷۲ جهت به دقیقاً `۰/۷۲` برای ۳ مورد از ۴ رسید).
- دیسک‌های با `contrast_sigma` واقعاً معنادار (۵.۷- تا ۸.۳-) هیچ تغییر مخربی ندیدند؛ رشدِ کمانی برایشان دقیقاً مثل قبل کار می‌کند.
- یک اثر جانبیِ مثبتِ غیرمنتظره: معیار «همگرایی دائمی» روی چند دیسکِ **واقعاً هاله‌دار** نشان داد threshold-crossing قدیمی زودتر از موعد متوقف می‌شد (مثلاً یک دیسک با `contrast_sigma=-7.45` که پروفایلش واقعاً تا حلقه‌ی بسیار دورتری ادامه داشت، قبلاً در `r=217.7` متوقف می‌شد، الان تا `r=304.1` — جایی که واقعاً و برای همیشه به پس‌زمینه می‌رسد — ادامه می‌یابد). این دقیقاً مکملِ رفعِ همان مشکلی است که کاربر در بخش ۱۰.۱۹ گزارش داد («دایره‌ی بنفش کوچک‌تر از مرز واقعی هاله است») — یک بررسی مستقل، از زاویه‌ای متفاوت، همان جهتِ اصلاحی را تایید کرد.
- بازبینی چشمیِ ۳ عکسِ دیگر (`20260101_143217.jpg`, `20260108_143806.jpg`, `20260104_170913.jpg`) هیچ رگرسیونی نشان نداد؛ یک ناسازگاریِ قدیمیِ گزارش‌شده (شعاعِ گزارشی کوچک‌تر از `r_disk` روی ۲ دیسک در `20260101_143217.jpg`) هم به‌طور جانبی رفع شد.

**وضعیت مشکل ب:** رفع شد و با ۵ عکس (۴ عکسِ قبلاً بررسی‌شده + یک عکسِ کاملاً جدید) اعتبارسنجی شد.

### ۱۰.۲۱) یافته‌ی جدید (باز، نیازمند تصمیم کاربر): رفتارِ لبه‌ی-پرتگاهیِ `fusion_strong_hough_branch_score` باعثِ یک دیسکِ گم‌شده‌ی دیگر شد

حین اعتبارسنجیِ گسترده‌ی بخش ۱۰.۲۰ (که عمداً روی عکس‌های بیشتر از آن‌چه کاربر درخواست کرده بود اجرا شد، طبق اصل «آزمایش روی عکس‌های متنوع، نه فقط نمونه‌ی درخواستی»)، در `20260105_155938.jpg` یک دیسکِ نهم (`CRO30`) دیده شد که در overlay هیچ دایره‌ای (نه حتی مرز قرمز) نداشت — دقیقاً همان الگویِ مشکل ج در بخش ۱۰.۱۹/۱۰.۲۰.

**ریشه‌یابی:** اسکریپت `debug_missing_disk_155938.py` نشان داد `CRO30` در Fusion (ماژول ۱۴) با `branch_score=0.482` و `reasons=['low_circularity','low_solidity']` **رد شده** — درحالی‌که `hough_support=0.806` (اطمینانِ خودِ Hough بالا و معتبر بود) و `blob_support=0.000` (شاخه‌ی Blob چیزی برای این دیسکِ خاص پیدا نکرده بود، مثل اکثر دیسک‌های این عکس). دلیل رد شدن: ماژول ۱۴ فقط وقتی از فیلترِ سخت‌گیرِ `circularity`/`solidity` صرف‌نظر می‌کند که `branch_score >= cfg.fusion_strong_hough_branch_score(=0.50)`. مقایسه با یک دیسکِ دیگر همان عکس (`CFM5`, `branch_score=0.588`, `circ=0.34` — حتی **بدتر** از `CRO30`) که چون از این آستانه عبور کرده بود، پذیرفته شد، نشان می‌دهد مشکل خودِ آستانه‌ی `circularity`/`solidity` نیست، بلکه رفتارِ **لبه‌ی-پرتگاهیِ دودویی** (binary cliff) این گیت است: دو کاندیدِ تقریباً یک‌جور (هر دو Hough-تنها، هر دو geometry ضعیف به‌خاطر شکستِ محلیِ Blob) صرفاً چون یکی ۰.۴۸۲ و دیگری ۰.۵۸۸ است، یکی رد و دیگری با geometry حتی ضعیف‌تر پذیرفته می‌شود.

**چرا این یک اصلاحِ سریع/بدون‌مشورت نیست:** تغییرِ خودِ عدد `0.50` دقیقاً همان نوعِ «تنظیم روی یک عکس خاص» است که قوانین پروژه صریحاً منع کرده‌اند — این عدد از قبل با توجیهِ مستند (بخش مربوط به `fusion_strong_hough_branch_score` در ماژول ۱۴) انتخاب شده. راه‌حلِ درست‌تر و اصولی‌تر احتمالاً حذفِ کاملِ این گیتِ دودویی و اتکا به امتیازِ ترکیبیِ پیوسته‌ای است که از قبل هم محاسبه می‌شود (`entry["score"]`, ترکیبِ `branch_score` و `geo_score`) و همان `cfg.fusion_min_score` که در جای دیگر پایپ‌لاین از قبل استفاده می‌شود (`CRO30` با `score=0.545` به‌راحتی از این آستانه عبور می‌کند) — اما این یک تغییرِ معماریِ Fusion است (نه یک عدد)، و طبق سابقه‌ی این پروژه، هر تغییرِ معماری/آستانه با کاربر در میان گذاشته شده. این بخش فقط برای مستندسازیِ یافته (طبق اصلِ «continuously update PROJECT_HISTORY») نوشته شده؛ **هنوز اصلاح نشده و منتظر تصمیم کاربر است.**

**وضعیت:** یافته مستند شد؛ پیاده‌سازیِ اصلاح منوط به تایید کاربر روی رویکردِ پیشنهادی (حذفِ گیتِ دودویی، اتکا به امتیازِ پیوسته‌ی موجود).

### ۱۰.۲۲) ارزیابیِ تجربیِ یک پیشنهادِ معماریِ کلانِ جایگزین از کاربر — رد شد (با شواهد عددی)، نه فقط تئوری

کاربر (نگران overfitting روی همان چند عکسی که تا اینجا استفاده شده) یک بازطراحیِ کاملِ ماژول‌های ۱۶/۱۶.۵ پیشنهاد داد: **مرحله‌ی ۱** — ۳۶ کمانِ ۱۰-درجه، هرکدام مستقل با یک آستانه‌ی adaptive متوقف شوند، سپس **میانه‌ی ۳۶ شعاع** به‌عنوان دایره‌ی اولیه؛ **مرحله‌ی ۲** — آن دایره را هر بار ۱mm (یک دایره‌ی واحد و هم‌سو، نه per-angle) رشد بده، در هر شعاعِ کاندید گرادیانِ شعاعی را روی تمامِ محیطِ دایره بسنج، میانگین/انحراف‌معیارش را ثبت کن، تا سقفِ «حداقل فاصله‌ی دو دیسک طبق استاندارد EUCAST»؛ در آخر، دایره‌ای که **کمترین انحراف‌معیارِ گرادیان** را دارد به‌عنوان مرزِ هاله انتخاب شود؛ به‌علاوه یک اعتبارسنجیِ «همگن‌بودنِ بافت» تا ۵mm جلوتر برای رد کردنِ مینیمم‌های کاذب.

**بررسیِ مقدماتی (قبل از هر پیاده‌سازی):** فایل‌های PDF موجود در ریپازیتوری (`v_16.0_Breakpoint_Tables.pdf`, `Dosages_v_16.0_Breakpoint_Tables.pdf`) به‌طور کامل جست‌وجو شدند. هیچ استانداردِ عمومیِ «حداقل فاصله‌ی بین دو دیسک» در آن‌ها پیدا نشد — تنها مقداری که به فاصله‌ی دیسک‌ها اشاره دارد، فاصله‌ی اختصاصیِ تست D (سینرژی اریترومایسین/کلیندامایسین، «۱۲ تا ۲۰ میلی‌متر لبه‌به‌لبه») است که برای یک آزمایشِ دوقلوی خاص تعریف شده، نه چیدمانِ عمومیِ چند دیسک روی یک پتری. **جایگزینِ پیشنهادی و دقیق‌تر:** به‌جای یک عددِ ثابتِ استاندارد، از همان سقف‌های هندسیِ دقیقاً-برای-همین-عکسِ از قبل پیاده‌شده (`_neighbor_voronoi_cap` + `_dish_edge_cap`، نیمسازِ عمودِ فاصله تا نزدیک‌ترین دیسکِ *واقعیِ* همین پتری + برخورد به لبه‌ی پتری) استفاده شود — این دقیق‌تر از هر عددِ کتابی است چون به چیدمانِ واقعیِ همین عکس وابسته است، نه یک میانگینِ استاندارد.

**تست تجربی (نه فقط تئوری):** الگوریتمِ پیشنهادی دقیقاً طبق مشخصاتِ بالا در یک اسکریپتِ مستقل (`proto_gradient_uniformity.py`) پیاده و روی همان ۸ دیسکِ شناخته‌شده‌ی `20260110_143424.jpg` اجرا شد — ۴ دیسکِ کاملاً بدون‌زون (تاییدشده با بررسیِ مستقیمِ عکس در بخش ۱۰.۲۰) و ۴ دیسکِ با زونِ واقعی.

**یافته‌ی اول (Stage 1 پُرنویزتر از روشِ فعلی):** میانه‌ی ۳۶ کمانِ ۱۰-درجه برای ۳ از ۴ دیسکِ بدون‌زون بسیار بزرگ‌تر از نسخه‌ی فعلیِ کمیته‌شده درآمد (مثلاً یک دیسک: `284.3px` در برابر `107.1px` فعلی؛ دیگری: `296.6px` در برابر `111.7px`). علت: هر کمانِ ۱۰-درجه فقط از پیکسل‌های همان کمان (چند ده پیکسل در هر حلقه) استفاده می‌کند، در حالی‌که پروفایلِ همه‌جهته‌ی فعلی از صدها/هزاران پیکسل در هر حلقه بهره می‌برد — دقیقاً همان مشکلِ نویزِ per-arc که در بخش ۱۰.۱۶/۱۰.۲۰ کشف و برایش راه‌حل ساخته شده بود، اینجا (این‌بار برای برازشِ *اولیه*، نه فقط گسترش) دوباره ظاهر شد. میانه‌گرفتن از ۳۶ نمونه‌ی نویزی آن‌ها را کاملاً پاک نمی‌کند.

**یافته‌ی دوم (مهم‌تر، نقصِ ساختاریِ خودِ معیار «کمترین انحراف‌معیار»):** روی یک دیسکِ واقعاً هاله‌دار (`global=(1575,1029)`)، کاندیدِ `r=282px` گرادیانِ میانگینِ بسیار قوی (`۲۷.۸`، ~۶ برابرِ کفِ نویزِ همان پتری) داشت اما چون انحراف‌معیارش (`۱۳.۸`) از دو کاندیدِ *ضعیف* بعدی (`r=318`: میانگین=`۵.۸`/انحراف‌معیار=`۷.۰`؛ `r=354`: میانگین=`۵.۰`/انحراف‌معیار=`۷.۰`) بیشتر بود، معیارِ «کمترین انحراف‌معیار» آن لبه‌ی واقعی و قوی را **رد** و یکی از آن دو ناحیه‌ی کم‌سیگنال/نزدیک-به-نویز را به‌جایش انتخاب می‌کرد. این یک ایرادِ پیاده‌سازی نیست؛ یک ویژگیِ ساختاریِ خودِ معیار است: نواحیِ **کاملاً صاف/بدون‌سیگنال** (لَونِ همگنِ دور از هر لبه) به‌طورِ ذاتی انحراف‌معیارِ کمتری از هر لبه‌ی واقعی دارند (چون لبه‌ی واقعی، حتی وقتی قوی و منسجم است، طبیعتاً کمی ناهمواریِ زیستی دارد) — پس «کمترین انحراف‌معیار» بدونِ یک الزامِ مطلق روی بزرگیِ خودِ سیگنال، سیستماتیک به‌سمتِ نواحیِ ضعیف/صاف کشیده می‌شود، نه لبه‌ی واقعی. آزمایشِ یک معیارِ ترکیبیِ جایگزین (نسبتِ میانگین/انحراف‌معیار، شبیهِ SNR) هم این مشکل را کامل حل نکرد: برای یکی از دیسک‌های کاملاً بدون‌زون، بیشترین SNR محاسبه‌شده (`≈۱.۰`) عملاً هم‌ارز با بیشترین SNR روی یک دیسکِ مبهمِ واقعاً‌هاله‌دار بود — یعنی بدونِ یک کفِ مطلقِ کالیبره‌شده (که کالیبراسیونِ درستش خودش نیازمندِ یک کارِ تجربیِ مجزا و گسترده است، دقیقاً همان ریسکِ overfitting که کاربر نگرانش بود)، این معیارِ ترکیبی هم قابلِ‌اتکا نبود.

**نتیجه:** پیشنهادِ کاربر رد نشد چون «فکرِ بدی» بود؛ رد شد چون **با داده‌ی واقعی آزمایش شد و در دو نقطه‌ی مشخص (نویزِ Stage 1، و سوگیریِ ساختاریِ معیارِ کمترین-انحراف‌معیار به‌سمتِ نواحیِ صاف) عملکردی بدتر یا نامطمئن‌تر از معماریِ فعلی (بخش ۱۰.۲۰، از قبل با ۵ عکس اعتبارسنجی‌شده) نشان داد** — دقیقاً طبقِ همان اصلِ «تست تجربی، نه فقط تئوری» که کل این پروژه بر آن بنا شده. بخشِ درستِ پیشنهاد (سقفِ فیزیکیِ رشد به‌جای رشدِ بی‌پایان) از قبل در معماریِ فعلی با روشی دقیق‌تر (سقفِ هندسیِ Voronoi/لبه‌ی پتریِ مخصوصِ همین عکس) پیاده‌سازی شده بود.

**وضعیت:** معماریِ فعلی (بخش ۱۰.۲۰) دست‌نخورده و به‌عنوانِ راه‌حلِ معتبر باقی می‌ماند؛ هیچ تغییری در نوت‌بوک اعمال نشد (فقط اسکریپتِ آزمایشیِ scratch). یافته با جزئیاتِ کامل برای کاربر گزارش شد.

### ۱۰.۲۳) رفعِ گیتِ لبه‌ی-پرتگاهیِ Fusion (`CRO30` در `20260105_155938.jpg`) — با تست ضدِّ-overfitting روی ۱۰ عکس

طبقِ یافته‌ی بخشِ ۱۰.۲۱، دو گیتِ دودویی در ماژول ۱۴ (`if geom is None and branch_score<0.50: reasons.append("no_geometry")` و `if geom is not None and branch_score<0.50: چک circ/solid سخت‌گیر`) حذف شدند. **هیچ آستانه‌ی عددیِ جدیدی اضافه نشد** — geometry (circularity/solidity/fill_ratio) همچنان محاسبه و ذخیره می‌شود (برای تشخیص/گزارش)، اما دیگر یک تصمیمِ رد/قبولِ جداگانه و دودویی نیست؛ به‌جایش فقط از طریقِ `geo_score` (که از قبل هم با همین وزن در `entry["score"]` وارد می‌شد) به‌صورتِ پیوسته تاثیر می‌گذارد، و تصمیمِ نهایی صرفاً با `cfg.fusion_min_score` (که از قبل هم برای همین منظور در پایپ‌لاین وجود داشت) گرفته می‌شود. پارامترِ `cfg.fusion_strong_hough_branch_score` که دیگر هیچ‌جا استفاده نمی‌شود، حذف شد (نه صرفاً کامنت "بازنشسته" — طبقِ قاعده‌ی پروژه در موردِ کدِ قطعاً بلااستفاده).

**تستِ ضدِّ-overfitting (دقیقاً طبقِ درخواستِ صریحِ کاربر «حواست باشه Overfitting رخ نده»):** به‌جای صرفاً تاییدِ اینکه `CRO30` حالا پذیرفته می‌شود، یک رگرسیونِ گسترده روی **۱۰ عکسِ متفاوت** (۵ عکسِ قبلاً بررسی‌شده + ۵ عکسِ کاملاً جدید از روزهای مختلفِ عکس‌برداری: `142413`, `130252`, `143305`, `143345`, `155818`) اجرا شد که برای هر دیسکِ پذیرفته‌شده با geometry ضعیف (`circularity<cfg.min_circularity` یا `solidity<cfg.min_solidity`) جزئیاتِ کامل (از جمله `branch_score`) چاپ می‌کند — دقیقاً برای پیدا کردنِ هر کاندیدِ *جدیداً* پذیرفته‌شده (یعنی `branch_score<0.50` که قبلاً حتماً رد می‌شد).

**نتیجه:** از میانِ ۱۴ پتری/۷۰+ دیسک در این ۱۰ عکس، تنها **یک** کاندید با `branch_score<0.50` در نتیجه‌ی نهایی دیده شد — دقیقاً همان `CRO30` (`branch_score=0.482`). تمامِ کاندیدهای «geometry ضعیف» دیگر که در گزارش ظاهر شدند از قبل هم `branch_score>=0.50` داشتند (یعنی طبقِ همان کدِ قدیمی هم پذیرفته می‌شدند — رفتارِ از-قبل-موجود، نه رگرسیونِ جدید). چند پتری/دیسک هم همچنان به‌درستی رد شدند (مثلاً پتری‌های ۲ در `143345` و `155818`، هرکدام با ۲ کاندیدِ رد‌شده) — یعنی `cfg.fusion_min_score` هنوز به‌درستی کاندیدهای واقعاً ضعیف را فیلتر می‌کند، صرفِ حذفِ گیتِ دودویی باعثِ سرازیر شدنِ کاندیدهای کاذب نشد.

**وضعیت:** رفع شد؛ با ۱۰ عکس (نه فقط عکسِ مشکل‌دار) اعتبارسنجی شد تا اطمینان حاصل شود اصلاح، تعمیم‌پذیر است و به یک عکسِ خاص overfit نشده.

## ۱۱) چهار درخواستِ جدید بعد از تثبیتِ ماژول هاله

بعد از تاییدِ کاربر که «ماژول هاله به اندازه‌ی تقریباً قابل‌قبولی اوکی شده»، ۴ کارِ مجزا درخواست شد: (۱) زیرساختِ کشِ تشخیصِ پتری برای سرعت‌بخشیدن به تست‌ها، (۲) رفعِ مشکلِ رخدادهای محلیِ داخلِ هاله که مرزِ تشخیص را به اشتباه می‌اندازند، (۳) بررسیِ استانداردِ صحیحِ اندازه‌گیریِ قطرِ هاله، (۴) بازطراحیِ ماژولِ تشخیصِ حباب بر پایه‌ی ماسکِ رینگِ واقعی.

### ۱۱.۱) زیرساختِ کشِ تشخیصِ پتری (`petri_cache/`)

**طراحی:** به‌جای تکرارِ تشخیصِ پتری (ماژول ۴+۴.۱، پایدار و تاییدشده) در هر بارِ تستِ ماژول‌های بعدی (هاله، رخدادها)، خروجیِ دقیقِ `dishes` بلافاصله بعد از ماژول ۴.۱ برای همه‌ی ۷۸ عکسِ ریپازیتوری در پوشه‌ی `petri_cache/<نامِ‌عکس>/` سریالایز می‌شود (`petri_cache_utils.py` + `build_petri_cache.py`، هردو در ریشه‌ی ریپازیتوری، خودشان بخشی از ابزارهای دائمیِ پروژه‌اند، نه اسکریپتِ یک‌بارمصرف). فقط ۳ آرایه‌ی مستقل per-dish با PNG بدون‌افت ذخیره می‌شوند (`roi_gray_masked`, `processing_mask_roi`, `precise_dish_mask`)؛ بقیه‌ی فیلدها (`mask_full`, `roi_gray_precise_faded`, `roi_gray`) با همان توابعِ کمکیِ خودِ نوت‌بوک در لحظه‌ی بارگذاری بازسازی می‌شوند — یعنی هیچ منطقی کپی/تکرار نشده و کش هرگز نمی‌تواند با نوت‌بوک ناهماهنگ (drift) شود.

**تصمیمِ مهم (بعد از یک آزمایشِ ناموفق):** ابتدا `roi_gray_masked` با JPEG کیفیت ۹۵ ذخیره شد (برای کاهشِ حجم)، ولی مقایسه‌ی مستقیمِ اجرای fresh در برابرِ cached (`validate_petri_cache.py`) نشان داد حتی این افتِ ناچیز، از طریقِ `roi_gray_precise_faded`، به ماژول‌های ۵/۱۰/۱۰.۵ نشت می‌کند و امتیازهای Hough/Blob را به‌طور سیستماتیک (نه فقط نویزِ جزئی) جابه‌جا می‌کند — دقیقاً همان مرحله‌ای که قرار است با این کش تست/تنظیم شود. جایگزین شد با PNG بدون‌افت برای هر سه آرایه؛ آزمایشِ مجدد نشان داد نتیجه‌ی fresh و cached **کاملاً یکسان (بیت‌به‌بیت)** هستند.

**نتیجه:** کش برای هر ۷۸ عکس ساخته شد (اسکریپت `build_petri_cache.py` بدون آرگومان). حجمِ نهایی و هرگونه ملاحظه‌ی مربوط به اندازه‌ی ریپازیتوری در گزارشِ نهایی به کاربر اعلام شد.

### ۱۱.۲) رفعِ لنگر گرفتنِ مرزِ هاله به لبه‌ی فیزیکیِ پتری (ماژولِ جدیدِ ۱۶.۶)

**ریشه‌یابی با بررسیِ بصریِ مستقیم:** روی عکسی که کاربر توصیف کرد («دو دیسک با هاله‌ی نسبتاً بزرگ که یک رخدادِ تصادفی مرزِ هاله را به اشتباه انداخته»)، آرایه‌ی `final_radii` (۷۲ مقدارِ per-angle، خروجیِ ماژول ۱۶.۵) برای چند دیسک چاپ شد. در چند دیسک، اکثریتِ زاویه‌ها یک مقدارِ پایه‌ی یکنواخت داشتند ولی ۳ تا ۹ زاویه‌ی *منفرد و کاملاً مجاور هم* به‌طور ناگهانی ۱.۵ تا ۲.۷ برابرِ آن مقدارِ پایه می‌جهیدند. کراپِ مستقیمِ عکسِ خام (نه overlay) دقیقاً در همان زاویه‌ها/شعاع‌ها نشان داد نقطه‌ی «پذیرفته‌شده» روی حلقه‌ی روشنِ **لبه‌ی فیزیکیِ خودِ ظرفِ پتری** قرار دارد — نه مرزِ واقعیِ هاله. این برای دو دیسکِ کاملاً متفاوت (یکی با ظاهرِ «ستاره‌ای/نامتقارنِ واقعی» که در نگاهِ اول به‌اشتباه «رشدِ واقعی» تصور شد) تکرار و تایید شد؛ یعنی حتی الگویِ ظاهراً معتبر هم می‌توانست همین باگ باشد — نکته‌ای که با فرضِ اولیه (بدونِ بررسیِ مستقیمِ عکس) اشتباه تشخیص داده می‌شد.

**چرا سقفِ هندسیِ موجود (`_dish_edge_cap`) این را حل نمی‌کند:** آن سقف، مسیرِ رشد را تا جایی مجاز می‌داند که ماسکِ پتری (ماژول ۴) معتبر است؛ ولی این ماسک گاهی کمی فراتر از حلقه‌ی روشنِ قابلِ‌مشاهده در عکس ادامه دارد (بخشی از لبه‌ی شیشه/فلز هم داخلِ ماسکِ معتبر است). پس `_per_angle_gradient_extension` (که بزرگ‌ترین گرادیانِ هر کمان را برمی‌گزیند) در جهتی که سیگنالِ واقعیِ هاله زودتر از پایانِ بازه‌ی جست‌وجو به پس‌زمینه رسیده، تنها گرادیانِ «بزرگِ» باقی‌مانده در آن کمان -- گذارِ آگار-به-لبه -- را انتخاب می‌کند، و چون این گذار واقعاً قوی و کم‌نویز است، از هر دو شرطِ پذیرشِ موجود هم عبور می‌کند.

**راه‌حل (سلولِ سومِ درخواست‌شده، `repair_angular_outliers`، ماژولِ ۱۶.۶):** بدونِ تغییرِ ماژول ۱۶.۵ یا ماسکِ پتری، یک لایه‌ی تصحیحِ کاملاً هندسی/آماریِ مستقل اضافه شد -- مبتنی بر یک اصلِ فیزیکی: مرزِ واقعیِ نفوذِ آنتی‌بیوتیک (پدیده‌ای پیوسته) نمی‌تواند در یک بازه‌ی باریکِ زاویه‌ای جهشِ ناگهانی نسبت به همسایه‌های بلافصلش داشته باشد. هر زاویه‌ای که شعاعش بیش از `cfg.halo_angular_outlier_ratio=1.4` برابرِ میانه‌ی همسایگیِ محلی‌اش (پنجره‌ای به‌پهنایِ `cfg.halo_angular_outlier_window_frac=0.14`×تعدادِ زاویه‌ها، به‌اندازه‌ی کافی پهن‌تر از یک رخدادِ باریک ولی کوچک‌تر از نیمِ دایره تا عدم‌تقارنِ واقعیِ پهن اشتباه گرفته نشود) باشد، با همان میانه جایگزین می‌شود.

**اعتبارسنجیِ گسترده (۷ عکس، ۶۸ دیسک):** ۱۵ از ۶۸ دیسک حداقل یک زاویه‌ی اصلاح‌شده داشتند (نشان می‌دهد این یک رخدادِ واقعاً رایج بوده، نه ویژگیِ یک عکسِ خاص)؛ یک عکس (`20260108_143305.jpg`) صفر اصلاح داشت (نشان می‌دهد ماژول به‌اشتباه روی داده‌ی سالم فعال نمی‌شود). بازبینیِ بصریِ overlayها (رنگِ قرمز = دیسکِ اصلاح‌شده) دایره‌های منطقی‌تر و منطبق‌تر با ناحیه‌ی واقعاً تیره نشان داد.

**وضعیت:** رفع شد و روی ۷ عکس (شاملِ عکسِ اصلیِ مشکل‌دار + ۶ عکسِ دیگر) اعتبارسنجی شد.

### ۱۱.۳) بررسیِ استانداردِ صحیحِ اندازه‌گیریِ قطرِ هاله

متنِ رسمیِ EUCAST (از همان PDF موجود در ریپازیتوری، بخشِ «Disk diffusion» هر ارگانیسم، مثلاً صفحه‌ی Enterobacterales) پیدا و نقل شد:

> **Reading:** Unless otherwise stated, read zone edges as the point showing no growth viewed from the back of the plate against a dark background illuminated with reflected light.

دو یافته‌ی مهم:
1. **معیارِ مرز = «نقطه‌ی بدونِ هیچ رشد»**، نه یک آستانه‌ی نرم/میانی. این تقریباً همان فلسفه‌ی `cfg.halo_threshold_inner_weight=0.65` (بایاسِ عمدی به‌سمتِ نیازِ به همگرایی نزدیک‌به-کامل با پس‌زمینه، نه صرفاً نقطه‌ی میانه‌ی ۵۰٪) و معیارِ «همگراییِ دائمی» بخشِ ۱۰.۲۰ را تایید می‌کند -- هردو همان اصل را دنبال می‌کنند، هرچند خودِ عددِ ۰.۶۵ یک انتخابِ معقول است، نه چیزی با دقتِ عددیِ استاندارد.
2. **روشِ خواندنِ استاندارد، از پشتِ پتری و در برابرِ پس‌زمینه‌ی تیره با نورِ بازتابی است** -- نه از روبه‌رو با نورِ مستقیم (که ظاهراً روشِ عکس‌برداریِ فعلیِ این پروژه است). این توضیحِ محتملِ خیلی از رخدادهایی است که کلِ این نشست با آن‌ها دست‌وپنجه نرم کرده (خط‌خوردگیِ سواب، بازتابِ نور، لبه‌ی پتری) -- نه نقصِ الگوریتم، بلکه عدمِ تطابقِ شرایطِ عکس‌برداریِ موجود با پروتکلِ استانداردِ خواندن. **پیشنهاد به کاربر:** اگر عملی است، عکس‌برداریِ آینده طبقِ همین پروتکلِ EUCAST (از پشت، پس‌زمینه‌ی تیره، نورِ بازتابی) انجام شود -- به‌احتمالِ زیاد کارِ خودِ الگوریتم را به‌طورِ چشمگیری ساده‌تر و دقیق‌تر می‌کند.

تعریفِ هندسیِ فعلیِ پایپ‌لاین (قطر = ۲× شعاع از *مرکزِ دیسک*، نه لبه‌ی دیسک، تا مرزِ هاله) از قبل با تعریفِ استانداردِ بالینی (قطرِ کاملِ ناحیه‌ی مهار، شاملِ خودِ دیسک) همخوان است -- نیازی به تغییر نداشت.

### ۱۱.۴) بازطراحیِ ماژولِ ۱۷ بر پایه‌ی ماسکِ رینگِ واقعی

**مشکل:** `detect_halo_bubbles` ناحیه‌ی جست‌وجوی حباب را با یک دایره‌ی *اسکالر* بازمحاسبه می‌کرد (`(rad>=r_disk)&(rad<=r_halo)`، با `r_halo` = میانگینِ ساده‌ی `halo_radius_px`) -- یعنی دقیقاً همان ماسکِ رینگِ per-angleِ واقعی که ماژول‌های ۱۶/۱۶.۵/۱۶.۶ با دقت ساخته بودند (`halo_mask`) را نادیده می‌گرفت و به‌جایش یک دایره‌ی میانگین/کور به عدمِ‌تقارنِ واقعی جایگزین می‌کرد.

**راه‌حل:** طبقِ درخواستِ دقیقِ کاربر، `ring_zone` اکنون مستقیماً از همان `halo_mask` (که خودش از دایره‌ی دیسک + مرزِ per-angleِ نهاییِ هاله ساخته شده) گرفته می‌شود -- بدونِ هیچ بازسازیِ مستقلِ دایره‌ای. بقیه‌ی خط‌لوله (residual نسبت به پروفایلِ شعاعی + سیگنالِ گرادیانِ ماژول ۱۰ + آستانه‌گذاریِ MAD + Opening + connectedComponents + شعاعِ هر حباب از Distance Transform + فیلترِ اندازه‌ی نسبی) که از قبل دقیقاً همان فلسفه‌ی Blob Analysisِ درخواست‌شده را پیاده می‌کرد، دست‌نخورده ماند.

**تست:** روی یک عکسِ واقعی اجرا شد -- همه‌ی ۹ دیسک بدونِ خطا و با `status`های معنادار (`ok`/`ok_no_bubbles`) پردازش شدند؛ هیچ‌کدام `no_ring_pixels` نگرفتند (یعنی ماسکِ جدید هم هنوز به‌درستی معتبر است).

**وضعیت:** هر ۴ کار پیاده‌سازی و در حدِّ زمانِ موجود اعتبارسنجی شد؛ commit و push در حالِ انجام است.

### ۱۱.۵) اسکریپتِ پایتونیِ هم‌ارزِ نوت‌بوک (اجرای کلی + اجرای ماژول‌به‌ماژول بدونِ Jupyter)

کاربر درخواست کرد کدِ نهایی هم در نوت‌بوک و هم در یک فایلِ پایتون -- برای اجرای کلی و اجرای ماژول‌به‌ماژول -- در ریپازیتوری موجود باشد تا خودش بتواند اجرا بگیرد.

**ابزارِ جدید (`convert_notebook_to_script.py`):** نوت‌بوک را به یک فایلِ `.py` با نشانه‌گذاریِ سلولیِ استانداردِ percent (`# %%`، همان فرمتِ jupytext) تبدیل می‌کند -- هر `# %%` دقیقاً معادلِ یک سلولِ نوت‌بوک است. این تبدیل **کاملاً خودکار و از رویِ خودِ سلول‌های نوت‌بوک** انجام می‌شود (نه بازنویسیِ دستی)، پس نتیجه (`disk_detection_pipeline_modular.py`) تضمین‌شده دقیقاً با نوت‌بوک یکسان است و هیچ منطقی کپی/تکرار نشده؛ بعد از هر تغییرِ آینده در نوت‌بوک، کافی است همین اسکریپت دوباره اجرا شود.

فایلِ خروجی دو روشِ اجرا را پشتیبانی می‌کند: **(۱) اجرای کلی** با `python disk_detection_pipeline_modular.py` (کل پایپ‌لاین از بالا به پایین، دقیقاً مثلِ Run All در نوت‌بوک) و **(۲) اجرای ماژول‌به‌ماژول** با بازکردنِ همین فایل در VS Code/PyCharm/Spyder -- این ادیتورها نشانه‌ی `# %%` را به‌صورتِ بومی به‌عنوانِ مرزِ یک سلولِ قابلِ‌اجرای مستقل می‌شناسند (دکمه‌ی «Run Cell»)، بدونِ نیاز به خودِ Jupyter.

تنها تفاوتِ لازم با خروجیِ خامِ نوت‌بوک: خطِ `%matplotlib inline` (که فقط در IPython معنا دارد) کامنت شد، چون خارج از Jupyter بی‌اثر/نامعتبر است؛ تابعِ `show()` در اجرای مستقیم همچنان با پنجره‌ی تعاملیِ matplotlib کار می‌کند.

**اعتبارسنجی:** نسخه‌ی تولیدشده هم با `py_compile` (صحتِ نحوی) و هم با یک اجرایِ کاملِ end-to-end (با بک‌اندِ Agg، مسیرِ عکس موقتاً به یک عکسِ واقعیِ این محیط تغییر یافت) تست شد -- خروجیِ نهایی (قطرِ دیسک/هاله برای هر ۹ دیسک) دقیقاً با نتایجِ قبلاً تاییدشده‌ی همین عکس از طریقِ نوت‌بوک یکسان بود.

**رفعِ یک نشانه‌ی از-رده-خارج در `batch_process_antibiogram.py`:** `HALO_MARKER` هنوز به متنِ قدیمیِ سلولِ ۱۶.۵ («جدید») اشاره می‌کرد که در بازطراحی‌های این نشست به «بازطراحی دوم» تغییر کرده بود -- یعنی این اسکریپت با نوت‌بوکِ فعلی اصلاً اجرا نمی‌شد (خطای «سلولی حاوی این نشانه پیدا نشد»). به سلولِ ماژولِ ۱۶.۶ (آخرین/نهایی‌ترین اصلاح‌کننده‌ی مرزِ هاله) به‌روزرسانی شد؛ همچنین یک خروجیِ جدید (`03_bubbles_dish<N>.png`، از ماژولِ ۱۷ که این نشست تازه درست کار می‌کند) اضافه و گزارشِ نهایی به `04_final_report.txt` تغییرِ شماره یافت. با اجرای مستقیمِ `process_one_image` روی یک عکسِ واقعی تست شد -- هر ۴ فایلِ خروجی به‌درستی ساخته شدند.

**وضعیت:** هر دو فایل (اسکریپتِ کاملِ جدید + اصلاحِ batch script) اعتبارسنجی و آماده‌ی commit/push هستند.

### ۱۱.۶) فایلِ بریفینگِ آماده‌سازیِ مقاله (`PAPER_PREP_BRIEFING.md`) + پوشه‌ی `paper_figures/`

کاربر درخواست کرد یک فایل جامع شاملِ همه‌ی مطالب/نمودار/عکس/مقایسه/منبع/دیتابیسِ این پروژه تهیه شود تا بعداً همراه با یک تمپلیتِ وردِ کنفرانسی به یک نشستِ جدیدِ Claude داده شود که مقاله را کامل بنویسد.

**اقدام:** یک پوشه‌ی جدید `paper_figures/` در ریشه‌ی ریپازیتوری ساخته شد و ۱۹ فایلِ منتخب (از میانِ صدها تصویرِ تشخیصی/دیباگِ تولیدشده در طولِ این نشست، در اسکرچ‌پدِ موقت) با نام‌های توصیفی در آن کپی شدند -- شاملِ: نمونه‌ی تشخیصِ هم‌زمانِ چند-پتری (۲ ظرف)، نمونه‌های قبل/بعدِ رفعِ باگِ Fusion cliff-edge (CRO30)، نمونه‌های باگِ لنگرشدنِ هاله به لبه‌ی دیسک و رفعِ آن (ماژولِ ۱۶.۶)، نمونه‌های رفعِ مثبت‌کاذبِ هاله (همگراییِ پس‌زمینه)، و یک مجموعه‌ی کاملِ ۴-فایلیِ خروجیِ سرتاسریِ پایپ‌لاین (`full_pipeline_example_155938/`) روی یک عکسِ واقعی با ۹ دیسک.

**نکته‌ی جانبیِ کشف‌شده (نه رفع‌شده، فقط مستندشده):** حینِ تولیدِ یک نمونه‌ی «تشخیصِ چند-پتری»، عکسِ `20260105_160124.jpg` (که ظاهراً باید فقط یک پتریِ واقعی داشته باشد) با کشِ تشخیصِ پتری بررسی شد و مشخص شد ماژولِ ۴ آن را به‌اشتباه به ۷ کاندیدِ هم‌پوشانِ کاذب تقسیم کرده -- این خارج از محدوده‌ی هیچ‌کدام از درخواست‌های این نشست بود، رفع نشد، و فقط به‌عنوانِ یک محدودیتِ شناخته‌شده در فایلِ بریفینگ (بخشِ ۶) ثبت شد تا در آینده (اگر کاربر خواست) بررسی شود.

**محتوایِ فایلِ `PAPER_PREP_BRIEFING.md`:** معرفی/انگیزه‌ی پروژه، توصیفِ کاملِ داده‌ها (۷۸ عکسِ واقعی + کشِ پتری + ۲ PDF استانداردِ EUCAST، و آنچه در داده‌ها *موجود نیست* مثلِ ground truth/متادیتا)، شرحِ معماریِ کلیِ پایپ‌لاین با نمودارِ جریان، شرحِ روش‌شناسیِ هر ماژول به‌همراهِ ارجاعِ شکل، یک جدولِ «مقایسه‌ی طراحی‌های پذیرفته‌شده در برابرِ ردشده» (برگرفته از تاریخچه‌ی کاملِ این فایل، شاملِ ایده‌ی جایگزینِ خودِ کاربر که رد شد)، بخشِ اعتبارسنجی و محدودیت‌هایش (شاملِ محدودیتِ تازه‌کشف‌شده‌ی بالا)، پیوستِ پارامترها، و یک بخشِ صریحِ «آنچه من نمی‌دانم» (عنوان/نویسندگان، مرورِ ادبیات که نیازمندِ جست‌وجویِ خودِ کاربر است چون من نمی‌توانم منابعِ دقیق/به‌روز را با اطمینانِ کافی تضمین کنم، آماره‌های کمّیِ اعتبارسنجی که بدونِ ground truth ممکن نیست، جزئیاتِ ترکیبِ نمونه، و مجوز/اخلاق).

**وضعیت:** فایل و پوشه‌ی شکل‌ها آماده‌ی commit/push هستند؛ این یک سندِ مرجع برای نشستِ بعدی است، نه تغییری در خودِ الگوریتم/نوت‌بوک.

## ۱۲) ساختِ دیتابیسِ Ground Truth و الگوریتمِ ارزیابیِ کمّیِ پایپلاین

کاربر ۱۱ عکسِ واقعی فرستاد که یک متخصصِ آزمایشگاه قطرِ هاله‌ی هر دیسک را (در صورتِ وجود) روی خودِ عکس با ماژیکِ آبی نوشته بود، و خواستِ یک الگوریتمِ ارزیابیِ رسمی بسازیم که خروجیِ پایپلاین را در برابرِ این حقیقتِ زمینی روی سه محور بسنجد: تشخیصِ پتری، تشخیصِ دیسک+شعاع، و تشخیص/قطرِ هاله -- با گزارشِ TP/FP/FN، دقتِ عددیِ میلی‌متری، و تحلیلِ Bland–Altman.

### ۱۲.۱) زیرساختِ ساختِ دیتابیس (بدونِ Circularity)

**پوشه‌ی `ground_truth/`:** عکس‌های خام در `raw_images/gt_01.jpg` تا `gt_11.jpg`.

**`annotate_disks.py` (لوکیتورِ کاملاً مستقل از پایپلاینِ اصلی):** برای این‌که شماره‌گذاریِ Ground Truth از خطاهای خودِ سیستمِ تحتِ ارزیابی رنگ نگیرد، این اسکریپت عمداً از یک مسیرِ ساده و متفاوت استفاده می‌کند -- نه ماژولِ ۴ (Multi-Otsu + illumination_normalize + containment-filter + دو self-check) و نه شاخه‌ی Fusion (ماژول ۱۴). به‌جایش:
- **تشخیصِ ظرف:** یک HoughCircle درشتِ روی نسخه‌ی کوچک‌شده‌ی عکس (نه Otsu/کانتور -- که در تست روی یکی از عکس‌ها به‌اشتباه لوگوی چاپیِ پارچه‌ی زیرِ ظرف را هم بخشی از «ظرف» حساب کرد و شعاعِ ظرف را کاذب بزرگ کرد).
- **تشخیصِ دیسک:** HoughCircles روی ROIِ داخلِ ظرف، با سه فیلترِ تصحیح‌شده در حینِ تست: (۱) سقفِ فاصله از مرکز `۰.۷۸×شعاعِ ظرف` (نه ۰.۹۸) چون بازتابِ نورِ لبه‌ی ظرف گاهی به‌اشتباه به‌عنوانِ یک دیسکِ کاذب تشخیص داده می‌شد؛ روی هر ۱۱ عکس دورترین دیسکِ واقعی حداکثر نسبتِ ۰.۶۸ داشت. (۲) کاهشِ آستانه‌ی Hough (`param2`) از ۴۵ به ۳۸ برای گرفتنِ دیسک‌های کم‌کنتراست که در آستانه‌ی بالاتر جا می‌افتادند. (۳) فیلترِ کنتراستِ روشنایی (دیسکِ واقعی حداقل ۱۰ واحد از پس‌زمینه‌ی محلی‌اش روشن‌تر است -- آستانه از تفاوتِ بینِ کمترینِ کنتراستِ یک دیسکِ واقعی (۱۳، متعلق به FM300ِ زردرنگ) و بیشترینِ کنتراستِ یک کاندیدِ کاذب در آگارِ خالی (۷) به‌دست آمد).
- **شماره‌گذاری:** ساعت‌گرد از ساعتِ ۱۲، نسبت به مرکزِ ظرف -- یک قاعده‌ی هندسیِ صرف.

هر عکس بعد از اجرا با overlay شماره‌گذاری‌شده بازرسیِ بصری شد (نه فقط اجرا و قبول کورکورانه)؛ سه دور اصلاحِ پارامتر (بالا) دقیقاً از همین بازرسی آمد.

### ۱۲.۲) خواندنِ دستیِ مقادیرِ کاغذی و ساختِ CSV

کاربر ۱۱ عکسِ ماژیک‌دار (+ یک عکسِ اضافه برایِ عکسِ سه‌دیسکی که ابتدا فراموش شده بود) فرستاد. هر عکس اول با تطبیقِ برچسبِ آنتی‌بیوتیک/چیدمانِ دیسک‌ها با عکسِ خامِ متناظرش شناسایی شد (نه با شماره‌گذاریِ پایپلاین)، سپس هر عددِ آبی به دیسکِ مربوطه (بر اساسِ زاویه/موقعیتِ محاسبه‌شده از `annotate_disks.py`، نه حدس) نسبت داده شد. نتیجه در قالبِ یک جدول به کاربر نمایش داده شد و **دو ابهام صریحاً از کاربر پرسیده شد** قبل از قطعی‌کردن:
1. عکسِ سه‌دیسکی (AN30/P17/GM10) هیچ عکسِ ماژیک‌داری نداشت -- کاربر تاییدِ کرد که آن هم هاله دارد و عکسش را جداگانه فرستاد.
2. در یک عکس، دو دیسکِ نزدیک‌به‌هم (CZ30 و CRO) یک هاله‌ی ادغام‌شده داشتند با فقط یک عددِ نوشته‌شده -- کاربر مشخص کرد که عدد متعلق به دیسکی است که هاله واقعاً دورِ آن را گرفته (CRO)، نه آن‌که فقط لبه‌اش به هاله چسبیده (CZ30).

**نتیجه (`ground_truth_expert_readings.csv`):** ۹۳ دیسک در ۱۱ عکس، ۶۴ تای آن‌ها دارایِ هاله‌ی اندازه‌گیری‌شده و ۲۹ تا بدونِ هاله (تایید‌شده، نه صرفاً خالی/فراموش‌شده).

### ۱۲.۳) الگوریتمِ ارزیابی (`evaluate_pipeline.py`)

**اصلِ طراحیِ محوری -- عدمِ Circularity در تطبیق:** دیسک‌های خروجیِ پایپلاین با دیسک‌های Ground Truth بر اساسِ **نزدیک‌ترین موقعیتِ پیکسلی** (نه شماره‌ی دیسک) تطبیق داده می‌شوند -- شعاعِ تطبیق برای هر عکس جداگانه و نسبی محاسبه می‌شود (نصفِ کمترین فاصله‌ی واقعیِ بینِ دو دیسکِ Ground Truthِ همان عکس)، نه یک آستانه‌ی پیکسلیِ مطلق.

**روش:** خودِ نوت‌بوکِ اصلی (بدونِ هیچ تغییر) با nbclient روی هر عکس اجرا می‌شود (دقیقاً همان الگوی `batch_process_antibiogram.py`)، و **خروجیِ متنیِ خودِ سلولِ گزارشِ نهایی** (ماژولِ ۱۸) با یک regex پارس می‌شود تا مرکز/قطرِ دیسک/قطرِ هاله را استخراج کند -- هیچ منطقِ موازی/کپی‌شده از داخلِ پایپلاین بیرون کشیده نشده.

**معیارهای گزارش‌شده:**
1. نرخِ موفقیتِ تشخیصِ پتری (آیا دقیقاً یک ظرفِ دارایِ دیسک تشخیص داده شد).
2. TP/FP/FN تشخیصِ دیسک (بر اساسِ تطبیقِ موقعیتی) + Precision/Recall/F1.
3. ماتریسِ ۲×۲ حضور/عدمِ هاله (هردو دارند / واقعی دارد ولی پایپلاین ندید / واقعی ندارد ولی پایپلاین کاذب دید / هردو ندارند) روی دیسک‌های درست‌تشخیص‌داده‌شده.
4. دقتِ عددیِ قطرِ هاله روی دیسک‌هایِ هم‌دارایِ هاله: MAE، Bias (میانگینِ خطای علامت‌دار)، و تحلیلِ Bland–Altman (حدودِ توافقِ ۹۵٪ = bias ± ۱.۹۶×SD).

**اعتبارسنجیِ اولیه:** روی عکسِ سه‌دیسکی (`gt_02.jpg`) تست شد -- پتری درست تشخیص داده شد، هر ۳ دیسک با فاصله‌ی تطبیقِ کمتر از ۲ پیکسل جفت شدند (تاییدِ درستیِ منطقِ تطبیق)، و خطاهای میلی‌متری (۰.۶- تا ۲.۸+) در محدوده‌ی معقولی بودند.

**اجرایِ اولِ کاملِ روی هر ۱۱ عکس (قبل از هرگونه رفعِ باگ):**
- تشخیصِ پتری: ۱۱/۱۱ ✅
- تشخیصِ دیسک: Precision=۰.۹۸۹, Recall=۰.۹۷۸, F1=۰.۹۸۴ (عالی)
- دقتِ قطرِ هاله: 🔴 MAE=۱۱.۱۲mm, Bias=−۱۰.۹۰mm (کم‌برآوردِ شدید و سیستماتیک)

### ۱۲.۴) ریشه‌یابی و رفعِ باگِ کم‌برآوردِ شدیدِ قطرِ هاله روی پنل‌های پرتراکم

**علتِ ریشه‌ای (تاییدشده با اجرایِ زنده/دیباگ، نه فقط خواندنِ کد):** `_compute_dish_background_noise` یک نویزِ پس‌زمینه‌ی *مشترک* برایِ کلِ پتری محاسبه می‌کرد، از ناحیه‌ای که «دورتر از `cfg.halo_extension_max_frac_of_petri_radius` (=۰.۵) × شعاعِ کلِ پتری از هر دیسکی» بود. روی `gt_07.jpg` (۸ دیسک): این اکسکلودِ نسبتاً بزرگ (۴۴۲px، حدودِ نیمِ شعاعِ پتری) برایِ هر دیسک، وقتی با ۸ دیسکِ پخش‌شده دورِ پتری جمع شود، تقریباً کلِ داخلِ پتری را می‌پوشاند و فقط یک نوارِ باریک/غیرِنماینده (نزدیکِ لبه/بازتابِ ظرف) به‌عنوانِ «پس‌زمینه» باقی می‌ماند -- نویزِ برآوردشده از آن‌جا مصنوعاً بزرگ می‌شد (۱۸.۳)، `contrast_sigma` را برایِ *همه‌ی ۸ دیسک* آن پتری کوچک می‌کرد (بین ۰.۱۴- تا ۲.۶-، همه زیرِ آستانه‌ی ۳σِ ماژولِ ۱۶.۵)، و همین هم گیتِ رشدِ ماژولِ ۱۶.۵ را می‌بست هم همگراییِ خودِ ماژولِ ۱۶ را زودهنگام می‌کرد. توضیح می‌دهد چرا فقط `gt_02` (پتریِ ۳-دیسکیِ کم‌تراکم، با ناحیه‌ی پس‌زمینه‌ی فراوان و تمیز) درست کار کرد.

**رفع (دو لایه، هردو نسبی):**
۱. اکسکلودِ هر دیسک علاوه‌بر سقفِ قبلی (کسری از شعاعِ کلِ پتری)، با کسری از فاصله‌ی واقعی تا نزدیک‌ترین دیسکِ دیگر هم سقف می‌خورد (`cfg.halo_noise_exclude_neighbor_frac=0.45`) -- هرچه دیسک‌ها متراکم‌تر، اکسکلودِ خودکار کوچک‌تر می‌شود.
۲. اگر بازهم ناحیه‌ی پس‌زمینه کمتر از یک نسبتِ حداقلی از مساحتِ پتری بود (`cfg.halo_noise_min_bg_area_frac=0.03`)، به‌جایِ اجبارِ یک عددِ نامطمئن بر همه‌ی دیسک‌ها، تابع `None` برمی‌گرداند تا هر دیسک به برآوردِ نویزِ محلیِ *خودش* (که از قبل در `_halo_radial_profile` محاسبه می‌شود و مستقل از این مشکل است) برگردد -- این مسیرِ fallback از قبل در کد وجود داشت، فقط هیچ‌وقت فعال نمی‌شد چون تابع همیشه یک عددِ (حتیِ نامطمئن) برمی‌گرداند.

**نتیجه‌ی اجرایِ کاملِ بعد از رفع:**
- MAE: ۱۱.۱۲ → **۸.۶۰mm** (بهبودِ واقعی، ~۲۳٪ کاهش)
- Bias: −۱۰.۹۰ → **−۷.۰۸mm**

**صادقانه: این رفع کامل نیست.** روی `gt_07` بعد از رفع، فقط ۲ از ۸ دیسک از گیتِ ماژولِ ۱۶.۵ عبور کردند (contrast_sigma از ۵ دیسکِ دیگر هنوز زیرِ ۱σ ماند، با این‌که Ground Truth برایِ چندتایشان هاله‌هایِ ۲۳-۳۴mm ثبت کرده). یعنی مشکلِ نویزِ پس‌زمینه فقط بخشی از قصه بود؛ یک عاملِ عمیق‌ترِ دیگر (شاید: تعریفِ contrast_sigma بر پایه‌ی «میانه‌ی رینگ‌هایِ نزدیکِ دیسک در برابرِ پس‌زمینه» برایِ هاله‌هایِ پهن/کم‌شیب به‌ذاتِ خودش کنتراستِ کمی می‌دهد، نه فقط نویزِ مخرج) هنوز باقی است و نیازمندِ بررسیِ بیشتر است -- در صورتِ درخواستِ کاربر برایِ ادامه.

### ۱۲.۵) کالیبراسیونِ px→mm از رویِ خودِ دیسک (نه فرضِ قطرِ پتری) + رفعِ دومِ باگِ هاله

**درخواستِ کاربر:** به‌جایِ `cfg.dish_reference_diameter_mm=90.0` (فرضِ ثابتِ قطرِ پتری، وابسته به دقتِ تشخیصِ شعاعِ خودِ پتری)، px→mm از رویِ اندازه‌یِ استانداردِ خودِ دیسک‌ها (۶mm) کالیبره شود: کوچک‌ترین خوشه‌ی قطرِ پیکسلیِ دیسک‌ها (کوچک‌ترین دیسک + هر دیسکِ دیگری که حداکثر ۳۰٪ بزرگ‌تر باشد -- دقیقاً زیرِ نسبتِ واقعیِ ۶/۸mm=۱.۳۳) میانگین گرفته و برابرِ ۶mm گذاشته شود.

**پیاده‌سازی:** تابعِ جدیدِ `_estimate_px_per_mm_from_disks` جایگزینِ محاسبه‌ی قدیمی شد؛ `cfg.disk_reference_diameter_mm=6.0` و `cfg.disk_calibration_cluster_tolerance=0.30` اضافه، `cfg.dish_reference_diameter_mm` (دیگر بلااستفاده) حذف شد.

**نتیجه:** دقتِ قطرِ خودِ دیسک تقریباً کامل شد (MAE=۰.۱۷mm، Bias≈۰ -- طبقِ ساخت، چون خوشه‌ی کالیبراسیون همین دیسک‌هاست)، و مهم‌تر: چون همه‌ی مقادیرِ mm به همین مقیاس گره خورده‌اند، بایاسِ قطرِ هاله هم بهبودِ چشمگیر یافت: MAE ۸.۶۰→۸.۰۳mm، **Bias −۷.۰۸→−۳.۸۶mm**.

**رفعِ دومِ (عمیق‌تر) باگِ کم‌برآورد:** بررسیِ پروفایلِ خامِ دیسک‌هایِ هنوز-گیرافتاده (`gt_07`) نشان داد پروفایلِ شعاعی به‌طورِ کامل و یکنواخت در سرتاسرِ کلِ پنجره‌ی جست‌وجو در حالِ افزایش بود (بدونِ هیچ فلاتِ واقعی) -- یعنی پنجره هنوز کاملِ گذار را ندیده بود. اما `max_allowed` (سقفِ فیزیکیِ واقعی، فاصله تا لبه‌ی پتری) ۲ تا ۴ برابرِ پنجره‌ی اولیه بود -- یعنی حلقه‌ی گسترشِ تطبیقیِ ماژولِ ۱۶ جایِ کافی برایِ رشد داشت اما هرگز فعال نمی‌شد، چون وقتی نویزِ محلی با کلِ بازه‌ی تغییراتِ پروفایل در همان پنجره‌ی باریک قابل‌مقایسه باشد، باندِ همگرایی (± نویز×۲) از کلِ آن بازه پهن‌تر می‌شود و تقریباً هر رینگی، even نزدیکِ ابتدایِ پنجره، به‌اشتباه «همگرا» تشخیص داده می‌شد -- شرطِ `if crossed or ...: break` بدونِ بررسیِ قابلِ‌اعتماد بودنِ آن همگرایی، حلقه را زودهنگام متوقف می‌کرد.

**رفع:** همگراییِ زودهنگام فقط وقتی پذیرفته می‌شود که `contrast_sigma` از قبل به‌وضوح معنادار باشد (همان آستانه‌ی ۳σِ ماژولِ ۱۶.۵ -- `cfg.halo_extension_require_min_contrast_sigma`)؛ وگرنه پنجره طبقِ طراحیِ اصلیِ همان حلقه (که تا امروز هرگز عملاً فعال نمی‌شد) گسترش می‌یابد.

**نتیجه‌ی اجرایِ کاملِ نهایی (هر سه رفع با هم):**
- MAE قطرِ هاله: ۱۱.۱۲ → **۵.۷۷mm** (کاهشِ ~۴۸٪)
- Bias قطرِ هاله: −۱۰.۹۰ → **−۰.۶۰mm** (تقریباً بدونِ سوگیریِ سیستماتیک -- کاهشِ ~۹۴٪)
- دقتِ قطرِ دیسک: MAE=۰.۱۷mm, Bias≈۰

**صادقانه -- یک عارضه‌ی جانبیِ جدید کشف شد:** بررسیِ ۲۸ دیسکِ «واقعاً بدونِ هاله» نشان داد ۹ تا هنوز به‌درستی مقدارِ کوچک (نسبت به قطرِ دیسک، ۱.۱-۱.۹×) گزارش می‌دهند، ولی **۱۹ تا** حالا مقدارِ به‌طرزِ معناداری بزرگ‌تر گزارش می‌دهند (نسبتِ ۲× تا ۵.۹×، یعنی تا ۳۶.۷mm برایِ یک دیسکِ ۶mm) -- گسترشِ پنجره که مشکلِ کم‌برآوردِ هاله‌هایِ واقعی را حل کرد، گاهی هم به یک روندِ نامرتبط (نورِ ناهموار، نشتِ هاله‌ی همسایه) اجازه می‌دهد آن‌قدر دنبال شود که به‌اشتباه از آستانه‌ی ۳σ عبور کند. چون این معیارِ «حضور/عدمِ هاله» فقط روی دیسک‌هایِ *بدونِ* هاله سنجیده می‌شود (نه روی MAEِ بالا که فقط دیسک‌هایِ *دارایِ* هاله را حساب می‌کند)، این عارضه در عددِ MAE/Bias بالا ظاهر نشده اما یک مشکلِ واقعیِ باقی‌مانده است -- بهبودِ کم‌برآورد با هزینه‌ی مقداری بیش‌برآوردِ تازه در دیسک‌هایِ سالم به‌دست آمده. رفعِ این مورد (مثلاً نیازِ به یک روندِ پایدار/غیرِتصادفی، نه فقط عبور از ۳σ در یک لحظه) قدمِ منطقیِ بعدی است.

**وضعیت:** هر دو رفع (کالیبراسیونِ دیسک‌محور + رفعِ گسترشِ پنجره) commit/push شدند. موارد باقی‌مانده: عارضه‌ی جانبیِ بیش‌برآوردِ تازه (بالا)، تکرارپذیریِ اندازه‌گیریِ دستی، طبقه‌بندیِ S/I/R، و نرخِ موفقیتِ تشخیصِ پتری روی کلِ ۷۸ عکسِ ریپازیتوری.

### ۱۲.۶) تلاشِ اول برایِ رفعِ عارضه‌ی بیش‌برآورد -- رگرسیون، revert شد

**ریشه‌یابی:** بررسیِ پروفایلِ خامِ سه دیسکِ بیش‌برآوردشده‌ی `gt_10` (۳۰.۸/۳۲.۹/۳۶.۷mm) نشان داد پروفایل ابتدا آرام بالا می‌رود، سپس درست در انتهایِ پنجره‌ی به‌شدت گسترش‌یافته (r_out تا ۶۳۹px) یک افتِ ناگهانی نشان می‌دهد -- دقیقاً امضایِ ورودِ پنجره به قلمروِ یک دیسکِ همسایه با هاله‌یِ واقعاً بزرگ (روی همان پتری، یک دیسکِ دیگر contrast=−۱۷.۱σ داشت، یک سیگنالِ عظیمِ واقعی). چون پنجره‌ی جست‌وجو فقط با فاصله تا لبه‌ی *پتری* محدود می‌شد (نه تا نزدیک‌ترین *دیسکِ دیگر*)، توانسته بود به قلمروِ همسایه برسد.

**رفعِ اول (اشتباه، revert شد):** یک سقفِ همه‌جهته/اسکالر روی شعاعِ پنجره اضافه شد (نصفِ فاصله تا نزدیک‌ترین دیسکِ دیگر -- همان فرمِ همه‌جهته‌یِ `_neighbor_voronoi_cap`). روی `gt_10` عالی کار کرد (هر ۳ کاذب به مقادیرِ معقول برگشتند)، اما اجرایِ کاملِ روی هر ۱۱ عکس نشان داد این یک **رگرسیونِ خالص** بود: MAE ۵.۷۷→۸.۱۸mm، Bias −۰.۶۰→−۵.۳۲mm.

**چرا رگرسیون شد:** چون این پروفایلِ همه‌جهته (میانگینِ حلقه‌ای) است، یک سقفِ *اسکالر* روی شعاع، اطلاعاتِ معتبرِ همه‌یِ جهت‌هایی که همسایه‌ی نزدیکی ندارند را هم دور می‌ریزد -- برایِ دیسکی که فقط در *یک* جهت همسایه‌ی نزدیک دارد ولی در بقیه‌ی جهت‌ها فضایِ باز و پس‌زمینه‌ی واقعیِ دورتر دارد، این سقف به‌اشتباه کلِ پنجره را (در همه‌ی جهت‌ها) زودتر می‌بندد و دوباره همان کم‌برآوردِ قبلی را برمی‌گرداند. رفعِ درست باید **جهت‌آگاه** باشد (فقط قوسِ زاویه‌ایِ روبه‌رویِ همسایه‌ی نزدیک از میانگینِ حلقه‌ای کنار گذاشته شود، نه کلِ شعاع)، نه یک سقفِ همه‌جهته -- کاری که این رفعِ اول انجام نداد.

**اقدام:** commit مربوطه (`329897d`) بلافاصله بعد از دیدنِ نتیجه‌ی رگرسیون با `git revert` برگردانده شد؛ اجرایِ کاملِ تاییدی نشان داد به‌درستی به وضعیتِ MAE=۵.۷۷mm/Bias=−۰.۶۰mm برگشتیم.

**وضعیت:** عارضه‌ی بیش‌برآورد روی دیسک‌هایِ واقعاً بدونِ هاله (۱۹ مورد) هنوز رفع‌نشده باقی است؛ رفعِ درستِ آن نیازمندِ طراحیِ جهت‌آگاه (حذفِ قوسِ سمتِ همسایه از میانگینِ حلقه‌ای به‌جایِ محدودکردنِ کلِ شعاع) است -- کارِ آینده، در صورتِ درخواستِ کاربر.

### ۱۲.۷) رفعِ جهت‌آگاهِ عارضه‌ی بیش‌برآورد -- بهبودِ واقعی و جزئی

**پیاده‌سازی:** به‌جایِ سقفِ اسکالر (بخشِ قبل)، برایِ هر پیکسلِ داخلِ پنجره‌ی جست‌وجو، یک تستِ نیم‌صفحه‌ای (همان فرمولِ بسته‌ی `_neighbor_voronoi_cap`، اینجا per-pixel به‌جایِ per-angle-روی-شکلِ-نهایی) اجرا می‌شود: فقط پیکسل‌هایی که از نیمسازِ عمودِ فاصله تا هر همسایه گذشته‌اند (واقعاً به قلمروِ او نزدیک‌ترند) از میانگینِ حلقه‌ای کنار گذاشته می‌شوند -- جهت‌هایِ بدونِ همسایه‌ی نزدیک هرگز محدود نمی‌شوند.

**نتیجه‌ی اجرایِ کامل:** دقتِ دیسک‌هایِ *دارایِ* هاله تقریباً بدونِ تغییر ماند (MAE ۵.۷۷→۵.۹۰mm، Bias −۰.۶۰→−۰.۹۶mm -- هر دو تغییرِ کوچک)، اما مشکلِ هدف بهبودِ واقعی یافت: از ۲۸ دیسکِ بدونِ هاله، مواردِ شدید (نسبتِ >۲×) از ۱۲→**۹** کاهش یافت و مواردِ متوسط (>۱.۵×) از ۱۹→**۱۶**.

**آزمایشِ اضافه (رد شد):** فرضِ دوم -- بعضی از موارد باقی‌مانده (به‌خصوص `gt_10`) نشتِ همسایه نیستند بلکه شیبِ نرمِ نورِ ناهموار (vignetting) در سرتاسرِ قاب‌اند، چون پروفایلشان بدونِ هیچ افتی به‌طورِ کاملاً یکنواخت بالا می‌رفت -- ترکیب شد با اعمالِ همان `illumination_normalize`ی موجود (که فقط برایِ ماژولِ ۴ استفاده می‌شد) روی وردیِ خودِ ماژولِ هاله. نتیجه: Bias به تقریباً صفر رسید (−۰.۰۲) اما مشکلِ هدف **بدتر شد** (موارد شدید ۹→۱۷) -- یعنی این تصحیح، حداقل با این کرنل و روی این ROIِ کراپ‌شده، خودش سرچشمه‌ی نویزِ تازه‌ای برایِ این مسئله‌ی خاص بود. **بلافاصله revert شد** (فقط این بخش، نه رفعِ جهت‌آگاه) و با اجرایِ ایزوله‌ی مجدد تایید شد رفعِ جهت‌آگاهِ به‌تنهایی همان نتیجه‌ی خوبِ بالا را می‌دهد.

**موارد شدیدِ باقی‌مانده (۴ از ۵ مورد >۳× روی یک عکس):** `gt_10.jpg` دیسک‌هایِ #۱،۲،۷،۹ -- یکی از این‌ها (#۹) یک تشخیصِ کاذبِ شناخته‌شده‌ی خودِ Fusion است (دیسکِ تکراری/duplicate با score=۰.۳۶، از قبل به‌عنوانِ FP در گزارشِ تشخیصِ دیسک هم علامت‌گذاری شده -- ربطی به ماژولِ هاله ندارد). بقیه هنوز علتِ دقیقشان مشخص نیست (نه‌فرضِ نشتِ همسایه به‌تنهایی، نه‌فرضِ vignetting به‌تنهایی آن‌ها را توضیح داد) -- نیازمندِ بررسیِ عمیق‌تر و مجزا، در صورتِ درخواستِ کاربر.

**وضعیت:** رفعِ جهت‌آگاه commit/push شد (بهبودِ خالص، هرچند جزئی). عارضه هنوز به‌طورِ کامل رفع نشده؛ بیشترِ موارد شدیدِ باقی‌مانده روی یک عکسِ خاص (`gt_10`) متمرکزند که علتِ دقیقشان (به‌جز یک FP شناخته‌شده‌ی Fusion) هنوز نامشخص است.


### ۱۲.۸) بررسیِ عمیقِ gt_10 -- کشفِ باگِ سیستمیِ «همیشه یک عددِ mm گزارش می‌شود» و رفعِ سه‌گانه

**درخواستِ کاربر:** بررسیِ دقیقِ این‌که رویِ `gt_10` (متمرکزترین عکسِ موارد شدیدِ باقی‌مانده از بخشِ ۱۲.۷) واقعاً چه مشکلی باقی مانده.

**روشِ بررسی:** یک debug harness مستقیم (اجرایِ سلول‌های ۰ تا ۴۲ + یک سلولِ چاپِ سفارشی) برایِ هر دیسکِ بیش‌برآوردشده، `profile` خام، `contrast_sigma`، و طولِ آرایه را چاپ کرد. دو ریشه‌یِ کاملاً مجزا پیدا شد:

1. **دیسک #۹ (محاصره‌شده با چند همسایه هم‌زمان):** پروفایل یک مقدارِ ثابتِ دقیقاً تکراری (مثلاً ۱۶۰.۰ هشت بار پشتِ سرِ هم) در انتهایِ آرایه داشت -- امضایِ فابریکِ `np.interp` وقتی یک دنباله‌ی حلقه‌هایِ خالی تا *انتهایِ* آرایه ادامه یابد (نه یک شکافِ داخلی): بدونِ هیچ نقطه‌ی معتبرِ *بعدی* برایِ درون‌یابیِ واقعی، `np.interp` فقط آخرین مقدارِ معتبر را تا ابد ثابت نگه می‌دارد -- یک «فلاتِ» ساختگی که دقیقاً امضایِ همگراییِ واقعی است.
2. **دیسک #۱ (vignetting خالص):** پروفایل کاملاً یکنواخت و بدونِ هیچ افتی از ۱۵۸.۱ تا ۱۷۳.۶ بالا می‌رفت؛ پنجره‌ی جست‌وجو (`r_out=236.8px`) حتی هرگز به نزدیک‌ترین همسایه (۲۷۸px) هم نمی‌رسید -- یعنی این اصلاً نشتِ همسایه نبود، یک شیبِ نرمِ نورِ سرتاسریِ قاب بود (که تلاشِ `illumination_normalize` در بخشِ ۱۲.۷ برایِ همین فرض شده بود، ولی چون رویِ کلِ اجرا رگرسیون داد، revert شده بود).

**رفعِ #۷ (کوتاه‌کردنِ دنباله‌یِ فابریکِ انتهایی):** به‌جایِ درون‌یابیِ کلِ آرایه هر وقت رینگِ خالی دیده شود، اول محلِ شروعِ یک دنباله‌ی خالیِ *انتهایی* (`trustworthy_n`) پیدا می‌شود؛ فقط شکاف‌هایِ *داخلی* (بینِ دو نقطه‌ی معتبرِ واقعی) طبقِ روالِ قبلی درون‌یابی می‌شوند، و آرایه دقیقاً تا `trustworthy_n` کوتاه می‌شود (بدونِ هیچ فابریک‌سازی). `n_rings_eff`/`tail_eff`/`near_n_eff` متناسب بازمحاسبه می‌شوند و حلقه‌ی همگرایی رویِ همین طولِ مؤثر اجرا می‌شود. **تاییدشده روی دیسک #۹:** از ۲۳.۶-۳۰mm کاذب به ۷.۰mm صحیح رسید.

**رفعِ #۸ (بازنشانیِ همگراییِ غیرِقابل‌اتکا در پایانِ حلقه‌ی گسترش):** بررسیِ دقیق‌ترِ دیسک #۲ (که بعد از رفعِ #۷ هنوز مقدارِ کاذب ۳۰mm می‌داد با contrast_sigma=−۲.۸، یعنی زیرِ آستانه‌ی ۳σ) یک باگِ جدیدِ مجزا آشکار کرد: `reliable_convergence` (بخشِ ۱۲.۵) فقط تصمیم می‌گرفت که آیا حلقه‌ی گسترشِ تطبیقی ادامه یابد یا نه -- ولی وقتی حلقه بدونِ رسیدن به یک همگراییِ واقعاً قابل‌اتکا به‌اجبار متوقف می‌شد (چون به `max_allowed` یا `max_widenings` رسیده)، آخرین «crossed»یِ ضعیفِ همان تکرارِ آخر همچنان به‌عنوانِ جوابِ نهایی پذیرفته می‌شد. **رفع:** اگر تا پایانِ کاملِ گسترشِ تطبیقی هیچ‌گاه یک همگراییِ قابل‌اتکا به دست نیامد، `r_halo_radial` به `r_in` و `crossed` به `False` بازنشانی می‌شود -- دقیقاً همان رفتاری که برایِ دیسکِ بدونِ هیچ گذارِ همگرا از قبل در نظر گرفته شده بود.

**کشفِ باگِ ریشه‌ای‌ترِ سوم (توضیح‌دهنده‌یِ FP=28ِ *ثابت* در همه‌ی اجراهایِ این نشست، حتی با رفع‌هایِ ۱۲.۴ تا ۱۲.۷):** با اجرایِ آزمایشیِ رفع‌هایِ #۷ و #۸ رویِ `gt_10`، مشخص شد `evaluate_pipeline.py` هنوز عددِ mm گزارش می‌کرد (نه «تشکیل نشد») -- حتی برایِ دیسک‌هایِ بدونِ سیگنالِ قابل‌اتکا. ریشه: سلولِ گزارشِ نهایی (و ماژول‌هایِ ۱۶.۵/۱۶.۶/۱۷) تصمیمِ «هاله تشکیل شد یا نه» را فقط از رویِ `status=="ok"` می‌گرفتند -- ولی `status` فقط یعنی «پروفایلِ هندسی معتبر محاسبه شد»، نه «سیگنالِ واقعیِ هاله دیده شد». برایِ یک دیسکِ کاملاً بدونِ هاله، پروفایل هنوز `status="ok"` برمی‌گرداند (چون هندسه معتبر است) و `r_halo_radial` همیشه دستِ‌کم برابرِ `r_in` می‌ماند -- یعنی همیشه یک عددِ mm غیرصفر گزارش می‌شد، حتی وقتی `halo_signal_detected=False` بود. این توضیح می‌دهد چرا FP=28 در چهار اجرایِ متوالیِ این نشست (پیش و پس از رفعِ جهت‌آگاه، پیش و پس از تلاشِ illumination) کاملاً یکسان ماند: هیچ‌کدام از آن رفع‌ها این مسیرِ گزارش‌دهی را لمس نکرده بودند.

**رفعِ #۹ (gating رویِ سیگنالِ واقعی، نه فقط status):** در `segment_dish_halos`، «سیگنالِ واقعی دیده شد» (`b["status"]=="ok" and b["halo_signal_detected"]`) اکنون پیش‌شرطِ صریحِ ادامه‌ی پردازشِ هر دیسک است. دیسکِ بدونِ سیگنالِ قابل‌اتکا `status="no_reliable_signal"` می‌گیرد (دقیقاً همان مسیرِ موجودِ `halo_radius_px=0.0` که برایِ `too_close_to_border`/`insufficient_ring_coverage` هم استفاده می‌شود) -- و چون همه‌یِ ماژول‌هایِ پایین‌دست (۱۶.۵، ۱۶.۶، ۱۷، گزارشِ نهایی) از قبل فقط `status != "ok"` را چک می‌کنند (تاییدشده با بررسیِ کاملِ کد، نه حدس)، این یک تغییرِ تک‌نقطه‌ای به‌طورِ هم‌زمان همه‌یِ آن‌ها را هم درست می‌کند.

**نتیجه‌ی اجرایِ کاملِ نهایی (هر سه رفعِ #۷/#۸/#۹ با هم، روی ۱۱ عکس/۹۱ دیسکِ درست‌تشخیص‌داده‌شده):**

| معیار | قبل (بخشِ ۱۲.۷) | بعد (این بخش) |
|---|---|---|
| وضعیتِ حضورِ هاله | TP=۶۳ FN=۰ FP=۲۸ TN=۰ | TP=۵۷ FN=۶ FP=۱۴ TN=۱۴ |
| دقتِ حضور (Accuracy) | ۰.۶۹۲ (۶۳/۹۱) | **۰.۷۸۰** (۷۱/۹۱) |
| Precisionِ حضور | ۰.۶۹۲ | **۰.۸۰۳** |
| Recallِ حضور | ۱.۰۰۰ | ۰.۹۰۵ |
| MAEِ قطرِ هاله (فقط TP) | ۵.۹۰mm (n=۶۳) | **۵.۵۰mm** (n=۵۷) |
| Biasِ قطرِ هاله | −۰.۹۶mm | **−۰.۵۳mm** |
| دقتِ قطرِ دیسک | MAE=۰.۱۷mm | بدونِ تغییر |

روی خودِ `gt_10`: دیسک #۱ (vignettingِ خالص) و #۹ (محاصره‌شده) اکنون هردو به‌درستی TN شدند (قبلاً ۳۵.۰mm و ۲۳.۶-۳۰mm کاذب). دیسک #۶ (تنها هاله‌یِ واقعیِ آن پتری) دقیقاً درست ماند (۱۳.۴mm در برابرِ ۱۳.۰mm GT).

**صادقانه -- یک مبادله‌یِ جدید:** بهبودِ چشمگیرِ specificity (FP نصف شد، TN از صفر به ۱۴ رسید) با هزینه‌یِ ۶ False Negativeِ تازه به دست آمد -- ۶ دیسکِ *واقعاً* دارایِ هاله (gt_02#۳=۲۶mm، gt_05#۹=۲۳mm، gt_06#۷=۲۴mm، gt_09#۸=۲۷mm، gt_11#۸=۲۵mm، gt_11#۳=۱۰mm) که سیگنالشان هرگز به آستانه‌یِ ۳σِ `reliable_convergence` نرسید (احتمالاً هاله‌هایِ کم‌کنتراست/گذارِ بسیار تدریجی) و اکنون به‌اشتباه «تشکیل نشد» گزارش می‌شوند. این آستانه‌یِ ۳σ خودش قبلاً (بخشِ ۱۲.۵) برایِ حلِ یک مشکلِ دیگر (همگراییِ زودهنگامِ کاذب) طراحی شده بود؛ حالا که هم برایِ ادامه/توقفِ حلقه و هم برایِ تصمیمِ نهاییِ حضور/عدمِ هاله استفاده می‌شود، این مبادله (Recall در برابرِ Precision) آشکار شده. رفعِ آن نیازمندِ یک معیارِ تفکیک‌کننده‌ی دقیق‌تر (نه یک آستانه‌یِ ثابتِ یکسان برایِ هردو تصمیم) است -- کارِ آینده، در صورتِ درخواستِ کاربر.

**موردِ بازِ باقی‌مانده روی `gt_10`:** دیسک‌هایِ #۲ (۲۳.۷mm) و #۷ (۳۱.۵mm) هنوز به‌اشتباه هاله گزارش می‌دهند. هردو در همان پتریِ دیسک #۶ (تنها هاله‌یِ واقعیِ ۱۳mm) هستند -- فرضِ محتمل (تاییدنشده) نشتِ جهت‌آگاهِ ناقص از سیگنالِ واقعیِ #۶ به این دو همسایه در طیِ گسترشِ پنجره است، نه یک مکانیزمِ جدید. بررسیِ دقیق‌ترِ این دو مورد هنوز انجام نشده.

**وضعیت:** هر سه رفع (#۷ کوتاه‌کردنِ دنباله‌یِ فابریک، #۸ بازنشانیِ همگراییِ غیرِقابل‌اتکا، #۹ gating رویِ سیگنالِ واقعی) commit/push شدند. موارد باقی‌مانده برایِ ادامه (در صورتِ درخواستِ کاربر): ۶ False Negativeِ تازه (بالا)، دیسک‌هایِ #۲/#۷ باقی‌ماندهِ `gt_10`، عارضه‌یِ vignettingِ به‌عنوانِ یک ریشه‌یِ کلی‌ترِ احتمالی در سایرِ عکس‌ها (بدونِ راه‌حلِ کارکردیِ شناخته‌شده تا این لحظه)، تکرارپذیریِ اندازه‌گیریِ دستی، طبقه‌بندیِ S/I/R، و نرخِ موفقیتِ تشخیصِ پتری روی کلِ ۷۸ عکسِ ریپازیتوری.


### ۱۲.۹) رفعِ دیسک #۲/#۷ روی gt_10 -- کارکرد کامل روی این عکس، رگرسیونِ جدی روی کلِ دیتاست، revert شد

**درخواستِ کاربر:** ادامه‌ی رفعِ دیسک‌هایِ #۲/#۷ باقی‌مانده از بخشِ ۱۲.۸، همراه با تاکیدِ صریح بر پرهیز از overfitting.

**بررسیِ ریشه‌ای (با debug harness، نه حدس):** فرضِ بخشِ ۱۲.۸ («نشتِ جهت‌آگاه از هاله‌یِ واقعیِ #۶») رد شد. پروفایلِ خامِ هر دو دیسک استخراج شد: هردو دقیقاً همان امضایِ vignettingِ خالصِ دیسک #۱ (بخشِ ۱۲.۸) را داشتند -- یکنواخت و کاملاً بدونِ فلاتِ واقعی در سرتاسرِ پنجره بالا می‌رفتند (دیسک #۲: ۱۵۸→۱۷۴؛ دیسک #۷: ۱۴۴→۱۶۴). چون contrast_sigma یک آماره‌ی *تجمیعیِ* دو سرِ پنجره است، هر شیبِ ملایمِ سراسری (even بدونِ هیچ گذارِ واقعی) وقتی پنجره به‌اندازه‌ی کافی گسترش یابد، سرانجام از هر آستانه‌ی ۳σِ ثابتی عبور می‌کند -- چون نویزِ محلی با فاصله رشد نمی‌کند ولی شیبِ تجمعی خطی رشد می‌کند.

**تلاشِ اول (رد شد در همان مرحله‌ی طراحی):** نسبتِ شیبِ میانگینِ بخشِ «همگراشده» به بیشینه‌ی شیبِ کلِ پروفایل. رویِ دیسک #۲ ناموفق بود چون بیشینه‌ی شیب از یک جهشِ تک‌رینگیِ نویزیِ نزدیکِ لبه‌یِ دیسک (۴.۱ در همان گامِ اول، نه از خودِ روندِ اصلی) می‌آمد و مرجعِ کاذبی برای مقایسه می‌ساخت.

**تلاشِ دوم (کارکردِ کاملِ تاییدشده روی gt_10):** آزمونِ آماریِ t تکْ‌نمونه‌ای رویِ شیبِ حلقه‌به‌حلقه‌یِ بخشِ ادعاشده‌ی «همگراشده»: میانگینِ شیب باید به‌طورِ آماری از صفر قابلِ‌تمایز نباشد (نویزِ متعادل حولِ صفر، نه شیبِ یک‌طرفه)، با کمینه‌ی نمونه‌یِ `tail_eff` (نه یک عددِ ثابتِ جدید) برایِ قدرتِ آماریِ کافی. رویِ داده‌یِ واقعی: هاله‌یِ واقعیِ دیسک #۶ با t≈۰.۸۴ (نویزِ متعادل، پذیرفته می‌شد) در برابرِ vignettingِ کاذبِ دیسک #۲ با t≈۵.۰۳ (شیبِ یک‌طرفه‌یِ آماری‌واضح، رد می‌شد) -- فاصله‌ای بسیار بزرگ. با آستانه‌ی t≈۲.۰، اجرایِ ایزوله‌یِ `gt_10` **کاملاً بی‌نقص** شد: هر ۸ دیسکِ درست‌تشخیص‌داده‌شده (FP=۰, TN=۷, TP=۱ با ۱۳.۴mm در برابرِ ۱۳.۰mm GT).

**رگرسیونِ جدی روی اجرایِ کاملِ ۱۱ عکس (طبقِ اصلِ صریحِ کاربر -- هرگز رویِ یک عکس تنها قضاوتِ نهایی نکن):**

| معیار | قبل (بخشِ ۱۲.۸، commit شده) | بعد (این آزمونِ t) |
|---|---|---|
| وضعیتِ حضورِ هاله | TP=۵۷ FN=۶ FP=۱۴ TN=۱۴ | TP=۳۵ FN=**۲۸** FP=۷ TN=۲۱ |
| Recallِ حضور | ۰.۹۰۵ | **۰.۵۵۶** |
| MAEِ قطرِ هاله | ۵.۵۰mm (n=۵۷) | ۲.۹۸mm (n=۳۵) |

اگرچه FP نصف شد (۱۴→۷) و MAEِ رویِ موارد باقی‌مانده بهتر شد (چون فقط موارد بسیار «تمیز» از فیلتر عبور می‌کردند)، Recall از ۰.۹۰۵ به ۰.۵۵۶ سقوط کرد -- یعنی ۲۲ هاله‌یِ *واقعیِ* دیگر (نه فقط دیسک‌هایِ گمراه‌کننده‌یِ gt_10) به‌اشتباه «تشکیل نشد» طبقه‌بندی شدند. **این دقیقاً همان overfittingی بود که کاربر هشدار داده بود**: آزمونِ t رویِ نمونه‌یِ بسیار تمیزِ gt_10 (پس‌زمینه‌یِ کم‌نویزِ آن عکسِ خاص) عالی جدا می‌کرد، ولی حلقه‌هایِ شعاعیِ مجاور در تصاویرِ واقعی، به‌خاطرِ همپوشانیِ فضایی، مستقل نیستند (فرضِ بنیادینِ آزمونِ t نقض می‌شود) -- در عکس‌هایِ با بافتِ agar/نورِ متفاوت، این هم‌بستگی خطایِ معیار را به‌طورِ کاذب کوچک نشان می‌داد و باعث می‌شد آزمونِ t حتی هاله‌هایِ واقعیِ کاملاً معتبر را هم به‌عنوانِ «شیبِ غیرِمعنادار» رد کند.

**اقدام:** بلافاصله پس از دیدنِ نتیجه‌ی رگرسیون (قبل از commit)، تغییراتِ نوت‌بوک/`.py`/overlayها با `git checkout` به حالتِ commit شده‌ی قبلی (`87440b9`) بازگردانده شد -- هیچ‌چیزِ ناقصی وارد تاریخچه‌ی گیت نشد.

**وضعیت:** دیسک‌هایِ #۲/#۷ روی `gt_10` هنوز به‌اشتباه هاله گزارش می‌دهند (رفع‌نشده). ریشه‌یِ فیزیکیِ آن‌ها اکنون درست شناخته شده (vignettingِ خالص، نه نشتِ همسایه)، ولی هیچ رفعِ عمومیِ کارکردی که رویِ کلِ دیتاست هم جواب بدهد هنوز پیدا نشده -- تلاشِ بعدی باید معیاری را امتحان کند که به هم‌بستگیِ فضاییِ حلقه‌های مجاور حساس نباشد (مثلاً مقایسه با نویزِ تخمین‌زده‌شده از نواحیِ واقعاً دورِ همه‌ی دیسک‌ها به‌جایِ پراکندگیِ درونیِ خودِ بخشِ «همگراشده»، یا یک مدلِ صریح‌ترِ vignetting که مستقیماً از تصویر برآورد شود، نه از پروفایلِ محلیِ هر دیسک) -- کارِ آینده، در صورتِ درخواستِ کاربر.


### ۱۲.۱۰) بازطراحیِ سگمنت‌کردنِ هاله — معماریِ چندشاخه‌ای رویِ «بومِ آگار»

**درخواستِ کاربر (سه اولویت):** ۱) سگمنت‌کردنِ به‌مراتب بهترِ هاله، ۲) تعیینِ تکلیفِ ماژول‌هایِ DT و Blob که فایده‌ی عملی‌شان دیده نمی‌شد، ۳) بالاتر بردنِ دقتِ تشخیصِ دیسک — به‌همراه فهرستی از ایده‌هایِ فنیِ مشخص.

#### الف) ممیزیِ سهمِ Blob و DT — پاسخ با شواهد

منبعِ هر ۹۲ دیسکِ پذیرفته‌شده مستقیماً از ساختارِ داده خوانده شد: **۸۰ فقط Hough، ۱۲ هردو، و صفر فقط Blob.** رویِ ۵ عکس از ۱۱ شاخه‌ی Blob اصلاً کاندیدی تولید نکرد (کیفیت=۰.۰۰). هر دو دیسکِ ازدست‌رفته (FN) هم توسطِ هر دو شاخه از دست رفته بودند. نتیجه: Blob در مسیرِ تشخیصِ دیسک هیچ recall اضافه‌ای تولید نمی‌کند و DT هم تنها کارکردش تولیدِ مارکرِ Watershed برایِ همان Blob است.

#### ب) کاتالوگِ تجربیِ حالت‌هایِ شکست

هر ۹۳ دیسک با معیارهایِ بی‌بعدِ اندازه‌گیری‌شده برچسب خورد (نه با حدس): `MM_ERROR=31، OK=26، TN=15، FP_other=9، FN_other=4، FP_neighbour_leak=4، FN_low_contrast=2، FP_gradient_like=1`.

**یافته‌ی محوری: مسئله‌ی اصلی تشخیصِ حضور نبود، دقتِ اندازه‌گیری بود** — ۳۱ از ۵۷ هاله‌ای که هم کارشناس هم سیستم قبول داشتند بیش از ۳mm خطا داشتند. و مهم‌تر: **خطاها در هر دو جهت بودند و همدیگر را خنثی می‌کردند** — بایاسِ ۰.۵۳− که سالم به‌نظر می‌رسید، میانگینِ کم‌برآوردهایِ بزرگ و بیش‌برآوردهایِ بزرگ بود. دو سازوکار: (۱) پنجره‌ی جست‌وجو به مرزِ واقعی نمی‌رسید (هاله‌هایِ ۳۱-۳۲mm با `r_out` فقط ۱۶۴-۱۸۰px)، (۲) هاله‌هایی با کنتراستِ عظیم (۲۲−σ، ۴۲−σ) کاملاً رد می‌شدند چون پروفایل در پنجره‌ای که به لبه‌ی پتری خورده بود هرگز مسطح نمی‌شد. هر دو یک ریشه دارند: **پروفایلِ یک‌بعدیِ شعاعی نمی‌تواند «به لَون رسیدم» را از «هنوز نرسیدم» یا «واردِ قلمروِ همسایه شدم» تشخیص دهد.**

#### پ) بومِ آگار (ماژولِ ۱۵.۵) — و ریشه‌یابیِ شکستِ تلاشِ قبلیِ تصحیحِ نور

تابعِ عمومیِ `illumination_normalize` میدانِ روشنایی را با بلورِ گاوسی از رویِ **کلِ تصویر شاملِ دیسک‌هایِ سفید** تخمین می‌زد. هر دیسکِ روشن تخمین را در همسایگیِ خودش بالا می‌برد و تفریقِ آن تخمینِ آلوده دورِ **هر** دیسک یک حلقه‌ی تاریکِ ساختگی می‌ساخت — یعنی دقیقاً همان آرتیفکتی که قرار بود حذف شود. این توضیح می‌دهد چرا آن تلاش (بخشِ ۱۲.۷) مثبت‌هایِ کاذب را از ۹ به ۱۷ برد.

`build_agar_canvas` میدان را با **کانولوشنِ نرمال‌شده فقط از پیکسل‌هایِ آگار** می‌سازد: `illum = GaussianBlur(gray×valid) / GaussianBlur(valid)`. باگِ دومی هم حین ساختِ شاخه‌ی Otsu پیدا شد: ماسکِ پتری شاملِ **دیواره‌ی پلاستیکی** است — یک کلاسِ نوریِ سوم که هم آستانه‌ی سراسری را منحرف می‌کرد و هم، چون دورترین ناحیه از همه‌ی دیسک‌هاست، دقیقاً همان جایی بود که تشخیصِ پلاریته نمونه‌برداری می‌کرد. کنارگذاریِ نسبیِ لبه اضافه شد.

#### ت) چهار شاخه‌ی موازی و سنجشِ کمّی‌شان

| شاخه | TP | FN | FP | TN | دقت | MAE |
|---|---:|---:|---:|---:|---:|---:|
| شعاعی (ماژول ۱۶) | ۵۷ | ۶ | ۱۴ | ۱۴ | ۰.۷۸۰ | ۵.۴۹ |
| Otsu، صدکِ ۹۰ (۱۵.۶) | ۲۶ | ۳۷ | ۱ | ۲۷ | ۰.۵۸۲ | ۳.۶۶ |
| Watershed (۱۵.۷) | ۱۱ | ۵۲ | ۲ | ۲۶ | ۰.۴۰۷ | **۱.۰۸** |
| آماری (۱۵.۸) | ۱۶ | ۴۷ | ۶ | ۲۲ | ۰.۴۱۸ | ۵.۰۲ |

الگویِ روشن: شاخه‌ی شعاعی بهترین **پوشش** را دارد ولی بدترین دقت و بیشترین کاذب؛ شاخه‌هایِ ناحیه‌ای تقریباً هرگز کاذب نمی‌زنند و وقتی می‌زنند به‌مراتب دقیق‌ترند. پس قاعده‌ی ادغام «میانگینِ شاخه‌ها» نیست بلکه تقسیمِ کار است.

آماره‌ی شعاع هم اهمیت داشت: برایِ Otsu میانگین MAE=۷.۰۲ می‌دهد ولی صدکِ ۹۰ می‌دهد ۳.۶۶ — چون میانگین‌گیری رویِ جهت‌هایی که هاله با همسایه بریده شده برآورد را پایین می‌کشد. واترشد این مشکل را ندارد (ناحیه‌اش از اول با لبه‌ی واقعی محدود است).

#### ث) دو بن‌بستِ سنجیده‌شده و کنارگذاشته‌شده

**۱) شاخه‌ی بافت/اسپکل.** ایده‌ی فیزیکی درست است (لَون نور را ناهمگن پراکنده می‌کند، آگارِ شفاف نه)، ولی در این کیفیتِ تصویر دانه‌دانگی تفکیک نمی‌شود: جدایی بینِ توزیعِ واریانسِ محلیِ هاله و لَون رویِ `gt_06` فقط ۰.۲۹+ تا ۰.۶۴+ انحرافِ‌معیار و رویِ `gt_11` عملاً صفر بود (۰.۰۷− تا ۰.۰۶+، هم‌پوشانیِ ۹۹-۱۰۰٪) — در سه اندازه‌ی پنجره. کانالی که دو کلاسش این‌قدر روی هم می‌افتند نمی‌تواند سهمی داشته باشد؛ حذف شد نه تنظیم.

**۲) گیتِ ۴σ در ادغام — و کشفِ overfitting.** قاعده‌ای اضافه شده بود: «تشخیصِ شعاعیِ ضعیف‌تر از ۴σ فقط با تاییدِ یک شاخه‌ی ناحیه‌ای پذیرفته شود». درون‌نمونه‌ای عالی بود (کاذب از ۱۴ به ۹، دقت ۰.۸۳۵). **در پاسخ به پرسشِ خودِ کاربر دربارهٔ خطرِ overfitting، اعتبارسنجیِ leave-one-image-out اجرا شد** و نشان داد آن عدد سراب بوده:

| پیکربندی | TP | FN | FP | دقت | MAE |
|---|---:|---:|---:|---:|---:|
| ادغام + گیتِ ۴σ، درون‌نمونه‌ای | ۵۷ | ۶ | ۹ | ۰.۸۳۵ | ۳.۷۸ |
| ادغام + گیتِ ۴σ، **LOO** | ۵۲ | ۱۱ | ۱۱ | **۰.۷۵۸** | ۳.۷۴ |
| ادغام بدونِ گیت | ۶۰ | ۳ | ۱۴ | ۰.۸۱۳ | ۳.۸۵ |

آستانه‌ای که رویِ ۱۰ عکس انتخاب و رویِ عکسِ یازدهم سنجیده شود دقتِ ۰.۷۵۸ می‌دهد — **بدتر از هیچ‌کاری‌نکردن (۰.۷۸۰)**. گیت و پارامترش حذف شدند. آن‌چه زیرِ LOO پابرجا ماند بهبودِ MAE بود (~۳.۷۵)، چون از خودِ اندازه‌گیریِ شاخه‌هایِ ناحیه‌ای می‌آید که هیچ پارامترِ تنظیم‌شده‌ای ندارند. انتخابِ صدکِ ۹۰ برایِ Otsu هم پایدار بود (در هر ۱۱ تایِ LOO همان انتخاب شد).

**شاخه‌ی آماری واردِ زنجیره نشد.** افزودنش MAE را از ۳.۸۵ به ۳.۷۳ می‌برد و بقیه را دست‌نخورده می‌گذارد — ولی ۰.۱۲mm حدودِ شش برابر کوچک‌تر از خطایِ استانداردِ خودِ MAE در n=60 است، یعنی از نویز قابلِ‌تفکیک نیست. رویِ ۵ دیسکی که فقط این شاخه سیگنال دارد MAE=۰.۵۱mm می‌دهد (در برابرِ ۴.۲۳mm شعاعی) که امیدوارکننده است ولی ۲ دیسکِ واقعی پایه‌ی تصمیم نیست. ماژولش به‌عنوانِ تشخیصیِ مستند باقی ماند تا با داده‌ی بیشتر دوباره سنجیده شود.

#### ج) نتیجه‌ی نهاییِ end-to-end (ماژولِ ۱۶.۷، بدونِ هیچ پارامترِ تنظیم‌شده)

| معیار | پیش از این بخش | پس از آن |
|---|---:|---:|
| هاله TP/FN/FP/TN | ۵۷/۶/۱۴/۱۴ | **۶۰/۳**/۱۴/۱۴ |
| دقتِ حضورِ هاله | ۰.۷۸۰ | **۰.۸۱۳** |
| MAE قطرِ هاله | ۵.۵۰mm | **۳.۸۵mm** |
| Bias | ۰.۵۳− | ۰.۶۱− |
| Bland–Altman ۹۵٪ | [۱۶.۱۲−, ۱۵.۰۵+] | **[۱۲.۳۳−, ۱۱.۱۱+]** |
| تشخیصِ دیسک F1 | ۰.۹۸۴ | ۰.۹۸۴ (بدون تغییر) |
| قطرِ دیسک MAE | ۰.۱۷mm | ۰.۱۷mm (بدون تغییر) |

سه هاله‌ی واقعی بازیابی شدند که شاخه‌ی شعاعی رد کرده بود — از جمله یک ناحیه‌ی ۲۵mm با کنتراستِ ۴۲−σ که فقط به این دلیل رد شده بود که پروفایلِ یک‌بعدی‌اش هرگز مسطح نشد.

**موارد باز:** اولویتِ سومِ کاربر (بالا بردنِ دقتِ تشخیصِ دیسک، تسک #۴۶) هنوز شروع نشده. ۱۴ مثبتِ کاذبِ حضورِ هاله دست‌نخورده مانده — تنها راهِ سنجیده‌شده برایِ کاهششان (گیتِ σ) زیرِ اعتبارسنجی رد شد. و مهم‌تر از همه: **۱۱ عکس برایِ این تعداد تصمیمِ طراحی کم است**؛ هر عکسِ اندازه‌گیری‌شده‌ی بیشتر از هر ترفندِ آماری ارزشمندتر است.

**شواهد در ریپازیتوری:** `ground_truth/diagnostics/halo_failure_catalogue.csv`، `halo_branch_comparison.csv`، `loo_validation.py`، `leave_one_out_validation.txt`.

---

### ۱۲.۱۱ — چهار پیشنهادِ فنی، و کشفِ سقفِ ساختاریِ روش‌های مبتنی بر شدت

این بخش چهار پیشنهادِ کاربر (پ۱ تا پ۴) و دو ایده‌ی جانبی (صورت‌بندیِ LCCDE، تغییرِ فضایِ نوری) را پوشش می‌دهد. جمع‌بندیِ کاملِ فنی در `ROADMAP.md` بخش‌های ۳ تا ۶ است؛ این‌جا فقط مسیرِ تصمیم و آن‌چه یاد گرفتیم ثبت می‌شود.

#### الف) پ۴ — مدلِ سیگموئیدِ گذار (ماژولِ ۱۵.۹)

پیشنهادِ کاربر مدل‌سازیِ نمایی/لگاریتمی بود؛ اصلاحِ من این بود که آن‌چه اندازه می‌گیریم غلظت نیست بلکه **چگالیِ رشد** است، پس تابعِ درست یک **لجستیک** است نه نمایی. کاربر پذیرفت و ماژول ساخته و روی هر ۱۱ عکس سنجیده شد.

نتیجه: روی مواردی که برازش موفق است دقیق است، ولی **افزونه** — روی همان زیرمجموعه‌ای فعال می‌شود که شاخه‌های ناحیه‌ای قبلاً حل کرده‌اند. واردِ زنجیره‌ی تصمیم نشد و به‌عنوانِ ماژولِ تشخیصی باقی ماند.

#### ب) صورت‌بندیِ LCCDE — پیشنهادِ کاربر

کاربر پرسید آیا می‌شود مسئله را به یک معادله‌ی دیفرانسیلِ خطی با ضرایبِ ثابت تبدیل کرد، با تجزیه به پاسخِ گذرا و حالتِ دائمی. نگاشتِ ریاضی درست است: `r·C″ + C′ = 0` معادله‌ی اویلر–کوشی است و با `u = ln(r)` به `d²C/du² = 0` تبدیل می‌شود که واقعاً ضریب‌ثابت است.

دو فرضیه‌ی آزمایش‌پذیر از آن بیرون آمد و هر دو سنجیده شدند:
- **(الف) برازش در فضایِ `ln(r)` باید بهتر باشد** — **رد شد** (z=۳.۴۶−، یعنی معنادار بدتر).
- **(ب) فاصله‌ی خوانشِ کارشناس تا حقیقتِ زیستی باید متناسب با پهنایِ گذار `w` باشد، نه ثابت** — **تایید شد**، ولی نه به‌شکلی که بشود به‌عنوانِ جمله‌ی تصحیح به‌کار برد.

#### پ) تغییرِ فضایِ نوری — و یک تشخیصِ غلط که خودم تصحیح کردم

بیر–لامبرت و کوبلکا–مونک روی پروفایل‌ها آزموده شدند. کوبلکا–مونک R² را بهتر کرد (۰.۵۵ → ۰.۶۵) ولی MAE را بدتر (۳.۴۰ → ۷.۰۰).

**تشخیصِ اولِ من غلط بود.** نوشتم علتش واگراییِ `(1−R)²/(2R)` در `R→0` است. اندازه‌گیریِ مستقیمِ دامنه‌ی `R` این را رد کرد: میانه‌ی کمینه‌ی هر پروفایل **۰.۹۰۷** است — `R` هرگز به صفر نزدیک نمی‌شود. سازوکارِ واقعی دو چیز بود: **بریدنِ سقف** (میانه ۳۵٪ از هر پروفایل `R ≥ 1` دارد و به صفر بریده می‌شد، یعنی فلاتِ لَون صاف می‌شد) و **فشرده‌سازی** (با `R≈0.9`، مقدارِ `(1−R)²` برابرِ ۰.۰۱ است).

بعد از رفعِ هر دو — با آزاد کردنِ `R_lawn` (بازتابِ مطلقِ لَون، همان چیزی که وصله‌ی سفیدِ مرجع می‌دهد) و جاروبِ آن — MAEِ کوبلکا–مونک از ۷.۰۰ به **۴.۵۷** رسید. یعنی تشخیصِ اصلاح‌شده درست بود. **ولی نتیجه عوض نشد:** حتی این برآوردِ خوش‌بینانه (که `R_lawn` را روی خودِ داده‌ی ارزیابی انتخاب می‌کند) از خطِ پایه‌ی ۳.۴۰ بدتر است.

یافته‌ی جانبیِ مهم: **R² و دقتِ مرز با هم ناسازگارند.** در جاروب، R² یکنواخت بهتر و MAE یکنواخت بدتر می‌شود. یعنی کیفیتِ برازش معیارِ معتبری برای انتخاب در این مسئله نیست — نکته‌ای که پ۴ را هم بازتفسیر می‌کند.

#### ت) پ۲ — و علتِ ریشه‌ایِ مشترکِ همه‌ی شکست‌ها

پس از سه بار ساختنِ شاخه‌ای که کار می‌کرد ولی افزونه بود، این‌بار **اول پیش‌فرض سنجیده شد**. قاعده‌ی دو-مرجعی (هاله‌ی خودِ دیسک در برابرِ لَونِ میدانِ دور، بدونِ هیچ پارامترِ آزاد) پیاده و روی ۹۱ دیسک اجرا شد: روی ۳۱ از ۳۳ دیسکِ بحرانی اصلاً شلیک نمی‌کند.

علتش اندازه‌گیری شد و **مهم‌ترین یافته‌ی این مرحله است:**

| جدایشِ «داخلِ هاله» از «لَون» (بر حسبِ σ خودِ لَون) | بحرانی | آسان |
|---|---:|---:|
| مرجع از حلقه‌های نزدیکِ دیسک | **۰.۳۷** | ۰.۸۵ |
| مرجع از داخلِ هاله‌ی **مرجعِ کارشناسی** (کنترل) | **۰.۲۹** | ۰.۶۷ |

دو کنترل نشان داد این مصنوعِ روش نیست: در ۰٪ موارد مرجع بیرونِ هاله افتاده (میانه‌ی نسبت ۰.۳۹)، و حتی با دادنِ مکانِ درست از روی داده‌ی مرجع، جدایش **کمتر** می‌شود.

**یعنی روی نیمی از دیسک‌ها، مرزِ هاله در تصویر — در سطحِ کنتراستی که قابلِ تفکیک باشد — حاضر نیست.** و این یک علت است با چهار نشانه: بی‌اثریِ Otsu/واترشد، هم‌پوشانیِ ۹۹–۱۰۰٪ کانالِ بافت، افزونگیِ مدلِ رشد، و نشلیک‌کردنِ پ۲. شکستِ تبدیل‌های نوری هم زیرِ همین چتر است: تبدیلِ نقطه‌ایِ یکنوا نمی‌تواند بینِ دو توزیعِ هم‌پوشان جدایی **بسازد**.

#### ث) آن‌چه این مرحله برای پروژه تغییر داد

۱. **ساختنِ شاخه‌ی جدیدِ مبتنی بر شدت متوقف شد** — سقفش اندازه‌گیری شده است.
۲. **پیشنهادِ نورپردازیِ پس‌زمینه‌ای حالا مشخصه‌ی کمّی دارد:** باید جدایش را از ~۰.۳σ به بالای ~۲σ ببرد.
۳. **اولویت به طبقه‌بندیِ EUCAST منتقل شد** — معیارِ بالینی قطرِ دقیق نیست، دسته‌ی S/I/R است و باندهایش پهن‌اند؛ ممکن است همین دقت توافقِ دسته‌ایِ خوبی بدهد.
۴. **درسِ روشی:** سه بار ساختیم و بعد فهمیدیم افزونه است؛ بارِ چهارم اول سنجیدیم و در چند دقیقه فهمیدیم نباید ساخته شود. سنجشِ پیش‌فرض پیش از پیاده‌سازی، الگویِ درست است.

**شواهد در ریپازیتوری:** `ground_truth/diagnostics/` — `p4_dump_profiles.py`، `p4_fit_sigmoid.py`، `p4_sigmoid_fits.json`، `p4_lccde_test.py`، `p4_optical_space_test.py`، `p4_optical_precond.py`، `p4_optical_precond_sweep.txt`، `p2_texture_probe.py`، `p2_texture_probe.txt`.

---

### ۱۲.۱۲ — طبقه‌بندیِ بالینیِ EUCAST: ترجمه‌ی خطا به زبانِ تصمیم

تا این‌جا کیفیتِ سیستم را با MAE بر حسبِ میلی‌متر می‌سنجیدیم. این معیار به‌تنهایی نه خوب است نه بد، چون آن‌چه به پزشک گزارش می‌شود عدد نیست، **دسته** است: `S` / `I` / `R`. این بخش آن ترجمه را انجام می‌دهد — و نتیجه‌اش تعیین‌کننده‌تر از هر چیزی است که تا حالا اندازه گرفته‌ایم.

#### الف) پایگاهِ نقاطِ شکست

جدولِ EUCAST v16.0 از PDF رسمی استخراج شد. استخراج **مختصات‌محور** است نه متنی: در جریانِ متنیِ PDF نامِ عامل و اعداد پشتِ هم می‌آیند و سلولِ خالی چیزی تولید نمی‌کند، پس شمارشِ اعداد جواب نمی‌دهد؛ ولی ستون‌ها در سند دقیقاً هم‌ترازند و تخصیص بر اساسِ `x` قطعی است.

خروجی: **۱۱۱۶ سطر از ۲۷ گروهِ ارگانیسم**، که ۳۴۹ سطرش نقطه‌ی شکستِ عددیِ قابلِ‌استفاده دارد (۴۸ جفتِ یکتایِ `(S,R)`). صحت با مقایسه‌ی دستی با نسخه‌ی منتشرشده تایید شد.

#### ب) مشکلِ داده — و چرا مسئله بن‌بست نشد

طبقه‌بندی به گونه‌ی باکتری و آنتی‌بیوتیکِ هر دیسک نیاز دارد. **هیچ‌کدام را نداریم:** کارشناس ثبت نکرده و ما هم برچسبِ رویِ دیسک را نمی‌خوانیم. این طبقه‌بندیِ همین ۱۱ عکس را غیرممکن می‌کند.

ولی پرسشِ بالینی بن‌بست نیست، چون هر نقطه‌ی شکست فقط یک عدد رویِ همان محورِ میلی‌متری است. پس به‌جایِ «این دیسک چه دسته‌ای می‌گیرد» پرسیدیم: **اگر نقطه‌ی شکست هرکدام از ۳۴۹ موردِ واقعیِ EUCAST باشد، سیستم چند درصد مواقع با کارشناس هم‌دسته می‌شود؟** جواب یک توزیع است نه یک عدد، و عدمِ‌قطعیتِ ناشی از ندانستنِ آنتی‌بیوتیک به‌صورتِ پهنایِ همان توزیع دیده می‌شود.

دیسکِ بدونِ هاله به‌صورتِ قطرِ ناحیه = ۶mm حساب شد (رشد تا لبه‌ی دیسک)، که زیرِ هر نقطه‌ی شکستی است یعنی `R`. با این قرارداد، ۱۴ مثبتِ کاذب و ۳ منفیِ کاذبِ حضورِ هاله هم واردِ حساب شدند، پس عدد واقعاً end-to-end است.

#### پ) نتیجه — سیستم به آستانه‌ی بالینی نمی‌رسد

۹۱ دیسک × ۳۴۹ نقطه‌ی شکست = ۳۱٬۷۵۹ تصمیمِ شبیه‌سازی‌شده.

| معیار (ISO 20776-2 / CLSI M23) | مقدار | سقفِ مرسوم | وضعیت |
|---|---:|---:|---|
| توافقِ دسته‌ای (CA) | ۸۲.۵٪ | — | — |
| خطایِ بسیار عمده (VME) | **۶.۹٪** | ۱.۵٪ | ❌ رد |
| خطایِ عمده (ME) | **۹.۲٪** | ۳.۰٪ | ❌ رد |
| خطایِ جزئی (mE) | ۱.۵٪ | — | — |

تکرارِ حساب رویِ ۴۸ جفتِ **یکتا** (تا موقعیت‌هایِ پرتکرار وزنِ اضافی نگیرند): CA=۸۲.۵٪، VME=۴.۴٪، ME=۷.۰٪ — هم‌خوان، پس نتیجه به این انتخاب حساس نیست.

#### ت) سه یافته‌ی عملی

۱. **خطا فقط نزدیکِ نقطه‌ی شکست به تصمیم تبدیل می‌شود.** وقتی خوانشِ مرجع تا ۴mmیِ نقطه‌ی شکست باشد، ۳۲.۵٪ مواقع دسته عوض می‌شود؛ دورتر از آن، ۱۱.۳٪. یعنی بهبودِ دقت دقیقاً در همان همسایگی بیشترین ارزش را دارد.

۲. **ریسکِ بالینی همان جایی است که یافته‌ی ۰.۳σ گفته بود.** به تفکیکِ شاخه: واترشد CA=۹۴.۲٪ و VME=۱.۲۶٪ — یعنی **به‌تنهایی سقفِ VME را برآورده می‌کند** — ولی فقط رویِ ۱۳ دیسک فعال است. شاخه‌ی شعاعی CA=۷۶.۲٪ و VME=۱۲.۸۲٪ رویِ ۴۴ دیسک. کلِ ریسک آن‌جاست. این سومین تاییدِ مستقلِ همان تقسیم‌بندی است، این‌بار بر حسبِ ریسکِ بیمار.

۳. **هدفِ مهندسیِ عددی به‌دست آمد.** با کوچک کردنِ خطا در ضریبِ `k` و سنجشِ دوباره، هر دو سقف از حدودِ **MAE ≈ ۱.۰mm** به پایین برآورده می‌شوند. یعنی برایِ قابلِ‌استفاده شدن، دقت باید حدودِ **چهار برابر** بهتر شود. این عدد از این پس معیارِ پذیرش/ردِ هر ایده‌ی بعدی است، به‌جایِ «هرچه کمتر بهتر».

#### ث) ماژولِ ۱۸

قاعده‌ی EUCAST قطعی است و ماژول **هیچ پارامترِ تنظیم‌شونده‌ای ندارد**. بازه‌ی ATU (عدمِ‌قطعیتِ فنی) هم علامت زده می‌شود. اگر ارگانیسم/آنتی‌بیوتیک اعلام نشوند، ماژول دسته اعلام نمی‌کند و به‌جایش بازه‌ی دسته‌هایِ ممکن را نشان می‌دهد — صادقانه‌تر از حدس زدن. هر سه مسیر (`S`، `I`، `R`) به‌علاوه‌ی حالتِ نامعلوم، end-to-end رویِ `gt_01` اجرا و تایید شدند.

#### ج) معنایِ این بخش برایِ پایان‌نامه

این بخش شکافِ «عنوانِ مصوب می‌گوید مطابقِ EUCAST، پیاده‌سازی طبقه‌بندی ندارد» را می‌بندد. ولی مهم‌تر از آن، یک نتیجه‌ی منفیِ صادقانه و قابلِ‌دفاع اضافه می‌کند: **سیستم در وضعِ فعلی ابزارِ غربالگری است، نه ابزارِ تصمیمِ بالینی.** این جمله باید صریح در پایان‌نامه بیاید، همراه با هدفِ کمّیِ ۱.۰mm و دلیلِ ساختاریِ نرسیدن به آن (یافته‌ی ۰.۳σ در بندِ ۱۲.۱۱).

**شواهد در ریپازیتوری:** `ground_truth/eucast/` — `README.md`، `parse_breakpoints.py`، `eucast_v16_zone_breakpoints.csv`، `categorical_agreement.py`، `categorical_agreement.csv`، `categorical_agreement_run.txt`.

---

### ۱۲.۱۳ — پ۱+پ۳: ماژولِ اعتبارسنجیِ دیسک ساخته شد، نیمِ دومِ نقشه رد شد

آخرین موردِ باقی‌مانده از چهار پیشنهاد. نقشه دو گام داشت: (۱) آستانه‌ی Hough را پایین بیاور تا دو دیسکِ ازدست‌رفته پیدا شوند، (۲) با اعتبارسنجیِ شکل، کاندیدهایِ کاذبِ حاصل را سرکوب کن. طبقِ الگویی که در پ۲ جواب داد، اول پیش‌فرضِ گامِ ۱ سنجیده شد.

#### الف) تشخیصی که فرضِ اولیه را عوض کرد

فرض این بود که هر دو FN «مشکلِ آستانه» دارند. اندازه‌گیری خلافش را نشان داد: رویِ `gt_10` دیسکِ ۸، Hough در همان آستانه‌ی فعلی دایره تولید می‌کند ولی با **شعاعِ ۹۰** در حالی که دیسک‌هایِ واقعیِ همان عکس شعاعِ ۳۷–۴۳ دارند — رویِ **هاله** قفل شده، نه رویِ دیسک. قاعده‌ی سازگاریِ شعاعِ ماژولِ ۱۴ به‌درستی ردش می‌کند.

نسخه‌ی اولِ آزمونِ من فقط *مکان* را تطبیق می‌داد و به همین دلیل این مورد را «قابلِ بازیافت در همه‌ی آستانه‌ها» گزارش کرد. با افزودنِ شرطِ شعاع معلوم شد پایین آوردنِ آستانه این مورد را اصلاً حل نمی‌کند. **درس: در تطبیقِ کاندید با مرجع، مکان به‌تنهایی معیارِ کافی نیست.**

#### ب) ماژولِ ۱۱.۵ — پذیرفته شد

پ۱ (کمانِ پیوسته) و پ۳ (الگویِ دیسک) در یک ماژول ادغام شدند، چون هر دو یک پرسش دارند. الگو رویِ حروفِ چاپی نیست — برچسب‌ها متفاوت و جهتشان تصادفی است — بلکه رویِ سه ویژگیِ **مستقل از برچسب و چرخش**: پیوستگیِ کمانیِ لبه، پله‌ی شعاعیِ لبه، و تقارنِ دورانی. مرجعِ تصمیم اجماعِ کاندیدهایِ همان تصویر است، نه عددِ سراسری.

| پیکربندی | TP | FP | FN | P | R | F1 |
|---|---:|---:|---:|---:|---:|---:|
| خطِ پایه | ۹۰ | ۱ | ۲ | ۰.۹۸۹ | ۰.۹۷۸ | ۰.۹۸۴ |
| **+ ماژولِ ۱۱.۵** | ۹۱ | **۰** | ۲ | **۱.۰۰۰** | ۰.۹۷۸ | **۰.۹۸۹** |
| + ۱۱.۵ + آستانه‌ی ۱۱ | ۹۰ | ۰ | ۳ | ۱.۰۰۰ | ۰.۹۶۸ | ۰.۹۸۴ |

تنها مثبتِ کاذبِ باقی‌مانده حذف شد، بدونِ از دست دادنِ هیچ دیسکِ واقعی. ماژول **هیچ پارامترِ انتخاب‌شده از داده ندارد**: کفِ پوششِ کمانیِ ۰.۷۵ از هندسه می‌آید (یک دیسکِ واقعی ممکن است تا ~یک‌چهارمِ محیطش با همسایه یا لبه‌ی ظرف هم‌پوشانی داشته باشد) و ۳σ همان سطحی است که ماژولِ ۱۶.۵ هم به‌کار می‌برد.

#### پ) پایین آوردنِ آستانه — رد شد

جاروب رویِ هر ۱۱ عکس نشان داد رفتار **یکنوا نیست**: پوششِ دیسکِ واقعی از ۹۱/۹۳ در آستانه‌ی ۲۰ به ۹۳/۹۳ در ۱۶ می‌رود، در ۱۳ به **۹۰**/۹۳ می‌افتد، و در ۱۱ دوباره ۹۳/۹۳ می‌شود — در حالی که کاندیدهایِ کاذبِ خام از ۵ به ۳۷ می‌رسند.

آزمونِ end-to-end با `param2=11` این را تایید کرد: `gt_07` درست شد ولی `gt_08` و `gt_11` خراب شدند و F1 به ۰.۹۸۴ برگشت. علت روشن است: با شلوغ شدنِ انباشتگرِ Hough، سرکوبِ `minDist` شروع به حذفِ دیسک‌هایِ *واقعی* می‌کند و مرحله‌ی اعتبارسنجی هم به‌جایِ ۱، هفت دیسکِ واقعی را رد می‌کند.

**فرضِ نقشه‌ی راه غلط بود:** پایین آوردنِ آستانه، precision را با recall معاوضه نمی‌کند؛ recall را با recall معاوضه می‌کند. آستانه در ۲۰ دست‌نخورده ماند.

#### ت) وضعیتِ نهاییِ تشخیصِ دیسک

Precision = ۱.۰۰۰، Recall = ۰.۹۷۸، F1 = ۰.۹۸۹. دو FN باقی مانده و علتشان یکی نیست: `gt_10#8` (قفل شدنِ Hough رویِ هاله) با آستانه حل نمی‌شود و کاندیدسازِ متفاوتی می‌خواهد؛ `gt_07#6` با آستانه‌ی سهل‌گیرتر پیدا می‌شود ولی همان آستانه در مجموع زیان‌ده است.

**شواهد:** `ground_truth/diagnostics/p13_hough_sweep.py`، `p13_hough_sweep.csv`، `p13_hough_sweep_run.txt`، `p13_verify_module_run.txt`، `p13_param2_11_run.txt`.

---

### ۱۲.۱۴ — تکرارپذیریِ کارشناس: ±۲ میلی‌متر، و بازتفسیرِ همه‌ی اعداد

کارشناسِ آزمایشگاه در گفت‌وگو اعلام کرد اندازه‌گیریِ کارشناسانِ آن‌ها **«از همه لحاظ ±۲ میلی‌متر تغییر دارد»**. این همان عددی است که در بندِ ۱۲.۱۲ به‌عنوانِ مهم‌ترین کارِ باقی‌مانده علامت زده بودیم، و رسیدنش سه چیز را هم‌زمان عوض کرد.

«±۲» مبهم است — بازه‌ی کامل؟ فاصله‌ی اطمینانِ ۹۵٪؟ یک انحرافِ معیار؟ — پس به‌جایِ فرضِ یکی، هر سه تفسیر تا انتها حمل شد. شبیه‌سازی با بذرِ ثابت است، پس قطعی می‌ماند.

#### الف) خطایِ ما را توجیه نمی‌کند

با فرضِ استقلالِ خطایِ سیستم و نویزِ مرجع، واریانس‌ها جمع می‌شوند. با کسرِ سهمِ مرجع، MAE از ۳.۸۵ فقط به **۳.۶۳ تا ۳.۸۰** می‌رسد. یعنی این عدد بهانه نیست؛ خطا عمدتاً مالِ خودِ سیستم است.

همین را رویِ مثبت‌هایِ کاذب هم بررسی کردیم: اگر ۱۴ مثبتِ کاذب محصولِ ابهامِ ±۲mm درباره‌ی *وجودِ* هاله بودند، قطرشان باید حولِ ۶mm جمع می‌شد. نیست — میانه ۱۰.۵mm و فقط ۲ مورد زیرِ ۸mm. این‌ها خطایِ واقعی‌اند.

#### ب) ولی سقفِ منصفانه ۱۰۰٪ نبود

کارشناسِ دوم شبیه‌سازی شد (همان خوانش + نویزِ ±۲mm، گردشده به میلی‌متر) و همان تحلیلِ دسته‌ای اجرا شد:

| مقایسه | MAE | CA | VME | ME |
|---|---:|---:|---:|---:|
| انسان در برابرِ انسان | ۱.۱۶–۲.۲۶ | ۹۲.۱–۹۵.۸٪ | ۱.۷۰–۳.۲۹٪ | ۱.۷۷–۳.۳۹٪ |
| سیستم در برابرِ کارشناس | ۳.۸۵ | ۸۲.۵٪ | ۶.۸۶٪ | ۹.۲۴٪ |

**دو نتیجه:**

۱. سقفِ واقعیِ CA حدودِ **۹۲–۹۶٪** است نه ۱۰۰٪. فاصله‌ی سیستم تا سقف ~۱۰ واحد است، نه ~۱۸. هنوز معنادار، ولی نه آن‌قدر که در بندِ ۱۲.۱۲ به‌نظر می‌رسید.

۲. **با ±۲mm، حتی دو کارشناسِ انسانی هم سقفِ VME=۱.۵٪ را برآورده نمی‌کنند.** پس آن سقف‌هایِ ISO/CLSI برایِ سنجشِ یک روش در برابرِ *مرجعِ کارشناس‌خوانده* معیارِ منصفانه‌ای نیستند — برایِ مقایسه با MICِ رقتِ براث تعریف شده‌اند. جمله‌ی «سیستم به آستانه‌ی پذیرشِ بالینی نمی‌رسد» در بندِ ۱۲.۱۲ باید با این قید خوانده شود.

#### پ) هدفِ ۱.۰mm اثبات‌ناپذیر است

هدفی که از سقف‌هایِ EUCAST درآمد (~۱.۰mm) در هر سه تفسیر **زیرِ** تکرارپذیریِ انسانی است. دست‌نیافتنی نیست — یک خوانشگرِ خودکار می‌تواند از انسان باثبات‌تر باشد — ولی **با این مرجع قابلِ نمایش نیست**: دقتی ظریف‌تر از نویزِ خودِ خط‌کش را نمی‌شود با همان خط‌کش نشان داد.

پس هدف بازتعریف شد. **هدفِ عملیِ قابلِ‌اثبات: رساندنِ MAE به باندِ ±۲mm انسانی.** الان **۴۸٪** اندازه‌گیری‌ها آن‌جا هستند و میانه‌ی خطا ۲.۰۸mm است — یعنی عملاً رویِ لبه‌ی باند. میانگین را اقلیتی از خطاهایِ بزرگ بالا می‌کشد، و باز هم در همان یک شاخه:

| شاخه | n | داخلِ ±۲mm | میانه |
|---|---:|---:|---:|
| Watershed | ۱۱ | **۹۱٪** | ۱.۱۷ |
| Otsu | ۱۶ | ۵۰٪ | ۱.۹۴ |
| شعاعی | ۳۳ | **۳۳٪** | ۳.۱۱ |

#### ت) تصویرِ نهاییِ محدودیت‌ها

حالا هر سه محدودیتِ اصلیِ پروژه یک شکلِ مشترک دارند — **هیچ‌کدام با الگوریتمِ بهتر حل نمی‌شوند:**

| محدودیت | چه چیزی لازم است |
|---|---|
| جدایشِ ۰.۳σ رویِ نیمی از دیسک‌ها (۱۲.۱۱) | تصویربرداریِ بهتر (نورِ پس‌زمینه‌ای) |
| نویزِ ±۲mm مرجع (همین بند) | مرجعِ بهتر (چند خوانش، یا MIC) |
| ۱۱ عکس برایِ این تعدادِ تصمیم (۱۲.۱۰) | داده‌ی بیشتر |

این برایِ پایان‌نامه نتیجه‌ی خوبی است، نه بد: نشان می‌دهد فضایِ راه‌حلِ نرم‌افزاری تا کجا کاوش شده و مرزِ واقعی کجاست.

**شواهد:** `ground_truth/eucast/expert_repeatability.py`، `expert_repeatability.csv`، `expert_repeatability_run.txt`؛ بخشِ ۳.۲ در `EVALUATION.md` (دوزبانه).
