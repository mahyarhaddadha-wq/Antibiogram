# تاریخچه و مستندات فنی پروژه آنتی‌بایوگرام (Antibiogram Detection Pipeline)

این سند برای حفظ تاریخچه‌ی پروژه، مفروضات، تصمیم‌های طراحی، باگ‌های پیدا‌شده و کارهای باقی‌مانده نوشته شده تا در جلسات بعدی (چه با کلود، چه با هر توسعه‌دهنده‌ی دیگر) زمینه‌ی کامل کار از دست نرود.

آخرین به‌روزرسانی: بر اساس شاخه‌ی `claude/antibiogram-detection-completion-kcq6si`.

---

## ۱) هدف پروژه

سیستمی بینایی ماشین برای **تشخیص خودکار نتایج آزمایش آنتی‌بایوگرام** از روی عکس، طبق استاندارد **EUCAST** (اتحادیه‌ی اروپا برای تست حساسیت میکروبی به آنتی‌بیوتیک). پایپلاین باید به‌ترتیب:

1. **ظرف(های) پتری** را در عکس پیدا کند (ممکن است بیش از یک پتری در قاب باشد).
2. **دیسک‌های آنتی‌بیوتیک** روی هر پتری را پیدا کند (سفید با نوشته‌ی سیاه، در دو قطر ۶ و ۸ میلی‌متر، تعداد رندوم).
3. **هاله‌ی مهار (Inhibition Halo)** دور هر دیسک را (در صورت تشکیل) تشخیص دهد و قطرش را تخمین بزند.
4. **رخدادهای روی هاله** (مثل حباب‌های ریز) را شناسایی و گزارش کند (تعداد + اندازه/حجم تقریبی).
5. در نهایت گزارشی بدهد: قطر دیسک → قطر هاله → رخدادهای حباب، برای هر دیسک در هر پتری.

هدف بلندمدت‌تر (خارج از محدوده‌ی کار فعلی): استفاده از جداول Breakpoint استاندارد EUCAST (فایل‌های PDF موجود در ریپازیتوری: `v_16.0_Breakpoint_Tables.pdf` و `Dosages_v_16.0_Breakpoint_Tables.pdf`) برای طبقه‌بندی نتیجه‌ی هر دیسک به حساس/متوسط/مقاوم (S/I/R) بر اساس قطر هاله. **این بخش هنوز پیاده‌سازی نشده است.**

---

## ۲) مفروضات و الزامات کلیدی پروژه

این‌ها الزاماتی هستند که از ابتدای پروژه توسط کاربر تعیین شده‌اند و باید در هر توسعه‌ی بعدی رعایت شوند:

### ۲.۱) پتری
- ممکن است **دایره‌ای یا مربعی/مستطیلی** باشد.
- تعداد پتری در تصویر **محدودیت ندارد** (می‌تواند صفر، یک یا چند تا باشد).
- حتی اگر تشخیص دقیق شکل ممکن نباشد، حداقل باید بک‌گراندی که جزو پتری نیست حذف شود (fallback عمومی).

### ۲.۲) دیسک
- سفید رنگ با نوشته‌ی سیاه روی خودش.
- تعداد دیسک در هر پتری **رندوم** است.
- دو سایز استاندارد: **۶ میلی‌متر** و **۸ میلی‌متر** قطر — ممکن است هر دو سایز هم‌زمان در یک پتری باشند.

### ۲.۳) هاله
- ممکن است تشکیل شود یا نشود.
- نتیجه‌ی آزمایش هر دیسک به **قطر هاله** بستگی دارد.
- روی هاله ممکن است **حباب‌های ریز** شکل بگیرد؛ شناسایی آن‌ها (تعداد + حجم تقریبی توده) هم مهم است.

### ۲.۴) الزام robust بودن (بسیار مهم — نقض‌نشدنی)
> **هیچ مقداردهی مستقیم فیزیکی (طول به پیکسل، تعداد ثابت، آستانه‌ی ثابت شدت و مانند آن) نباید برای هیچ پارامتری — به‌خصوص پارامترهای فیزیکی — انجام شود.**
> همه‌ی اندازه‌ها و بازه‌ها باید **نسبت به اندازه‌ی خودِ تصویر، پتری یا دیسک** تعریف شوند (مثلاً کرنل مورفولوژی = فلان‌درصد قطر دیسک، نه فلان پیکسل ثابت).

این الزام در کل کد رعایت شده: تمام پارامترهای جدیدی که در این نشست اضافه شدند (کانفیگ‌های `cfg.dish_detect_*`, `cfg.bubble_*`, `cfg.morph_*_frac` و غیره) نسبت‌محورند. برخی فیلدهای **قدیمی و مطلق** در `Phase2Config` (سلول ۳) که در طراحی اولیه‌ی نوت‌بوک (پیش از این نشست) وجود داشتند و در کد فعلی استفاده نمی‌شوند (مثلاً `HoughSweepConfig`، `dish_roi_*`، `score_*_weight`) عمداً دست‌نخورده باقی مانده‌اند چون مرده/بلااستفاده‌اند؛ پاک‌سازی کامل آن‌ها خارج از محدوده‌ی این نشست بوده تا ریسک اضافه به پروژه تحمیل نشود.

### ۲.۴.۱) دو قانون تکمیلیِ نقض‌نشدنی (تأکید صریح کاربر — همیشه باید رعایت شوند)

**(الف) هیچ سقف/محدودیت ثابتی روی پارامترهای فیزیکی — نه فقط اندازه، بلکه تعداد هم — گذاشته نشود.**
یعنی نه‌فقط طول/قطر/شعاع نباید پیکسل مطلق باشند (بند ۲.۴ بالا)، بلکه **تعداد** (مثلاً حداکثر تعداد پتری در یک عکس، حداکثر تعداد دیسک در یک پتری، حداکثر تعداد کاندید Hough که پردازش می‌شود) هم نباید با یک عدد ثابت و دلخواه سقف زده شود — مگر این‌که واقعاً به این نتیجه برسیم که **هیچ راه‌حل دیگری وجود ندارد** (یعنی آخرین راه‌حل، نه اولین). اگر برای کارایی/سرعت نیاز به محدود کردن یک حلقه یا جست‌وجو بود، باید راه‌حل الگوریتمی (مثل early-stopping بر اساس همگرایی امتیاز، نه یک عدد ثابت دلخواه مثل «حداکثر ۱۰ پتری») در اولویت باشد.

**(ب) Overfitting روی عکس‌های موجود در ریپازیتوری قطعاً ممنوع است — تکرار: به هیچ عنوان.**
هیچ پارامتر، آستانه، یا مسیر منطقی نباید طوری تنظیم/نوشته شود که مخصوص یکی از عکس‌های موجود در ریپازیتوری کار کند (حتی اگر نتیجه‌ی آن عکس خاص را بهتر کند). هر اصلاح باید بر اساس **استدلال عمومی و مستقل از عکس خاص** باشد (تئوری CV، فیزیک مسئله مثل دو سایز استاندارد دیسک، یا رفتار آماری robust) — نه reverse-engineering یک آستانه از روی اندازه‌گیری دستی یک عکس. عکس‌های موجود فقط برای **تست/اعتبارسنجی** استفاده می‌شوند، نه برای طراحی. قبل از هر کامیت، باید حداقل روی چند عکس **متنوع و قبلاً بررسی‌نشده** (نه فقط عکس‌هایی که مشکل ازشون کشف شده) هم تست شود تا اطمینان حاصل شود رفتار عمومی است.

### ۲.۵) ساختار کد
- نوت‌بوک Jupyter، **ماژولار و سلول‌به‌سلول** — هر سلول یک بلوک/فیلتر مستقل با یک خروجی تصویری.
- سلول‌ها پشت‌سرهم متصل‌اند (خروجی سلول قبلی = ورودی سلول بعدی).
- این ساختار باید **حفظ شود** — رویه‌ی توسعه نباید آن را به یک اسکریپت تک‌تکه تبدیل کند.

### ۲.۶) هدف انتقال به ++C
- پروژه قرار است در آینده به C++ ترجمه/کامپایل شود.
- به همین دلیل، الگوریتم‌ها باید **deterministic** باشند (مرتب‌سازی صریح، tie-break مشخص، بدون وابستگی به رفتار تصادفی یا ترتیب هش).
- ساختارهای داده باید ساده و قابل‌نگاشت به `struct` باشند (در کد فعلی: دیکشنری‌های ساده با کلیدهای ثابت).

---

## ۳) معماری فعلی پایپلاین (نسخه‌ی بعد از این نشست)

فایل اصلی: **`disk_detection_pipeline_modular.ipynb`** (۴۳ سلول).

| ماژول | نام | نقش |
|---|---|---|
| ۱ | Config + Config Extension | تعریف تمام پارامترهای نسبی (`cfg`) |
| ۲ | Helper Functions | توابع مشترک: کرنل بیضوی، نمایش تصویر، NMS، متریک‌های شکل، Watershed، multi-Otsu، پروفایل شعاعی |
| ۳ | بارگذاری تصویر | خواندن عکس ورودی |
| **۴** | **تشخیص همه‌ی پتری‌ها** | **بازطراحی کامل در این نشست — بخش ۴ همین سند را ببینید** |
| ۴.۱ | ماسک دقیق پتری | فید مرز + ماسک دقیق‌تر، به‌صورت per-dish |
| ۵ | Disk Feature Image | جایگزین White Top-Hat ساده؛ ترکیب Top-Hat + کنتراست محلی با نرمال‌سازی robust |
| ۶ | Adaptive Threshold | **بازطراحی‌شده در این نشست** (رجوع به بخش ۵.۱) |
| ۷/۸ | Morphological Closing/Opening | کرنل‌ها نسبت به قطر کمینه‌ی دیسک (نه پیکسل ثابت) |
| ۹ | Distance Transform | ورودی مشترک برای Watershed (ماژول ۱۲) و نمایش تشخیصی (ماژول ۱۳) |
| ۱۰ | Halo Gradient Map | سیگنال گرادیان که اکنون در ماژول ۱۷ (حباب) بازیافت می‌شود |
| ۱۰.۵ | Disk-Specific Edge Image | ورودی edge-support برای شاخه‌ی Hough |
| ۱۱ | Hough (شاخه‌ی اصلی/دقیق) | شاخه‌ی قابل‌اعتماد تشخیص دیسک |
| ۱۲ | Blob Analysis | **بازطراحی‌شده در این نشست** (رجوع به بخش ۵.۲) |
| ۱۳ | DT Peaks / Watershed Markers | دیگر شاخه‌ی مستقل نیست؛ فقط نمایش تشخیصی نشانگرهایی که ماژول ۱۲ استفاده می‌کند |
| ۱۴ | Fusion (ادغام دو‌شاخه‌ای) | **بازطراحی‌شده** — Hough + Blob، بدون DT به‌عنوان رأی‌دهنده مستقل |
| ۱۵ | نمایش نتایج نهایی | رسم دیسک‌های نهایی + پرچم پتری‌های مشکوک به FP |
| ۱۶ | تشخیص هاله‌ی مهار | پروفایل شعاعی نسبی — بدون تغییر منطقی، فقط per-dish شد |
| **۱۷** | **تشخیص حباب روی هاله** | **ماژول کاملاً جدید** |
| ۱۸ | گزارش نهایی یکپارچه | **سلول جدید** — خروجی نهایی: قطر دیسک → قطر هاله → رخداد حباب |

**معماری per-dish:** به‌جای متغیرهای سراسری تکی (`roi_gray`, `processing_mask_roi`, ...)، یک لیست `dishes: List[Dict]` ساخته می‌شود؛ هر عضو یک پتری با تمام فیلدهای مربوط به خودش (ماسک، ROI، شعاع، کاندیدهای هر شاخه، نتایج هاله/حباب و ...) است. هر ماژول بعدی با `for dish in dishes:` روی همه‌ی پتری‌ها حلقه می‌زند و خروجی هرکدام را جداگانه نمایش/چاپ می‌کند.

---

## ۴) بازطراحی ماژول ۴ (تشخیص پتری) — مهم‌ترین تغییر این نشست

### مشکل اولیه (گزارش‌شده توسط کاربر)
کد قبلی فقط **بهترین کاندید تک** را نگه می‌داشت؛ حتی با وجود چند پتری در تصویر، فقط یکی تشخیص داده می‌شد.

### نسخه‌ی اول اصلاح (Canny + Contour + Hough چندگانه)
ابتدا `detect_petri_dish` به `detect_all_petri_dishes` تبدیل شد تا همه‌ی کاندیدهای معتبر (نه فقط بهترین) را نگه دارد، با پشتیبانی از دایره/مربع/fallback عمومی (بر اساس circularity، rectangularity، solidity کانتور). ماسک هر پتری از کانتور واقعی ساخته می‌شود (`cv2.drawContours`)، نه یک دایره‌ی اجباری.

### مشکل بزرگ‌تری که در تست روی عکس واقعی پیدا شد
با آپلود ۴۱ عکس واقعی توسط کاربر و تست سیستماتیک، مشخص شد:
- **`HoughCircles` به‌تنهایی روی یک عکس ۴۰۰۰×۳۰۰۰ حدود ۶۰ ثانیه طول می‌کشد.**
- **مسیر Canny+Contour تقریباً هیچ‌وقت روی عکس واقعی به یک مرز بسته نمی‌رسد** (نور ناهموار، بازتاب لبه‌ی شیشه‌ای پتری، پس‌زمینه‌ی شلوغ) — حتی با closing خیلی بزرگ.
- در نتیجه تقریباً هر عکس واقعی مجبور بود به fallback کند (Hough) سقوط کند، و آن Hough هم گاهی چند دایره‌ی کاذب برمی‌گرداند (چون هیچ اعتبارسنجی مرزی نداشت) یا هیچ‌چیز پیدا نمی‌کرد.

### راه‌حل نهایی: Segmentation ناحیه‌ای (Region-based) به‌جای لبه‌محور
جایگزین شد با:
1. **آستانه‌گذاری Multi-Otsu دوگانه** (بسط کلاسیک Otsu به ۳ کلاس: پس‌زمینه / آگار / برچسب روشن دیسک) — تابع `_multi_otsu_threshold` در سلول Helper Functions. روی نسخه‌ی کوچک‌شده‌ی تصویر (400×400) محاسبه می‌شود، پس فقط چند صدم ثانیه طول می‌کشد.
2. ماسک foreground = پیکسل‌های بالاتر از آستانه‌ی اول (`t1`) → یعنی «آگار + برچسب»، جدا از پس‌زمینه‌ی تیره.
3. **closing سبک** برای پر کردن حفره‌های ریز بافت آگار.
4. هر مولفه‌ی متصل هم‌اندازه‌ی یک پتری = یک کاندید.
5. اگر segmentation ناحیه‌ای هیچ کاندیدی پیدا نکرد، **یا** همه‌ی کاندیدها فقط با مسیر عمومی/کم‌اعتماد (`region_generic` — یعنی نه دایره‌ای نه مربعی قابل‌تشخیص، مثلاً دو پتری که در یک ناحیه‌ی متصل ادغام شده‌اند) طبقه‌بندی شدند، مسیر قدیمی Canny+Contour+Hough به‌عنوان **تاییدکننده/تکمیل‌کننده** (نه پیش‌فرض) اجرا و کاندیدهایش به استخر کاندیدها اضافه می‌شود؛ امتیازدهی + NMS موجود تصمیم می‌گیرد کدام مجموعه بهتر است.

### یک مسیر رفته و برگشته: Watershed-splitting در سطح پتری
در حین توسعه، ابتدا برای مولفه‌های بزرگ (احتمال ادغام دو پتری چسبیده) یک شکستن با Watershed (نشانگر از قله‌های Distance Transform، در مقیاس پتری) اضافه شد. اما تست روی عکس واقعی نشان داد **این کار روی پتری‌های منفرد معتبر (به‌خصوص مربعی، که Distance Transform آن‌ها یک «تیغه» دارد نه یک قله‌ی تیز) هم قله‌های کاذب پیدا می‌کند** و به‌جای پتری کامل، یک تکه‌ی کوچک‌تر و ظاهراً «تمیزتر» را برنده‌ی امتیازدهی می‌کند — یعنی خودِ مکانیزم شکستن باعث false positive جدید می‌شد. **این مکانیزم حذف شد.** راه‌حل واقعی برای پتری‌های چسبیده همان مکانیزم «تاییدکننده‌ی Hough برای کاندیدهای `region_generic`» در بالاست که به‌شکل ایمن‌تری همین مشکل را حل می‌کند.

### نتایج تایید‌شده (روی عکس‌های واقعی)
- دو پتری چسبیده/نزدیک‌به‌هم در یک قاب: قبلاً یا ۷ پتری کاذب یا فقط ۱ پتری تشخیص داده می‌شد؛ الان هر دو با شعاع دقیق پیدا می‌شوند.
- هر دو پتری مربعی نمونه (`1.1.1. original.jpg`, `2.2.1. original.jpg`): قبلاً بدترین حالت پایپلاین بودند (شعاع خیلی کوچک‌تر از واقعی چون فقط دایره فیت می‌شد)؛ الان با روش `region_rect` و شعاع تقریباً دقیق (~۵۵۶px، در عرض ۰.۰۵ ثانیه) تشخیص داده می‌شوند.
- پتری‌های ساده‌ی تک‌دایره: نتیجه درست است (از طریق fallback ایمن)، ولی هنوز همیشه از مسیر سریع استفاده نمی‌کند — **این یک هدف بهینه‌سازی باز است، نه یک باگ** (بخش ۷ را ببینید).

### ۴.۱) باگ جدید (کشف‌شده بعد از تست گسترده‌ی ۶ عکس واقعی): پتری‌های کاذب اضافی

بعد از بازطراحی بالا، بازرسی چشمی thumbnail چند عکس واقعی (که از قبل مشخص بود دقیقاً ۱ پتری واقعی دارند) نشان داد پایپلاین همچنان **پتری‌های کاذب اضافی** گزارش می‌کند — همیشه از طریق مسیر `hough_fallback`، نه segmentation ناحیه‌ای. نتیجه‌ی اسکن کامل (با فیلتر containment، بخش بعد را ببینید):

| عکس | تعداد پتری واقعی | تعداد گزارش‌شده | یادداشت |
|---|---|---|---|
| `20260101_142413.jpg` | ۱ | ۱ | ✅ رفع شد |
| `20260101_142534.jpg` | ۱ | ۲ | ۱ کاذب باقی‌مانده |
| `20260101_142659.jpg` | ۱ | ۳ | ۲ کاذب باقی‌مانده |
| `20260101_142548.jpg` | ۱ | ۵ | ۴ کاذب باقی‌مانده (قبل از فیلتر containment: ۶) |
| `20260101_142502.jpg` | ۲ (واقعی) | ۳ | ۱ کاذب باقی‌مانده |
| `20260101_142518.jpg` | ۲ (واقعی) | ۲ | ✅ درست (دو پتری چسبیده) |

**اولین تلاش برای رفع — فیلتر containment (کامیت `3668eab`):** فرض اولیه این بود که پتری‌های کاذب داخل/هم‌پوشان با پتری واقعی هستند (هاله مرز کاذب نزدیک به مرز پتری می‌سازد)، پس یک فیلتر اضافه شد: هر کاندید کم‌امتیازتر که ≥۸۰٪ مساحتش داخل یک کاندید پرامتیازتر دیگر باشد، حذف می‌شود (`cfg.dish_containment_reject_frac`, تابع `_circle_overlap_ratio` که به سلول Helper Functions منتقل شد تا در ماژول ۴ و ۱۴ به اشتراک برود).

**نتیجه‌ی عیب‌یابی دقیق (محاسبه‌ی واقعی `_circle_overlap_ratio` روی کاندیدهای `20260101_142534.jpg`):**
```
region candidates: [('region2_generic', 0.699, r=1741)]  → این تقریباً خودِ پتری واقعی است
needs_edge_check: True   (چون کاندید فقط generic است)
edge candidates: [('hough_fallback', 0.848, r=1347, x=1331, y=2113),   ← پتری واقعی (Hough آن را هم پیدا کرد)
                   ('hough_fallback', 0.587, r=699,  x=2170, y=115)]    ← کاذب، در گوشه‌ی مقابل تصویر
overlap(کاذب, واقعی) = 0.0000   ← کاملاً منفصل، نه تودرتو!
```
یعنی فرض اولیه غلط بود: کاندید کاذب **هیچ هم‌پوشانی‌ای با پتری واقعی ندارد** — یک تشخیص کاملاً مستقل و بی‌ربط در گوشه‌ی دیگر عکس است (احتمالاً یک لبه‌ی ضعیف نامرتبط با پتری: انعکاس، برچسب دوربین، یا بافت پس‌زمینه که به‌طور تصادفی معیارهای شکل/edge_support را رد کرده). **فیلتر containment از نظر طراحی هرگز نمی‌توانست این حالت را بگیرد** چون مبتنی بر هم‌پوشانی مکانی است.

**تحلیل ریشه‌ای اولیه (ایده‌ی «Anchoring» — بررسی بیشتر آن را رد کرد، نگه داشته شده به‌عنوان یک مسیر رفته‌وبرگشته‌ی مستند):** طبق مستندسازی خودِ کد (بخش ۴)، مسیر Hough قرار است فقط به‌عنوان «تاییدکننده/تکمیل‌کننده»ی کاندیدهای segmentation ناحیه‌ای اجرا شود، نه یک جست‌وجوی کاملاً مستقل. اما در پیاده‌سازی فعلی، وقتی اجرا می‌شود همه‌ی کاندیدهایش (هرجای تصویر) بدون قید به استخر مشترک اضافه می‌شوند. ایده‌ی اول: «فقط کاندید Hough را نگه دار اگر با حداقل یک کاندید ناحیه‌ای هم‌پوشانی معنادار داشته باشد؛ اگر segmentation ناحیه‌ای اصلاً چیزی پیدا نکرد، همه را نگه دار.» روی `20260101_142518.jpg` (دو پتری واقعی و چسبیده) این ایده امیدوارکننده به نظر رسید: segmentation ناحیه‌ای یک بلاب ادغام‌شده (`region_generic`, یک ناحیه‌ی بزرگ شامل هر دو پتری) پیدا کرد، و **هر دو** کاندید واقعی Hough با این بلاب هم‌پوشانی داشتند (۱.۰۰۰ و ۰.۶۳۷) — برخلاف کاندید کاذب در `142534.jpg` (هم‌پوشانی = ۰.۰۰۰).

**اما بررسی دو عکس دیگر این ایده را رد کرد:**
- `20260101_142548.jpg`: segmentation ناحیه‌ای **هیچ کاندیدی اصلاً پیدا نکرد** (`region_c` کاملاً خالی) — یعنی طبق قاعده‌ی «اگر ناحیه‌ای خالی بود، همه‌ی Hough نگه داشته شوند»، هر ۶ کاندید (که ۵ تای آن‌ها کاذب‌اند) بدون فیلتر باقی می‌مانند. این ایده برای این عکس هیچ کمکی نمی‌کند.
- `20260101_142659.jpg`: segmentation ناحیه‌ای کاندیدهایی با شعاع خیلی بزرگ پیدا کرد (`r=1779`, `r=1673` — به‌احتمال زیاد oversized، بلعیدن بخشی از پس‌زمینه، همان مشکل شناخته‌شده‌ی بخش ۷). چون این بلاب‌ها آن‌قدر بزرگ‌اند، **هر سه** کاندید Hough (هم ۲ تای واقعی و هم ۱ تای کاذب) با آن‌ها هم‌پوشانی بالا دارند (۱.۰۰۰, ۱.۰۰۰, ۰.۹۶۰) — معیار هم‌پوشانی دیگر هیچ تفکیکی نمی‌دهد.
- حتی با IoU (اشتراک/اجتماع) به‌جای «هم‌پوشانی نسبت به کوچک‌تر»، مشکل حل نشد: IoU پتری دوم واقعی در `142518.jpg` (~۰.۱۵) عملاً هم‌مرتبه‌ی IoU کاندیدهای کاذب در `142659.jpg` (~۰.۱۴–۰.۲۳) است. یعنی از نظر آماری، «پتری دوم واقعی داخل یک بلاب ادغام‌شده» و «کاندید کاذب داخل یک بلاب oversized» با این سیگنال قابل تفکیک نیستند.

**نتیجه‌گیری صادقانه:** ایده‌ی anchoring به‌تنهایی **کافی نیست** و کنار گذاشته شد. ریشه‌ی مشکل عمیق‌تر است: وقتی segmentation ناحیه‌ای یا (الف) هیچ‌چیز پیدا نمی‌کند یا (ب) یک بلاب oversized/نادقیق پیدا می‌کند، هیچ سیگنال هندسی ساده‌ای (هم‌پوشانی، IoU، فاصله) نمی‌تواند «پتری واقعی دوم» را از «تشخیص کاذب Hough» تفکیک کند — چون از نظر مکانی هر دو مشابه به نظر می‌رسند. **مسیرهای بررسی‌نشده‌ی بعدی** (پیشنهاد، نه تصمیم قطعی): (۱) تقویت خودِ گیت `edge_support` در `_detect_petri_dishes_edge_fallback` تا کاندیدهای با پشتیبانی لبه‌ی ضعیف/جزئی اصلاً وارد استخر نشوند (شاید ریشه‌ی مشترک هر سه عکس مشکل‌دار همین باشد — باید edge_support واقعی هر ۹ کاندید بالا، هم واقعی هم کاذب، مقایسه شود که هنوز انجام نشده)؛ (۲) رفع مستقیم مشکل شناخته‌شده‌ی «segmentation ناحیه‌ای گاهی هیچ‌چیز پیدا نمی‌کند یا oversized می‌شود» به‌جای patch کردن پیامدش در لایه‌ی Hough. **وضعیت: باز، نیاز به بررسی بیشتر با داده (نه حدس) در جلسه‌ی بعد.**

---

## ۵) سایر باگ‌های واقعی پیدا و رفع‌شده در این نشست

### ۵.۱) ماژول ۶ (Adaptive Threshold) — یافته‌ی ساختاری مهم
آستانه‌گذاری **محلی/windowed** (`cv2.adaptiveThreshold`) روی تصویر feature (که خودش از Top-Hat/کنتراست محلی ساخته شده) در حضور یک هاله‌ی نسبتاً پهن، **حدود ۶۵ تا ۷۰٪ از کل تصویر را به‌اشتباه «فورگراند» تشخیص می‌دهد**. دلیل: هاله یک گرادیان نرم و پیوسته است، نه نویز موضعی؛ آستانه‌گذاری محلی روی چنین سیگنالی، اصولاً نیمی از هر پنجره را «بالاتر از میانگین محلی» علامت می‌زند (یک خاصیت ریاضی خودِ adaptive threshold، نه ایراد یک عکس خاص). این ریشه‌ی اصلی ضعف شاخه‌ی Blob Analysis بود (که قبل از این اصلاح همیشه ۰ کاندید برمی‌گرداند).

**راه‌حل:** جایگزینی با آستانه‌ی سراسری robust (percentile نسبت به آمار خودِ پتری) — هم‌راستا با منطقی که ماژول ۵ از قبل داشت.

### ۵.۲) ماژول ۱۲ (Blob Analysis) — بازطراحی کامل
مشکلات ریشه‌ای که پیدا شد (علاوه بر ۵.۱):
- `blob_min_area`/`blob_max_area` مقدار **پیکسل مطلق** بودند (نقض مستقیم الزام ۲.۴) — در رزولوشن‌های دیگر، بازه‌ی واقعی دیسک از این بازه‌ی ثابت خارج می‌شد.
- فیلتر اعتبارسنجی شعاع از `hough_min_r`/`hough_max_r` (فیلدهای مطلق یک نسخه‌ی خیلی قدیمی‌تر) استفاده می‌کرد، بدون ربط به بازه‌ی نسبی واقعی دیسک.
- کرنل‌های مورفولوژی (`adaptive_block_size`, `morph_closing_ksize_small`, `morph_opening_ksize_small`) پیکسل مطلق بودند.
- دیسک‌های چسبیده/نزدیک به هم هیچ مکانیزم جداسازی نداشتند.

**راه‌حل:**
1. بازه‌ی مساحت/شعاع از همان سیستم نسبی مشترک (`disk_radius_bounds_px`, تابع کمکی جدید) مشتق می‌شود.
2. کرنل‌های closing/opening نسبت به قطر کمینه‌ی دیسک.
3. منبع باینری از **اجتماع** دو ماسک (`thr_open_small` و `disk_feature_mask` ماژول ۵) برای پوشش بهتر.
4. **Watershed** (نشانگر از قله‌های Distance Transform ماژول ۹) برای جدا کردن دیسک‌های چسبیده — این‌جا دقیقاً همان جایی است که DT به‌عنوان **ابزار کمکی** (نه شاخه‌ی مستقل) به کار می‌آید.

### ۵.۳) نقش ماژول DT (۱۳) — بازتعریف فلسفی
پارامترهای قدیمی DT (`dt_peak_min_value=8.0`, `dt_peak_min_distance=35`, `candidate_r_min/max=8/60`) همگی **پیکسل مطلق** بودند و DT را به‌عنوان شاخه‌ی سوم مستقل (رأی‌دهنده در Fusion) ناسازگار با مقیاس و نویزپذیر می‌کردند.

**تصمیم طراحی:** DT دیگر کاندید مستقل تولید نمی‌کند. نقش آن به ابزار زیرساختی نسبی تغییر کرد:
- **ابزار ۱:** نشانگر Watershed برای جداسازی دیسک‌های چسبیده در ماژول ۱۲.
- **ابزار ۲:** تخمین شعاع بلاب‌ها (`dt_patch` در ماژول ۱۲).
- **ابزار ۳ (ماژول ۱۷ جدید):** تخمین شعاع هر حباب روی هاله.

سلول ماژول ۱۳ اکنون فقط یک **نمایش تشخیصی** است (چه نشانگرهایی برای Watershed استفاده شدند)، نه تولیدکننده‌ی کاندید.

### ۵.۴) ماژول ۱۴ (Fusion) — از سه‌شاخه به دو‌شاخه
فلسفه‌ی جدید: **Hough** (شاخه‌ی اصلی/دقیق، طبق تایید کاربر روی داده‌ی واقعی خوب کار می‌کند) + **Blob اصلاح‌شده** (شاخه‌ی پوششی/recall). منطق clustering/NMS موجود (`_circle_overlap_ratio`) بدون تغییر بازاستفاده شد، فقط ورودی از ۳ به ۲ شاخه کاهش یافت.

### ۵.۵) باگ Quoting در شِل (خطای مهندسی، نه الگوریتمی)
هنگام اصلاح دستی یک شرط از طریق `python3 -c "..."` در Bash، quoting دوتایی باعث حذف کوتیشن‌های داخلی و ایجاد `KeyError` شد. رفع شد با نوشتن پچ‌ها همیشه به‌صورت فایل اسکریپت مستقل (نه inline `-c`).

### ۵.۶) باگ امتیازدهی دوگانه در Hough Fallback
کاندیدهای بازیابی‌شده از Hough (در نقش تاییدکننده) دو بار جریمه می‌شدند: یک‌بار با gate حداقلی `edge_support`، و یک‌بار دیگر با ضرب شدن دوباره‌ی `edge_support` در امتیاز نهایی. این باعث می‌شد کاندیدهای درستِ Hough در NMS از یک بلاب `region_generic` نادرست (که تخفیف مشابهی نداشت) امتیاز کمتری بگیرند و ببازند. رفع شد: `edge_support` فقط به‌عنوان gate استفاده می‌شود، نه ضریب امتیاز.

---

## ۶) ماژول جدید ۱۷ — تشخیص حباب روی هاله

برای هر دیسکِ دارای هاله‌ی تاییدشده:
1. از پروفایل شعاعی هاله (که ماژول ۱۶ از قبل محاسبه می‌کند) به‌عنوان **پس‌زمینه‌ی نرم محلی** استفاده می‌شود؛ `residual = شدت واقعی − مقدار درون‌یابی‌شده‌ی پروفایل در همان شعاع`.
2. سیگنال `halo_map` (ماژول ۱۰ — که تا این نشست خروجی‌اش هیچ‌جا استفاده نمی‌شد، یک dead output) به‌عنوان سیگنال گرادیان کمکی به residual اضافه می‌شود — بازیافت یک خروجی موجود، هم‌راستا با فلسفه‌ی «خروجی هر سلول = ورودی سلول بعد».
3. آستانه‌گذاری با ضریب سیگمای robust (MAD) — بدون هیچ مقدار مطلق.
4. Opening نسبی + `connectedComponents` + **Distance Transform** برای شعاع هر حباب (سومین کاربرد DT-as-tool).
5. خروجی: تعداد حباب، مجموع مساحت (mm²)، و برآورد حجم توده (Σ (4/3)πr³ بر مبنای شعاع هر حباب) — **با ذکر صریح که این یک تقریب کروی از تصویر دوبعدی است، نه اندازه‌گیری مستقیم حجم.**

⚠️ این ماژول هنوز روی داده‌ی واقعی (حباب‌های واقعی روی هاله‌ی واقعی) عمیقاً اعتبارسنجی نشده — چون در عکس‌های تست‌شده تا الان، اکثر دیسک‌ها یا هاله نداشتند یا هاله‌شان حباب نداشت. **کالیبراسیون این بخش با نمونه‌ی واقعی حباب‌دار، کار باقی‌مانده است.**

---

## ۷) وضعیت تست و محدودیت‌های شناخته‌شده

### تست‌شده و تایید‌شده (به‌صورت کامل، همه‌ی ماژول‌ها)
- تصویر مصنوعی (چند پتری دایره/مربع، دیسک‌های دوسایز، هاله، حباب، دیسک‌های چسبیده) — end-to-end بدون خطا.
- چند عکس واقعی متنوع: دو پتری دایره‌ای چسبیده، پتری ساده‌ی تک‌دایره، هر دو پتری مربعی نمونه.

### در حال انجام / قدم بعدی فوری (اولویت ۱)
اسکن ماژول ۴ روی ۶ عکس واقعی هدفمند (بخش ۴.۱) کامل شد. ایده‌ی اول رفع (anchoring کاندیدهای Hough به کاندیدهای ناحیه‌ای) با داده‌ی واقعی روی هر ۶ عکس تست و **رد شد** — نه صرفاً «هنوز پیاده‌سازی نشده»، بلکه بررسی دقیق‌تر نشان داد به‌تنهایی کافی نیست (جزئیات کامل در بخش ۴.۱). جلسه به‌خاطر محدودیت زمانی کاربر متوقف شد. قدم بعدی واقعی این است (نه یک راه‌حل آماده — یک نقطه‌ی شروع تحقیق):

1. **قبل از هر اصلاح جدید**، `edge_support` واقعی (نه فقط امتیاز نهایی) هر ۹ کاندید Hough که تا الان روی این ۶ عکس دیده شده (هم ۴ تای واقعی و هم ۵ تای کاذب) را استخراج و مقایسه کن — فرضیه‌ی بررسی‌نشده این است که کاندیدهای کاذب پشتیبانی لبه‌ای ضعیف‌تری دارند و می‌شود گیت `cfg.dish_detect_hough_min_edge_support` (فعلاً ۰.۳۵) را بر اساس این داده‌ی واقعی (نه حدس) بالاتر برد.
2. به‌موازات، بررسی کن **چرا** segmentation ناحیه‌ای گاهی هیچ‌چیز پیدا نمی‌کند (`142548.jpg`) یا oversized می‌شود (`142659.jpg`، r~۱۷۰۰ در حالی‌که پتری واقعی احتمالاً کوچک‌تر است) — این دو حالت هم ریشه‌ی مستقیم مشکل کندی (بخش ۷، بند ۱) هم مشکل پتری‌های کاذب‌اند؛ رفع این‌ها ممکن است هر دو مشکل را هم‌زمان حل کند و ترجیح دارد به patch کردن پیامد در لایه‌ی Hough.
3. هر راه‌حل پیشنهادی باید روی هر ۶ عکس این جدول (با اسکریپت‌های آماده در `/tmp/.../scratchpad/`: `debug_anchor_check.py`, `debug_534_overlap.py`, `verify_containment_fix.py` — الگوهای اجرای نوت‌بوک با `nbclient`) تست شود قبل از کامیت — هدف: صفر پتری کاذب در ۴ عکس تک‌پتری، همچنان ۲ پتری درست در ۲ عکس چندپتری.
4. سپس اسکن سریع (فقط ماژول ۴) روی **هر ۴۱+ عکس واقعی** موجود در ریپازیتوری برای اطمینان کلی، و ادامه‌ی بررسی سایر ماژول‌ها طبق درخواست اصلی کاربر (گزارش مشکلات + پیشنهاد اصلاحات کلیدی، با کمک کتاب‌های مرجع Computer Vision در گوگل‌درایو).

فیلتر containment قبلی (کامیت `3668eab`) به‌عنوان یک بهبود جزئی/بی‌ضرر نگه داشته شد (یک مورد را رفع کرد، در هیچ موردی رگرسیون ایجاد نکرد) ولی به‌تنهایی کافی نیست.

### محدودیت‌های شناخته‌شده (کارهای باز)
0. **پتری‌های کاذب باقی‌مانده در ۴ از ۶ عکس تست‌شده** — بخش ۴.۱ و بند «قدم بعدی فوری» بالا را ببینید. مهم‌ترین کار باز فعلی.
1. **کندی باقی‌مانده در برخی عکس‌ها:** حتی با segmentation ناحیه‌ای، برخی عکس‌ها (وقتی segmentation ناحیه‌ای به یک ناحیه‌ی `region_generic` می‌رسد) هنوز به fallback کند (Hough، ۶۰ تا ۲۲۰+ ثانیه بسته به رزولوشن) نیاز دارند. این برای پتری‌های چسبیده/ادغام‌شده رفتار درستی است (نتیجه صحیح می‌شود)، اما برای برخی پتری‌های ساده‌ی تک هم رخ می‌دهد که ایده‌آل نیست (کندتر از لازم، هرچند نتیجه درست است). علت شناسایی‌شده: ماسک ناحیه‌ای گاهی کمی از مرز واقعی پتری بیرون می‌زند (بلعیدن بخشی از پس‌زمینه) و همین باعث افت circularity/rectangularity کافی برای رد شدن از مسیر سریع می‌شود. **پیشنهاد رفع بعدی:** استفاده‌ی جایگزین از آستانه‌ی دوم (`t2`) خروجی multi-Otsu وقتی کاندید مبتنی بر `t1` oversized/غیر-دایره‌ای/غیر-مربعی باشد (تست اولیه‌ی مجزا نشان داد `t2` مرز تمیزتری می‌دهد).
2. **بازیابی ناقص دیسک در برخی عکس‌ها:** مثلاً در یکی از پتری‌های مربعی با ~۱۴ دیسک قابل‌مشاهده، فقط ۶ دیسک نهایی تایید شد. علت دقیق هنوز عمیقاً بررسی نشده (به‌احتمال ترکیبی از فیلترهای هندسی Fusion و کیفیت شاخه‌ی Blob روی این پس‌زمینه‌ی خاص). عمداً برای رعایت هشدار صریح کاربر («overfitting نکن») روی این یک تصویر خاص tune نشد.
3. **ماژول ۱۷ (حباب)** هنوز با نمونه‌ی واقعی حباب‌دار اعتبارسنجی نشده.
4. **طبقه‌بندی S/I/R بر اساس EUCAST breakpoint tables** اصلاً پیاده‌سازی نشده — فایل‌های PDF مرجع در ریپازیتوری هست ولی هنوز به کد وصل نشده.
5. **پاک‌سازی فیلدهای کانفیگ مرده/قدیمی** (`HoughSweepConfig`, `dish_roi_*`, `score_*_weight`, و غیره در سلول ۳) عمداً انجام نشده — نه یک باگ، بلکه یک تصمیم آگاهانه برای کاهش ریسک در این نشست.
6. **ترجمه به C++** هنوز شروع نشده — کد فعلی طوری نوشته شده که این کار را ساده‌تر کند (الگوریتم‌های deterministic، دیکشنری‌های ساده)، ولی هیچ کد C++ای هنوز تولید نشده.

---

## ۸) منابع مرجع موجود در پروژه

- `v_16.0_Breakpoint_Tables.pdf`, `Dosages_v_16.0_Breakpoint_Tables.pdf` — جداول استاندارد EUCAST (برای مرحله‌ی طبقه‌بندی S/I/R، هنوز استفاده نشده).
- پوشه‌ی گوگل‌درایو «Computer Vision Textbooks» (به اشتراک‌گذاشته‌شده توسط کاربر) شامل: *Digital Image Processing* (Gonzalez & Woods، ویرایش ۴)، *Digital Image Processing: PIKS Inside* (Pratt)، *Feature Extraction and Image Processing* (Nixon & Aguado)، *Computer Vision: Algorithms and Applications* (Szeliski)، *Multiple View Geometry in Computer Vision* (Hartley & Zisserman)، *Computer Vision: Foundations and Applications* (Stanford/Krishna)، *Fundamentals of Digital Image Processing* (Easton)، و دو کتاب DSP/Kalman Filtering. این‌ها برای طراحی الگوریتم‌های بعدی (به‌خصوص بخش segmentation و رفع محدودیت‌های بخش ۷) قرار است استفاده شوند.
- `disk_detection_pipeline_modular - Claude Eddition.ipynb` — نسخه‌ای که کاربر اجرا کرده بود؛ در زمان بررسی، فقط سلول‌های ۱، ۳ و ۸ خروجی ذخیره‌شده داشتند (بقیه اجرا/ذخیره نشده بودند)، پس برای عیب‌یابی مستقیم قابل استفاده نبود.

---

## ۹) خلاصه‌ی درخواست‌های اصلی کاربر (برای مرجع سریع)

1. رفع تشخیص چند‌پتری + هر ماژول بعدی روی همه‌ی پتری‌ها اجرا و خروجی هرکدام جدا نمایش داده شود. ✅ انجام شد.
2. رفع/بهینه‌سازی شاخه‌ی Blob Analysis (کل زنجیره‌ی منتهی به آن، نه فقط فیلتر نهایی) + تصمیم‌گیری در مورد نقش DT. ✅ انجام شد.
3. بازطراحی کامل جمع‌بندی سه‌شاخه بر اساس فلسفه‌ی هرکدام + استفاده از شاخه‌های نگه‌داشته‌شده برای baseline segmentation هاله و رخدادهای درون آزمایش. ✅ انجام شد.
4. تست روی همه‌ی عکس‌های واقعی در ریپازیتوری + گزارش مشکلات هر ماژول + پیشنهاد اصلاحات کلیدی بعدی. 🔄 در حال انجام (این سند بخشی از آن است).
5. عدم overfitting به نمونه‌های آپلودشده — برنامه باید جامع/عمومی بماند. ✅ در تمام تصمیم‌های طراحی رعایت شده (رجوع به بخش ۷، بند ۲ برای نمونه‌ی صریح خودداری از tune کردن).
